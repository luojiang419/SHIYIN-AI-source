# 项目发布约定

- 自2026-09-16起以2.0.0为新的分发基线。1.0.x用户通过2.0.0全量安装包覆盖升级；不再恢复hot-bootstrap/hot-updater旧引导链或旧迁移工具入口。2.0.0及后续客户端使用正常局域网热更新。
- 发布名称固定区分：`2.0.0` 是可供新安装或 1.0.x 覆盖升级的“全量安装基准包”；`20260918084551` 是包含桌面宿主、后端和前端的“完整热更新基准检查点”，仅供已安装 2.0.x 客户端通过更新器应用；其后仅含 `app/web` 的发布称为“纯前端热更新”，属于检查点之后的增量更新。不得把热更新检查点称为全量安装包。

- 自 2026-09-14 起，后续更新默认通过独立局域网分发中心发布热更新。
- 只有用户明确要求“全量编译/全量安装包”时，才运行 `installer:build`、`release-installer.ps1` 或其他全量安装器构建流程。
- 默认入口：`npm run update:publish`，或 `python tools/build-hot-update.py --publish --notes "更新说明"`。
- 热更新允许按需重新编译桌面宿主和 backend sidecar；这不等于全量安装包编译。只有纯前端变更才可使用 `--web-only`。
- 分发中心独立运行。普通客户端不得恢复内置分发服务器、监听地址或服务器启停控件。
- 深度图（person-depth）和深度视频（video-depth）模型作为固定分发资源保留，不随软件热更新重新打包或发布；只有用户明确要求替换模型时才变更。固定版本记录保存在分发中心数据目录的 `fixed-models.json`。
- 已部署路径和接入验证记录见 `开发任务/175-独立局域网分发服务与控制面板方案.md`；后续沿用该文档，保护其他任务未提交改动。
