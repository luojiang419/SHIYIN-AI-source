# 源码备份 101：工程页弹窗整理与批量网格排版

## 备份范围

本次备份记录工程页创建/整理交互和批量媒体导入排版的源码差异，配合 Git 工作区保存当前完整源代码状态。

## 文件差异

### 工程页

- `static/canvas-list.html`：在“新建画布”左侧加入 `arrangeCanvasesBtn`。
- `static/js/canvas-list.js`：加入 `arrangeCanvasesInProject()`，根据当前工程宽度计算网格并调用 `persistMeta()` 保存每张画布的位置；创建成功后调用 `openCanvas(nc)`。
- `static/js/canvas-list.js`：创建表单从 `boardWorld` 的绝对定位卡片改为 `document.body` 下的 `ws-create-modal-backdrop` 遮罩弹窗，回车触发同一创建流程。
- `static/css/canvas-list.css`：新增整理按钮样式、暗色主题样式、全屏暗化模糊遮罩和放大版创建弹窗样式。

### 批量导入

- `static/js/canvas.js`：`layoutUploadedMediaNodes()` 改用统一布局间距常量。
- `static/js/canvas.js`：`createGroupForUploadedNodes()` 创建分组后立即调用 `arrangeCanvasGroupContents(group.id, {skipUndo:true})`，根据真实节点尺寸再次排版并更新分组尺寸。

### 测试

- `tests/test_canvas_unified_entry.py`：增加新建弹窗、回车进入和工程页网格整理契约测试。
- `tests/test_canvas_multi_file_drop_layout.py`：增加布局常量和分组复核排版契约测试。

## 验收

- `node --check static/js/canvas-list.js`
- `node --check static/js/canvas.js`
- `node --check static/js/smart-canvas.js`
- 工程页、批量导入及相关回归测试：61 passed。
