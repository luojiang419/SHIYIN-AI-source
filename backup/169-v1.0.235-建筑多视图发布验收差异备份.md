# 169 - v1.0.235 建筑多视图发布验收差异备份

生成时间：2026-08-22

## 发布变更

### 版本号

`1.0.234` → `1.0.235`，同步更新：

- `VERSION`
- `package.json` / `package-lock.json`
- `src-tauri/tauri.conf.json`
- `src-tauri/Cargo.toml` / `Cargo.lock`
- `main.py` 的 `APP_VERSION`
- `static/update-notes.json`

### 建筑任务恢复边界修复

修改前：建筑任务在输入发生变化后仍可能保留本地 pending 追踪，阻塞新规划或让旧结果回填。

修改后：输入签名失效时清空本地 `buildingPendingTasks` 与活动视图；恢复任务检测到输入变化会解除旧任务追踪；点击“生成多视图”前若仍有 pending 任务，先恢复任务，恢复不完则阻止重复提交。

### 构建产物

- Tauri 宿主：`src-tauri/target/release/SHIYIN-AI.exe`
- 安装包：`dist/installer/SHIYIN-AI-Setup-1.0.235.exe`
- 后端：PyInstaller `canvas-backend` 已成功收集进安装器暂存目录。

## 验证结果

- 全量测试：625 passed，16 个既有警告，42 个子测试通过。
- JavaScript 语法检查：智能画布、普通画布、共享建筑脚本均通过。
- Tauri release 构建成功。
- PyInstaller + Inno Setup 构建成功，安装包大小约 68 MB。
- `git diff --check` 通过。

## 已知限制

本轮浏览器视觉回归访问本地服务时被登录页拦截；未猜测、代填或传输账号密码。获取有效登录态后可直接打开最新智能画布继续回归。
