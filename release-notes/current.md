# SHIYIN AI v1.0.94
- 自由创作现已支持零参考图生成：只输入提示词即可直接生成，也可以按需上传参考素材。
- 自由创作仍会将用户提示词原样发送给生成模型，不拼接电商规则、保真锁或自动组合提示词。
- 页面移除参考图必传标记，并明确显示“创作素材（可选）”及无图时的默认比例行为。
- 修复本地浏览器烟测脚本依赖项目内置 Python 的问题，缺少内置运行时时会自动使用系统 Python。
