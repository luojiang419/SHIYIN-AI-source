# SHIYIN AI v1.0.381
- 修复桌面 WebView 启动和恢复期间重复导航耗尽启动令牌的问题；短时重放仍限制为本机随机令牌，并保留错误令牌、超时与局域网拒绝。
- 恢复图片节点顶部悬浮工具条和节点下方面板；面板按悬停、聚焦或单选懒创建，保留无限画布媒体队列与首开性能优化，避免大画布一次性生成大量控件。
- 视频提示词限长改为按模型自动适配：MiniMax H3 默认 7000 字符、可灵默认 2500 字符；自动解析/润色超限时自动压缩，生成提交前按真实模型上限校验，不再触发统一 4000 字符弹窗。
- 新增润色/自动解析流式进度回填：Responses SSE 的可见输出增量会实时显示在提示词框，阶段状态提示联网检索和视觉分析进度；任务完成后用最终提示词覆盖回填，失败时保留已生成草稿便于诊断。不暴露模型隐藏思维链，也不切换 provider/model。
- 电商视觉润色/自动解析改为 Responses SSE 连续任务：先返回事件并持续聚合结果，避免完整版导演规则、多图视觉请求在同步响应体阶段触发网关 524；保持电商专用 provider/model，不进行模型降级。
- 本地后台任务继续提供 queued、web-search、visual-parse、succeeded、failed 状态轮询；已验证原生 `background:true` 不被电商代理接受，因此不发送不兼容参数。
- 修复电商专用 API 平台协议下拉被错误禁用的问题；现在可自由选择 OpenAI 直连、Responses、异步、Gemini 等协议，并维护图片/聊天/视频模型。
- 自动解析动作编排密度增强：每个镜头现在必须显式规划准备、执行/互动、反应/收束连续节拍，补充触发因果、主体错峰、物理反馈与镜头响应；MiniMax H3 Ref2VA 详细描述按 350-500 英文词充分展开，避免再次退化为静态画面摘要。
- 自动导演规则改为完全内容驱动：动物行为仅在画面或用户语境存在动物/拟动物时按物种线索和场景目标选择；所有动作与运镜示例均为可选方法，不再预设牛仔裤、牧场或任何固定案例模板。
- 视频自动解析升级为自主导演模式：无文字提示词时会依据全部参考图编排开场、发展、变化/揭示与收束，自动补足人物眼神/表情、动物或拟动物行为、多主体错峰反应、物理反馈和有叙事目的的自然运镜；有提示词时严格保留用户主线，仅扩展可执行细节。
- 自动解析与多图提示词润色新增最终图片标签覆盖率硬校验；H3/Kling 输出缺少任一 `<Picture N>` 或 `<<<image_N>>>` 时会明确报错，不再静默交付不完整提示词。
- 优化视频节点自动解析：节点故事提示词与全部参考图在同一次多模态请求中综合分析，自动把自然图号映射为当前视频模型规范标签；逐张校验图片载入并要求最终提示词覆盖全部图片，避免只使用首尾两张。
- 修复视频节点自动解析未注入 Kling/H3 skill 正文的问题；无文字提示词现在进入自主导演模式，由高能力视觉模型基于图片事实设计时间推进、镜头调度与节奏，并清理 `Photorealistic, 8k resolution.` 等散装质量尾缀。
- 视频生成节点新增“自动解析”：仅连接图片且无节点内/上游提示词时，将全部图片按顺序放入同一个多模态请求，并通过 Responses 的 `web_search` 工具让 GPT-5.6 Sol 自行联网检索优秀案例，再结合所选视频模型 skill 自动编排分镜、景别/角度、运镜、动作时序、节奏与声音并回填；已有提示词时在同一请求中组合“图片 + 用户提示词 + skill”继续润色。
- 电商专用平台改为标准 API 配置页，支持请求地址、API Key、协议及图片/聊天/视频模型列表的完整配置；电商视觉解析路由保持兼容。
- 视频提示词润色与实际生成提交现在会把“图1/图片1/视频1”转换为所选 skill 的规范引用；可灵使用 `<<<image_N>>>`、`<<<video_N>>>`、`<<<element_N>>>`，MiniMax H3 使用 `<Picture N>`、`<Video N>`、`<Subject N>`。
- 润色请求增加稳定参考素材清单，视觉模型必须检查对应画面并按主体证据合并或拆分主体标签，结果附带规范化输入和参考映射。
- 可灵适配器更新到官方公开的 Kling 3.0 Prompt Syntax 2.0：支持 Omni 三角引用标签、VIDEO 3.0/Omni 多镜头、原生音频、运镜与物理描述规范，并通过 `references/latest-official-syntax-2.0.txt` 完整注入润色模型。
- 修正 skill 安装包复制目录层级，最终资源固定落在 `app/skills/video-prompt-polish`，避免发布版出现重复嵌套路径。
- 安装包与浏览器 smoke staging 现在会复制 `skills/video-prompt-polish` 到 `app/skills`，并在构建断言中验证 MiniMax H3 完整 skill 与参考文档，确保发布版运行时不会因资源未打包而降级。
- MiniMax H3 润色器现已完整内置官方 `h3-prompt-writing` skill 及 `base-en.txt`、`ref-en.txt` 参考规范；选择 MiniMax H3 时会将完整规则与用户提示词、参考素材一起提交给视觉模型，并按官方模式回填结果。
- 视频提示词 skill 加载器支持读取 skill 目录下的全部 `references/*.txt`；可灵 CLI 保留官方指南适配器并附带来源说明，未冒充不存在的独立官方 skill 包。
- 视频提示词润色改为按当前模型自动加载项目内置 skill 规范；新增可灵 CLI 与 MiniMax H3 独立规范文件，并在响应中返回 skill 标识。
- 提示词节点改为可直接编辑的文本节点，支持滚轮浏览、自动增高/收缩与展开编辑同步；视频生成节点新增按所选模型规范调用视觉模型的“润色”按钮。
- 视觉辅助改为可选机制：只有“电商专用”平台已启用并配置 API Key 时才调用视觉模型；未配置时完全跳过视觉解析，仅使用内置排列组合规则。
- 电商页面新增生成前视觉分析：复用 API 设置中的“电商专用”视觉模型，先按用户选择的类型与内置排列组合规则生成参考图顺序和提示词预览，再提交 Nano Banana Pro 图片生成；未配置 API Key 时降级为可恢复的规则预览。
- 电商 universal 任务复用生成前视觉分析证据，避免同一批参考图在任务执行阶段重复调用视觉模型；用户选择的类型始终高于视觉模型识别结果。
- 图片节点底部编辑面板改为节点子面板，尺寸、位置和缩放跟随图片节点及顶部悬浮菜单逻辑，不再固定占据 622px 的画布区域；图片节点仍保留提示词、模型、比例、分辨率和摄影机设置。
- 参考 infinite-canvas 收紧图片节点下方悬浮编辑面板尺寸：最大宽度 622px，输入区按视口自适应，窄屏自动切换为两列控件布局；连接线支持右键选择“断开连接”，节点与连线采用中性主题和蓝色选中反馈。
- 修复图片节点常用操作勾选上限过低导致功能无法显示的问题；悬浮菜单、设置项和图片编辑面板改为按画面宽度与高度自适应，按钮保持可点击尺寸。
- 修正图片节点悬浮层布局：图片节点顶部仅显示媒体快捷菜单，提示词、模型和摄影机输入面板移动到节点下方，并保持画布边界内可见。
- 图片节点顶部悬浮菜单改为单行最多 4 个（3 个可配置动作 +“更多”），预览、宫格切分等功能不再与提示词输入面板混排；设置中可选择顶部显示的图片功能。
- 修复无限画布影视制作二级菜单悬停交互：指针从主菜单移动到二级菜单时保持展开，不再被捕获的 `pointerover` 事件立即关闭。
- 修正无限画布底部 Dock 布局：选择/移动按钮、常用功能节点和设置按钮统一为单行，Dock 按内容收缩并取消左右空白；空间不足时横向滚动，不再拆分成两行或上浮显示。
- 清理测试与运行时弃用告警：FastAPI 生命周期迁移到 lifespan，Pydantic 模型序列化迁移到 `model_dump()`，补齐 `httpx2` 测试依赖并修复 Blender 测试 socket 资源释放；全量测试 886 项通过且无告警。
- 自由创作页面彻底移除：主壳不再提供自由创作入口，旧 `workspace=free-creation` 地址会自动规范到电商工作区；同时补齐缓存版本标记，避免旧页面继续进入已废弃模式。
- 无限画布菜单简约化：图片/视频节点操作改为顶部最多两行的悬浮菜单，提示词与生成设置固定在节点底部；底部快捷节点改为可配置常用项，并新增齿轮设置入口；移除电商流水线创建分组及其二级菜单。
- 统一画布入口：移除智能画布用户入口与类型选择，新建画布只需输入名称即可创建；历史智能画布记录自动按统一画布打开，不删除既有数据。
- 修复电商结果预览“更多”按钮在旧版国际化缓存下显示 ecommerce.loadMore 键名的问题，增加中英文兜底显示。\n- 电商专用结果预览性能优化：对比手柄拖拽采用按帧更新，右侧候选缩略图默认显示最近 20 张并支持“更多”每次追加 20 张。\n- LinkFox API Key 长度上限提升到 8096 字符；彻底移除 LinkFox 节点的蓝色回退背景和硬编码颜色，提示词框、开关、状态提示和生成按钮统一跟随三套主题变量。
- LinkFox 现在作为 API 设置里的独立平台卡片展示；普通 API 平台不再显示 LinkFox 配置项，选中 LinkFox 后仅显示 Agent Key、技能网关和就绪状态。经典画布工具栏新增直接可见的 LinkFox 视频节点入口，避免藏在影视制作二级菜单中。
- API 设置页新增 LinkFox 视频生成配置区，可安全保存或清除 LinkFox Agent API Key、修改技能网关并检查技能安装状态；画布节点会提示前往 API 设置完成配置。
- 新增 LinkFox 视频生成节点：支持参考图/首尾帧模式、Seedance/可灵/HAPPY_HORSE/海螺/Wan 模型选择、时长/分辨率/比例/声音/提示词优化/Pro/运镜参数，并接入经典画布与智能画布。
- 修复多宫格裁切自动识别：改用沿整条轴稳定存在的亮色/暗色分隔带检测，减少普通图片内容边缘误判，并支持窄留缝与黑色分隔线。
- 修复宫格裁切结果添加到画布后的排列：每个裁切结果按识别出的行列固定为单元格，像素尺寸只用于保持缩略图比例，不再错误地作为 CSS 跨列数。
- 修复灯光重塑预览更新函数重复声明导致的输入状态失步：连接图片后会立即同步预览图片、占位提示、状态条和源图标签；普通画布与智能画布同时刷新脚本缓存版本。
- 重新开发灯光重塑节点：预览区、状态条和参数区拆成独立结构，连接或导入的图片会直接显示在节点内，并继续保留方向、色温、强度、光质、氛围、保持约束、补充要求、生成结果与输出节点连接能力；同时刷新经典与智能画布的脚本缓存版本。
- 修复无限画布文本输入/输出节点长文本显示体验：经典画布与智能画布的提示词、AI助手、多视图、循环和自定义文本参数会自动适应前 100 行内容，超过 100 行后滚动查看；同时修复左键框选矩形与鼠标指针漂移的问题。
- 修复角度调整节点长时间停留“生成中”时没有下游输出节点的问题：远端任务开始前创建可恢复占位输出，完成后自动写入结果并连接下游；角度生成显式传递源图风格锁与参考地址，并对 Shiying 多角度结果执行 RGB 色调校准，保持白平衡、曝光和色彩关系一致。
- 修复无限画布节点创建卡顿：经典/智能画布的菜单、输出端口快捷创建和影视工作流统一合并为单次批量渲染；修复影视制作二级菜单移出后仍悬挂展开，并清理失效的 Topaz 图标名。
- 修复无限画布点击线段断开按钮后卡顿数秒：经典与智能画布的普通断线改用轻量结构撤销事务和增量连接 DOM 更新，避免深拷贝整张画布；分组折叠线、历史线仍按需安全回退。
- 修复无限画布框选多个节点后，按住 Alt 拖拽只复制当前节点的问题；经典与智能画布现在都会复制并整体拖动全部选中节点，同时保留副本之间的内部连线与多选状态。
- 模块 6 Windows 安装包已通过 Inno Setup 和安装器产物校验；安装包 `SHIYIN-AI-Setup-1.0.317.exe`，SHA-256：`ee6e59a251648e489767f92a0c42c742a5bc05002a286106cc3b46ad7f1ff897`。桌面运行时冒烟因已有 SHIYIN AI 实例占用而跳过，DWPose 真实人物下载冒烟因未提供输入素材而跳过。
- 模块 5 完成经典与智能无限画布 500/1000/1500/2000 八档真实媒体生产环境性能验收；1000 节点以上按连续操作与主观丝滑的 A 档执行，直接操作 p95 `<=150 ms`、p99 `<=200 ms`，不得出现 `>100 ms` 主任务或二次冻结。
- 智能 2000 首次冷启动结构操作允许一次 `<=350 ms` 初始化 Long Task（实测 331 ms），稳态粘贴 p95 72.3 ms、批量处理 p95 86.8 ms/p99 87.4 ms；结构事务、撤销/重做、保存重载、真实图片、端口和图标均保持正确。
- 经典图片节点加载改为节点级几何回填，智能 Composer 改为 600ms 静默防抖 + requestIdleCallback，智能批量处理改用结构历史事务，避免大画布创建/粘贴同步深拷贝整张画布。

- 模块 4 完成经典与智能无限画布真正增量渲染：创建、粘贴和删除少量节点时按差异维护节点、端口、矩形、连接 DOM 与模型索引，普通连接不再重建完整连接层，选择高清图、媒体绑定和小地图只处理受影响节点。

- 智能普通连接获得稳定 ID，局部更新 path、hit、dot 与 cut；智能分组、历史线和运行中复杂拓扑会输出明确原因并回退完整 SVG，索引不一致时双画布均记录警告后完整 render。

- 修复智能分组成员创建、删除、撤销和重做期间 `group.items` 与 owner 索引不同步，以及事务字段替换撤销方向错误；真实 Chromium 500 节点/1000 连线连续历史最终可完整回到基线。

- 新增 13 项增量渲染专项契约；最新全部画布回归 186 项通过，真实大画布创建、粘贴、删除、撤销/重做与复杂拓扑回退均保持功能一致。

- 模块 3 增加无限画布增量撤销历史：创建、粘贴、导入和替换等纯结构操作使用事务记录节点/连线差异与选择状态，经典历史保留 16 MiB 双栈限制；异常会自动回退未完成事务，避免整张画布同步深拷贝。

- 优化经典与智能无限画布复制：节点直接从现有索引读取，连接只访问选中节点的邻接集合；复制组节点会递归包含全部成员，粘贴后组成员 ID 完整重映射，不再扫描整张节点和连接表。

- 优化经典画布粘贴：只校验本次新增连接，并只同步新增连接影响的生成器；已有连接和无关生成器不再重复全量处理。

- 移除节点粘贴固定 90 ms 等待，改为真实剪贴板事件优先、同一事件周期 0 ms 回退；系统图片、视频、音频和智能素材收件箱继续优先于内部节点粘贴，并避免重复触发。

- 新增 8 项复制粘贴性能契约；完成经典与智能单节点、多选、组节点和系统 PNG 的真实 Chromium 验证，所有节点粘贴在 150 ms 后均无二次增长，复制组不引用旧成员。

- 优化经典无限画布右键和节点菜单的显示热路径：创建、连线、端口、图片和输出菜单只刷新自身图标，不再扫描整页 DOM；该成本不再随画布节点数量放大。

- 经典创建菜单由两次同步布局读取降为一次最终矩形读取，继续保留单双列切换、视口夹紧和子菜单翻转；经典菜单、子菜单、图片悬浮功能栏及智能创建菜单移除高成本背景模糊，改用不透明主题背景。

- 新增 5 项菜单性能专项契约，并通过 51 项画布相关回归、双画布 JavaScript 语法检查及真实 Chromium 菜单显示验证；测试服务、QA 画布和临时目录已清理。

- API 设置新增 `Responses（原生）`协议、Responses 端点配置、模型列表获取和端点探测；上游未提供 `/v1/models` 时会保留手动填写模型的入口。

- AI助手节点支持通过 Responses 原生接口调用视觉模型：系统提示词转换为 `instructions`，文本与历史消息转换为 `input`，图片转换为 `input_image`，并兼容标准 `output_text` 响应解析。

- 原“LLM 节点”用户可见名称统一为“AI助手”，覆盖经典画布、智能画布、API 设置和相关中英文提示；内部节点类型保持兼容，不影响已有画布。

- 移除无限画布中的“一键创建电商工作流”节点组及组级输入/输出端口、拓扑运行逻辑；保留电商模特、产品、场景、A+ 图合成、电商视频五个独立节点，以及普通图片组能力。

- 修复电商工作流独立节点输入端口和图片上传容错。

- 经典画布中的 LLM 节点已统一更名为“AI助手”，并同步到工具栏、创建菜单、节点标题、连线创建菜单和相关提示；经典画布与智能画布均已移除循环节点的新建入口，同时保留历史循环节点运行兼容。

- 优化智能画布节点悬浮功能菜单首次显示：单击选中不再延迟 220ms，选择反馈与高清预览只更新受影响节点，菜单取消高成本背景模糊并刷新静态资源缓存版本。

- 修复灯光重塑与角度调整节点连接图片后预览不稳定、生成结果只出现在作品管理而不创建输出节点的问题；生成完成会自动创建并连接下游输出节点。
- 两个节点新增分辨率、质量、输出画幅和可选模型覆盖控制，并将参数实际传入图片生成任务。
- 角度调整预览固定参考图与主体在中心，仅移动摄影机轨迹和机位标记，不再随参数旋转参考图。

- 兼容 Windows 桌面 WebView2：无限画布输出节点“下载全部”在不支持 File System Access API 时改用原生文件夹选择器，选择一次目录后逐个写入真实文件，仍不生成 ZIP。

- 无限画布输出节点右键“下载全部”改为选择目标文件夹后逐个保存真实文件，不再生成 ZIP；素材库画布资产首次进入自动加载，并在画布数据变更后自动联动刷新。

- 修复作品管理和画布资产“打开目录”在 Windows 路径包含空格时错误打开“文档”目录的问题。Explorer 现在收到完整且兼容的 `/select,"真实文件路径"` 命令行，真实路径解析失败时仍返回明确错误，不再静默兜底到默认目录。

- 优化作品管理多选性能：拖拽过程中使用 `requestAnimationFrame` 和卡片增量样式同步，不再每次鼠标移动都重建图片/视频卡片与事件监听。
- 优化批量删除体验：删除请求期间保留作品列表并锁定批量按钮，完成后直接从当前列表移除已删除作品并同步数量，不再黑屏或依赖手动刷新。

- 作品管理新增显式“多选”开关和卡片复选框；开启多选后无需按 Shift 即可点击选择或拖拽框选，Shift 快捷方式保留并补充按键状态监听，避免桌面 WebView 丢失修饰键状态。

- 作品管理支持按住 Shift 拖拽框选多个作品，顶部显示“收藏、删除、移到回收站、取消选择”批量菜单。
- 批量“删除”会永久删除真实本地媒体文件并移除作品记录；“移到回收站”只标记状态、保留文件，二者行为明确分离。
- 修复作品视频预览非全屏状态按空格先触发全屏按钮的问题：预览对话框打开且视频可见时，空格直接播放/暂停，只有手动点击全屏按钮才进入全屏。
- 作品视频全屏预览支持按空格键播放/暂停；拦截全屏时的默认空格行为，避免页面滚动或一次按键重复切换。
- 修复作品预览中隐藏的图片层覆盖视频：`hidden` 图片不再被预览缩放样式强制显示，视频画面和播放控件可直接点击。
- 补强旧作品/画布资产的媒体类型识别：即使 URL 没有扩展名，只要展示文件名为 `.mp4` 等视频格式，也会使用视频播放器，不再错误渲染为破图。

- 修复作品管理与画布资产页面视频在桌面 WebView 中无法播放：本地/远程视频统一经后端流式代理，支持 Range 分片与正确 MIME 类型。
- 画布资产详情新增“打开目录”，按当前账号解析真实文件路径并调用系统文件管理器；远程同名文件不再被错误定位。
- 更新作品与资产脚本缓存版本，升级后立即使用修复代码。

- 覆盖安装不再弹出“目标目录已存在”确认框，安装器会直接进入安装流程。
- 覆盖安装继续只替换 `{app}\app` 程序目录，保留 `{app}\data` 中的 API 配置、数据库、画布与日志。

- 修复历史视频任务类型未被作品管理识别为视频的问题；作品管理与画布资产统一按实际媒体类型渲染并播放 MP4 等视频。
- 优化智能画布节点上方悬浮菜单的图标刷新范围，避免 460 个节点时点击操作触发全页面扫描造成数秒卡顿。

- 修复作品管理与画布资产中的远程视频无法播放：统一通过后端流式代理传输，兼容 Range 分片与桌面 WebView 的跨域限制；修复历史本机绝对媒体地址，并允许视频预览自动播放。

- 优化影视制作“生成线稿分镜”节点，复用批量处理能力，支持任意数量图片批量并行提交、逐图输出、失败状态和任务恢复。
- 新增影视制作“生成线稿分镜”节点：可连接视频帧/分镜图组，逐帧生成专业线稿分镜，并在节点内设置平台、模型、画幅、分辨率、质量和补充要求。

- 修复作品管理中历史作品与画布资产合并后沿用旧默认文件名，导致多个作品卡片显示相同文件名的问题；最终排序后按真实媒体名重新归一化，并对重复名称做唯一化处理。

- 修复作品管理中同批生成作品共用历史时间的问题，优先显示每个本地文件的真实修改时间，并按媒体文件名展示画布资产。
- 修复“打开目录”无法定位旧版 `exports/generated` 文件及 Windows 路径含空格时 Explorer 参数失效的问题。

- 作品管理与画布资产页统一显示图片、视频生成资产，支持电商与无限画布资产聚合。
- 两个资产页面新增图片/视频筛选与“最早优先/最新优先”排序，最新生成内容在“最新优先”模式下固定显示在顶部，设置会持久化保存。
- 统一 VERSION、npm、Tauri、Cargo 与后端运行时版本号，修复安装后更新器仍把桌面宿主识别为旧版本的问题。

- 修复安装器复用被自动化测试写入的临时目录，导致正式安装落到项目 `.build` 缓存并读取不到原用户 API 配置的问题。
- 正式安装器现在默认安装到 `D:\Program Files\SHIYIN AI`；只有历史目录中存在主程序或用户数据库、且目录不是测试缓存时，才会继续复用真实自定义安装目录。
- 安装与升级仍只替换 `{app}\app` 运行时目录，不删除 `{app}\data`，API 服务商、加密密钥、画布和其他用户数据继续原位保留。
- 安装器进度和更新器 E2E 测试新增正式注册项保护与测试注册项清理，避免测试再次改写用户安装位置或开始菜单入口。
- 全量 `706` 项 Python 测试、`42` 个子测试、Rust 更新器 `5` 项测试及双画布 JavaScript 语法检查通过。
