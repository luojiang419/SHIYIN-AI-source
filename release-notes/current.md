# SHIYIN AI v1.0.272

- 修复图片与视频生成完成后，输出节点可能遗留持续转圈占位卡的问题；任务成功、失败或取消后均会清理与任务不再匹配的等待状态。
- 新版首次启动会自动迁移历史画布：移除没有可恢复任务 ID 的旧输出占位，同时保留仍带 `canvasTaskId` 或 `recoverTaskId` 的真实待恢复任务；回收站画布也会一并处理，并通过迁移标记保证只执行一次。
- 调整影视视频节点的多镜头解析：只有连接到“分镜图”参考图输入端口的多张图片才会触发多镜头；演员、服装或道具端口的图片不再被误判为参考图。
- 完成目标回归测试、全量自动化测试、Windows 安装包构建、安装后端冒烟与安装产物 SHA-256 校验。
