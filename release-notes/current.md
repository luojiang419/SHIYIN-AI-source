# SHIYIN AI v1.0.228
- 软件设置新增完整快捷键管理，可搜索和分类查看 64 项无限画布动作，支持直接录入、清空、单项恢复默认和全部恢复默认。
- 12 种当前可见节点创建，以及画布、视图、面板、选中素材、分组、预览和编辑器功能全部接入统一快捷键引擎；修改后立即同步到已打开画布。
- 快捷键支持单键、Ctrl / Shift / Alt 组合键和按住型动作；同作用域冲突、系统保留组合键和输入框误触会被自动拦截。
- 补齐画布恢复栈，`Ctrl+Shift+Z` 现在可以真实恢复撤销操作；Ctrl、Space 和 R 临时工具操作均可自定义。
- 隐藏的遗留角度节点继续保持兼容但不重新开放；全量自动化测试 `584 passed`、`42 subtests passed`，桌面与窄屏视觉验收通过。

# SHIYIN AI v1.0.227
- 普通画布与智能画布的批量处理悬浮菜单和节点顶部标题统一显示中文“批量处理”。
- 移除批量导入和本地路径导入的 20 张数量截断，支持一次导入更多图片、视频和音频；单文件大小校验保持不变。
- 批量导入后按 4:3 比例自动适应网格，48 张素材会排列为 8 列 6 行；本地路径导入也会自动排列并创建图片分组。
- 普通画布 GROUP、智能画布分组及多图节点标题旁显示当前图片数量，方便用户核对导入结果。
- 全量自动化测试 `576 passed`、`42 subtests passed`，双脚本语法、Python 编译和 CSS 构建通过。

# SHIYIN AI v1.0.226
- 普通画布与智能画布的批量处理节点统一显示中文标题“批量处理”，新建节点和历史节点均不会再显示 `CANVAS.BATCHPROCESS` 等翻译键名。
- 批量拖入图片、视频和文件夹时按文件名进行稳定自然排序，数字序号遵循 1、2、10 的直觉顺序；本地路径导入和上传结果也保持一致。
- 普通画布批量拖入媒体后复用既有整理算法，按节点真实宽高计算动态行列间距，并在整理完成后生成分组边界，消除不同尺寸图片/视频节点的局部重叠。
- 新增双画布排序、中文标题、动态尺寸排列和分组创建顺序专项回归；全量测试 573 项及 42 个子测试通过。

# SHIYIN AI v1.0.225
- 普通画布与智能画布的“创建三视图”统一输出 5 张资产：纯三视图 16:9 横板、独立 3:4 细节板，以及正面、侧面、背面三张 9:16 单视图。
- 16:9 横板严格只保留同一主体正面、标准侧面、背面三栏全身图；头发、面部、面料、剪裁、口袋、鞋包与配饰等内容改由独立细节板生成。
- 生成占位、输出顺序、文件名、重启任务恢复、节点状态和双画布预览均扩展为 5 槽；新增提示词隔离与画幅专项回归。

# SHIYIN AI v1.0.214
- 创建三视图节点在普通画布和智能画布中新增图片平台、模型、1K/2K/4K 与质量参数，节点可独立控制生成；智能画布从来源节点继承设置。
- API 设置页支持所有平台拖拽排序，列表靠前项为首选；禁用、缺模型、缺地址、缺密钥或本地 CLI 不可用时，图片任务提交前自动顺延到下一项。
- 移除图片生成节点对时影平台的硬编码偏好，前后端统一使用配置顺序；任务响应会返回并回写实际使用的平台和模型。
- 完成 546 项自动化测试、42 项子测试、JavaScript/Python 语法检查和 CSS 构建。

# SHIYIN AI v1.0.213
- 三视图产品输入拆分为上装正面/侧面/背面与下装正面/侧面/背面 6 个端口，普通画布与智能画布保持一致。
- 移除必须连接产品正面、侧面、背面的限制；任意一张模特或产品图片即可作为起点，缺失视图由提示词规则自动扩展。
- 为每张参考图附加端口语义、参考编号与使用指令，并将映射写入生成提示词，避免上下装和角度引用混淆；兼容旧版三产品端口连接并自动拆分。
- 12 个输入端口使用更充足的紧凑节点高度，完成双画布回归测试与版本缓存更新。

# SHIYIN AI v1.0.212
- 三视图节点输入区改为单列逐行布局，9 个端口与对应说明文本严格对齐，移除三列排列造成的下方大面积空白。
- 新节点默认采用紧凑高度；历史无输出的 720 高度节点会自动收缩，生成 4 张资产时再保留预览所需空间。
- 完成普通画布与智能画布单列端口对齐专项回归和缓存版本更新。
- 优化普通无限画布与智能画布的三视图节点：9 个输入端口按“模特主体 / 产品角度 / 细节与配饰”分组，每个端口显示对应文字、连接提示和无障碍标签。
- 生成三视图按钮固定在节点右下角，横版模卡与三张竖版视图预览保持清晰层级；新增响应式节点布局和多排悬浮菜单样式，窄屏也不会遮挡节点。
- 新增双画布三视图布局、端口标签和悬浮菜单专项契约测试；JavaScript 语法检查及相关回归测试通过。
- 修复普通无限画布选中图片节点后，顶部悬浮功能菜单缺少“创建三视图”的问题；智能画布与普通画布现在均可进入该功能。
- 点击“创建三视图”会新建多视图节点，并将当前图片自动连接到“模特正面”；节点提供模特/产品正侧背、正背细节与可多接配饰共 9 类语义输入端口。
- 生成时按正面、侧面、背面自动组合对应主体、产品、细节与配饰，并并行输出一张 16:9 三视图模卡和三张 9:16 独立视图，共 4 张资产图。
- 完成普通画布真实页面点击、自动连线、9 端口与无控制台错误验收；全量 532 项测试及 42 个子测试通过。
- 统一源码、桌面端、安装包和 GitHub Release 的版本来源，发布流程改为严格读取根目录 `VERSION`，不再在云端隐式递增版本号。
- 多视图三视图节点生成横版模卡与正面、侧面、背面三张竖版资产。
- 修复局域网与内网穿透普通账号调用 MiniMax H3 连续返回 502：SHIYIN 后端不再通过公网 FRP 地址绕回本机，内置 H3 自动改用同机 `127.0.0.1:7860`；旧版保存的 `115.231.35.105:7866` 配置会自动迁移，自定义远程 H3 地址仍保留。
- 无限画布与智能画布新增 H3 就绪状态检查，生成前会明确显示本地 H3 是否可用；服务未启动时提示联系本机管理员启动 H3 控制面板，不再让同事反复提交后只看到模糊的 502。
- 已登录普通账号可读取 H3 状态、可灵真实模型并调用视频生成；可灵 CLI 安装和登录继续仅限本机管理员，远程同事无法修改主机 CLI 配置。
- 使用隔离数据目录完成普通账号真实验收：可灵请求通过权限层并到达模型校验，H3 以 1 秒、4 steps 真实生成成功并回传视频；全量 465 项测试与 38 个子测试通过。

- 普通无限画布的图片生成与视频生成节点新增内嵌提示词输入框，可直接输入后生成，也可继续连接提示词节点；两者同时存在时按节点内输入、上游输入顺序组合。
- 选中图片节点或 Output 中的某张图片后，图片上方会显示快捷操作栏，可快速预览、编辑、宫格切分、继续生成图片/视频、创建全景/多角度/灯光/动作提取节点或下载。
- 普通无限画布顶部 15 个功能入口默认完整双排展示，不再依赖横向滚动；新增“收纳”按钮，可保留全部入口并切换为图标单排，状态自动记忆。
- 新增三组前端交互契约测试；全量 457 项测试与 38 个子测试、CSS 构建、API 冒烟和 v1.0.177 Tauri release 宿主编译通过。

- 普通无限画布与智能无限画布新增“一键复刻”节点：通过“动作参考”和“目标图”两个输入端口接图，不再在节点内部重复上传素材。
- 动作参考接入后会自动复用本地 DWPose 提取姿势骨架，并以“动作参考 / 姿势骨架 / 目标图”三栏预览展示；生成引用按骨架、动作原图、目标图顺序提交。
- 每次点击“一键复刻”都会立即创建独立输出节点并提交可恢复任务；上一次仍在生成时按钮保持可点击，支持连续抽卡和重启续查。
- 完成经典与智能画布真实页面验收；JavaScript 语法、CSS 构建、397 项自动化测试、38 个子测试及桌面端 `cargo check` 通过。

- 修复智能画布中的 720° 取景器连接下游节点后，点击画布空白区域会闪黑一次的问题；添加人偶后的全景预览同样保持稳定。
- 取消节点选中改为局部更新选中样式、连线高亮与底部输入区，不再销毁并重建全景 WebGL 上下文。
- 智能画布脚本缓存标记已更新；JavaScript 语法检查、全量自动化测试 344 项及 38 个子测试通过。

- 修复升级后作品管理中的历史图片全部显示损坏：`/assets/output/` 地址现在会正确映射到当前账户的生成媒体目录，数据库记录和磁盘图片无需迁移或重建。
- 作品索引中的旧版 `localhost` / `127.0.0.1` 绝对媒体地址会在返回页面前自动归一为当前登录来源，继续兼容升级前保存的历史记录。
- 新增历史生成图片路由和旧地址归一回归测试；作品管理专项测试 19 项、全量自动化测试 343 项及 38 个子测试、API 冒烟、前端语法、桌面端编译和安装包静态校验通过。

- 修复 DWPose 导入高像素手机/相机照片后长期停在“正在提取”的问题：安全硬上限内的图片会自动等比缩放到适合 CPU 推理的尺寸，不再因超过 2500 万像素直接失败。
- 修复 DWPose 同一输入失败后随节点重绘无限自动重试的问题；现在会保留明确错误状态，只有用户主动重试或输入变化才重新运行，避免连续 `413` 请求风暴。
- 骨架结果使用后端返回的实际输出宽高，避免大图缩放后节点保存错误尺寸；特殊节点缓存提升为 `special-nodes.6`。
- 新增打包后端真实人物发布门禁，覆盖已有模型升级复用和空目录首次下载两种环境；本机从 1.0.151 升级到 1.0.154 后，两份模型哈希、大小和时间戳保持不变，实际安装目录处理 5464×8192 问题图用时 1.769 秒并识别 1 人。
- 全量自动化测试 341 项、38 个子测试通过；API 冒烟、三个画布脚本语法、桌面端编译、完整安装包构建和安装产物校验通过。

- 修复升级到多账号版本后，电商专用页面历史任务图片因 `localhost` / `127.0.0.1` 登录态不互通而显示损坏的问题；历史本机媒体地址会自动归一为当前登录来源。
- 电商历史任务若原生成文件缺失但默认导出副本仍存在，会自动回退到导出副本；任务仍在引用的媒体也不会再被孤儿文件清理误删。
- 修复 Blender 3D 导演节点渲染的视频无法拖入画布的问题；视频缩略图和点击播放后的真实视频元素现在都携带标准媒体拖拽数据，拖入后会创建视频节点，图片拖拽保持兼容。
- 新增电商升级兼容、导出副本恢复、任务媒体保留与 Blender 视频拖拽回归检查；全量自动化测试 336 项、38 个子测试通过，API 冒烟、前端语法与桌面端编译检查通过，版本递增为 `1.0.153`。

- 修复升级后首次使用 DWPose 时模型仍在后台下载，节点会直接失败且模型就绪后不会恢复的问题；现在会显示准备进度，并在模型就绪后自动继续提取，无需重复导入或手动重试。
- 新增登录用户可读取的脱敏 DWPose 准备状态，只返回状态、进度和用户可读提示；模型路径、文件清单及下载诊断继续仅管理员可见。
- 特殊节点脚本缓存版本提升为 `special-nodes.5`；真实延迟就绪回归已验证从 35% 准备态自动恢复并输出 768×1024 骨架图；全量自动化测试 333 项、38 个子测试通过，API 冒烟与桌面端 `cargo check` 通过。

- 修复 720°取景器随画布选中、点击和重绘后偶发黑屏的问题：旧 WebGL 纹理、缓冲、程序和上下文会在节点替换前主动释放，浏览器回收上下文后可自动重建并恢复全景纹理。
- DWPose 动作提取节点新增“导出骨架图”；点击后会在右侧自动创建带骨架参考图的专用输出节点并自动连线，可直接继续连接下游。重复导出复用专用节点，不覆盖用户已有的其他输出。
- 特殊节点脚本缓存版本提升为 `special-nodes.4`；完成真实页面 24 次交替点击重绘回归，未出现新增运行期错误；全量自动化测试 331 项、38 个子测试通过。

- 普通无限画布与智能无限画布同步新增“灯光重塑”节点。节点以生成式重打光方式工作，支持上、下、左、右、正面、逆光和轮廓光方向，并可调整色温、强度、柔硬光与自然、影棚、电影、日落、夜景、霓虹氛围。
- 灯光重塑沿用所在画布的默认图片 API 平台和模型，只提交一张源图参考并自动保持源图宽高比；生成结果以标准图片引用回写节点，可继续连接下游图片、视频、LLM、循环和输出节点。
- 用户调整灯光参数时不会自动调用 API，只有点击“生成重塑图”才会提交任务；源图或参数变化后会自动清除不再匹配的旧结果。
- “角度调整”暂不进入本版新建菜单；保留历史画布兼容代码，等待可靠机位后端方案完成后再开放。
- 完成双画布真实页面创建与交互验收；全量自动化测试 329 项、38 个子测试通过，桌面端 `cargo check` 通过。

- 普通无限画布与智能无限画布同步新增 720°取景器节点。产品交互使用“720°”名称，底层采用兼容主流全景工具的标准 2:1 等距柱状全景图，可直接导入，也可调用已配置的图片模型生成完整球面场景。
- 取景器支持拖动改变水平角和俯仰角、滚轮调整 FOV，并提供 16:9、9:16、1:1、4:3、3:4 取景画幅与多档导出尺寸。
- 用户可在选定视角中添加、移动和缩放人偶占位；导出时会把透视视图与人偶合成为一张位置参考图，并作为普通图片引用连接到下游图片、视频或提示词节点。
- 两种画布同步新增 DWPose 动作提取节点。连接或导入人物图片后自动调用本地 DWPose，输出黑底彩色身体、手部和面部骨架图；上游图片变化会触发重新提取，结果可继续连接下游作为姿势参考。
- 新增共享节点样式和双画布特殊节点回归合同；全量自动化测试 323 项、38 个子测试通过。

- 新增 DWPose 本地姿态能力：使用 YOLOX 与 RTMPose ONNX 模型在 CPU 上识别多人身体、手部和面部关键点，不依赖云端 API 或独立显卡。
- DWPose 模型不塞入安装包；软件首次启动在后台自动补齐到 `data/system/models/dwpose`，优先腾讯国内镜像，失败后依次尝试 Windows 系统代理和 Hugging Face 官方源，升级后继续复用。
- 模型下载支持断点续传、临时文件、大小与 SHA-256 双重校验、原子安装和并发去重；国内压缩包安装成功后自动删除，避免重复占用空间。
- 本机管理员页面新增 DWPose 下载状态、进度、来源、失败原因和重新下载按钮；普通局域网账号可以使用本地姿态检测，但不能查看模型目录或管理下载。
- 安装态内置 NumPy、OpenCV Headless 和 CPU ONNX Runtime，移除无关 CUDA/TensorRT 运行库；DWPose/MMPose Apache-2.0 许可与改编说明随安装包分发。

- 局域网 Web 普通账号扩展为中文、英文字母大小写和数字，英文字母大小写视为同一账号；密码仅要求非空，不限制长度或复杂度，首次访问仍可自动注册。
- 已有纯数字账号和独立数据目录会自动兼容升级，每个普通账号继续拥有独立数据库、画布、素材、生成资源、任务和实时同步空间。
- 普通账号的数据归档到 `data/accounts/<内部ID>` 专属目录，HTTP 文件访问、生成任务和 WebSocket 广播均按账号隔离。
- 普通局域网账号只能修改主题和显示语言；API 地址、密钥状态、API 设置、软件设置、运行路径和更新功能均不可见且后端拒绝越权访问。
- 新增仅限安装软件本机使用的管理员页面，管理员 `jiang` / `jiang` 可查看原账号和原密码、修改或重置账号密码、启用或禁用账号，并浏览每个账号的生成媒体、导出文件、素材、作品、画布和项目。
- 普通无限画布新增多宫格图片自动识别，可按识别结果拆分，并通过横纵标尺精确调整裁切边界。
- 新增 Blender 3D 导演节点，支持自动发现或启动 Blender、无配对码本地鉴权、相机参数控制、静帧与动画渲染回传。
- Windows 安装器可选安装或更新 Blender 连接插件，并保持现有用户数据和配置不变。
- 普通画布、画布列表和智能画布全面移除旧蓝黑主题来源，节点、输入框、媒体缩略图、弹窗、滚动条、焦点环和交互状态统一为中性灰与品牌暖棕。
- 三个画布页面不再加载旧 `theme.css`，并增加新的静态资源缓存版本，避免升级后继续显示旧蓝色样式。
- 新增严格色相回归检查，自动阻止蓝色、青色、靛蓝和低饱和蓝灰色重新进入画布界面。
- 完整 Windows 安装包继续执行后端健康、单实例、父子进程退出和安装产物版本校验；本版全量自动化测试 316 项、38 个子测试通过，并完成账号隔离、管理员页面、DWPose 自动下载、真实模型推理和电商 6 类操作回归。
- 创建三视图节点点击生成后立即在输出端创建 4 个带转圈动画的占位卡片，任务完成按模卡/正面/侧面/背面逐张替换；输出节点使用 3 列网格布局，智能与普通无限画布同步显示生成进度。
- 创建三视图节点内部不再显示生成结果，图片只保留在右侧输出节点；节点默认最小尺寸固定容纳 12 个输入端口、生成参数和操作按钮，生成按钮采用底部固定布局。
- 三视图输出节点改用普通图片输出节点的缩略图规格，自动清除旧版 700×620 固定尺寸并按图片数量自然撑高，四张图片无需拖拽缩放即可完整预览。
