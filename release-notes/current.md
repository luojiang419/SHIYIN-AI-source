# SHIYIN AI v1.0.280

- 修复普通无限画布“整理选中”把无连线的同类型节点拆成多个单节点分量、最终排成超长单列的问题。
- 孤立同类型节点现在会先合并为布局批次，再依据节点真实宽高整理成紧凑网格；已连接节点流继续使用原有拓扑层级布局。
- 网格优先选择紧凑的完整矩形，支持 `15→5×3`、`8→4×2`、`4→2×2`、`30→6×5`，无法整除时回退到近似 `4:3`。
- 新增真实 JavaScript 函数执行与坐标级回归；全量 `706` 项 Python 测试、`42` 个子测试通过，双画布 JavaScript 语法检查、Tauri `1.0.280` release 宿主和 Windows 完整安装包构建通过。
