# SHIYIN AI v1.0.274

- 修复普通无限画布“视频生成”节点选择可灵 CLI 后，6 秒等 Video 3.0 合法时长仍被旧 `5/10 秒`枚举拒绝的问题。
- 普通视频生成节点切换到可灵 CLI 时默认选择 Video 3.0 Omni，与影视“生成视频”节点保持一致；Video 3.0、3.0 Omni、3.0 Turbo 均提供完整的 `3–15 秒`整数选项。
- 时长选项改为按当前模型与文生/图生模式动态展示；Video 2.5/2.6 继续保留真实的 `5/10 秒`限制，避免界面允许模型实际不支持的参数。
- 后端为 Video 3.0 系列兼容旧能力缓存，并在加载旧画布或切换模型时修正无效保存值，确保界面、节点数据与 CLI 提交参数一致。
- 新增 3.0 旧能力缓存、6 秒 CLI 参数透传、旧模型限制隔离和普通视频节点默认模型回归；全量 696 项测试及 42 个子测试通过。
