# SHIYIN AI v1.0.275

- 修复软件设置中自定义快捷键保存后，普通无限画布仍执行写死按键、导致改键和禁用操作无效的问题。
- 普通画布与智能画布统一读取同一份快捷键配置，并支持设置变更即时同步、刷新后持久化、单项禁用与恢复默认。
- 修复 Web 页面和桌面 WebView 切换侧栏后焦点停留在父窗口、画布 iframe 收不到快捷键的问题；仅当前激活画布监听父窗口，切页或隐藏时自动卸载。
- 普通画布补齐按配置驱动的临时工具、动态快捷键说明和真实重做历史；输入框、编辑框及弹窗继续阻止快捷键穿透。
- 更新快捷键相关静态资源缓存版本，并新增双画布动作分发、父窗口监听生命周期、消息转发和缓存失效回归检查。
- 全量 `702` 项 Python 测试及 `42` 个子测试通过，Rust 桌面宿主 `8` 项测试通过，Tauri `1.0.275` release 宿主编译完成。
