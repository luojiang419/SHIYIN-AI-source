export type CanvasBackgroundMode = "blank" | "dots" | "lines";

const theme = { canvas: { background: "transparent", dot: "transparent", line: "transparent" } };
export const canvasThemes = { light: theme, dark: theme, "pure-white": theme };
