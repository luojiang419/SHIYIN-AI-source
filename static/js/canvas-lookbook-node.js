(function(){
    'use strict';
    const TYPE = 'lookbook';
    const DEFAULT_STYLE_ID = 'auto';
    // 预设来自对公开作品中“参考图优先、动作/情绪先行、系列化输出”方法的抽象，
    // 不复制任何博主画面、品牌、文案或第三方 Skill。
    const STYLES = [
        {id:'auto',name:'自动',description:'视觉模型先解析参考图与需求，再自主选择最合适的创作类型',prompt:'Autonomous visual direction. First inspect every supplied reference for people, products, scene, material, pose, light, color, narrative and physical relationships. Combine those facts with the user brief, choose the single most suitable built-in lookbook style, explain the choice internally, then execute that style with strict reference ownership and series continuity. Do not average styles or fall back to generic fashion defaults.'},
        {id:'fw-cream-cyan-film',name:'2026 FW 奶油青蓝胶片抓拍',description:'自然直射阳光、空气透亮、奶油黄与青蓝互补色、不规则粗颗粒和真实人物微动作',prompt:'2026 FW fashion-film visual system derived from the supplied reference images: real unfiltered afternoon sunlight with clear airy transparency, warm directional highlights, translucent hair and fabric, natural specular glints and soft-edged cast shadows opened by cool sky bounce. Use warm ivory, cream and pale lemon-yellow as the dominant palette, turquoise and faded cyan architectural accents as the controlled complement, warm-neutral truthful skin, lifted but structured blacks, open midtones and creamy highlight roll-off. Use the supplied scene as the exact set—preserve its recognizable geometry, dominant paint colors, storefront or kiosk details and printed graphics; change only camera position, crop and natural exposure, never relocate to a beach, cafe, generic street or studio. Photograph the supplied person, product and scene as one lived-in world with tilted or rotated off-axis framing, non-centered composition, human eye or waist-height handheld viewpoint, foreground rails/doorframes/fabric occlusion, occasional environmental wide shot alternating with intimate medium and tactile detail. Direct relaxed micro-actions—reclining, leaning, turning, looking down, eyes briefly closed, soft smile, resting a hand, adjusting knit or touching the environment—so emotion reads through breath, gaze, fingers and weight shift instead of a catalog pose. Ordinary imperfect faces are welcome: preserve freckles, pores, flyaway hair, uneven smiles and small asymmetries instead of airbrushing. Finish with genuinely irregular coarse 16mm/35mm film grain—visible clumps of different sizes, random spacing, variable density and slight non-uniform RGB response, never a regular grid, checkerboard or uniform digital noise—plus restrained print-scan texture and barely perceptible warm halation only on bright edges. Keep sharpening low and motion softness natural. Each generated output is one full-bleed photograph only; never a contact sheet, grid, split screen, storyboard or collage. Keep a four-image arc across separate outputs: sunlit establishing place, observed gesture, private pause, tactile fabric/product proof; change distance and decisive moment while maintaining the same light and color script.'},
        {id:'candid-lifestyle',name:'生活化真实抓拍',description:'通用真实感基底：自然动作、环境互动与系列一致性',prompt:'Reference-first candid lifestyle photography. Treat supplied people, products and locations as factual anchors; infer a concrete lived-in situation even when no written brief is provided. Use relaxed micro-expressions, natural weight shifts, eye-lines, walking, leaning, adjusting clothing and environmental contact. Preserve identity, wardrobe, product facts, skin, hair and fabric texture. Prefer motivated daylight filtered through leaves or thin cloud, broad bounced fill, lifted blacks, open midtones, creamy highlight roll-off, warm honey highlights, quiet olive shadows, fine organic film grain and subtle analog halation. Keep one photographic world with believable perspective, occlusion, contact shadows and restrained imperfections. Build a series arc from sunlit place to human gesture to private pause to tactile detail; vary framing and decisive moments across the series. Never use static catalog posing, isolated cutouts, collage, HDR contrast or a synthetic studio backdrop.'},
        {id:'multi-person-interaction',name:'多人互动纪实',description:'两人及以上的关系、视线和共同动作，避免各自摆拍',prompt:'Create a documentary-style fashion series with every supplied person kept as a distinct identity. Stage observable relationships: conversation, shared gaze, listening, touch, passing an object, walking together or reacting to the same environment. Preserve each person’s face, skin tone, hair, body proportions, wardrobe and accessories; do not merge faces, swap clothing or duplicate limbs. Use believable distance, occlusion, scale, contact shadows and a specific shared location. Capture imperfect decisive moments rather than frontal posing, and vary camera distance and angle across images.'},
        {id:'single-person-emotion',name:'单人情绪时装',description:'一个人物的情绪、呼吸和细微动作驱动画面',prompt:'Create an intimate single-person fashion editorial driven by a clear emotional beat rather than a pose. Preserve the supplied person’s identity, face, skin, hair, body proportions, existing wardrobe and accessories. Show a readable inner state through gaze, breath, hands, posture, turning, waiting or interacting with the environment; keep micro-asymmetry and natural skin/fabric detail. Use layered foreground, a specific time and light, and varied close, medium and environmental frames. Avoid mannequin stillness, beauty-plastic skin and generic studio gradients.'},
        {id:'sports-dynamic',name:'运动动态',description:'奔跑、跳跃、对抗和瞬间定格，强调速度与身体逻辑',prompt:'Create a high-energy sports fashion campaign from the supplied references. Preserve each athlete’s identity, body anatomy, outfit, footwear, product details and team/brand marks. Build physically coherent action with anticipation, acceleration, landing, balance, grip, sweat, fabric tension, motion blur and decisive freeze moments; camera angle and shutter character should support the movement. Keep the real field, court, street or snow environment integrated with correct scale, shadows and perspective. No impossible joints, duplicated limbs, floating feet or static catalog stance.'},
        {id:'casual-friends',name:'休闲朋友随拍',description:'朋友之间松弛、自然、不看镜头的日常片段',prompt:'Create a relaxed lifestyle series of friends in an ordinary but visually specific place. Keep every supplied person recognizable and separately dressed; preserve existing clothing, accessories, skin, hair and body proportions. Prefer candid shared attention, teasing, walking, sitting, eating, taking a photo or helping one another over smiling at camera. Use available light, lived-in surfaces, partial occlusion, imperfect framing and small gestures to make the frame feel observed rather than staged. Maintain location and color continuity across the series; no studio cutouts or collage.'},
        {id:'street-film',name:'街头电影叙事',description:'城市层次、硬切式瞬间与真实街拍电影感',prompt:'Create a street-film fashion story in a concrete, recognizable urban location. Use the supplied references as factual anchors, then design a short visual narrative with a beginning state, a decisive action and a changed emotional beat across the series. Favor layered architecture, reflections, foreground passersby, available light or motivated flash, restrained grain and purposeful camera movement. Preserve identity, wardrobe, product geometry and readable marks. Avoid generic neon, empty backdrops, stock smiles and over-processed HDR.'},
        {id:'travel-dream',name:'旅行环境梦境',description:'南法、海岸、老街等旅行场景中的轻盈浪漫与空间感',prompt:'Create a travel-editorial fashion series grounded in the supplied location reference. Preserve recognizable architecture, climate, horizon, materials and local light while placing the supplied person or group naturally inside it. Use wind, walking, resting, looking outward, touching a railing or sharing a view to create a quiet narrative. Keep skin and garment color truthful, with airy but controlled grading, depth and tactile atmosphere. The result must feel photographed on location, not pasted over a postcard or replaced by a generic resort.'},
        {id:'product-story',name:'商品情境叙事',description:'鞋、包、表、眼镜、首饰等商品在真实使用场景中的质感',prompt:'Create a product-led fashion story using the supplied SKU as the sole source of product truth. Preserve exact silhouette, color, material, stitching, hardware, logo and readable text. Show the product being worn, held, used or resting with believable hand contact, weight, reflections and environmental shadows; connect it to a real person and specific place instead of a floating cutout. Use tactile close-ups mixed with wider lifestyle frames and keep the product consistent across the series. No generic replacement SKU, plastic surfaces or empty showroom render.'},
        {id:'pet-fashion',name:'宠物时尚实拍',description:'人物与宠物的服饰大片，强调接触、牵引和真实互动',prompt:'Create a warm fashion editorial with the supplied person, pet and wardrobe references. Keep human and animal identities, anatomy, fur/hair, clothing, collars, leashes and product details accurate. Build real interaction—walking, holding, grooming, eye contact, shared attention or gentle touch—with correct scale, grip, contact shadows and motion. Use an actual environment and varied documentary framing; avoid posed cutouts, fused limbs, duplicated animals, plastic fur or a generic studio background.'},
        {id:'material-closeup',name:'材质细节特写',description:'织物、皮革、金属、玻璃与肌理的近距离艺术观察',prompt:'Create an art-directed detail series that makes supplied material references tangible. Preserve exact weave, grain, pores, stitching, scratches, translucency, gloss and color; pair macro details with enough surrounding context to prove scale and use. Use motivated light, shallow depth of field, real contact, restrained grain and deliberate negative space. If a person or product is present, keep identity and geometry truthful. Avoid smoothing, CGI plastic, invented textures and meaningless abstract gradients.'},
    ];
    const esc = value => String(value == null ? '' : value).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#39;');
    const clamp = (value,min,max) => Math.max(min,Math.min(max,Number(value)||min));
    const outputUrl = item => typeof item === 'string' ? item : String(item?.url || item?.path || item?.src || '');
    const stored = () => { try { const value = JSON.parse(localStorage.getItem('lookbook_styles') || '[]'); return Array.isArray(value) ? value : []; } catch(_) { return []; } };
    const saveStored = value => { try { localStorage.setItem('lookbook_styles', JSON.stringify(value)); } catch(_) {} };
    const styles = () => [...STYLES,...stored()].filter((item,index,list) => item?.id && list.findIndex(other => other.id === item.id) === index);
    function imageProviders(){ return typeof window.imageApiProviders === 'function' ? window.imageApiProviders() : []; }
    function imageModels(providerId){ return typeof window.allImageModels === 'function' ? window.allImageModels(providerId) : []; }
    function syncGenerationSelection(node){
        const defaults = typeof window.defaultImageGenerationSelection === 'function' ? window.defaultImageGenerationSelection() : {};
        const resolveProvider = typeof window.resolveImageProviderId === 'function' ? window.resolveImageProviderId : value => String(value || '');
        const providers = imageProviders();
        const requestedProvider = String(node.apiProvider || defaults.providerId || providers[0]?.id || '').trim();
        node.apiProvider = resolveProvider(requestedProvider);
        const models = imageModels(node.apiProvider);
        if(!node.model || (models.length && !models.includes(node.model))) node.model = models[0] || String(node.model || '');
        return {providerId:node.apiProvider,model:node.model,providers,models};
    }
    function choiceProviderLabel(providerId,providers){ const provider=(providers||[]).find(item=>item.id===providerId); return provider?.name || provider?.id || providerId || '暂无平台'; }
    function choiceModelLabel(model){ return model || '暂无模型'; }
    function generationChoicesHtml(node){
        const selected = syncGenerationSelection(node);
        const providerItems = selected.providers.length ? selected.providers.map(provider => `<button type="button" class="image-quick-choice-item ${provider.id===selected.providerId?'active':''}" role="menuitem" data-lookbook-provider-value="${esc(provider.id)}"><span>${esc(provider.name||provider.id)}</span>${provider.id===selected.providerId?'<i data-lucide="check"></i>':''}</button>`).join('') : '<button type="button" class="image-quick-choice-item empty" disabled>暂无图片平台</button>';
        const modelItems = selected.models.length ? selected.models.map(model => `<button type="button" class="image-quick-choice-item ${model===selected.model?'active':''}" role="menuitem" data-lookbook-model-value="${esc(model)}"><span>${esc(model)}</span>${model===selected.model?'<i data-lucide="check"></i>':''}</button>`).join('') : '<button type="button" class="image-quick-choice-item empty" disabled>暂无图片模型</button>';
        return `<div class="lookbook-generation-settings" data-lookbook-generation-settings><div class="image-quick-choice image-quick-choice-provider" data-lookbook-choice="provider"><input type="hidden" data-lookbook-provider value="${esc(selected.providerId)}"><button type="button" class="image-quick-select image-quick-choice-trigger" data-lookbook-choice-trigger="provider" aria-haspopup="menu" aria-expanded="false" title="图片生成平台"><span>${esc(choiceProviderLabel(selected.providerId,selected.providers))}</span><i data-lucide="chevron-down"></i></button><div class="image-quick-choice-panel" data-lookbook-choice-panel="provider" role="menu">${providerItems}</div></div><div class="image-quick-choice image-quick-choice-model image-quick-model" data-lookbook-choice="model"><input type="hidden" data-lookbook-model value="${esc(selected.model)}"><button type="button" class="image-quick-select image-quick-choice-trigger" data-lookbook-choice-trigger="model" aria-haspopup="menu" aria-expanded="false" title="图片生成模型"><span>${esc(choiceModelLabel(selected.model))}</span><i data-lucide="chevron-down"></i></button><div class="image-quick-choice-panel" data-lookbook-choice-panel="model" role="menu">${modelItems}</div></div></div>`;
    }
    let pickerNode = null, pickerOptions = null;
    const actionBindings = new WeakMap();
    let actionDelegationInstalled = false;
    function activateAction(event){
        // 生成动作只接受 click。pointerdown/mousedown/pointerup 同时绑定会在
        // 节点重绘或长按时重复提交同一个 Lookbook 任务。
        if(event.type !== 'click') return;
        const button = event.target?.closest?.('[data-lookbook-choose],[data-lookbook-run]');
        const binding = button ? actionBindings.get(button) : null;
        if(!button || !binding) return;
        const now = Date.now();
        if(now - Number(button.dataset.lookbookLastActionAt || 0) < 250) return;
        button.dataset.lookbookLastActionAt = String(now);
        event.preventDefault();
        event.stopPropagation();
        if(button.hasAttribute('data-lookbook-choose')) openPicker(binding.node,{onChange:binding.options.onChange});
        else binding.options.run?.(binding.node);
    }
    function installActionDelegation(){
        if(actionDelegationInstalled || !document?.addEventListener) return;
        actionDelegationInstalled = true;
        document.addEventListener('click', activateAction, true);
    }
    async function uploadCover(file){ const form = new FormData(); form.append('files',file); const response = await fetch('/api/ai/upload',{method:'POST',body:form}); const data = await response.json().catch(()=>({})); if(!response.ok) throw new Error(data.detail || '封面上传失败'); return data.files?.[0]?.url || ''; }
    function ensureModal(){
        let modal = document.getElementById('lookbookStyleModal'); if(modal) return modal;
        modal = document.createElement('dialog'); modal.id='lookbookStyleModal'; modal.className='lookbook-style-modal'; modal.setAttribute('aria-hidden','true');
        modal.innerHTML='<div class="lookbook-style-dialog" role="dialog" aria-modal="true"><header><div><small>LOOKBOOK SKILLS</small><h2>选择创作类型预设</h2><p class="lookbook-style-attribution">本地视觉预设：参考图优先 · 动作/情绪先行 · 真实环境融合 · 系列化输出</p></div><button type="button" class="lookbook-modal-close" data-lookbook-close>×</button></header><div class="lookbook-github-install"><input type="url" data-lookbook-github-url placeholder="粘贴 GitHub Skill 地址"><button type="button" data-lookbook-install>安装 Skill</button><span data-lookbook-status></span></div><div class="lookbook-style-grid" data-lookbook-grid></div></div>';
        document.body.appendChild(modal);
        modal.addEventListener('click',event => { if(event.target===modal || event.target.closest('[data-lookbook-close]')) closePicker(); });
        modal.addEventListener('cancel',event => { event.preventDefault(); closePicker(); });
        modal.querySelector('[data-lookbook-install]').addEventListener('click',async()=>{ const input=modal.querySelector('[data-lookbook-github-url]'),status=modal.querySelector('[data-lookbook-status]'); const url=String(input.value||'').trim(); if(!url){status.textContent='请先输入 GitHub 地址';return;} status.textContent='正在解析 Skill…'; try{const response=await fetch('/api/lookbook/skills/install',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({url})}); const data=await response.json().catch(()=>({})); if(!response.ok) throw new Error(data.detail||'Skill 安装失败'); saveStored([...stored(),...(data.skills||[])]); status.textContent=(data.skills||[]).length>1?'已发现多个 Skill，请勾选所需风格':'Skill 已安装'; renderModal();}catch(error){status.textContent=error.message||'Skill 安装失败';} });
        return modal;
    }
    function closePicker(){ const modal=document.getElementById('lookbookStyleModal'); if(modal?.open && typeof modal.close === 'function') modal.close(); modal?.classList.remove('open'); if(modal) modal.style.display='none'; modal?.setAttribute('aria-hidden','true'); pickerNode=null; pickerOptions=null; }
    async function choose(style){ if(!pickerNode||!style) return; const status=document.querySelector('[data-lookbook-status]'); if(!style.cover && style.source==='github'){ status.textContent='正在根据 Skill 生成封面…'; try{const response=await fetch('/api/lookbook/skills/cover',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({prompt:style.prompt||style.description||style.name,aspect_ratio:pickerNode.aspectRatio||'3:4',resolution:pickerNode.resolution||'2k',quality:pickerNode.quality||'high'})}); const data=await response.json().catch(()=>({})); if(!response.ok) throw new Error(data.detail||'封面生成失败'); style.cover=data.image?.url||data.image?.path||''; const list=stored(),index=list.findIndex(item=>item.id===style.id); if(index>=0) list[index]=style; else list.push(style); saveStored(list);}catch(error){status.textContent=error.message||'封面生成失败';} } Object.assign(pickerNode,{lookbookStyleId:style.id,lookbookStyleName:style.name,lookbookStylePrompt:style.prompt||style.description||'',lookbookStyleCover:style.cover||'',lookbookStyleSource:style.source||'builtin',lookbookAutoDecision:style.id==='auto'?{}:(pickerNode.lookbookAutoDecision||{}) }); pickerOptions?.onChange?.(pickerNode,{render:true}); closePicker(); }
    function renderModal(){ const modal=ensureModal(), grid=modal.querySelector('[data-lookbook-grid]'), list=styles(); modal.querySelector('.lookbook-style-batch-actions')?.remove(); grid.innerHTML=list.map(style=>`<article class="lookbook-style-card"><div class="lookbook-card-cover">${style.cover?`<img src="${esc(style.cover)}" alt="${esc(style.name)}">`:'<i data-lucide="image"></i>'}<label title="上传封面"><i data-lucide="upload"></i><input type="file" accept="image/*" data-lookbook-cover="${esc(style.id)}"></label></div><div class="lookbook-card-body"><div class="lookbook-card-title"><strong>${esc(style.name)}</strong>${style.source==='github'?`<label class="lookbook-skill-check"><input type="checkbox" data-lookbook-check="${esc(style.id)}">勾选</label>`:''}</div><p>${esc(style.description||'')}</p><small>${style.source==='github'?'GitHub Skill':'内置 Skill'}</small><div class="lookbook-card-actions"><button type="button" data-lookbook-select="${esc(style.id)}">使用此风格</button></div></div></article>`).join('');
        if(list.some(item=>item.source==='github')){ const footer=document.createElement('div'); footer.className='lookbook-style-batch-actions'; footer.innerHTML='<span>安装多个 Skill 后可勾选所需风格</span><button type="button" data-lookbook-apply>应用所选</button>'; modal.querySelector('.lookbook-style-dialog').appendChild(footer); footer.querySelector('[data-lookbook-apply]').onclick=()=>{const checked=modal.querySelector('[data-lookbook-check]:checked'); if(checked) choose(list.find(item=>item.id===checked.dataset.lookbookCheck));}; }
        grid.querySelectorAll('[data-lookbook-select]').forEach(button=>button.onclick=event=>{event.preventDefault();event.stopPropagation();choose(list.find(item=>item.id===button.dataset.lookbookSelect));});
        grid.querySelectorAll('[data-lookbook-cover]').forEach(input=>input.onchange=async event=>{const style=list.find(item=>item.id===event.target.dataset.lookbookCover); if(!style||!event.target.files?.[0]) return; try{style.cover=await uploadCover(event.target.files[0]); const custom=stored(),index=custom.findIndex(item=>item.id===style.id); if(index>=0) custom[index]=style; else custom.push({...style,source:style.source||'builtin'}); saveStored(custom); renderModal();}catch(error){modal.querySelector('[data-lookbook-status]').textContent=error.message||'封面上传失败';} event.target.value='';});
        window.lucide?.createIcons?.();
    }
    function openPicker(node,options){ pickerNode=node; pickerOptions=options||{}; renderModal(); const modal=ensureModal(); modal.classList.add('open'); modal.style.display='flex'; modal.style.pointerEvents='auto'; modal.setAttribute('aria-hidden','false'); if(typeof modal.showModal === 'function' && !modal.open){ try{ modal.showModal(); }catch(_){ modal.setAttribute('open',''); } } else modal.setAttribute('open',''); }
    function normalize(node){ if(!node||node.type!==TYPE)return node; const defaultStyle=STYLES.find(item=>item.id===DEFAULT_STYLE_ID)||STYLES[0]; node.lookbookPrompt=String(node.lookbookPrompt||''); node.lookbookStyleId=String(node.lookbookStyleId||DEFAULT_STYLE_ID); node.lookbookStyleSource=String(node.lookbookStyleSource||'builtin'); if(node.lookbookStyleSource==='builtin'&&!STYLES.some(item=>item.id===node.lookbookStyleId)) node.lookbookStyleId=DEFAULT_STYLE_ID; const selectedStyle=STYLES.find(item=>item.id===node.lookbookStyleId)||defaultStyle; node.lookbookStyleName=String(node.lookbookStyleName||selectedStyle.name); node.lookbookStylePrompt=String(node.lookbookStylePrompt||selectedStyle.prompt); node.lookbookStyleCover=String(node.lookbookStyleCover||''); node.lookbookSearch=node.lookbookSearch!==false; node.lookbookResearchStatus=String(node.lookbookResearchStatus||'idle'); node.lookbookAgentStage=String(node.lookbookAgentStage||''); node.lookbookResearch=String(node.lookbookResearch||''); node.lookbookReferenceAnalysis=String(node.lookbookReferenceAnalysis||''); node.lookbookVisualSystem=node.lookbookVisualSystem&&typeof node.lookbookVisualSystem==='object'?node.lookbookVisualSystem:{}; node.lookbookAutoDecision=node.lookbookAutoDecision&&typeof node.lookbookAutoDecision==='object'?node.lookbookAutoDecision:{}; node.apiProvider=String(node.apiProvider||''); node.model=String(node.model||''); node.aspectRatio=node.aspectRatio||'16:9'; node.resolution=node.resolution||'2k'; node.quality=node.quality||'high'; node.count=clamp(node.count||4,1,4); node.generatedOutputs=Array.isArray(node.generatedOutputs)?node.generatedOutputs:[]; node.running=Boolean(node.running); node.runError=String(node.runError||''); return node; }
    function createNode(point={}){ const defaultStyle=STYLES.find(item=>item.id===DEFAULT_STYLE_ID)||STYLES[0]; return normalize({id:'',type:TYPE,x:Number(point.x||0),y:Number(point.y||0),w:430,lookbookPrompt:'',lookbookStyleId:DEFAULT_STYLE_ID,lookbookStyleName:defaultStyle.name,lookbookStylePrompt:defaultStyle.prompt,lookbookStyleCover:'',lookbookStyleSource:'builtin',lookbookSearch:true,lookbookResearchStatus:'idle',lookbookAgentStage:'',lookbookAutoDecision:{},aspectRatio:'16:9',resolution:'2k',quality:'high',count:4,generatedOutputs:[],inputs:[],running:false,runError:''}); }
    function bodyHtml(node){ normalize(node); const researchStatus=node.lookbookAgentStage || (node.lookbookResearchStatus==='running'?'正在执行当前 Skill':'等待生成'); const autoHint=node.lookbookStyleId==='auto'?'生成时由视觉模型自主选型':'按所选创作类型执行'; const autoDecision=node.lookbookAutoDecision?.selected_style_name?` · 上次选型：${esc(node.lookbookAutoDecision.selected_style_name)}`:''; return `<div class="ecom-node-panel lookbook-node-panel"><div class="lookbook-style-row"><div class="lookbook-style-cover ${node.lookbookStyleCover?'has-cover':''}">${node.lookbookStyleCover?`<img src="${esc(node.lookbookStyleCover)}" alt="${esc(node.lookbookStyleName)}">`:'<i data-lucide="palette"></i>'}</div><div class="lookbook-style-copy"><span>视觉风格 Skill</span><strong>${esc(node.lookbookStyleName)}</strong><small>${node.lookbookStyleSource==='github'?'GitHub Skill':'内置 Skill'} · ${esc(autoHint)}${autoDecision}</small></div><button type="button" class="lookbook-style-button" data-lookbook-choose><i data-lucide="sparkles"></i>选择风格</button></div><label class="ecom-node-field"><span>创作需求（可选）</span><textarea data-lookbook-field="lookbookPrompt" rows="4" placeholder="描述主题、商品、人物、场景或版式；自动风格会结合需求自主选型">${esc(node.lookbookPrompt)}</textarea></label><label class="lookbook-search-toggle"><input type="checkbox" data-lookbook-field="lookbookSearch" ${node.lookbookSearch?'checked':''}><span>联网研究杂志与品牌时尚大片</span></label>${generationChoicesHtml(node)}<div class="ecom-node-params"><select data-lookbook-field="aspectRatio">${['1:1','2:3','3:4','4:3','4:5','9:16','16:9'].map(value=>`<option value="${value}" ${node.aspectRatio===value?'selected':''}>${value}</option>`).join('')}</select><select data-lookbook-field="resolution">${['1k','2k','4k'].map(value=>`<option value="${value}" ${node.resolution===value?'selected':''}>${value.toUpperCase()}</option>`).join('')}</select><select data-lookbook-field="quality">${['auto','medium','high'].map(value=>`<option value="${value}" ${node.quality===value?'selected':''}>${value.toUpperCase()}</option>`).join('')}</select><label class="ecom-count">数量<input type="number" min="1" max="4" value="${node.count}" data-lookbook-field="count"></label></div><button class="ecom-node-run lookbook-run" type="button" data-lookbook-run aria-busy="${node.running?'true':'false'}"><i data-lucide="wand-sparkles"></i>${node.running?'生成中…':'生成 Lookbook'}</button>${node.runError?`<div class="ecom-node-status failed"><span></span>${esc(node.runError)}</div>`:''}<div class="lookbook-research-status"><i data-lucide="sparkles"></i>${researchStatus}</div></div>`; }
    function bindGenerationChoices(root,node,options={}){
        const choiceRoot=root.querySelector('[data-lookbook-generation-settings]'); if(!choiceRoot) return;
        const closeChoices=except=>choiceRoot.querySelectorAll('[data-lookbook-choice]').forEach(menu=>{const open=menu===except; menu.classList.toggle('open',open); menu.querySelector('[data-lookbook-choice-trigger]')?.setAttribute('aria-expanded',String(open));});
        choiceRoot.querySelectorAll('[data-lookbook-choice]').forEach(menu=>{
            const trigger=menu.querySelector('[data-lookbook-choice-trigger]'); let closeTimer=0;
            const cancelClose=()=>{if(closeTimer){clearTimeout(closeTimer);closeTimer=0;}};
            const scheduleClose=()=>{cancelClose();closeTimer=window.setTimeout(()=>{closeTimer=0;closeChoices(null);},140);};
            trigger?.addEventListener('pointerenter',()=>closeChoices(menu)); trigger?.addEventListener('focus',()=>closeChoices(menu)); menu.addEventListener('pointerenter',cancelClose); menu.addEventListener('pointerleave',scheduleClose);
            trigger?.addEventListener('click',event=>{event.preventDefault();event.stopPropagation();closeChoices(menu.classList.contains('open')?null:menu);});
        });
        const update=()=>options.onChange?.(node,{render:false});
        const bindModels=()=>choiceRoot.querySelectorAll('[data-lookbook-model-value]').forEach(button=>button.onclick=event=>{event.preventDefault();event.stopPropagation();node.model=button.dataset.lookbookModelValue||'';const modelInput=choiceRoot.querySelector('[data-lookbook-model]');if(modelInput)modelInput.value=node.model;const menu=choiceRoot.querySelector('[data-lookbook-choice="model"]');menu?.querySelector('.image-quick-choice-trigger span')&&(menu.querySelector('.image-quick-choice-trigger span').textContent=choiceModelLabel(node.model));choiceRoot.querySelectorAll('[data-lookbook-model-value]').forEach(item=>item.classList.toggle('active',item===button));closeChoices(null);update();});
        choiceRoot.querySelectorAll('[data-lookbook-provider-value]').forEach(button=>button.onclick=event=>{event.preventDefault();event.stopPropagation();const resolveProvider=typeof window.resolveImageProviderId==='function'?window.resolveImageProviderId:value=>String(value||'');node.apiProvider=resolveProvider(button.dataset.lookbookProviderValue||'');const models=imageModels(node.apiProvider);node.model=models[0]||'';const providerMenu=choiceRoot.querySelector('[data-lookbook-choice="provider"]');providerMenu?.querySelector('.image-quick-choice-trigger span')&&(providerMenu.querySelector('.image-quick-choice-trigger span').textContent=choiceProviderLabel(node.apiProvider,imageProviders()));choiceRoot.querySelectorAll('[data-lookbook-provider-value]').forEach(item=>item.classList.toggle('active',item===button));const modelMenu=choiceRoot.querySelector('[data-lookbook-choice="model"]');if(modelMenu){modelMenu.querySelector('.image-quick-choice-trigger span').textContent=choiceModelLabel(node.model);const panel=modelMenu.querySelector('[data-lookbook-choice-panel="model"]');panel.innerHTML=models.length?models.map(model=>`<button type="button" class="image-quick-choice-item ${model===node.model?'active':''}" role="menuitem" data-lookbook-model-value="${esc(model)}"><span>${esc(model)}</span>${model===node.model?'<i data-lucide="check"></i>':''}</button>`).join(''):'<button type="button" class="image-quick-choice-item empty" disabled>暂无图片模型</button>';window.lucide?.createIcons?.();bindModels();}closeChoices(null);update();});
        bindModels();
    }
    function bind(root,node,options={}){ normalize(node); installActionDelegation(); root.querySelectorAll('[data-lookbook-field]').forEach(control=>control.addEventListener(control.type==='checkbox'?'change':'input',event=>{const key=control.dataset.lookbookField; node[key]=key==='count'?clamp(control.value,1,4):control.type==='checkbox'?Boolean(control.checked):control.value; if(key==='lookbookPrompt'){node.lookbookPlan='';if(node.lookbookStyleId==='auto')node.lookbookAutoDecision={};} options.onChange?.(node,{render:false}); event.stopPropagation();})); bindGenerationChoices(root,node,options); const handleAction=event=>{if(event.type !== 'click') return; const button=event.target.closest?.('[data-lookbook-choose],[data-lookbook-run]'); if(!button||!root.contains(button)) return; const now=Date.now(); if(now-Number(button.dataset.lookbookLastActionAt||0)<250) return; button.dataset.lookbookLastActionAt=String(now); event.preventDefault(); event.stopPropagation(); if(button.hasAttribute('data-lookbook-choose')) openPicker(node,{onChange:options.onChange}); else options.run?.(node);}; const chooseButton=root.querySelector('[data-lookbook-choose]'); const runButton=root.querySelector('[data-lookbook-run]'); [chooseButton,runButton].filter(Boolean).forEach(button=>{ actionBindings.set(button,{node,options}); button.onclick=handleAction; }); root.addEventListener('click',handleAction,true); }
    function mediaRefs(node){ return (node.generatedOutputs||[]).map((item,index)=>({url:outputUrl(item),name:`lookbook-${index+1}.png`,kind:'image'})).filter(item=>item.url); }
    const INPUT_PORTS=[
        {role:'lookbook-person',label:'人物',title:'连接人物、模特或人物身份参考图'},
        {role:'lookbook-product',label:'商品',title:'连接商品、服装、配饰或包装参考图'},
        {role:'lookbook-scene',label:'场景',title:'连接场景、空间或背景参考图'},
        {role:'lookbook-material',label:'材质',title:'连接面料、纹理、工艺或材质细节图'},
        {role:'lookbook-logo',label:'Logo',title:'连接 Logo、字标或品牌规范图'},
        {role:'lookbook-pose',label:'姿态',title:'连接人物姿态、动作或镜头机位参考图'},
        {role:'lookbook-layout',label:'版式',title:'连接广告版式、留白或构图参考图'},
    ];
    // 端口查询会在统一画布渲染所有节点时调用；非 Lookbook 节点必须返回空数组，
    // 否则会抢先进入通用多端口分支，覆盖三视图等节点自己的角色端口。
    window.CanvasLookbookNode={TYPE,isType:type=>type===TYPE,isGenerator:type=>type===TYPE,isMediaOutput:type=>type===TYPE,canOutput:type=>type===TYPE,inputPorts:type=>type===TYPE ? INPUT_PORTS.map(item=>({...item})) : [],title:type=>type===TYPE ? 'Lookbook 平面广告' : '',size:type=>type===TYPE ? ({w:430,h:0}) : null,normalize,createNode,bodyHtml,bind,mediaRefs,openPicker};
})();
