## 当前分发方式
用户已选择不上架商店。扩展随热更新内置于 `app/web/extensions/chrome-kling-bridge`，也提供同目录ZIP；首次在Chrome开启开发者模式并加载文件夹，后续更新后刷新扩展。商店方案不作为本次发布前置条件。

# 拾影 · 可灵画布助手 0.4.0

安装引导开发版，商店尚未上架。详见 [发行与企业部署](发行与企业部署.md)。下方保留0.3阶段记录，当前功能和待验收项以发行文档为准。

# 拾影 · 可灵画布助手 0.3.0

桌面与本机 Web 无限画布选择可灵视频 3.0 Omni，连接图片/视频、填写提示词，点击“生成”或“发送到可灵并生成”。画布自动打开 Chrome 交接页，扩展接收任务后关闭交接页，上传素材、绑定原生引用、同步时长/分辨率/比例/音画同步，最后单次点击生成。

## 安装与使用

1. 在 Chrome 扩展管理开启开发者模式，加载此文件夹（包含 manifest.json）；不要直接加载 ZIP。
2. 升级时点击扩展卡片的刷新图标。保持 Chrome 中的可灵账号已登录。
3. 使用包含新后端与前端的拾影桌面或 Web 画布。无需打开插件弹窗，不需要在 Chrome 再次登录拾影；地址随任务自动传入，不固定使用 3000。
4. 查看画布提交回执。submitted 表示可灵页面出现与提示词匹配的新任务，生成进度与成片暂在可灵查看。unknown 表示已经尝试提交但没有确认回执，必须先核对可灵历史记录，不能直接重发。

本次开发 Web 在 http://127.0.0.1:13170；源码桌面实例自动选用 13171。安装版需要后续发布含后端的热更新才获得这些接口，本轮未发布。

## 范围和保护

- 自动网页生成支持视频 3.0 Omni；其他可灵模型保留原 CLI 生成流程。
- 支持本机 Chrome、localhost 与 127.0.0.1 的任意端口。远程 LAN/公网 Web 来源不在本扩展当前授权范围内。
- 上传 PNG/JPEG、MP4/MOV，单文件最多 200 MB、最多 10 个文件；网页限制不满足时停止。
- 每次仅生成一条视频。参数不可用时不使用网页旧值偷偷提交。
- 每个任务独立随机短期凭据；扩展只可领取该任务、读取该任务素材及回传结果，不取得桌面登录 Cookie。凭据不出现在普通状态查询中。
- 队列在内存中，后端重启后失效，最多保存一小时。上传超时、提交回执丢失都不自动重放生成点击。
- 同一素材组可复用；按用户授权，已有人工草稿会通过可灵原生“重置”自动覆盖。提交前再次校验提示词、素材和生成按钮，后端原子许可与页面单次标记共同防止重复点击。
- 插件弹窗保留手动连接作为旧画布备用入口。新版画布发送会自动接收。

## 验证

- python -m pytest tests/test_kling_web_bridge.py -q
- node tests/support/kling_autofill_runtime.cjs
- node tests/support/kling_submit_guard.cjs

真实页面的免连接交接、参数配置、填充、回执及人工草稿保护已验证。本轮没有实际消耗积分生成视频；单次点击与异常回执通过运行用例验证。

## 后台处理

- 已连接时，扩展每 30 秒检查一次队列，后端不打开交接页、不启动新的 Chrome 进程。连接仅授权当前账号的可灵任务队列，有效期 24 小时，后端重启后失效。
- 可灵页面后台复用；新标签设置 active:false，无浏览器窗口时尝试创建最小化且不聚焦的窗口。不会主动把现有可灵标签切到前台。
- Chrome 关闭或连接失效时，通过最小化启动参数及 Windows SW_SHOWMINNOACTIVE 唤起。首次交接或系统接管窗口时仍可能闪现，不能保证所有 Chrome/Windows 版本完全不可见。
- 登录、验证码、页面错误需要用户处理。提交过程中若检测到新的人工编辑仍停止，避免在点击前被改成意外内容。
