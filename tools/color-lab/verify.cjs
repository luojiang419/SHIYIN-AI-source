const {chromium}=require('playwright');
(async()=>{const browser=await chromium.launch({headless:true});const page=await browser.newPage({viewport:{width:1500,height:1080}});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('http://127.0.0.1:13229');await page.locator('#ref').setInputFiles('案例/批量复刻培训-20260920/evidence/color-fidelity-real-pose-replicate-postprocess/reference-roi-100pct.png');await page.locator('#src').setInputFiles('案例/批量复刻培训-20260920/evidence/color-fidelity-real-pose-replicate-postprocess/generated-before-roi-100pct.png');await page.waitForFunction(()=>document.querySelector('#status').textContent.startsWith('实时预览已更新'));const before=await page.locator('#oi').getAttribute('src');await page.locator('#lightness').fill('8');await page.waitForFunction(old=>document.querySelector('#oi').src!==old,before);await page.waitForFunction(()=>document.querySelector('#status').textContent.startsWith('实时预览已更新'));await page.locator('#a_shift').fill('4');await page.locator('#a_shift').fill('-2');await page.locator('#b_shift').fill('3');await page.waitForFunction(()=>document.querySelector('#status').textContent.startsWith('实时预览已更新'));await page.locator('#name').fill('实时预览自动化测试');await page.locator('#save').click();await page.waitForFunction(()=>document.querySelector('#status').textContent.includes('参数已保存'));await page.screenshot({path:'tools/color-lab/verification.png',fullPage:true});await page.locator('#view-split').click();await page.locator('#wipe').fill('73');if(!await page.locator('#split').isVisible())throw Error('划像未打开');await page.locator('#run').click();await page.waitForFunction(()=>!document.querySelector('#download').hidden);await page.setViewportSize({width:390,height:844});await page.screenshot({path:'tools/color-lab/verification-mobile.png',fullPage:true});const overflow=await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth);if(overflow||errors.length)throw Error(JSON.stringify({overflow,errors}));console.log(JSON.stringify({autoPreview:true,sliderChangesImage:true,split:true,fullExport:true,mobileOverflow:false,errors}));await browser.close()})().catch(e=>{console.error(e);process.exit(1)});
