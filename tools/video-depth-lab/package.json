{
  "name": "shiyin-video-depth-lab",
  "private": true,
  "version": "0.1.0",
  "description": "GemDepth 与 Video Depth Anything 本地视频深度技术验证工具",
  "type": "module",
  "scripts": {
    "dev": "node ../../node_modules/@tauri-apps/cli/tauri.js dev",
    "build": "node ../../node_modules/@tauri-apps/cli/tauri.js build --no-bundle",
    "check": "cargo check --manifest-path src-tauri/Cargo.toml",
    "test": "node --test tests/*.test.mjs && python -m unittest discover -s tests -p test_*.py"
  }
}
