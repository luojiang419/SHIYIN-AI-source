# 创建三视图 API 优先级修复报告

## 结论

创建三视图节点此前默认使用 Grsai，并非单一配置错误，而是默认模板顺序、前端硬编码偏好、三视图缺少节点级参数、智能画布未继承来源设置、API 页面固定重排共同造成的结果。1.0.214 已统一为“API 设置页列表顺序即优先级”，并在图片任务入队前跳过本地可判定为无效的配置。

## 原因分析

1. 后端默认平台模板把 Grsai 放在时影前方，合并默认平台时保留该顺序。
2. 旧的首选平台逻辑会读取 `primary` 或第一个启用项，但没有针对图片生成验证模型和凭据是否完整。
3. 普通三视图节点创建时没有保存平台、模型、分辨率和质量，运行时只能临时取全局默认。
4. 普通图片生成节点还存在显式偏好时影的硬编码，形成“不同入口使用不同默认规则”的分裂行为。
5. 智能三视图没有自己的 `runSettings`，快捷创建时不会明确继承来源节点的平台设置。
6. API 设置页用固定 ID 数组重新排序，用户看到的顺序和实际希望保存的顺序可能不一致；部分内置平台又禁止拖拽。

## 修复方案

### API 顺序

- API 设置页所有平台都可拖拽。
- 平台卡显示优先级序号。
- 前端不再按平台 ID 固定重排。
- 保存数组的顺序就是后端选择顺序；第一个启用项同步标记为兼容 `primary`。

### 有效性预检与回退

图片任务提交前依次检查：

- 平台是否启用；
- 是否配置图片模型；
- 远程平台是否配置请求地址；
- 远程平台是否配置 API Key；
- Codex/即梦等本地 CLI 是否存在。

首选配置未通过时，自动选择列表中的下一项，并向前端返回实际平台、模型、是否回退及跳过原因。回退发生在任务入队前，不会在上游已经受理后盲目重试，因此不会因自动切换造成重复任务或重复扣费。

### 三视图节点

- 普通画布与智能画布的创建三视图节点底部均新增：平台、模型、1K/2K/4K、质量。
- 四张资产的画幅保持业务固定：一张 16:9 设定板和三张 9:16 正/侧/背视图，所以不提供会破坏组合结构的统一画幅和数量参数。
- 智能画布从来源图片节点继承生成设置。
- 后端发生顺延时，节点回写实际使用的平台和模型。
- 普通图片生成节点移除时影硬编码，统一按列表首个有效配置选择。

## “无效配置”的边界

本版自动顺延针对提交前能够本地确定的配置问题。若 Key 已填写但已被服务商撤销，或上游在请求过程中临时宕机，本地预检无法在不发起请求的情况下确认；任务一旦可能被上游受理，系统不会自动改投第二家，以避免重复生成和扣费。此类问题仍会返回明确上游错误，可在 API 设置中执行连接测试后调整顺序或配置。

## 验收结果

- 全量自动化测试：546 项通过。
- 子测试：42 项通过。
- API 优先级专项测试：5 项通过。
- JavaScript 语法检查：API 设置、普通画布、智能画布全部通过。
- Python 语法检查与 CSS 构建通过。
- Tauri 1.0.214 release 宿主编译成功。
- 安装包校验成功：`dist/installer/SHIYIN-AI-Setup-1.0.214.exe`。
- SHA-256：`FA96D6402B06882F3E8576181B7FD3C3223946925CEDAF302576473E03259213`。

## 已知验证边界

完整打包态桌面冒烟要求关闭所有 SHIYIN AI 实例。当前实例承载本次开发会话，因此未强制关闭；本次已完成宿主编译、静态资源版本注入、安装包封装与安装包版本/哈希校验。
