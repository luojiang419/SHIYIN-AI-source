# 电商视觉上游 524 兼容修复 v1

状态：已完成
最后更新：2026-08-29

## 当前状态

已确认电商专用平台的 API Key、`gpt-5.6-sol` 模型和 Responses 地址均配置正确；AI 助手页面请求正常。视频节点“润色/自动解析”之前在 4 张参考图的长提示词请求中附带 `web_search` 工具，电商代理返回 Cloudflare 524。

## 已完成

- 对 `ecommerce-vision`（及其 xsy proxy 地址）的视频视觉请求关闭内置 `web_search` 工具，保留其它 Responses 提供商原行为。
- 非流式 LLM 请求遇到上游 524 时自动退避重试 2 次。
- 连续 524 时向用户明确说明是上游网关等待超时，不再误导为 API 配置或模型错误。
- 针对重试期间连接异常及空响应补充重试/错误说明，避免前端只显示空的“上游接口错误：”。
- 视频自动解析在完整提示词收到 524/502/504 时自动切换为紧凑提示词重试；重试保持同一 `ecommerce-vision` provider、同一模型和同一组图片，不会切换其它模型。

## 验证

- 直接调用电商 Responses：4 张图 + 完整视频导演提示词 + `web_search` 会稳定返回 524；去掉工具后可返回 200（偶发 524 重试后成功）。
- `python -m py_compile main.py`
- `pytest -q tests/test_responses_protocol.py tests/test_video_auto_parse.py tests/test_ecommerce.py`：163 passed。
- 安装版日志复核：请求仍记录为 `provider=ecommerce-vision model=gpt-5.6-sol images=4/4`，未发现错误切换到其它模型；此前空错误提示来自上游 524/空响应。
- 真实运行时（使用安装目录数据和真实 DPAPI API Key）验证：
  - `/api/canvas-prompt-polish`：1 图返回 200，11.7 秒；4 图返回 200，11.4 秒，结果包含全部图片引用。
  - `/api/canvas-video-auto-parse`：4 图真实上传，首次完整提示词触发 524 后自动紧凑重试，最终返回 200，90.3 秒，结果包含 `<<<image_1>>>` 至 `<<<image_4>>>`。
  - 请求日志明确记录 `provider=ecommerce-vision model=gpt-5.6-sol ... tools=0`，确认有实际图片上传且未调用 `web_search` 或其它模型。

## 安装包

已重新编译包含本修复的 Windows 安装包：

- `dist/installer/SHIYIN-AI-Setup-1.0.371.exe`
- 大小：73,207,797 bytes
- SHA-256：`A414F5617078EDE212AC3197AFBEF00A86D06FCAB55DAF816215F95EDC771B48`
- 运行时 health、启动、二次启动、内存和清理检查通过。

## 遗留说明

尚未在用户实际安装后的 GUI 中再次点击验证；真实后端接口已使用安装目录配置和真实上游完成润色、4 图上传和自动解析闭环验证。首次 524 时会进行紧凑提示词重试，因此自动解析在该上游上可能需要约 1–2 分钟。
