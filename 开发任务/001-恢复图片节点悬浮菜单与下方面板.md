# 恢复图片节点悬浮菜单与下方面板

状态：已完成
当前阶段：4/4
最后更新：2026-08-31

## 当前状态

已确认 1.0.375 的图片节点顶部工具条和节点下方面板在性能提交 `816e928` 中被整体移到全局 `selectionHub`，导致交互行为回归。当前工作区为 `feat/storyboard-merge-node`，源码版本 1.0.378，工作区干净。

## 下一步

1. 在 `static/js/canvas.js` 恢复节点级工具条/面板的行为，但只在悬停、聚焦或单选时懒创建。已完成。
2. 更新菜单回归契约，保留媒体预览队列和首开性能约束。已完成。
3. 执行 JavaScript 检查、专项测试和浏览器验证，再递增版本并重建安装包。已完成。

## 当前 TODO

- [x] 图片节点悬浮菜单与下方面板懒创建
- [x] 选择、多选、焦点和生成状态回归代码
- [x] 静态检查、专项测试与浏览器验证
- [x] 版本递增、安装包构建、任务文档和 Git 推送

## 最近验证状态

- 静态检查：`node --check static/js/canvas.js`、`git diff --check`、版本同步检查通过
- 单元测试：菜单/首开/媒体队列专项 18 项通过；画布全量 341 项通过，8 项为既有移除节点/旧入口基线失败
- 编译：Tauri release、PyInstaller、Inno Setup 均通过
- 运行测试：打包桌面 smoke 通过，health=ok，startup_ms=6286，内存 222.58 MB/250 MB，二次启动保护和退出清理通过
- 最近 Git commit：待提交版本更新

## 任务目标

恢复 1.0.375 的图片节点顶部悬浮工具条和节点下方面板，同时保留 `816e928` 引入的媒体队列、懒加载和大画布首开性能优化。非活动图片节点不能一次性生成完整控件 DOM。

## 技术方案

- 保留现有 `classicMediaToolbarHtml`、`imageNodeQuickPromptHtml` 和绑定函数。
- 图片节点默认只渲染媒体内容；节点进入 hover/focus 或成为唯一选中节点时，按需物化工具条和面板。
- 离开节点且未选中、未聚焦时延迟清理按需 DOM；控件输入已通过现有绑定函数写回节点状态。
- 图片节点选中时不再向全局 `selectionHub` 注入提示词面板；`selectionHub` 继续服务输出媒体、视频、组节点操作。

## 已完成内容

- `static/js/canvas.js` 增加图片节点工具条/面板按需物化、焦点与悬停事件委托、离开节点后的延迟清理；单选图片恢复节点级面板，图片生成按钮重新从节点面板取控件。
- `tests/test_canvas_menu_preferences.py` 更新为懒物化契约，覆盖不在全量 render 中创建控件。
- 版本递增到 1.0.379，构建 `dist/installer/SHIYIN-AI-Setup-1.0.379.exe`。

## 验证结果

- 安装包 SHA-256：`0414926C93D06DAB74AB56D93F3443491C8D80658B85AD261BB15754A9559712`
- DWPose 实人冒烟未执行（未提供输入文件）；CUDA/TensorRT DLL 为可选 GPU 警告。

## 验收标准

- 图片节点 hover/单选时顶部工具条可见、下方面板可见且跟随节点。
- 面板中的提示词、平台、模型、画幅、分辨率、摄影机和生成按钮可用。
- 多选时隐藏并清理图片节点局部控件，保留多选操作行为。
- 500 个图片节点初始 DOM 不包含 500 份完整面板/工具条。
- 现有媒体预览队列和首开性能测试不回归。
- JavaScript 语法、相关 pytest 和浏览器冒烟通过。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]
