# 桌面启动令牌重复导航修复

状态：开发中
当前阶段：2/4
最后更新：2026-08-31

## 当前状态

1.0.379 桌面窗口实际出现 `{"detail":"桌面启动令牌无效或已使用"}`。根因是 WebView 启动、恢复和 Cookie 建立竞态可能重复导航 `/api/auth/bootstrap`，现有后端只允许首次兑换后重放 2 次、有效窗口 15 秒，超出后直接返回 401 JSON。

## 下一步

1. 已把本机启动令牌短时重放窗口调整为 60 秒、最多 8 次，并保持 loopback 和随机令牌校验。
2. 完成鉴权专项、桌面运行时 smoke、版本递增和安装包构建。

## 当前 TODO

- [x] 定位令牌重复消费路径
- [x] 扩大有界重放窗口并补充边界测试
- [ ] 完成桌面启动 smoke
- [ ] 递增版本、构建安装包、提交推送

## 最近验证状态

- 静态检查：`python -m py_compile canvas_core/auth.py main.py`、`git diff --check` 通过
- 单元测试：安全与桌面 bootstrap 专项 5 项通过
- 编译：待执行
- 运行测试：待执行
- 最近 Git commit：6ff29ea

## 技术方案

- 桌面令牌仍由 Tauri 每次启动生成 64 位 UUID 组合随机值。
- `/api/auth/bootstrap` 继续限制为 loopback；错误令牌继续拒绝。
- 首次兑换后只保存哈希，在 60 秒内允许最多 8 次无 Cookie 重放，用于吸收 WebView 恢复/重复导航竞态。
- 超出次数或窗口后继续返回 401，不把本机所有请求提升为管理员。

## 验收标准

- 首次兑换和 8 次无 Cookie 重复导航均返回 303。
- 第 9 次重放、错误令牌、超时令牌和非 loopback 来源均拒绝。
- 桌面安装包首次启动不再显示令牌错误 JSON。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]
