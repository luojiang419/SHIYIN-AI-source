# 图片节点单行控件与风格弹窗修复

状态：开发中
当前阶段：2/3
最后更新：2026-08-31

## 当前状态

图片节点底部面板已是紧凑尺寸，但窄面板仍将四个下拉项、摄影机和生成按钮排成两行。Lookbook 节点的“选择风格”依赖节点内部多种事件绑定，用户反馈按钮反复无法打开弹窗。

## 下一步

1. 将图片节点六个控件固定为单行响应式布局。
2. 使用文档级事件捕获和原生 `dialog` 顶层显示风格选择器。
3. 更新版本、执行专项测试并编译安装包。

## 当前 TODO

- [x] 图片节点控件单行布局
- [x] 风格弹窗可靠触发与顶层显示
- [ ] 版本、测试、安装包编译与推送

## 最近验证状态

- 静态检查：待执行
- 单元测试：待执行
- 编译：待执行
- 运行测试：待执行
- 最近 Git commit：`9147b15`

## 任务目标

让图片节点底部的四个下拉项、摄影机和生成按钮始终在一排显示；让“选择风格”不依赖节点局部事件冒泡，稳定打开不受画布裁剪影响的风格选择弹窗。

## 技术方案

- 面板参数区使用六列 CSS Grid，窄宽度压缩列宽并仅隐藏操作按钮文字，宽度足够时显示完整按钮。
- Lookbook 模块为操作按钮注册 `WeakMap`，并在 `document` 捕获阶段统一处理点击/指针按下，绕过节点拖拽和局部重绘事件。
- 风格选择器改用原生 `<dialog>`；支持 `showModal()`/`close()`，保留非原生环境的 class/display 回退。

## 文件 / 模块清单

- 修改：`static/js/canvas-lookbook-node.js`
- 修改：`static/css/canvas.css`
- 修改：`static/canvas.html`
- 修改：版本源文件（按项目版本脚本）
- 新增：本任务文档

## 验收标准

- 图片节点设置区不再因面板宽度换成两行。
- 窄面板控件不横向溢出，摄影机和生成按钮仍可点击。
- 点击“选择风格”后 `dialog[open]` 可见，弹窗位于顶层，不受画布滚动或节点边界裁剪。
- 风格卡片“使用此风格”、关闭和上传/安装操作可用。
- 相关前端测试、JavaScript 检查和安装包编译通过。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]
