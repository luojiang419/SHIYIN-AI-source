# 编译安装包 1.0.387

状态：已完成
当前阶段：4/4
最后更新：2026-09-01

## 当前状态

当前源码版本 `1.0.387` 的完整 Windows 安装包构建已成功。PyInstaller、Tailwind CSS、Tauri release、暂存资源校验、桌面运行时 smoke 和 Inno Setup 均通过，最终产物已完成版本、大小、SHA-256、签名和内含画布修复校验。

## 下一步

当前任务已完成。

## 当前 TODO

- [x] 核对 Git、版本源和构建依赖
- [x] 完成后端与桌面端构建
- [x] 完成暂存资源和运行时 smoke
- [x] 完成 Inno Setup 安装包构建
- [x] 校验产物并记录、提交、推送

## 最近验证状态

- 版本同步：`1.0.387`，9 sources 通过
- Python：3.12.7；PyInstaller：6.19.0
- Rust：cargo/rustc 1.94.1
- Inno Setup：`C:\ProgramData\chocolatey\bin\ISCC.exe`
- 运行时 smoke：health `ok`；startup `5867 ms`；进程树私有内存 `210.97 MB / 250 MB`；二次启动保护、父进程退出后的 backend 清理和 data 创建检查通过
- 安装包：`dist/installer/SHIYIN-AI-Setup-1.0.387.exe`，`92,192,900 bytes`（`87.92 MB`）
- SHA-256：`D03F6C92A0C528D2C511BD7545D92FC4155D4BD79699178973AC6EFAF1266246`
- 版本信息：FileVersion / ProductVersion 均为 `1.0.387`
- 签名状态：`NotSigned`
- 暂存资源：确认包含 `canvas.css` 和 `canvas.js` 中的平移媒体空白修复
- Git：用户未跟踪目录 `测试/` 保留

---

## 任务目标

基于当前提交 `6656384` 和版本 `1.0.387` 完成 Windows 安装包全流程构建，交付可验证的安装程序。

## 技术方案

使用项目标准入口 `tools/build-installer.ps1`，依次完成版本同步检查、PyInstaller sidecar、Tailwind CSS、Tauri release、安装暂存目录、Web 资源校验、桌面运行时 smoke 和 Inno Setup 打包。构建完成后使用文件哈希、Authenticode 状态和 PE 版本信息核验产物。

## 文件 / 模块清单

- 构建入口：`tools/build-installer.ps1`
- 最终产物：`dist/installer/SHIYIN-AI-Setup-1.0.387.exe`
- 更新：本文档与 `功能开发文档/编译安装包-v1.md`

## 验收标准

- 完整构建脚本退出码为 0。
- 暂存 Web 资源与桌面运行时 smoke 通过。
- 安装包存在、非空、版本名为 `1.0.387`，SHA-256 可读取。
- 构建记录提交并推送，用户原有未跟踪目录不受影响。

## 已完成内容

- 使用 `tools/build-installer.ps1` 完成未跳过 smoke 的全流程构建。
- 暂存 Web 资源版本戳写入 `1.0.387`，必需 Web/skill 资源校验通过。
- 桌面运行时 smoke 验证 health、启动时间、内存、二次启动、backend 生命周期和 data 创建。
- Inno Setup 6.7.1 编译成功，安装包版本与九处源码版本一致。

## 已知问题

- DWPose 实人 smoke 因未提供 `-DwposeSmokeInput` 跳过。
- PyInstaller 报告缺少 CUDA/TensorRT DLL，属于可选 GPU provider 依赖，不影响 CPU/DirectML 构建。
- 安装包未进行 Authenticode 签名。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]
