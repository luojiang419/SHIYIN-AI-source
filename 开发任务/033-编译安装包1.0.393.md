# 编译安装包 1.0.393

状态：已完成
当前阶段：4/4
最后更新：2026-09-02

## 当前状态

基于当前源码版本 `1.0.393` 完成 Windows x64 安装包全流程构建。版本同步、后端 PyInstaller、Tailwind CSS、Tauri release、暂存资源校验、桌面运行时 smoke 和 Inno Setup 均通过，安装包契约校验已完成。

## 下一步

当前任务已完成。安装包位于 `dist/installer/SHIYIN-AI-Setup-1.0.393.exe`。

## 当前 TODO

- [x] 核对版本同步和构建依赖
- [x] 完成后端、CSS、Tauri release 构建
- [x] 完成暂存资源与桌面运行时 smoke
- [x] 完成 Inno Setup 安装包构建
- [x] 完成产物版本、大小、哈希和签名校验

## 最近验证状态

- 版本同步：`1.0.393`，9 个版本源通过
- PyInstaller：6.19.0，后端构建成功；CUDA/TensorRT DLL 缺失为可选 GPU provider 警告
- Tailwind CSS：构建成功；Browserslist 数据库过期提示不影响产物
- Tauri release：构建成功
- 桌面 runtime smoke：health `ok`；startup `10158 ms`；进程树私有内存 `215.07 MB / 250 MB`；二次启动保护、backend 清理和 data 创建均通过
- Inno Setup：6.7.1，成功编译
- 安装包：`dist/installer/SHIYIN-AI-Setup-1.0.393.exe`，`92,262,553 bytes`
- SHA-256：`d602af6429e4799c8196e65e3f60ce34de9e493e2b6fd4848b7bc65d952b6614`
- FileVersion / ProductVersion：`1.0.393`
- 签名状态：`NotSigned`
- DWPose 真人 smoke：未提供 `-DwposeSmokeInput`，按脚本规则跳过
- Git：保留用户已有源码修改及未跟踪的 `测试/` 目录

---

## 任务目标

基于当前源码构建可安装、可验证的 Windows x64 安装器，并记录完整验证结果。

## 技术方案

使用 `tools/build-installer.ps1`，依次执行版本同步检查、PyInstaller sidecar、Tailwind CSS、Tauri release、安装暂存目录、Web/skill 资源校验、桌面 runtime smoke 和 Inno Setup。构建后使用 `tools/verify-installer-artifact.ps1` 校验 PE 版本、文件大小、SHA-256 和 Authenticode 状态。

## 文件 / 模块清单

- 构建入口：`tools/build-installer.ps1`
- 安装器定义：`installer/shiyin_ai.iss`
- 产物：`dist/installer/SHIYIN-AI-Setup-1.0.393.exe`
- 校验记录：`dist/installer/SHIYIN-AI-Setup-1.0.393.exe.sha256`

## 验收标准

- 完整构建脚本退出码为 0。
- 暂存 Web/skill 资源校验通过。
- 桌面运行时 smoke 的 health、二次启动、backend 生命周期和 data 创建检查通过。
- 安装包存在、非空、版本为 `1.0.393`，并通过哈希、版本和签名状态检查。

## 已完成内容

- 完成当前源码版本 `1.0.393` 的完整构建。
- 完成运行时 smoke；DWPose 真人测试因缺少输入跳过。
- 完成安装包契约校验并生成 SHA-256 校验文件。

## 已知问题

- 安装包未进行 Authenticode 签名。
- PyInstaller 的 CUDA/TensorRT DLL 警告属于可选 GPU provider 依赖缺失，不影响 CPU/DirectML 构建。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

