# 编译安装包 1.0.394

状态：已完成
当前阶段：4/4
最后更新：2026-09-03

## 当前状态

基于当前源码版本 `1.0.394` 完成 Windows x64 安装包重编译。版本同步、PyInstaller 后端、Tailwind CSS、Tauri release、Web 资源暂存和 Inno Setup 均通过；因已有 SHIYIN AI 实例（PID 50924）正在运行，本次使用 `-SkipRuntimeSmoke`，未执行桌面运行时 smoke。

## 下一步

当前任务已完成。安装包位于 `dist/installer/SHIYIN-AI-Setup-1.0.394.exe`。

## 当前 TODO

- [x] 递增并同步版本到 1.0.394
- [x] 完成 PyInstaller、Tailwind CSS 和 Tauri release
- [x] 完成 Web/Skill 暂存资源校验
- [x] 完成 Inno Setup 安装包编译
- [x] 完成安装包版本、大小、哈希和签名状态校验
- [x] 记录 smoke 跳过原因和构建警告

## 最近验证状态

- 版本同步：1.0.394，9 个版本源通过
- PyInstaller：6.19.0，构建成功；CUDA/TensorRT DLL 缺失为可选 GPU provider 警告
- Tailwind CSS：构建成功；Browserslist 数据过期提示不影响产物
- Tauri release：构建成功（release profile）
- Inno Setup：6.7.1，成功编译
- 桌面 runtime smoke：跳过，原因是已有实例 PID 50924 正在运行
- 安装包：`dist/installer/SHIYIN-AI-Setup-1.0.394.exe`，92,318,636 bytes（本次重编译）
- SHA-256：`33630a3324eeaca6e29e4b19b4eebf439131c0614149cd7587f99ee300c10b0a`
- 签名状态：`NotSigned`
- 产物校验：`tools/verify-installer-artifact.ps1 -Version 1.0.394` 通过

## 技术方案

使用 `tools/build-installer.ps1 -SkipRuntimeSmoke` 完成版本检查、PyInstaller sidecar、前端 CSS、Tauri release、安装暂存、资源校验和 Inno Setup；再使用 `tools/verify-installer-artifact.ps1` 校验版本、大小、SHA-256 和 Authenticode 状态。

## 已知问题

- 本次没有执行桌面 runtime smoke；关闭所有 SHIYIN AI 实例后可单独运行 `tools/smoke-desktop.ps1` 补验。
- 安装包未进行 Authenticode 签名。
- CUDA/TensorRT DLL 警告不影响 CPU/DirectML 构建。

## Git

- 当前分支：`feat/storyboard-merge-node`
- 构建前代码提交：`b3417fa docs: 对齐侧栏最终提交记录`
- 本次构建命令：`powershell -ExecutionPolicy Bypass -File tools/build-installer.ps1 -SkipRuntimeSmoke`
- 版本与构建记录提交：待提交

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

当前任务已完成；如需补充运行时 smoke，先关闭已运行的 SHIYIN AI 实例，再从“已知问题”中的命令继续。
