# 左侧边栏语言按钮与底部箭头优化

状态：已完成
当前阶段：2/2
最后更新：2026-09-03

## 当前状态

已定位左侧边栏设置组被 `max-height:220px` 裁剪，导致语言按钮不可见；底部方向箭头来自 `project-version-badge` 的伪元素。正在修复高度并移除无用徽标节点。

## 下一步

当前任务已完成。

## 当前 TODO

- [x] 增大设置折叠组高度上限，显示语言按钮
- [x] 移除底部方向箭头对应的版本徽标节点
- [x] 执行测试并提交

## 最近验证状态

- 静态检查：`git diff --check` 通过
- 单元测试：侧栏/启动/主题定向测试 19 项通过
- Git commit：`e42e418 fix: 修复侧栏语言按钮遮挡并移除底部箭头`

## 任务目标

确保展开左侧边栏设置时语言按钮完整显示，并移除最底部无实际作用的方向箭头按钮，不影响主题、语言切换和更新检测逻辑。

## 技术方案

将最终生效的 `.side-actions .settings-fold-group:not(.is-collapsed)` 高度上限从 220px 调整为 300px，覆盖管理员账号管理、API 设置、软件设置、主题和语言五项。删除 `project-version-badge` DOM 节点；其相关 JS 通过现有空判断继续兼容，不再渲染伪元素箭头。

## 文件 / 模块清单

- `static/index.html`：侧栏高度和底部徽标节点。
- `tests/test_sidebar_layout.py`：静态布局契约测试。

## 验收标准

1. 设置展开后语言按钮可见且不被裁剪。
2. 侧栏最底部不再出现方向箭头按钮。
3. 主题切换、语言切换、更新检查无 JavaScript 报错。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

实现与验证已完成；统一主题测试另有 1 项既有版本断言失败，详见最终汇报。
