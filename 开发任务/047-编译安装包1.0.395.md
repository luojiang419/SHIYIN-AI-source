# 编译安装包 1.0.395

状态：已完成
当前阶段：4/4
最后更新：2026-09-03

## 当前状态

基于已完成的 FW2026 Lookbook 色调优化提交 `121fd28`，准备将当前版本从 `1.0.394` 递增至 `1.0.395` 并编译 Windows x64 安装包。

检测到已安装的 SHIYIN AI 正在运行（桌面进程与 `canvas-backend` 均位于 `D:\Program Files\SHIYIN AI`），为避免打断用户当前实例，本次使用 `-SkipRuntimeSmoke`；其余版本同步、PyInstaller、Tailwind CSS、Tauri release、资源暂存、Inno Setup 和产物校验完整执行。

工作区另有用户未提交改动，本任务仅提交版本源与构建记录，不提交无关源码。

## 下一步

当前任务已完成。安装包位于 `dist/installer/SHIYIN-AI-Setup-1.0.395.exe`。

## 当前 TODO

- [x] 版本递增与同步
- [x] PyInstaller 后端构建
- [x] Tailwind CSS 与 Tauri release
- [x] Web/Skill 安装暂存资源检查
- [x] Inno Setup 安装包编译
- [x] 产物哈希与签名验证
- [x] 文档、Git 提交与推送

## 最近验证状态

- 版本同步：`1.0.395`，9 个版本源通过
- PyInstaller：6.19.0，构建成功；CUDA/TensorRT DLL 缺失为可选 GPU provider 警告
- Tailwind CSS：构建成功；Browserslist 数据过期提示不影响产物
- Tauri：release profile 构建成功，耗时约 1 分 30 秒
- Web 缓存：扫描 56 个文件，更新 17 个版本引用；Skill/Topaz staging 断言通过
- Inno Setup：6.7.1，成功编译
- Runtime smoke：跳过，原因是已安装的 SHIYIN AI 与 `canvas-backend` 正在运行
- 安装包：`dist/installer/SHIYIN-AI-Setup-1.0.395.exe`，92,332,047 bytes
- SHA-256：`5835ada2068473f4d3e26b025460b1a3ab03a1830f4a86f47825c64dcb9c121d`
- 签名状态：`NotSigned`
- 产物校验：`tools/verify-installer-artifact.ps1 -Version 1.0.395` 通过
- Git：`feat/storyboard-merge-node`，版本与构建记录提交见当前任务最新提交

## 已完成内容

- 首次 `-IncrementVersion` 正确递增到 `1.0.395`，但同步门禁发现 `release-notes/current.md` 仍是旧版本，构建在编译前安全停止。
- 修复 `tools/increment-version.ps1`，以后递增版本会自动同步 release notes 标题，并更新既有避坑指南。
- 重新执行版本同步、PyInstaller、Tailwind CSS、Tauri release、Web/Skill staging 和 Inno Setup，全部成功。
- 生成 SHA-256 文件并核对 ProductVersion、大小和 Authenticode 状态。

## 已知问题

- 本次未执行桌面 runtime smoke；关闭正在运行的安装版实例后可单独补验。
- 安装包未做 Authenticode 签名。
- CUDA/TensorRT DLL 警告不影响 CPU/DirectML 构建。

## 验收标准

- 版本源全部同步为 `1.0.395`。
- `dist/installer/SHIYIN-AI-Setup-1.0.395.exe` 成功生成且大于 5 MB。
- 安装包 ProductVersion、SHA-256 和 Authenticode 状态校验通过。
- 构建日志中的可选依赖警告被准确记录，不误报为成功验证。
- 本任务版本修改与文档已提交并推送，用户原有工作区改动保持不变。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]
