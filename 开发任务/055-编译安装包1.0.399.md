# 编译安装包 1.0.399

状态：已完成
当前阶段：4/4
最后更新：2026-09-04 09:28

## 当前状态

源码和安装包版本已统一递增到 `1.0.399`。Lookbook 场景母图保真修复已进入 PyInstaller sidecar，Windows x64 安装包已完成构建与独立产物校验。

检测到已安装的 SHIYIN AI 和 `canvas-backend` 正在运行。为避免中断用户当前实例，本次使用 `-SkipRuntimeSmoke`；版本同步、PyInstaller、Tailwind、Tauri release、Web/Skill staging、Inno Setup 和独立产物校验仍完整执行。

安装包位于 `dist/installer/SHIYIN-AI-Setup-1.0.399.exe`。当前工作区存在其他未提交源码和素材；本次构建按当前工作树生成安装包，但 Git 仅提交版本同步、发布说明与本任务文档，不擅自提交其他用户修改。

## 下一步

下一步首先执行：

当前任务已完成。安装包位于 `dist/installer/SHIYIN-AI-Setup-1.0.399.exe`。

## 当前 TODO

- [x] 核对当前版本、Git 状态和构建依赖
- [x] 版本递增与同步校验
- [x] PyInstaller 后端构建
- [x] Tailwind 与 Tauri release 构建
- [x] Web/Skill staging 与 Inno Setup 编译
- [x] 安装包独立校验
- [x] Git 提交与推送

## 最近验证状态

- Lookbook / Premium / 电商专项：`202 passed`
- 全量测试：`1134 passed, 15 failed`；失败为既有智能画布、特殊节点、快捷键、主题和桌面缓存契约
- Python：`3.12.7`
- PyInstaller：`6.19.0`
- Node.js：`v24.15.0`
- npm：`11.12.1`
- Cargo / rustc：`1.94.1`
- Inno Setup：`C:\ProgramData\chocolatey\bin\ISCC.exe`
- 版本同步：`1.0.399`，9 个版本源通过
- PyInstaller：通过；CUDA/TensorRT DLL 缺失为可选 GPU provider 警告
- Tailwind CSS：通过；Browserslist 数据过期提示不影响产物
- Tauri release：通过，约 1 分钟
- Web/Skill staging：扫描 56 个文件，更新 17 个版本引用，断言通过
- Inno Setup：6.7.1，通过，约 37 秒
- Runtime smoke：因已安装实例运行而跳过
- 安装包：`dist/installer/SHIYIN-AI-Setup-1.0.399.exe`，`92,343,118 bytes`（`88.07 MB`）
- SHA-256：`D481C0598C21F79813C3717F27726C9C3BB1F9001465683D1492DF27F6285302`
- FileVersion / ProductVersion：`1.0.399`
- 签名状态：`NotSigned`
- sidecar 归档：确认包含 `main` 与 `canvas_core.lookbook_story`
- 构建 Git commit：`34aa168 chore: build 1.0.399 installer`
- 远程备份：已推送至 `origin/feat/storyboard-merge-node`

---

## 任务目标

将包含 Lookbook 场景母图保真与镜头区域扩展修复的当前 Windows x64 应用构建为 `SHIYIN-AI-Setup-1.0.399.exe`，并完成可复核的版本、文件大小、哈希和签名状态校验。

## 当前项目现状

- `VERSION` 与 `package.json` 当前均为 `1.0.398`。
- 标准构建入口是 `tools/build-installer.ps1`。
- `1.0.398` 已有安装包，但不包含提交 `77ce034`。
- 已安装桌面实例正在运行，不能执行会争用稳定 WebView profile 的 runtime smoke。

## 技术方案

- 使用 `tools/increment-version.ps1 -Version 1.0.399` 同步版本源。
- 使用 `tools/build-installer.ps1 -SkipRuntimeSmoke` 完成后端、桌面端、暂存资源和安装包编译。
- 使用 `tools/verify-installer-artifact.ps1 -Version 1.0.399` 独立验证最终产物。
- 检查暂存的 `main.py` 与 `lookbook_story.py` 是否包含场景母图参考包关键符号，确认修复实际进入安装包。

## 文件 / 模块清单

- 修改 9 个项目版本源
- 新增本任务文档
- 生成 `dist/installer/SHIYIN-AI-Setup-1.0.399.exe`

## 开发阶段

- [x] 阶段 1：环境与版本核对
- [x] 阶段 2：版本同步和完整构建
- [x] 阶段 3：产物与修复内容校验
- [x] 阶段 4：文档、Git 提交与推送

## 验收标准

- 9 个版本源一致为 `1.0.399`。
- 完整构建脚本退出码为 0。
- 安装包存在且 ProductVersion / FileVersion 为 `1.0.399`。
- 安装包大小和 SHA-256 可读取，签名状态明确记录。
- 暂存后端包含 Lookbook 场景母图保真修复。
- 用户原有未提交文件保持原状态且不进入本次 Git 提交。

## 已完成内容

- 已核对版本、运行进程和构建依赖。
- 已将 9 个版本源同步为 `1.0.399`。
- 已完成 PyInstaller、Tailwind、Tauri、Web/Skill staging 和 Inno Setup 构建。
- 已使用 `tools/verify-installer-artifact.ps1 -Version 1.0.399` 独立验证安装包。
- 已核对 sidecar 归档，确认包含 Lookbook 场景修复所在模块。

## 当前关键修改

- 9 个项目版本源由 `1.0.398` 更新为 `1.0.399`。
- `release-notes/current.md` 增加 Lookbook 场景母图保真修复说明。
- 新增本构建任务文档。

## 已知问题

- 因已安装实例正在运行，本次跳过 packaged desktop runtime smoke。
- 全量测试当前有 15 个与本次 Lookbook 修复无关的既有失败。

## 开发日志

- 2026-09-04：确认构建目标为 `1.0.399`，构建依赖可用，运行实例需保留。
- 2026-09-04：完整构建与独立产物校验通过，安装包和 SHA-256 文件已生成。
- 2026-09-04：构建提交 `34aa168` 已推送到 `origin/feat/storyboard-merge-node`。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

新会话启动：

1. 阅读项目规则
2. 阅读本任务文档
3. 检查 Git branch
4. 检查 git status
5. 从“下一步”直接继续
6. 不重复已经完成的阶段
7. 不重复询问已经确认的问题
8. 继续遵守本协议
