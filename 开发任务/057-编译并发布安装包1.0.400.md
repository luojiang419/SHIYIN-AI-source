# 编译并发布安装包 1.0.400

状态：已完成
当前阶段：4/4
最后更新：2026-09-04 10:34

## 当前状态

已从提交 `242713a` 创建隔离发布工作树 `release/1.0.400`，避免主工作区未提交的电商、作品管理和视频研究改动进入正式安装包。9 个版本源已同步为 `1.0.400`，发布说明已加入图片输出连线修复。

Windows x64 安装包已完成完整构建和独立校验；staging 中确认存在 `image -> output` 合法连接规则。发布源码已推送，GitHub `v1.0.400` 已发布为正式 Latest Release，远端更新清单与本地产物一致。

目标修复 `d482c81` 已通过相关测试与隔离浏览器持久化验证。已安装 SHIYIN AI 与 `canvas-backend` 正在运行，因此本次构建跳过 packaged runtime smoke，避免争用用户当前 WebView profile。

## 下一步

当前任务已完成。

## 当前 TODO

- [x] 版本源与发布说明更新
- [x] 发布前测试
- [x] PyInstaller、Tailwind、Tauri 与 Inno Setup 构建
- [x] 安装包哈希、版本和内容校验
- [x] Git 提交、推送与 GitHub Release 发布

## 最近验证状态

- 图片输出连线相关测试：19 passed
- 全量画布测试：367 passed，9 项既有失败
- 隔离浏览器：输出节点、SVG 连线及保存后连接持久化通过
- 版本同步：`1.0.400`，9 个版本源通过
- 发布相关测试：39 passed
- 全量测试：1123 passed，28 项既有跨模块契约失败
- PyInstaller：通过；CUDA/TensorRT DLL 缺失为可选 GPU provider 警告
- Tailwind CSS：通过；Browserslist 数据过期提示不影响产物
- Tauri release：通过，约 1 分 51 秒
- Web/Skill staging：扫描 56 个文件，更新 17 个版本引用，断言通过
- Inno Setup：6.7.1，通过，约 38 秒
- Runtime smoke：因已安装实例运行而跳过
- 安装包：`dist/installer/SHIYIN-AI-Setup-1.0.400.exe`，`92,336,040 bytes`
- SHA-256：`64d73ea2a1db90cb13d65a0412944415fc9bbf1a6b10ea7eec3c3155358b47be`
- FileVersion / ProductVersion：`1.0.400`
- 签名状态：`NotSigned`
- staging：确认包含图片输出连接修复与 sidecar
- GitHub Release：`https://github.com/luojiang419/SHIYIN-AI/releases/tag/v1.0.400`
- 远端安装包：`https://github.com/luojiang419/SHIYIN-AI/releases/download/v1.0.400/SHIYIN-AI-Setup-1.0.400.exe`
- 发布清单目标 commit：`a54d7ded83abf22578dc29055d9fc817bb35a60c`
- 发布源码 commit：`7d0783a`

---

## 任务目标

将图片节点快速生成输出连线持久化修复编译为 Windows x64 安装包 `SHIYIN-AI-Setup-1.0.400.exe`，完成产物校验并发布为 GitHub 最新正式版本。

## 当前项目现状

- 当前正式源码版本为 `1.0.399`。
- 修复提交已经推送到 `origin/feat/storyboard-merge-node`。
- 主工作区存在无关未提交修改，发布必须从隔离工作树进行。
- 标准构建与发布入口位于 `tools/build-installer.ps1`、`tools/verify-installer-artifact.ps1` 和 `tools/publish-release.ps1`。

## 技术方案

- 使用 `tools/increment-version.ps1 -Version 1.0.400` 同步 9 个版本源。
- 发布说明只新增本次图片节点输出连线修复，不将主工作区未提交功能写入版本。
- 使用 `tools/build-installer.ps1 -SkipRuntimeSmoke` 完整构建后端、桌面宿主、Web/Skill staging 和 Inno Setup 安装包。
- 使用独立产物校验脚本检查文件版本、大小、签名状态与 SHA-256。
- 构建完成后提交发布状态，将隔离分支快进合并回当前功能分支并推送，再向 `luojiang419/SHIYIN-AI` 发布正式 Release。

## 文件 / 模块清单

- 修改 9 个版本源
- 修改 `release-notes/current.md`
- 修改 `static/update-notes.json`
- 新增本任务文档
- 生成 `dist/installer/SHIYIN-AI-Setup-1.0.400.exe`

## 开发阶段

- [ ] 阶段 1：版本与发布说明
- [ ] 阶段 2：发布前验证
- [ ] 阶段 3：构建与产物校验
- [ ] 阶段 4：源码推送与 GitHub Release

## 验收标准

- 9 个版本源一致为 `1.0.400`。
- 图片输出连线修复存在于安装包 staging Web 资源。
- 构建脚本与独立产物校验成功。
- 安装包版本、大小、SHA-256 和签名状态明确记录。
- GitHub `v1.0.400` Release 为正式、非预发布并包含 EXE 与 SHA-256 文件。
- 主工作区原有未提交修改保持不变且不进入安装包。

## 已完成内容

- 已创建隔离发布工作树并核对发布工具链。
- 已同步版本源、发布说明和软件内更新说明。
- 已完成版本、JS、JSON、diff 静态检查和发布前测试。
- 已完成 PyInstaller、Tailwind、Tauri release、Web/Skill staging 与 Inno Setup 构建。
- 已通过独立产物校验，并确认修复代码实际进入 staging。
- 已将发布提交快进合并到 `feat/storyboard-merge-node` 并推送至 origin。
- 已发布 GitHub `v1.0.400` 正式 Release，并核对远端清单与本地 SHA-256。

## 当前关键修改

- 9 个版本源由 `1.0.399` 更新为 `1.0.400`。
- `release-notes/current.md` 与 `static/update-notes.json` 增加图片输出连线持久化修复说明。
- 新增本构建发布任务文档。

## 已知问题

- 已安装桌面实例正在运行，packaged runtime smoke 需跳过。
- 全量画布测试存在 9 项既有契约失败，与本次修复无关。
- 全量项目测试共 28 项既有失败，集中在智能画布旧契约、已移除特殊节点、菜单/主题/快捷键及电商/Lookbook 参数契约。

## 开发日志

- 2026-09-04：启动 `1.0.400` 隔离构建与 GitHub 发布流程。
- 2026-09-04：完成版本同步与发布前验证，进入完整安装包构建。
- 2026-09-04：完整构建与独立产物校验通过，进入源码推送和 GitHub 发布阶段。
- 2026-09-04：源码提交 `7d0783a` 已推送，GitHub `v1.0.400` 已发布并完成远端校验。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

新会话启动：

1. 阅读项目规则
2. 阅读本任务文档
3. 检查 Git branch
4. 检查 git status
5. 从“下一步”直接继续
6. 不重复已经完成的阶段
7. 不重复询问已经确认的问题
8. 继续遵守本协议
