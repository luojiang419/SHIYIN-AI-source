# 编译安装包 1.0.401

状态：已完成
当前阶段：4/4
最后更新：2026-09-04 12:03

## 当前状态

Windows x64 安装包 `SHIYIN-AI-Setup-1.0.401.exe` 已完成全链路构建和独立校验。9 个版本源一致，完整 Python 与 Rust 回归、PyInstaller、Tailwind、Tauri release、Web/Skill staging、packaged runtime smoke 和 Inno Setup 均通过。

staging 已确认包含素材快捷入库、视频节点动态尺寸、H3 参数网格、作品 `.jpg_x` 扩展名修复及 `1.0.401` 更新说明；冻结 sidecar 包含 `main`、`canvas_core.ecommerce` 和 `canvas_core.lookbook_story`。

当前分支已包含视频节点动态尺寸、素材快捷入库和画布回归清理等已提交功能；工作区另有用户尚未提交的作品下载扩展名与 Lookbook 数量相关修改，本次按当前工作树编译并保留其 Git 状态。

## 下一步

下一步首先执行：

当前任务已完成。

## 当前 TODO

- [x] 核对 Git 状态、版本和构建依赖
- [x] 版本同步与发布说明更新
- [x] 完整回归测试
- [x] PyInstaller、Tailwind、Tauri 与 Inno Setup 构建
- [x] 安装包版本、大小、哈希、签名和 staging 内容校验
- [x] 更新任务文档并提交构建元数据

## 最近验证状态

- 静态检查：`node --check static/js/works.js`、`git diff --check` 通过
- 单元测试：作品管理 `37 passed`；电商/Lookbook `171 passed`；完整 Python `1161 passed`；Rust `10 passed`
- 编译：PyInstaller、Tailwind、Tauri release、Web/Skill staging、Inno Setup 全部通过
- 运行测试：packaged runtime smoke 通过；启动 `9669ms`，进程树私有内存 `180.55MB`，第二实例和后端退出清理正常
- 安装包：`dist/installer/SHIYIN-AI-Setup-1.0.401.exe`，`92,358,843 bytes`
- SHA-256：`7a7f9847cd0657ce79d1589fc6e728f0928fbba8a22e5d54a4e57812d4edaf5f`
- FileVersion / ProductVersion：`1.0.401`
- 签名状态：`NotSigned`
- 最近 Git commit：本次构建提交（当前文档所在提交）

---

## 任务目标

将当前工作树构建为 Windows x64 安装包 `SHIYIN-AI-Setup-1.0.401.exe`，并完成版本、文件大小、SHA-256、签名状态和关键 staging 内容校验。

## 当前项目现状

- 当前版本为 `1.0.400`。
- 标准构建入口为 `tools/build-installer.ps1`。
- 标准产物校验入口为 `tools/verify-installer-artifact.ps1`。
- Python、Node.js、npm、Cargo、rustc 和 Inno Setup 均可用。
- 当前没有 `SHIYIN-AI` 或 `canvas-backend` 进程占用，可执行 packaged runtime smoke。
- 工作区存在用户未提交修改和研究输出，构建过程中不得删除或覆盖。

## 技术方案

- 使用 `tools/increment-version.ps1 -Version 1.0.401` 同步版本源。
- 在发布说明中记录本版已完成的画布功能及当前工作树中的用户可见修复。
- 执行完整 Python 测试和 Rust 单元测试，确认当前工作树可交付。
- 使用 `tools/build-installer.ps1` 完成 PyInstaller、Tailwind、Tauri、Web/Skill staging、packaged runtime smoke 和 Inno Setup 构建。
- 使用 `tools/verify-installer-artifact.ps1 -Version 1.0.401` 独立验证最终产物。
- 仅提交版本源、发布说明和本任务文档，不擅自提交用户已有的其他修改。

## 文件 / 模块清单

预计修改：

- 9 个版本源
- `release-notes/current.md`
- `static/update-notes.json`
- `开发任务/061-编译安装包1.0.401.md`

预计生成：

- `dist/installer/SHIYIN-AI-Setup-1.0.401.exe`
- `dist/installer/SHIYIN-AI-Setup-1.0.401.exe.sha256`

## 开发阶段

- [x] 阶段 1：环境、版本与工作区核对
- [x] 阶段 2：版本同步和回归验证
- [x] 阶段 3：完整构建与产物校验
- [x] 阶段 4：文档、Git 提交与远程备份

## 验收标准

- 9 个版本源一致为 `1.0.401`。
- 完整 Python 与 Rust 测试通过。
- 完整构建脚本退出码为 0，packaged runtime smoke 通过。
- 安装包存在且 ProductVersion / FileVersion 为 `1.0.401`。
- 安装包大小、SHA-256 和签名状态明确记录。
- staging 包含当前画布功能、作品扩展名修复与 Lookbook 数量逻辑。
- 用户原有未提交文件与研究输出保持原状态。

## 已完成内容

- 已核对项目规则、最近构建任务、当前分支和工作区。
- 已确认构建工具链可用且无运行实例占用。
- 已完成作品管理、电商与 Lookbook 专项回归。
- 已同步 9 个版本源为 `1.0.401`，并更新发布说明与软件内更新说明。
- 已完成完整 Python 与 Rust 回归，全部通过。
- 已完成 PyInstaller、Tailwind、Tauri release、Web/Skill staging、packaged runtime smoke 和 Inno Setup 构建。
- 已使用独立脚本验证安装包版本、大小、SHA-256 和签名状态。
- 已核对 staging 前端内容与冻结 sidecar 模块清单。

## 当前关键修改

- 9 个版本源由 `1.0.400` 更新为 `1.0.401`。
- 发布说明增加本版画布功能、桌面修复和当前工作树用户可见改动。
- 新增本构建任务文档。

## 已知问题

- 当前工作区仍保留用户尚未提交的应用修改与大量研究输出，未纳入本次构建元数据提交。
- 安装包未配置代码签名，校验状态为 `NotSigned`。
- 未提供 DWPose 真实人物输入图，本次发布脚本中的 DWPose 推理 smoke 按预期跳过；通用 packaged runtime smoke 已通过。

## 开发日志

- 2026-09-04：启动 `1.0.401` 当前工作树安装包构建，专项测试通过。
- 2026-09-04：完成版本同步与发布说明更新，进入完整回归阶段。
- 2026-09-04：完整 Python 与 Rust 回归全部通过，进入安装包构建阶段。
- 2026-09-04：完整构建、packaged runtime smoke 和独立产物校验通过，安装包生成完成。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

新会话启动：

1. 阅读项目规则
2. 阅读本任务文档
3. 检查 Git branch
4. 检查 git status
5. 从“下一步”直接继续
6. 不重复已经完成的阶段
7. 不重复询问已经确认的问题
8. 继续遵守本协议
