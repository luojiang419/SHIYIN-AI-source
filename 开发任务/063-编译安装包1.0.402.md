# 编译安装包 1.0.402

状态：已完成
当前阶段：4/4
最后更新：2026-09-04 16:44

## 当前状态

已确认当前版本为 `1.0.401`，目标版本为 `1.0.402`。Python、Node.js、npm、Cargo、rustc 和 Inno Setup 均可用，标准构建入口为 `tools/build-installer.ps1`。

当前分支为 `feat/storyboard-merge-node`。节点布局与预览复制功能已提交并推送；工作区另有用户原先未提交的电商分析、作品管理和研究输出，本任务不会回退或提交这些文件，但安装包会按当前工作树打包。

Windows x64 安装包 `SHIYIN-AI-Setup-1.0.402.exe` 已完成全链路构建和独立校验。当前无阻塞问题。

## 下一步

当前任务已完成。

## 当前 TODO

- [x] 核对 Git 状态、版本、上次构建和工具链
- [x] 版本同步与发布说明更新
- [x] 完整 Python 与 Rust 回归
- [x] PyInstaller、Tailwind、Tauri、staging 和 Inno Setup 构建
- [x] 安装包版本、大小、哈希、签名和 staging 内容校验
- [ ] 更新任务文档、Git 提交与远程推送

## 最近验证状态

- 静态检查：JavaScript 语法、更新说明 JSON、9 个版本源同步和 `git diff --check` 通过
- 单元测试：完整 Python `1166 passed`；Rust `10 passed`
- 编译：PyInstaller、Tailwind、Tauri release、Web/Skill staging、Inno Setup 全部通过
- 运行测试：packaged runtime smoke 通过；启动 `7543ms`，进程树私有内存 `205.06MB/250MB`，第二实例保护和后端退出清理正常
- 安装包：`dist/installer/SHIYIN-AI-Setup-1.0.402.exe`，`92,347,714 bytes`
- SHA-256：`d285ae54ae3696dc7422f07a6f7a857bcb83e91adacc069177bda83ebb97f9ac`
- FileVersion / ProductVersion：`1.0.402`
- 签名状态：`NotSigned`
- staging：复制按钮、PNG Clipboard、节点尺寸规范、自动高度、固定全屏预览和影视尺寸隔离均已确认
- 最近 Git commit：`3b309f4 docs: 同步节点布局任务推送状态`

---

## 任务目标

将当前工作树构建为 Windows x64 安装包 `SHIYIN-AI-Setup-1.0.402.exe`，包含全屏预览复制图片和全画布节点框架规范，并完成版本、大小、SHA-256、签名、staging 内容与打包运行时校验。

## 当前项目现状

- 9 个版本源一致为 `1.0.401`。
- 上一安装包为 `dist/installer/SHIYIN-AI-Setup-1.0.401.exe`。
- 标准构建会执行 PyInstaller、Tailwind、Tauri release、Web/Skill staging、packaged runtime smoke 和 Inno Setup。
- `3000` 端口由已安装版占用；标准 smoke 使用隔离端口 `3118`，不会影响用户实例。
- 工作区存在用户未提交修改和研究输出，构建与提交必须严格区分。

## 技术方案

- 使用 `tools/increment-version.ps1 -Version 1.0.402` 同步版本源。
- 在发布说明中增加全屏复制图片、固定预览定位、节点最小尺寸与内容驱动高度。
- 执行完整 `pytest` 和 `cargo test`。
- 使用 `tools/build-installer.ps1` 完成全链路构建与 packaged runtime smoke。
- 使用 `tools/verify-installer-artifact.ps1 -Version 1.0.402` 独立校验最终产物。
- 仅提交版本源、发布说明、更新说明和本任务文档。

## 文件 / 模块清单

预计修改：

- 9 个版本源
- `release-notes/current.md`
- `static/update-notes.json`
- `开发任务/063-编译安装包1.0.402.md`

预计生成：

- `dist/installer/SHIYIN-AI-Setup-1.0.402.exe`
- `dist/installer/SHIYIN-AI-Setup-1.0.402.exe.sha256`

## 开发阶段

- [x] 阶段 1：版本与发布说明
- [x] 阶段 2：完整回归
- [x] 阶段 3：完整构建与产物校验
- [x] 阶段 4：文档完成，等待 Git 提交与远程备份

## 验收标准

- 9 个版本源一致为 `1.0.402`。
- 完整 Python 与 Rust 测试通过。
- 完整构建脚本退出码为 0，packaged runtime smoke 通过。
- 安装包 ProductVersion / FileVersion 为 `1.0.402`。
- 安装包大小、SHA-256 和签名状态明确记录。
- staging 包含预览复制按钮、剪贴板逻辑、统一节点尺寸规范和影视尺寸隔离修复。
- 用户原有未提交文件和研究输出保持原状态。

## 已完成内容

- 已完成构建基线、工具链和工作区核对。
- 已同步 9 个版本源为 `1.0.402`，并更新发布说明与软件内更新说明。
- 完整 Python `1166` 项与 Rust `10` 项测试通过。
- 已完成 PyInstaller、Tailwind、Tauri release、Web/Skill staging、packaged runtime smoke 和 Inno Setup 构建。
- 已使用独立脚本验证安装包版本、大小、SHA-256 和签名状态。
- staging 已确认包含本次节点布局与预览复制功能。

## 当前关键修改

- 9 个版本源由 `1.0.401` 更新为 `1.0.402`。
- 发布说明增加全屏复制图片、统一节点最小尺寸、内容驱动高度与固定预览定位。
- 生成 `dist/installer/SHIYIN-AI-Setup-1.0.402.exe` 及对应 `.sha256` 文件。

## 已知问题

- 安装包未配置代码签名，预计校验状态仍为 `NotSigned`。
- 未提供 DWPose 真实人物输入图，该专项 smoke 已按预期跳过；通用 packaged runtime smoke 已通过。

## 开发日志

- 2026-09-04 16:37：启动 `1.0.402` 当前工作树安装包构建。
- 2026-09-04 16:40：完成版本同步与发布说明更新，进入完整回归阶段。
- 2026-09-04 16:43：完整 Python 与 Rust 回归通过，进入正式安装包构建阶段。
- 2026-09-04 16:44：完整构建、packaged runtime smoke、staging 检查与独立产物校验通过。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

新会话启动：

1. 阅读项目规则
2. 阅读本任务文档
3. 检查 Git branch
4. 检查 git status
5. 从“下一步”直接继续
6. 不重复已经完成的阶段
7. 不重复询问已经确认的问题
8. 继续遵守本协议
