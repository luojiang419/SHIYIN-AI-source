# 一键复刻节点深度模式与多输入方案

状态：`pose-replicate.v3.1` 已应用，待真实生成验收
当前阶段：v3.1 场景融合提示词与自由编辑设置 2/3
最后更新：2026-09-05 17:24

## 当前状态

用户提供的 `一键复刻八种组合提示词-pose-replicate.v3.1.md` 已同步到节点运行链路。文档中的 8 个 `text` 代码块已按原模板键逐字提取，参考图顺序保持不变；4 个无新场景组合与 v3.0 相同，4 个带新场景组合替换为 v3.1 的场景相机、人物落位、全主体重打光、接触投影、统一成像和场景融合检查规则。说明与验收清单没有混入实际提示词。

服务端模板版本已升级为 `pose-replicate.v3.1`，八个完整模板继续由 `canvas_core/pose_replicate_templates_v3.py` 统一保存，编译器只负责选择组合并替换 `{{output_aspect_ratio}}` 与 `{{user_instruction}}`。无补充要求时后者替换为空；有补充要求时继续先经过既有 AI 助手冲突过滤，再写入模板。

经典画布、兼容智能画布、提示词设置面板、静态资源缓存标记和测试期望均已同步为 v3.1。设置页允许自由编辑全部 8 种组合，最大长度与服务端统一为 30000 字符；输入时立即写入节点、保存刷新后保持，自定义模板完整覆盖内置模板并随下一次生成请求提交，恢复默认只清理当前组合覆盖。83 项相关 Python 回归、Node 生成运行测试、浏览器三主题/窄屏交互和 1254 项全量回归均通过。尚未提交会消耗额度的 v3.1 真实 Shiying 生成，因此不能声称实际效果已经提升。

用户复核发现 `pose-replicate.v2.3` 虽能正确分配人物、服装、动作和场景所有权，但深度模式只达到姿势基本相似，没有严格复刻深度图中的动作几何、服装褶皱峰谷和局部受力形态。

已定位提示词根因：现有深度图职责被压缩为一般“空间结构辅助”，并明确写有“不得照搬原服装外轮廓或表面”；自然褶皱段又要求模型按目标服装重新计算数量和幅度。这两条共同削弱了深度图的几何控制，使模型可以重画关节附近轮廓和褶皱。

本轮将提示词升级为 `pose-replicate.v2.4`：深度图作为姿势、可见轮廓、遮挡、深度梯度和服装表面几何的硬约束；目标图继续独占服装款式、颜色、材质和设计细节。骨架模式保持关节控制，不伪装成深度级表面复刻。

进一步核验发现传输层会把动作参考和深度图统一压到最长边 `1536px`，且灰度深度图会转成 `JPEG quality=88`，导致细小褶皱深度梯度在模型接收前已经损失。本轮同时把动作参考/控制图配准对提升到最长边 `2048px`，深度图强制无损 PNG；其他参考图继续使用现有 `1536px` 策略，控制请求体积。

`pose-replicate.v2.4` 已完成：基础三图使用最小区域编辑，非换装像素保持不变；扩展组合按人物区域归一化映射动作/深度几何，避免与独立模特体型或新场景产生不可同时满足的绝对像素约束。提示词、Gemini 贴身角色说明、双画布版本与缓存标记已同步。

语法检查、82 项相关回归和 1221 项全量测试均通过。`127.0.0.1:3000` 已重启并确认加载 `pose-replicate.v2.4`，本机 `person-depth 1.0.0-candidate.3` 保持就绪。尚未提交会消耗额度的真实 Shiying 任务，因此不能声称褶皱级完全复刻已经通过目视验收。

已完成现有一键复刻节点、经典/智能画布端口、DWPose 自动提取、图片生成请求、深度图接口与 AI 助手提示词优化链路核验。

已完成深度图首选模式、节点级平台/模型参数、四输入端口组合路由、固定提示词模板与用户文本 AI 规范化策略设计，并输出可交互 HTML 论述报告。

已补充高精度人物深度组件集成方案：一键复刻深度模式使用安装包外下载的 `person-depth-worker` 与约 1.79GB 模型权重；首次运行弹窗确认，国内镜像优先、系统代理外网回退。提示词新增动作参考自然褶皱的受力拓扑复刻约束。

新增节点内下载进度设计：下载期间在内部控制图区域下方显示系统级组件进度，包含整数百分比、已下载/总量、当前下载源和阶段；生成按钮保持禁用但参数与提示词可继续编辑。

高精度人物深度组件/API、服务端 `pose-replicate.v2.2` 编译器、专用任务入口与经典/智能画布接线均已完成。新节点默认 `depth / shiying / gemini-3-pro-image-preview / 2k / 自动跟随原图`，存量节点保留骨架模式与已有手动画幅；四个外部端口、内部控制图、独立缓存、共享组件进度、安装确认弹窗与严格模型可用性检查已落地。

代码静态检查、1197 项全量测试和隔离后端浏览器验证均通过。当前不能完成真实高精度深度生成验收：正式 worker/模型发布包、镜像 URL、SHA-256 尚未产出，且 Depth Anything V2 Large 的 `CC-BY-NC-4.0` 许可需要先确认或替换；内置 manifest 因此保持禁用。

正式高精度组件发布 manifest、国内镜像包和 SHA-256 尚未生成。开发阶段先实现 manifest 驱动、无发布清单时明确不可安装的组件管理与 API，不伪造下载地址，也不以现有快速深度静默降级。

高精度组件本地候选发布物 `1.0.0-candidate.3` 已验证：BiRefNet 固定 revision 自定义代码必须通过 SHA-256 白名单；两个 ZIP 已完成真实大小与哈希记录，并通过本地 HTTP 下载、校验、安全解压、原子激活和 packaged worker CUDA smoke。Depth Anything V2 Large 许可证决策前，正式 manifest 仍保持禁用且 URL 为空。

用户已确认本软件只由本人非商业使用且不会商业分发。当前按本机私有组件安装继续使用 Depth Anything V2 Large；候选 ZIP 不上传公开 Release，模型权重不进入 Git，内置公开 manifest 继续禁用。

candidate.3 已安装到本机 `data/system/components/person-depth`；实际 `3000` 应用已加载本机 manifest。新建“一键复刻本机验收”画布后，动作参考、目标图和内部深度控制图均已就绪，生成按钮已启用。下一步会向 Shiying 发送三张参考图并消耗一次图片生成额度，当前等待用户确认。

用户确认后已提交一次 Shiying 真实生成，任务成功返回 `SHIYIN-000041-20260905.jpg`，但结果错误地生成三栏对比拼图。两张输入均为单幅竖图；根因是模板要求保留动作参考裁切，同时节点输出为 16:9，模型用分栏化解冲突。现已升级 `pose-replicate.v2.2`，将单张连续画面、单一人物实例和输出画幅设为最高优先级；源图裁切降为内容取舍参考。

第二次 Shiying 真实生成返回 `SHIYIN-000042-20260905.jpg`，输出尺寸为 `2752×1536`，说明 16:9 尺寸参数和 `pose-replicate.v2.2` prompt 均已正确下发；但结果仍在同一背景内并列三个相同人物，证明仅增加文字禁令不足以解决竖图强制横幅时的人物复制。

按用户最新要求改为自动适配动作参考原尺寸：画幅新增“自动（跟随原图）”，服务端读取动作参考尺寸并映射到最接近的上游支持比例。当前 `4000×6000`（2:3）样本稳定解析为 `3:4`，2K 请求尺寸为 `1536×2048`；当前验收画布已持久化为自动画幅。

自动画幅真实生成已完成：用户在确认前已通过当前画布提交 `SHIYIN-000043-20260905.jpg`，因此接力会话没有重复点击。请求审计为 `source → 3:4 / 1536×2048`，约 39.64 秒返回；实际输出 `1792×2400`，为单张连续画面且只含一个人物，动作、身份、背景与目标服装迁移均符合基础三图验收要求。

完整五图扩展验收素材已加入当前画布：模特主体使用独立白色上衣模特图 `ai_ref_6e902b414d28.png`（3584×4800），场景使用无人荒野牛仔裤装置图 `ai_ref_0f1975cce203.png`（2752×1536）。动作参考、深度控制图、目标服装、模特主体与场景五个角色均显示就绪，自动画幅仍将按动作参考解析为 3:4；尚未提交付费生成。

首次完整五图真实生成返回 `SHIYIN-000044-20260905.jpg`：`model-full-look-scene / source → 3:4 / 1536×2048` 和五张参考顺序均正确，实际输出 `1792×2400`，单人物、单画面与荒野场景正确；但最终人物身份错误取自动作参考，服装错误取自模特主体的白色上衣，未使用目标图豹纹外套。根因是 Gemini 请求只发送“总提示词 + 五张裸图”，已有 `asset_index/role_label` 未贴近对应图片发送，导致模型混淆图1、图3和图4所有权。

已完成 Gemini 多参考图角色绑定修复：带角色元数据的请求现在按“图号与角色说明 → 紧邻图片”逐组发送，动作参考、控制图、目标服装、模特主体、场景各自附带只读职责；全部图片后再追加角色不可交换锚点。无角色普通参考图保持原协议，不受影响。

角色绑定修复后的两次后台任务分别生成 `SHIYIN-000045-20260905.jpg` 与 `SHIYIN-000046-20260905.jpg`。两张都正确使用 3:4 和荒野场景；`000045` 仍为动作参考身份与白色上衣，`000046` 已正确使用豹纹目标服装但仍为动作参考身份。证据说明贴身标签改善了服装归属，但 Gemini 仍优先继承第一张人物图的身份。

模板已升级为 `pose-replicate.v2.3`：仅在存在模特主体时，将模特主体提升为图1和唯一人物编辑底图，随后依次发送动作参考、控制图、目标服装和场景；没有模特主体的基础三图/四图顺序保持不变。前端模板版本、缓存标记、服务端编译器和八路组合测试已同步。

用户手动完成 `pose-replicate.v2.3` 完整五图真实生成，结果为 `SHIYIN-000047-20260905.jpg`。请求审计确认 `model-full-look-scene / source → 3:4 / 1536×2048`、`assistant_calls=0`，参考顺序为 `model_subject → pose_reference → control_map → target_image → scene`；约 44.92 秒返回 `1792×2400` 成片。用户确认结果正确，目视复核也确认模特主体身份、豹纹目标服装、动作姿势、荒野场景、单人物与单画面全部满足验收。

已完成节点内手动图片输入：动作参考、目标图、模特主体和场景四张缩略图卡片均可点击上传、替换和移除手动图片。每个角色独立保存手动图片；手动图片存在时优先使用且忽略该角色连线，移除后立即恢复读取已有连线。内部控制图仍只由动作参考自动生成。

已完成输入端口布局规范化：参照分镜合成节点，将四个输入端口与文本状态收拢到节点顶部固定行距列表，缩略图区域整体下移，端口标签不再覆盖图片和参数区。

## 下一步

下一步首先执行：

1. 经用户确认后，使用既有一键复刻样本提交一次 `pose-replicate.v3.1` Shiying 真实生成。
2. 对照动作参考和深度图逐区检查肩线、肘腕、手指、遮挡边界及主要褶皱峰谷。
3. 若仍只有基本相似，记录请求审计和输出，评估切换到支持原生 ControlNet/depth conditioning 的生成链路；纯多图提示词无法提供数学意义的像素级保证。

## 当前 TODO

- [x] 复核 `pose-replicate.v2.3` 编译器与 Gemini 多图传输链路
- [x] 将用户提供的 v3.0 八个完整模板逐字同步为运行时唯一内置模板源
- [x] 同步服务端、经典/智能画布、提示词设置与静态资源版本为 `pose-replicate.v3.0`
- [x] 验证八模板键、图序、占位符和源文档内容 8/8 一致
- [x] 将 v3.1 八个完整模板逐字同步，并确认 4 个场景组合更新、4 个无场景组合保持一致
- [x] 将提示词设置编辑上限统一到 30000，并验证输入即生效、持久化、完整提交和按组合恢复默认
- [x] 定位深度图与自然褶皱约束互相削弱的根因
- [x] 升级 `pose-replicate.v2.4` 深度几何硬锁模板
- [x] 同步 Gemini 角色说明、双画布模板版本和缓存标记
- [x] 保持动作/深度配准对为 `2048px` 并对深度图使用无损 PNG
- [x] 补充并执行提示词契约测试
- [x] 执行全量回归并重启开发服务
- [ ] 完成一次 `pose-replicate.v3.1` 真实生成与场景融合/褶皱级目视验收

- [x] 阅读项目规则、README、Git 状态和相关任务文档
- [x] 核验经典画布与智能画布的一键复刻实现
- [x] 核验深度图 API 与高精度人物深度项目现状
- [x] 核验 AI 助手图片提示词优化链路
- [x] 完成节点 UI、端口语义和默认参数设计
- [x] 完成组合模板、AI 规范化和 API 数据流设计
- [x] 完成交互式 HTML 报告与视觉验证
- [x] 完成节点内模型下载百分比与校验阶段交互设计
- [x] 实现外置高精度 worker 与组件管理器
- [x] 实现 person-depth 状态、安装、重试与推理 API
- [x] 实现版本化提示词编译器与八条组合路由测试
- [x] 实现一键复刻专用提交语义
- [x] 改造共用节点 UI、经典画布与智能画布四端口运行逻辑
- [x] 完成静态检查、全量测试与浏览器布局/交互回归
- [x] 实现四个角色卡片的手动上传、替换和移除
- [x] 实现手动图片覆盖连线、移除后恢复连线
- [x] 完成手动输入的双画布回归验证
- [x] 实现顶部紧凑输入端口状态列表
- [x] 完成端口圆点与文本行固定对齐
- [x] 完成缩略图下移后的布局与交互回归
- [x] 完成首个 4000×6000 真实人物 packaged worker 回归与 trace manifest
- [x] 完成本机私有高精度组件安装和隔离 API 验证
- [x] 完成实际应用节点动作参考、目标图和内部深度控制图就绪验证
- [x] 修复 16:9 输出错误生成三联画的问题
- [x] 完成动作参考原尺寸到最近支持画幅的自动适配
- [x] 完成实际应用一键复刻节点基础三图真实生成验收
- [x] 准备完整五图扩展验收素材并确认五个角色全部就绪
- [x] 完成 Gemini 多参考图图号与角色贴身绑定
- [x] 升级 `pose-replicate.v2.3`，在完整组合中将模特主体提升为图1编辑底图
- [x] 完成完整五图组合真实生成验收
- [x] 确认复杂人物深度样本矩阵继续由 065 深度项目承担

## 最近验证状态

- v3.1 模板源核对：外部 Markdown 的 8 个 `text` 代码块与运行时模板键和值完全一致，所有模板各含一个画幅占位符和一个补充要求占位符
- v3.1 相关回归：83 项 Python 测试全部通过；Node 运行测试确认自定义完整模板进入实际任务 payload
- v3.1 浏览器设置页：8 个组合可自由编辑，输入即时写入节点，空值保护、保存刷新持久化、恢复单组合默认、三种主题和 720×920 窄屏均通过，页面脚本错误为 0
- v3.1 全量回归：`python -m pytest tests -q`，1254 项全部通过；相关 Python/JavaScript 语法检查通过
- v3.0 模板源核对：外部 Markdown 的 8 个 `text` 代码块与 `POSE_REPLICATE_V3_TEMPLATES` 键和值完全一致；每项各含一个画幅占位符和一个补充要求占位符
- v3.0 静态检查：`python -m py_compile canvas_core/pose_replicate_prompts.py canvas_core/pose_replicate_templates_v3.py` 通过；`git diff --check` 通过
- v3.0 相关回归：`python -m pytest tests/test_pose_replicate_prompts.py tests/test_pose_replicate_custom_templates.py tests/test_canvas_special_nodes.py -q`，78 项全部通过
- v3.0 Node 运行回归：`node tests/js/pose_replicate_runtime.test.cjs` 通过并发、持久化、失败隔离、克隆和刷新批处理检查
- v3.0 全量回归：`python -m pytest tests -q`，1249 项全部通过；两个已修改 JavaScript 文件均通过 `node --check`
- 当前 3000 端口由 `D:\Program Files\SHIYIN AI` 已安装版后端占用，源码模板需进入后续安装包后才会替换该安装实例；本轮未擅自覆盖安装目录或构建安装包
- 静态检查：`node --check` 通过三个画布 JavaScript；`python -m py_compile` 通过后端与提示词编译器；`git diff --check` 通过
- 单元测试：`python -m pytest tests -q`，1197 项全部通过
- 编译：未执行正式安装包构建；外置 worker 发布包尚未构建
- 运行测试：隔离后端 `127.0.0.1:3119` 验证新节点默认值、5 张卡片、4 个端口、深度/骨架切换和组件不可用阻断
- 布局测试：1440×1000 与 720×920 下节点各区域无相交，文本无 `scrollWidth > clientWidth`，浏览器无 console error
- 交互测试：固定模板路径测试确认为 0 次 AI；有用户文本路径确认为 1 次 AI；八条组合路由全部覆盖
- 补充验证：报告共 11 个 section 且标签闭合；下载状态切换为“国内镜像 / 38%”成功；4 条下载源链路存在；485px 下 `clientWidth = scrollWidth`
- 节点进度验证：Chrome DOM 确认节点标题、控制图卡、进度条、下载详情和禁用生成按钮同步为 38%；1440×2200 渲染无重叠
- 本轮静态检查：`node --check` 通过三个画布 JavaScript；`git diff --check` 通过
- 本轮单元测试：`python -m pytest tests -q`，1197 项全部通过
- 本轮浏览器交互：经典画布真实上传、手动覆盖连线、移除后恢复原连线 URL 全部通过；四个输入槽等宽等高，移除按钮位于卡片内，节点 `scrollWidth = clientWidth`
- 智能画布说明：项目已下线独立智能画布并由后端将兼容页归一化到经典画布；保留代码路径由静态契约测试覆盖
- 端口布局验证：默认视口与 `720×920` 下四个端口圆点和文本行中心偏差均为 `0.85px`，缩略图与列表间距约 `8.46px`
- 端口布局溢出：节点正文 `clientWidth = scrollWidth`、`clientHeight = scrollHeight`，缩略图点击仍可打开文件选择器，console 无 warning/error
- 候选组件：`1.0.0-candidate.3` 总计 `5,984,927,484` bytes；本地下载、SHA-256、安全解压、原子激活和 packaged worker CUDA smoke 状态为 `ready`
- 候选包策略：内置 manifest 保持 `enabled:false`、下载 URL 为空，未绕过 Depth Anything V2 Large 的 `CC-BY-NC-4.0` 许可门
- 真实人物回归：UNC `XSY_1801.jpg` 通过 candidate.3 packaged worker 生成 `4000×6000 uint16` 深度图，推理 `7,626ms`，与原型基线平均差 `0.0092/65535`
- 本机组件：candidate.3 安装后管理器重载状态 `ready`；隔离 API 状态为 100%，16-bit 深度接口返回 200
- 最新全量测试：`python -m pytest tests -q`，1207 项全部通过
- 实际应用节点：`127.0.0.1:3000` 中两张手动输入与自动深度图均已就绪，生成按钮可用，节点无溢出且 console 无异常
- 首次真实生成：只提交一次并成功返回，但输出为三栏对比拼图，不符合单张复刻验收标准；未重复提交
- `pose-replicate.v2.2`：实际输出画幅进入编译器和审计元数据；最高优先级与最终检查双重禁止拼图、分栏、三联画、对比布局、参考图复现和人物重复
- 修复验证：相关 64 项测试通过；全量 `python -m pytest tests -q` 为 1208 项全部通过
- 第二次真实生成：仅提交一次，`SHIYIN-000042-20260905.jpg` 为 `2752×1536`，约 40 秒返回；结果仍包含三个并列人物，验收不通过
- 参数审计：历史记录确认 `pose-replicate.v2.2 / fixed-template / assistant_calls=0 / 16:9 / 2048×1152` 与三张参考图顺序均正确进入实际请求
- 自动画幅验证：真实动作参考为 `4000×6000`，解析为最近支持比例 `3:4`，2K 请求为 `1536×2048`；当前画布节点已保存 `poseReplicateRatio=source`
- 自动画幅测试：相关 60 项通过；全量 `python -m pytest tests -q` 为 1215 项全部通过
- 静态检查：三个画布 JavaScript、`main.py` 与提示词编译器语法检查通过，`git diff --check` 通过
- 运行服务：`127.0.0.1:3000` 已加载自动画幅版本，页面显示“自动（跟随原图）”并保留三次真实生成 Output
- 第三次真实生成：检测到用户已提交的 `SHIYIN-000043-20260905.jpg` 后未重复点击；请求为 `source → 3:4 / 1536×2048`，约 39.64 秒返回
- 第三次输出验收：实际 `1792×2400`，单张连续画面、单一人物，无拼图、分栏或人物复制；基础三图链路验收通过
- 五图准备：新增模特主体 `ai_ref_6e902b414d28.png`（3584×4800）与场景 `ai_ref_0f1975cce203.png`（2752×1536）；四个外部角色和内部深度图全部显示“手动图片/已就绪”，未提交付费任务
- 首次五图生成：`SHIYIN-000044-20260905.jpg`，约 43.86 秒，实际 `1792×2400`；单人物、单画面和场景正确，但身份/服装所有权错误，验收不通过
- 五图请求审计：`model-full-look-scene / fixed-template / assistant_calls=0 / source → 3:4 / 1536×2048`，五张参考 URL 与角色顺序全部正确
- 角色绑定修复：Gemini 请求内容改为五组“角色文本 + inlineData”并追加不可交换锚点；普通无角色参考图无额外文本
- 修复测试：`python -m pytest tests/test_pose_replicate_prompts.py -q` 为 28 项通过；全量 `python -m pytest tests -q` 为 1217 项通过
- 修复运行状态：`127.0.0.1:3000` 已重启加载新后端，五图输入和四个历史 Output 均保留，未再次付费提交
- 角色绑定复测：后台存在两个成功任务，分别输出 `SHIYIN-000045-20260905.jpg` 与 `SHIYIN-000046-20260905.jpg`；后一张服装归属正确，但两张身份仍错误取自动作参考
- 并发说明：等待首个长任务期间检测到另一个独立 POST；接力流程没有再补发，最终只验收已经存在的两个结果
- `pose-replicate.v2.3`：有模特主体时参考顺序改为 `model_subject → pose_reference → control_map → target_image → scene`，并明确图1为唯一人物编辑底图
- v2.3 测试：相关 62 项通过；全量 `python -m pytest tests -q` 为 1217 项通过
- v2.3 运行状态：`127.0.0.1:3000` 已重启，浏览器实际加载 `feature=pose-replicate-v2.3`，五图输入保持就绪，未提交 v2.3 付费任务
- v2.3 五图真实生成：`SHIYIN-000047-20260905.jpg`，约 44.92 秒，实际 `1792×2400`；请求为 `model-full-look-scene / source → 3:4 / 1536×2048 / assistant_calls=0`
- v2.3 参考顺序：`model_subject → pose_reference → control_map → target_image → scene`，与编译器审计一致
- v2.3 目视验收：模特主体身份、豹纹目标服装、动作参考姿势、荒野场景、单人物和单画面均正确；用户确认结果正确
- v2.4 提示词：基础三图恢复最小区域编辑；深度模式新增逐像素配准表面、关节/轮廓/遮挡硬锁、主要褶皱峰谷映射、局部拓扑变化白名单和逐区输出前检查
- v2.4 传输：动作参考与控制图最长边统一为 `2048px`；深度控制图保持无损 PNG，不再转换为 `JPEG quality=88`
- v2.4 相关测试：`python -m pytest tests/test_pose_replicate_prompts.py tests/test_canvas_special_nodes.py tests/test_static_angle_reference_panel.py tests/test_image_prompt_optimization.py -q`，82 项全部通过
- v2.4 全量测试：`python -m pytest -q`，1221 项全部通过；`python -m py_compile canvas_core/pose_replicate_prompts.py main.py` 与 `git diff --check` 通过
- v2.4 运行状态：`127.0.0.1:3000` 已重启；授权请求确认页面包含 `feature=pose-replicate-v2.4`、不含 v2.3，高精度组件 `1.0.0-candidate.3` 为 `ready`
- v2.4 真实生成：尚未提交，等待用户确认消耗一次 Shiying 额度
- 当前 Git branch：`feat/storyboard-merge-node`
- 立项前最近 Git commit：`37aec0b fix: 断开连接后立即清理输入缩略图`
- 本任务方案 commit：`6b47064 docs: 完成一键复刻节点调整方案`
- 本任务代码 commits：`0fce9c1`、`13ea9eb`、`4ff314f`、`66fd344`、`861ecbf`、`68f755d`、`244eae3`、`c27cb64`、`a197c16`、`98b48c5`、`c6e0d71`、`13e0c69`
- 最近功能 Git commit：`13e0c69 fix: 优先一键复刻模特主体身份`
- 本轮深度几何硬锁实现 commit：`7460191 fix: 强化一键复刻深度几何约束`
- push：本任务代码与状态文档均正常推送至 `origin/feat/storyboard-merge-node`

---

## 任务目标

为一键复刻节点形成可直接进入开发的完整调整方案：

- 新增并默认启用深度图复刻模式，保留骨架图兼容模式。
- 补全节点级平台、模型、画幅、分辨率和复刻模式参数。
- 新节点默认使用 `shiying / gemini-3-pro-image-preview / 2k / 自动跟随原图 / depth`；显式画幅仍可手动覆盖。
- 集成用户已验证的三图高保真局部换装提示词。
- 增加 `模特主体` 与 `场景` 输入端口，形成四输入组合能力。
- 无用户文本时使用版本化固定模板；有用户文本时才调用 AI 助手规范化为 Nano Banana Pro 提示词。
- 输出包含节点示意图、逻辑链路、组合矩阵、数据契约、实施步骤和验收标准的 HTML 报告。

## 当前项目现状

- `static/js/canvas-special-nodes.js` 维护经典/智能画布共用的一键复刻节点 UI，当前仅显示动作参考、自动骨架和目标图三栏。
- 当前外部输入端口只有 `pose-reference` 与 `target-image`。
- 当前固定参考顺序为骨架图、动作参考、目标图，默认提示词为英文 DWPose 动作迁移提示。
- 当前节点仅保存 `poseReplicateRatio` 与 `poseReplicateResolution`；经典画布运行时取第一个图片平台/模型，智能画布继承节点或全局运行参数。
- `/api/depth/estimate` 只提供本地快速 8-bit 深度图，继续服务角度参考，不作为一键复刻高精度深度模式的静默降级。
- 高精度人物深度项目已完成单图原型和集成方案；将通过安装包外置 worker/模型组件提供，节点使用前需完成首次下载与 smoke 验证。
- 图片生成统一入口已经支持 AI 助手多模态提示词优化，但普通图片节点当前默认强制优化；一键复刻固定模板需要专用策略避免无文本时仍产生 AI 调用。

## 技术方案

### 节点语义

- 外部输入：动作参考、目标图、模特主体（可选）、场景（可选）。
- 内部控制图：从动作参考自动生成深度图或骨架图，不暴露为额外输入端口。
- 基础组合使用动作参考作为原始人物底图，目标图提供新服装。
- 连接模特主体后，身份与身体归属切换为模特主体；连接场景后，最终环境归属切换为场景图。

### 提示词路由

- 参考图顺序固定为：动作参考、控制图、目标图、模特主体（可选）、场景（可选）。
- 以用户验证的三图局部换装提示作为 `pose-replicate.v2.1.base-depth` 基础模板；2.1 增加自然褶皱受力拓扑复刻约束。
- 根据可选端口组合附加身份归属、全套服装迁移和场景归属片段，不复制多份完整长模板。
- 用户文本为空时由固定模板编译器直接生成最终提示词，不调用 AI 助手。
- 用户文本非空时，AI 助手只规范化用户增量，不能改写端口角色、引用顺序和硬约束；合并后只提交一次最终提示词。
- 深度模式额外复刻动作参考中由姿势和受力产生的自然褶皱位置、走向、层次、拉伸与堆叠；目标服装仍控制材质、版型、松量及最终褶皱幅度，禁止机械复制旧服装表面。

### API 方向

- 推荐建立一键复刻专用编译/任务入口，服务端维护模板版本与组合路由，再复用现有图片任务执行器。
- 请求保存控制模式、模板版本、用户原始文本、实际最终提示词、参考图角色、平台、模型、尺寸和优化元数据。
- 无提示词路径标记 `prompt_source=fixed-template`；有提示词路径标记 `prompt_source=assistant-merged`。
- 深度模式在提交前查询 `/api/person-depth/component/status`，未就绪时进入下载弹窗或允许用户切换骨架模式，不提交不完整任务。

## 文件 / 模块清单

本阶段新增：

- `开发任务/066-一键复刻节点深度模式与多输入方案.md`
- `输出/一键复刻节点功能调整方案/一键复刻节点功能调整设计报告.html`

正式开发预计修改：

- `static/js/canvas-special-nodes.js`
- `static/js/canvas.js`
- `static/js/smart-canvas.js`
- `static/css/pose-replicate-node.css`
- `main.py`
- `canvas_core/pose_replicate_prompts.py`（建议新增）
- `tests/test_canvas_special_nodes.py`
- `tests/test_image_prompt_optimization.py`
- 一键复刻专用后端测试文件（建议新增）

## 开发阶段

- [x] 阶段 1：现状核验与方案边界
- [x] 阶段 2：交互式 HTML 方案报告
- [x] 阶段 3：报告验证与交付
- [x] 开发阶段 1：高精度人物深度组件契约与 API
- [x] 开发阶段 2：版本化提示词编译器与专用提交语义
- [x] 开发阶段 3：双画布节点、端口、状态与运行逻辑
- [x] 开发阶段 4：代码整体回归
- [x] 开发阶段 5：节点内手动图片输入与连线优先级
- [x] 开发阶段 6：输入端口顶部紧凑布局
- [x] 发布阶段：本机私有高精度组件与真实样本最终验收

## 验收标准

1. 报告明确展示新节点四个输入端口、内部控制图和五项生成参数。
2. 新节点默认值为 `shiying / gemini-3-pro-image-preview / 2k / 自动跟随原图 / depth`，并能把 `4000×6000` 动作参考映射为最接近的 `3:4`。
3. 报告覆盖四种可选端口组合及深度/骨架两种控制模式。
4. 报告完整保留用户已验证提示词，并说明如何版本化集成。
5. 报告说明无文本固定模板零 AI 调用、有文本仅一次 AI 规范化的逻辑。
6. 报告给出请求字段、状态机、兼容迁移、错误处理、实施文件和可执行验收项。
7. HTML 可直接打开，桌面与窄屏无明显内容重叠或溢出。
8. 报告明确说明高精度 worker/模型不进入安装包，并给出首次运行弹窗、国内镜像、系统代理和校验状态机。
9. 提示词模板升级为 `pose-replicate.v2.1`，明确复刻动作引起的自然褶皱拓扑，同时由目标服装控制材质与最终褶皱幅度。
10. 深度模式未就绪时不提交生成任务，不得静默使用快速 Small ONNX 冒充高精度控制图。
11. 模型下载期间节点内显示百分比、已下载/总量、下载源和阶段；多个节点共享同一下载状态，生成按钮禁用但参数与提示词仍可编辑。
12. 字节下载到 100% 后分别显示校验、安装和 smoke 阶段，避免用户误判卡死。
13. 动作参考、目标图、模特主体和场景缩略图卡片均支持点击上传、替换和移除手动图片。
14. 同一角色存在手动图片和连线时只使用手动图片；移除手动图片后恢复使用该角色已有连线，不删除连线。
15. 手动替换或移除动作参考时清理旧控制图，并基于当前生效的动作参考重新提取深度图或骨架图。
16. 四个输入端口及文本状态位于节点顶部紧凑列表，端口圆点与对应行中心对齐，不再覆盖缩略图、控制图或参数区。
17. 缩略图区域位于输入列表下方，四张卡片尺寸稳定，桌面与窄屏均无重叠和横向溢出。

## 已完成内容

- 完成当前实现与依赖链路核验。
- 明确一键复刻深度模式使用安装包外置的 `/api/person-depth/estimate` 高精度组件；现有 `/api/depth/estimate` 只保留快速角度参考职责。
- 明确四外部输入、内部控制图、稳定参考顺序和版本化模板路由方向。
- 完成可交互 HTML 报告，包含节点示意图、组合实验台、逻辑链条、完整已验证提示词、API 契约、兼容策略与验收标准。
- 完成桌面和窄屏浏览器渲染检查，并验证组合实验台交互状态。
- 补充高精度组件首次运行下载弹窗、三段下载源回退、运行时/模型分包、校验 smoke 与许可证边界。
- 完成 `pose-replicate.v2.1` 自然褶皱增强，并保留原提示词的身份、服装和场景所有权。
- 完成节点内下载进度 UI 设计，并让报告安装状态切换器同步驱动节点的等待、下载、校验、失败和就绪状态。
- 完成外置 worker 协议、manifest 组件管理、person-depth API 与安全下载状态机。
- 完成版本化提示词编译器、八条组合路由、冲突过滤和专用任务入口。
- 完成四输入节点、节点级生成参数、深度/骨架独立缓存、安装确认弹窗及双画布运行逻辑。
- 完成全量测试与 1440×1000、720×920 浏览器运行验证。
- 完成四个角色缩略图卡片的手动上传、替换和移除；输入解析统一为手动图片优先，移除后恢复对应角色现有连线。
- 为手动/连线来源增加可见标识，并固定四张缩略图卡片高度，避免竖图撑高布局。
- 完成顶部四行输入端口状态列表，显示等待输入、可选输入、已连接或手动图片状态。
- 经典画布端口使用 `69px + 36px × index` 固定定位，兼容智能画布使用独立顶部基准；缩略图整体下移到列表下方。
- 新节点默认高度调整为 `820px`，存量节点最小高度调整为 `760px`，保证新增列表后内容不被压缩。
- 完成自动画幅选项与服务端尺寸解析；上游不支持原始 2:3 时选择最近的 3:4，而不是强制扩展为 16:9。

## 当前关键修改

- `开发任务/066-一键复刻节点深度模式与多输入方案.md`：记录现状、方案、验证和后续开发入口。
- `输出/一键复刻节点功能调整方案/一键复刻节点功能调整设计报告.html`：可直接打开的完整图文方案报告。
- `static/js/canvas-special-nodes.js`：增加角色级手动图片持久化、上传/替换/移除和连线回退逻辑。
- `static/css/pose-replicate-node.css`：增加上传态、来源标识、移除按钮和固定缩略图尺寸。
- `static/canvas.html`、`static/smart-canvas.html`：更新一键复刻节点资源缓存版本。
- `static/js/smart-canvas.js`：更新节点拖放提示，不再宣称仅接受端口连线。
- `tests/test_canvas_special_nodes.py`、`tests/test_blender_director_canvas.py`：更新资源契约并覆盖手动输入优先级。
- `static/js/canvas.js`、`static/js/smart-canvas.js`：统一端口固定行距定位与一键复刻节点新高度。
- `main.py`、`static/js/canvas-special-nodes.js`、双画布运行脚本：新增自动画幅请求、动作参考尺寸解析和最近支持比例映射。
- `tests/test_pose_replicate_prompts.py`、`tests/test_canvas_special_nodes.py`：覆盖自动比例映射、显式比例保留、请求审计和静态资源版本。
- `canvas_core/pose_replicate_templates_v3.py`：保存用户提供的八个 v3.1 完整提示词模板，避免运行时重新拼接改变原稿。
- `canvas_core/pose_replicate_prompts.py`：升级模板版本并按组合选择 v3.1 完整模板，只替换画幅与补充要求占位符。
- `static/js/pose-replicate-settings.js`、`static/js/smart-canvas.js`、双画布 HTML：同步 v3.1 请求策略和缓存标记；设置页自由编辑上限统一为 30000。
- 对应 Git commit：`6b47064`
- 高精度组件与自然褶皱补充 commit：`399472b`
- 节点内下载进度补充 commit：`4808f6a`
- 高精度组件/API commit：`0fce9c1`
- 提示词编译器/专用入口 commit：`13ea9eb`
- 双画布节点与运行逻辑 commit：`4ff314f`
- 节点内手动图片输入 commit：`66fd344`
- 输入端口顶部紧凑布局 commit：`861ecbf`
- 自动画幅适配 commit：`98b48c5`
- Gemini 参考图角色绑定 commit：`c6e0d71`
- 模特主体图1优先与 `pose-replicate.v2.3` commit：`13e0c69`

## 已知问题

- 两次 16:9 真实生成均出现三个人物；自动解析到 3:4 后同一组输入已稳定输出单人物，画幅冲突问题已解决。
- 完整五图组合已通过 `pose-replicate.v2.3` 真实付费回归，身份、服装、动作、场景所有权均正确。
- 两个已成功后台任务对应的 Output 在长时间生成和多会话保存竞争后未自动回填；服务重启后内存任务记录失效，图片文件与生成历史仍完整保留。该恢复缺口需后续单独修复。
- 当前仅个人非商业使用，本机私有 candidate.3 已就绪；公开镜像与商业分发不在本次范围。
- 固定模板已通过专用服务端入口受控跳过通用二次优化；该问题已解决并有测试覆盖。
- `pose-replicate.v3.1` 已完成代码、设置页交互与契约验证，但尚未进行会消耗额度的真实生成对照，效果结论待目视验收。

## 开发日志

- 2026-09-04：完成一键复刻节点、深度接口、图片任务与 AI 助手提示词链路核验，建立方案任务文档。
- 2026-09-04：完成交互式设计报告、桌面/窄屏渲染与组合路由交互验证，方案阶段结束。
- 2026-09-04：补充安装包外置高精度组件、首次运行下载与代理回退方案，并增强自然褶皱复刻提示词约束。
- 2026-09-04：补充节点内模型下载百分比、字节进度、来源和后处理阶段展示方案。
- 2026-09-04：用户确认继续功能开发；核对源码后进入高精度人物深度组件契约阶段。
- 2026-09-04：完成外置 worker 源码、组件管理器、持久进程客户端与 person-depth API；相关 27 项测试通过。
- 2026-09-04：完成 `pose-replicate.v2.1` 编译器、八条组合路由、用户增量冲突过滤、专用任务入口和请求快照；相关 70 项回归通过。
- 2026-09-04：完成四输入节点、深度/骨架独立缓存、组件进度与确认弹窗、节点级生成参数和经典/智能画布专用提交；1197 项全量测试通过，浏览器双视口无内部重叠或文本溢出。
- 2026-09-04：新增四个角色缩略图卡片手动上传/移除需求，明确手动图片优先于同角色连线，移除后恢复连线。
- 2026-09-04：完成节点内手动图片输入与连线优先级；真实浏览器验证上传、覆盖、移除和回退，1197 项全量测试通过。
- 2026-09-04：完成输入端口顶部紧凑布局、缩略图下移和节点高度适配；双视口端口对齐、溢出及点击回归通过，1197 项全量测试通过。
- 2026-09-04：完成高精度组件 `1.0.0-candidate.3` 本地组包和安装链验证；正式发布继续受 Depth Anything V2 Large 非商业许可约束。
- 2026-09-05：完成 `XSY_1801.jpg` 真实 packaged worker 回归和 trace manifest，输出与原型基线一致。
- 2026-09-05：用户确认仅个人非商业使用；candidate.3 已完成本机私有安装、重载和隔离 API 验证。
- 2026-09-05：实际应用节点完成动作参考、目标图和高精度深度控制图准备；在 Shiying 付费提交前等待用户确认。
- 2026-09-05：用户确认后完成首次 Shiying 生成；输出成功但错误成为三联画，定位为竖版裁切硬约束与 16:9 输出冲突，进入 `pose-replicate.v2.2` 修复。
- 2026-09-05：完成 `pose-replicate.v2.2`，输出画幅成为最高优先级，新增单一人物和禁止拼图硬约束；1208 项全量测试通过。
- 2026-09-05：第二次 Shiying 生成约 40 秒返回 `2752×1536`，仍为三个并列人物；确认不是旧代码、缓存或参数未下发。
- 2026-09-05：按用户要求新增自动跟随原图画幅；真实 `4000×6000` 样本解析为 `3:4 / 1536×2048`，1215 项全量测试通过，等待下一次付费验收确认。
- 2026-09-05：识别到用户已提交第三次自动画幅任务，未重复扣费；`SHIYIN-000043-20260905.jpg` 为单人物 3:4 连续成片，基础三图端到端验收通过。
- 2026-09-05：从本机既有素材选择独立模特主体与无人荒野场景，完成完整五图输入准备；停在 Shiying 付费提交前等待用户确认。
- 2026-09-05：首次五图真实生成场景和单人物约束正确，但身份取自动作参考、服装取自模特主体，确认 Gemini 裸图传输丢失角色绑定语义。
- 2026-09-05：完成 Gemini 逐图角色标签与末尾不可交换锚点，普通参考图维持兼容；相关 28 项、全量 1217 项测试通过，开发后端已重启。
- 2026-09-05：角色标签复测输出 `000045/000046`；场景稳定、后一张目标服装正确，但身份仍被首张动作参考覆盖。
- 2026-09-05：升级 `pose-replicate.v2.3`，完整组合将模特主体作为图1唯一编辑底图；相关 62 项、全量 1217 项测试通过，开发后端已加载新版本。
- 2026-09-05：用户手动完成 `v2.3` 五图真实生成；`SHIYIN-000047-20260905.jpg` 的身份、目标服装、动作、场景、单人物与单画面全部正确，066 任务完成。
- 2026-09-05：将用户新调整的八个完整提示词逐字接入 `pose-replicate.v3.0`，同步双画布版本与契约测试；78 项 Python 回归和 Node 运行回归通过，真实生成待后续确认。
- 2026-09-05：将 v3.1 场景融合稿逐字接入八组合模板，开放设置页 30000 字自由编辑并确认自定义内容即时持久化和实际提交；83 项相关测试、浏览器交互和 1254 项全量回归通过。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

新会话启动：

1. 阅读项目规则与本任务文档。
2. 检查 Git branch 与 `git status`，保留用户已有未提交修改。
3. 对照源代码确认任务文档现状。
4. 从“下一步”继续，不重复已经完成的现状核验。
5. 代码开发阶段已完成；从顶部“下一步”继续发布物与真实样本验收，不重复实现已完成模块。
