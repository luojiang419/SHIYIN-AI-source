# 编译安装包 1.0.403

状态：已完成
当前阶段：4/4
最后更新：2026-09-05 10:18

## 当前状态

当前源码版本为 `1.0.402`，目标版本为 `1.0.403`。Python、Node.js、npm、Cargo、rustc 和 Inno Setup 均可用，E 盘剩余空间约 291GB。

`pose-replicate.v2.4` 深度几何硬锁、动作/深度 2048px 配准传输与无损 PNG 深度图已提交并推送。源码后端测试正常，但浏览器源码运行方式会受桌面壳和现有数据保存竞争影响，本任务改用正式安装包验证部署资源和桌面运行链路。

当前分支为 `feat/storyboard-merge-node`。工作区另有用户原先未提交的电商、作品管理、研究脚本和输出素材；不会回退或提交这些内容，但标准构建会按当前工作树打包源码内容。

当前无已安装的 `SHIYIN AI.exe` 进程，隔离 packaged runtime smoke 可以执行。源码调试后端占用 `3000`，标准 smoke 使用 `3118`，不会冲突。

Windows x64 安装包 `SHIYIN-AI-Setup-1.0.403.exe` 已完成全链路构建、隔离 packaged runtime smoke 和独立产物校验。冻结后端 archive 与 staging Web 资源均确认包含 `pose-replicate.v2.4`，且旧 `v2.3` 模板常量未进入新后端。

构建完成后用户启动了当前已安装版 `1.0.402`。该桌面实例占用 `3000` 并会主动停止源码后端；其安装目录确认不含 `pose-replicate.v2.4`，这是浏览器自动刷新、回到旧状态和生成被取消的直接原因。需关闭 `1.0.402` 后手动安装 `1.0.403` 测试。

## 下一步

下一步首先执行：

当前构建任务已完成。用户手动测试首先执行：

1. 完全退出正在运行的 `SHIYIN AI 1.0.402`。
2. 运行 `dist/installer/SHIYIN-AI-Setup-1.0.403.exe` 完成覆盖升级。
3. 从安装后的桌面程序打开画布，不再同时运行源码 `127.0.0.1:3000` 服务。
4. 确认版本为 `1.0.403` 后，使用一键复刻深度模式提交一次真实生成。

## 当前 TODO

- [x] 核对 Git 状态、当前版本、上次安装包和工具链
- [x] 确认安装包按当前工作树构建且不提交无关用户改动
- [x] 同步版本与发布说明
- [x] 完整 Python 与 Rust 回归
- [x] PyInstaller、Tailwind、Tauri、staging 和 Inno Setup 构建
- [x] packaged runtime smoke
- [x] 安装包版本、哈希、签名和关键 staging 内容校验
- [x] 更新任务文档
- [x] Git 提交与远程推送

## 最近验证状态

- 静态检查：9 个版本源同步、更新说明 JSON、画布 JavaScript 语法和 `git diff --check` 通过
- 单元测试：Python `1221 passed`；Rust `10 passed`
- 编译：PyInstaller 6.19.0、Tailwind、Tauri release 和 Inno Setup 6.7.1 全部通过
- 运行测试：packaged runtime smoke 通过；启动 `4590ms`，进程树私有内存 `213.21MB/250MB`，第二实例保护和 sidecar 退出清理正常
- 安装包：`dist/installer/SHIYIN-AI-Setup-1.0.403.exe`，`92,399,174 bytes`
- SHA-256：`54b2d6ac3e35bee68903c21a249bab75f91073bfb33889a89c0572a3a6d5e06a`
- FileVersion / ProductVersion：`1.0.403`
- 签名状态：`NotSigned`
- staging：`app/VERSION` 与 `update-notes.json` 为 `1.0.403`；经典/智能画布均提交 `pose-replicate.v2.4`；页面含 v2.4 缓存标记且不含 v2.3
- 冻结后端 archive：包含 `pose-replicate.v2.4`、深度几何硬锁、主要褶皱峰谷、Gemini 三维几何角色说明以及 `reference_to_data_url(..., lossless=...)` 参数
- 构建 Git commit：`4d82533 chore: build 1.0.403 installer`
- 远程推送：构建元数据已同步到 `origin/feat/storyboard-merge-node`

---

## 任务目标

将当前工作树构建为 Windows x64 安装包 `SHIYIN-AI-Setup-1.0.403.exe`，确保安装包包含 `pose-replicate.v2.4` 提示词、Gemini 深度角色绑定、动作/深度 2048px 配准传输和无损 PNG 深度图，并完成版本、大小、SHA-256、签名、staging 内容与桌面运行时校验。

## 当前项目现状

- 9 个版本源一致为 `1.0.402`。
- 上一安装包为 `dist/installer/SHIYIN-AI-Setup-1.0.402.exe`。
- 标准构建入口为 `tools/build-installer.ps1`。
- 标准构建包含 PyInstaller、Tailwind、Tauri release、Web/Skill staging、packaged runtime smoke 和 Inno Setup。
- 高精度人物深度组件不进入安装包，继续从用户数据目录复用本机私有 `1.0.0-candidate.3`；安装包只包含组件管理和调用能力。

## 技术方案

- 使用 `tools/increment-version.ps1 -Version 1.0.403` 同步版本源。
- 在发布说明和软件内更新说明中加入一键复刻深度几何硬锁与无损控制图传输。
- 执行 `python -m pytest -q` 和 `cargo test --manifest-path src-tauri/Cargo.toml`。
- 使用 `tools/build-installer.ps1` 完成全链路构建和隔离桌面 smoke。
- 使用 `tools/verify-installer-artifact.ps1 -Version 1.0.403` 独立校验安装包。
- 检查 `dist/installer-stage` 中的后端与 Web 资源，确认 `pose-replicate.v2.4` 和 `lossless=is_depth_map` 已进入实际安装内容。

## 文件 / 模块清单

预计修改：

- `VERSION`
- `package.json`
- `package-lock.json`
- `src-tauri/tauri.conf.json`
- `src-tauri/Cargo.toml`
- `src-tauri/Cargo.lock`
- `main.py`（仅版本字段）
- `release-notes/current.md`
- `static/update-notes.json`
- `开发任务/067-编译安装包1.0.403.md`

预计生成：

- `dist/installer/SHIYIN-AI-Setup-1.0.403.exe`
- `dist/installer/SHIYIN-AI-Setup-1.0.403.exe.sha256`

## 开发阶段

- [x] 阶段 1：版本与发布说明
- [x] 阶段 2：完整回归
- [x] 阶段 3：完整构建与产物校验
- [x] 阶段 4：文档完成，等待 Git 提交与远程备份

## 验收标准

- 9 个版本源一致为 `1.0.403`。
- 完整 Python 与 Rust 测试通过。
- 标准构建脚本退出码为 0，packaged runtime smoke 通过。
- 安装包 ProductVersion / FileVersion 为 `1.0.403`。
- 安装包大小、SHA-256 和签名状态明确记录。
- staging 后端包含 `pose-replicate.v2.4`、深度角色硬约束与无损 PNG 传输逻辑。
- staging 画布资源提交模板版本为 `pose-replicate.v2.4`。
- 用户原有未提交文件和研究输出保持原状态。

## 已完成内容

- 完成构建基线、工具链、磁盘空间、端口和运行进程核对。
- 已同步 9 个版本源为 `1.0.403`，并更新发布说明与软件内更新说明。
- 完整 Python `1221` 项和 Rust `10` 项测试通过。
- 已完成 PyInstaller、Tailwind、Tauri release、Web/Skill staging、packaged runtime smoke 和 Inno Setup 构建。
- 已使用独立脚本验证安装包版本、大小、SHA-256 和签名状态。
- 已检查 staging 与 PyInstaller archive，确认 `pose-replicate.v2.4` 及无损深度传输实际进入安装包。
- 已定位浏览器测试自动回退为正在运行的已安装版 `1.0.402` 抢占 `3000` 和停止源码后端。
- 构建元数据已提交为 `4d82533` 并推送到远程分支。

## 当前关键修改

- 9 个版本源由 `1.0.402` 同步为 `1.0.403`。
- 发布说明增加一键复刻深度几何硬锁和无损 PNG 控制图传输。
- 生成 `dist/installer/SHIYIN-AI-Setup-1.0.403.exe` 及对应 `.sha256` 文件。

## 已知问题

- 安装包未配置 Authenticode 代码签名，预计签名状态为 `NotSigned`。
- 当前安装包按脏工作树构建，会包含现有未提交源码内容；Git 提交仍严格限制为版本与构建记录。
- 未提供 DWPose 真实人物输入，本次不执行其专项 fresh-download smoke；通用桌面 packaged runtime smoke 必须通过。
- 当前运行中的已安装程序仍为 `1.0.402`，需要用户手动退出并覆盖安装 `1.0.403` 后才能进行真实生成验收。

## 开发日志

- 2026-09-05 10:02：启动 `1.0.403` 当前工作树 Windows 安装包构建。
- 2026-09-05 10:06：版本同步、静态检查和完整 Python/Rust 回归通过，进入正式构建。
- 2026-09-05 10:12：PyInstaller、Tauri、staging、packaged runtime smoke 与 Inno Setup 全部通过。
- 2026-09-05 10:13：安装包版本、大小、SHA-256、签名、staging 和冻结后端 archive 校验通过。
- 2026-09-05 10:18：确认当前占用 `3000` 的安装版仍为 `1.0.402`，定位浏览器自动回退根因。
- 2026-09-05 10:20：构建元数据提交 `4d82533` 并推送，任务完成。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

新会话启动：

1. 阅读项目规则
2. 阅读本任务文档
3. 检查 Git branch
4. 检查 git status
5. 从“下一步”直接继续
6. 不重复已经完成的阶段
7. 不重复询问已经确认的问题
8. 继续遵守本协议
