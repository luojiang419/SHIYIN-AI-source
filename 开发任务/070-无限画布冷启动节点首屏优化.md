# 无限画布冷启动节点首屏优化

状态：启动架构重构及源码回归完成，安装态待验收
当前阶段：5/5
最后更新：2026-09-05

## 当前状态

- 2026-09-05 追加资源链路根因修复：预览 Future 完成回调移出非重入锁；文件解析/stat/缓存命中检查与画布数据库读取移到工作线程；复用基于文件版本的磁盘缓存键，减少重复 stat。
- 特殊节点状态变化按帧合并为局部刷新，过滤已离开画布的旧节点对象；深度组件强制刷新也合并在途请求。普通图片 DOM 不再因无关深度状态通知被拆除重建。
- 新增 `classic.navigation-to-first-preview-loaded` 记录实际预览 load 成功；既有 `navigation-to-first-preview` 是发起请求时间，不能把它当作图片可见时间。
- 最新要求：不带崩其他功能，通过启动依赖与生命周期优化解决问题，禁止继续堆积延迟补丁。
- `canvas-startup.js` 统一管理当前页面启动会话：并行请求项目和基础配置、15 秒请求超时、取消、错误、首帧后的任务以及隐藏恢复。
- 渲染 generator/video 会修改节点平台与模型，因此基础配置是首屏必需依赖。项目与基础配置均成功后才提交节点；失败保留错误界面并允许重试。
- 配置应用和外部能力分离；RunningHub 只补齐当前节点使用的工作流；H3 保留已配置平台的后台预热，兼容后续新建节点。完成后只刷新相关节点。已移除旧预取消费分支、scheduleCanvasConfigSecondary 和 scheduleCanvasSecondaryStartup 的独立调度。
- 语言更新统一到单个入口，同语言重复通知不重绘。旧会话资源检查和旧配置工作流回包不能污染新状态。
- 分支：`fix/canvas-cold-start`。工作区其他任务原有改动保持未暂存。

## 下一步

1. 下一次构建安装包后，冷启动同一实际画布测量；本次不构建/发布安装包。
2. 使用 `?id=<画布ID>&canvasPerf=1`，对照 editor-ready、initial-data-ready、initial-config-ready、navigation-to-first-render、navigation-to-first-preview。

## 当前 TODO

- [x] 基础配置与节点首次渲染依赖重构
- [x] 请求和后续任务生命周期统一
- [x] 配置变化、旧响应、失败和隐藏恢复验证
- [x] 全项目自动化回归与真实浏览器验证
- [x] 本轮 Git 提交和远程备份：`af0e0b8` 已推送 `origin/fix/canvas-cold-start`

## 最近验证状态

- 本轮最终全量回归：`python -m pytest -q --tb=no`，1242 passed，16.80s。

- 本轮定向回归：18 项通过，覆盖 Future 注册时已经完成（成功/失败）、缓存重启复用、文件检查及数据库读取不阻塞事件循环、账号 ContextVar 传递、自定义模板展开边界。
- 本轮浏览器：真实 HTML/JS/CSS，组件状态更新时全量渲染由 3 次降为 1 次；局部状态变化后无关图片 DOM 身份保持不变。
- 302 节点、512px PNG、固定 80ms 预览响应延迟，基线 `b6e762c` 与当前各运行三次：全量渲染基线 3/2/2 次，当前 1/1/1 次；首张加载完成基线 533/491/481ms，当前 437/447/427ms，中位数 491→437ms。测试不使用用户媒体或外部生成 API；不能外推安装态提速。
- 复现脚本：`tests/support/canvas_resource_benchmark.cjs`；原始结果在 `测试/画布资源与节点回归-20260905/resource-benchmark.json`。PNG 是专用测试纹理，不是实际用户照片。
- 全项目：`python -m pytest -q`，1227 passed，最终复跑 13.51s。
- 在上述全量回归后继续补充浏览器入口、无 ID、URL 编码和无 requestIdleCallback 用例；定向测试再次通过。
- JavaScript：`node --check static/js/canvas.js`、`node --check static/js/canvas-startup.js` 通过。
- Python fixture：`python -m py_compile tests/support/canvas_startup_fixture.py` 通过。
- `git diff --check` 通过。
- Node 行为测试执行真实启动模块、openCanvas、配置应用和模型修正函数；覆盖慢配置、旧/同 ID 会话、失败重试、超时、退出、隐藏恢复、配置竞态、语言去重、局部能力刷新和保存参数完整性，以及 H3 预热与点击生成共享 Promise、等待真实就绪结果。
- 真实浏览器：完整仓库 HTML/JS/CSS + 独立内存 HTTP fixture；不读取用户数据、不调用外部生成 API。
- 浏览器慢配置场景：基础配置故意延迟 2 秒，外部能力故意延迟 4 秒；8 个节点完整渲染，自定义图片/视频平台和模型保持原值。该场景实测首屏约 2.2 秒，仅用于验证依赖隔离，不能当作安装态提速数据。
- 浏览器失败场景：配置返回 503 时节点数为 0、渲染次数为 0；点击重试后完整恢复 8 个节点。
- 浏览器操作：提示词编辑、保存、选择/移动工具切换成功；英文切换只有 1 次全量渲染，8 个节点保留，之后恢复中文。
- 浏览器媒体：图片 complete=true、naturalWidth=1（1×1 fixture）；正常页面无错误日志。故障场景的 503 日志符合预期。
- 本轮实现提交：`af0e0b8`，已推送；隔离浏览器标签页和测试服务已结束。
- 避坑记录位于 `避坑指南/画布首屏配置依赖与模型参数保护.md`，依照项目现有忽略规则仅本地保留。
- 上一轮提交：`7a3873f`（加载顺序优化）、`8686d5a`（记录）；本轮替换并收敛其启动实现。

## 任务目标

缩短冷启动后的节点等待，明确启动依赖和单一所有权，保持现有节点、生成参数、保存、媒体和语言切换语义。

## 技术方案

### 1. 必需数据与可延后能力

- 必需：项目详情、无密钥本地 runtime config、现有全部节点脚本注册。
- 可延后：RunningHub 工作流细节、H3 状态、历史任务恢复、touch、资源存在性检查。
- 静态脚本保持依赖顺序 defer；DOM 就绪初始化。
- 禁止用默认平台渲染依赖真实配置的节点，也不在基础配置返回后再全量重画。

### 2. 启动会话所有权

- 每次打开创建独立会话，首次复用页面前部提前启动的会话。
- 新会话取消旧请求和未执行任务；响应必须仍属于当前会话才能提交。
- 两次 rAF 跨过绘制机会后进入 idle 任务；隐藏时保留未执行任务，返回后执行一次。
- 页面退出取消当前会话和配置读取；bfcache 恢复重新打开项目。
- 配置刷新失败保留已知配置；乱序响应以最新刷新为准。H3 并发状态读取合并为一次，调用者等待同一最终结果。

### 3. 更新范围

- 基础配置应用不主动渲染，不执行无权威清单的工作流清理。
- 外部能力加载只更新对应 RH/H3 节点，复用已有 refreshNodes 能力。
- 语言更新去重，保留实际语言切换刷新。
- 现有特殊节点业务、保存队列、生成请求和媒体队列保持原实现；H3 状态读取改为共享在途 Promise，避免“加载中”被误报为不可用。

## 文件 / 模块清单

- `static/js/canvas-startup.js`：会话、请求与后续任务生命周期。
- `static/js/canvas.js`：应用层启动适配、配置分层、语言入口、过期结果保护。
- `static/canvas.html`、`static/css/canvas.css`：安全加载/重试界面和资源版本标识。
- `tests/js/canvas_startup.test.cjs`、`tests/test_canvas_startup_runtime.py`：行为测试。
- `tests/test_canvas_initial_load_performance.py`：新依赖契约。
- `tests/test_canvas_save_media_isolation.py`：删除旧函数后调整源码截取边界，保存断言不变。
- `tests/support/canvas_startup_fixture.py`：可复现隔离浏览器回归服务。

## 验收标准

- [x] 项目与基础配置并行读取，配置就绪前不提交节点。
- [x] 有效已存平台和模型不被启动默认值覆盖。
- [x] 旧请求、错误和退出不能提交错误项目；失败可重试。
- [x] 后续任务有唯一所有者，隐藏恢复无遗漏和重复执行。
- [x] 能力补齐仅更新相关节点，语言通知不重复重绘。
- [x] 自动化与隔离浏览器通过。
- 安装版端到端冷启动耗时：待下一构建实测。

## 已知问题 / 后续优化

- 特殊节点 onChange 全量 render 问题已在本轮改为合并局部刷新；`onOutput` 仍沿用已有下游输入同步业务契约。
- 首屏渲染尚有数据修正副作用；本轮通过明确基础配置依赖保护数据，未来如需要配置未到也可交互，应先将节点规范化与纯渲染分离。
- 未运行付费生成、安装包构建和安装态 A/B；不宣称任何实际提速倍数或覆盖所有设备场景。

## 开发日志

- 2026-09-05 资源链路：消除 Future 同步回调锁重入；数据库及预览元数据 I/O 移出事件循环；特殊节点状态通知改为合并局部刷新；完成 302 节点浏览器对照。
- 2026-09-05 第一轮：提前项目请求、defer 与 DOM 就绪启动，397 项画布测试通过。
- 2026-09-05 架构复核：发现配置/渲染数据竞争，修订依赖屏障，统一会话和后续调度。
- 2026-09-05 验证：1227 项全项目测试通过；隔离浏览器验证慢配置、失败重试、节点参数、媒体、编辑保存和语言切换。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

阅读项目规则与本文件，检查 Git 状态，从“下一步”继续；只维护本任务文件，不另建重复快照。
