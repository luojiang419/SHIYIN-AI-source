# 编译安装包 1.0.405

状态：构建及验证完成，Git 交付中
当前阶段：4/4
最后更新：2026-09-05

## 当前状态

- 当前版本 1.0.404，目标版本 1.0.405；基线 `ffa48c1`，分支 `fix/canvas-cold-start`。
- 标准 Windows x64 构建工具链可用，E 盘剩余约 272.7GB。
- 当前已安装桌面版正在运行。标准 `smoke-desktop.ps1` 明确要求关闭现有实例，本轮构建使用 `-SkipRuntimeSmoke`，另用独立目录/端口验证冻结后端，不停止用户实例。
- 构建按当前工作树打包。电商/作品管理等原有未提交修改保持原状态，不混入版本提交。

## 下一步

完成 Git 提交和正常 push 后交付安装器；用户完全退出当前软件后可运行安装包覆盖升级。

## 当前 TODO

- [x] 版本、构建流程、Git 和运行状态检查
- [x] 版本及更新说明
- [x] 回归测试
- [x] 完整安装包构建
- [x] 产物及冻结后端验证
- [ ] Git 提交和 push

## 最近验证状态

- PyInstaller、Tailwind、Tauri release 和 Inno Setup 构建完成；脚本退出码 0。
- 独立冻结后端：健康检查启动 1678ms（仅后端，不代表桌面或真实画布耗时），版本 1.0.405；八组合模板、三个关键静态接口、节点模板和 Output ID 保存读取、8 路并发 512×512 预览均通过；隔离进程已退出。
- staging：六个关键 JS/CSS 缓存版本为 1.0.405，包含提示词设置、局部刷新、共享 Output、节点尺寸适配和实际预览加载计时。
- 安装器及桌面主程序 FileVersion/ProductVersion 均为 1.0.405。
- 安装包：`dist/installer/SHIYIN-AI-Setup-1.0.405.exe`，92,410,386 bytes。
- SHA-256：`c433035ded0e4ed1387aba318ca5559d94afc0847c650c96d74748d0fa4f7e15`；校验文件已生成，签名状态 `NotSigned`。
- 版本一致性和 `git diff --check` 通过。
- 运行验证记录：`.build/packaged-405-verification.json`；后端日志 `.build/packaged-405-backend.log`。

版本变更后：9 个版本源一致；Python 1242 passed（24.17s），Rust 10 passed。构建日志：`.build/build-1.0.405.log`。

## 任务目标与方案

输出 `dist/installer/SHIYIN-AI-Setup-1.0.405.exe` 及 SHA-256 文件，确保包含画布启动和媒体资源优化、八组合提示词设置、共享 Output 与节点尺寸修复。

使用现有版本脚本和构建脚本。大型模型继续走既有独立组件下载机制。仅编译本地安装包，不发布 GitHub Release、不自动安装。

## 文件 / 模块清单

版本源：VERSION、npm package/lock、Tauri config、Cargo manifest/lock、main.py、release-notes/current.md、static/update-notes.json；本任务文档。

## 开发阶段与验收标准

- [x] 版本源全部一致且回归通过。
- [x] 完整构建成功，安装器 FileVersion/ProductVersion 正确。
- [x] staging 包含最新前端文件与缓存版本；冻结后端能启动并提供八组合模板接口。
- [ ] 产物大小、SHA-256、签名状态有实际结果；提交和正常 push 完成。

## 已知限制

当前已安装桌面版在运行，本轮不执行标准桌面壳双实例/父进程退出 smoke；使用隔离冻结后端验证。安装后的真实画布冷启动耗时仍需安装升级后实测。

## 开发日志

- 2026-09-05：完成构建前检查，确定 1.0.405 和独立运行验证方案。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

阅读项目规则和本文件，检查 Git/构建进程及日志，从“下一步”继续，不重复递增版本，不停止用户正在运行的桌面实例。

## 已完成内容

安装包构建与验证完成，包含 070/071 的本轮修复。未安装、未发布 GitHub Release，用户当前桌面实例保持运行。
