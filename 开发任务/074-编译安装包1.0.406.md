# 编译安装包 1.0.406

状态：开发中
当前阶段：4/4（等待 Git 收尾）
最后更新：2026-09-05 15:21

## 当前状态

Windows x64 安装包 `SHIYIN-AI-Setup-1.0.406.exe` 已构建完成。PyInstaller、Tailwind、Tauri release、staging 缓存盖章和 Inno Setup 均成功。

独立产物校验、staging 高级控制检查和隔离冻结后端验证均通过；当前运行中的正式版后端与人物深度 worker 仍保持原进程，没有被构建打断。

九个版本源已确认一致为 `1.0.405`，上一安装包 `dist/installer/SHIYIN-AI-Setup-1.0.405.exe` 存在。Python、Node、npm、Cargo、Inno Setup 工具链可用，E 盘剩余约 272GB。

当前已安装桌面版后端与人物深度 worker 正在运行。为不打断用户，本轮标准构建使用 `-SkipRuntimeSmoke`，随后用隔离端口验证冻结后端与 staging；不自动安装新版本。

构建按当前工作树打包，因此会包含已提交的深度图高级控制，也会包含用户当前尚未提交的电商、作品页与视频分析改动。Git 收尾仅提交版本、更新说明和本任务记录，不混入这些用户改动。

## 下一步

下一步首先执行：

1. 仅暂存九个版本源、更新说明和本任务文档。
2. 复核 staged diff 不包含用户无关改动。
3. 创建构建提交并正常 push。

## 当前 TODO

- [x] 核对项目规则、上一构建任务、Git 与运行状态
- [x] 核对版本、工具链、磁盘和旧安装包
- [x] 同步 1.0.406 版本源
- [x] 更新发布说明与软件内更新说明
- [x] 执行完整 Python/Rust 回归
- [x] 完成 PyInstaller、Tailwind、Tauri release 和 Inno Setup 构建
- [x] 完成安装包、staging 与冻结后端独立验证
- [ ] 更新任务文档、提交并推送

## 最近验证状态

- 版本一致性：`1.0.406`，9 个版本源同步
- 静态检查：三个画布 JavaScript 语法、`static/update-notes.json` 和 `git diff --check` 通过
- Python 测试：`1242 passed in 20.01s`
- Rust 测试：`10 passed`
- 构建：`tools/build-installer.ps1 -SkipRuntimeSmoke` 退出码 0；PyInstaller、Tailwind、Tauri release、17 个 Web 文件缓存盖章与 Inno Setup 均成功
- 安装包：`dist/installer/SHIYIN-AI-Setup-1.0.406.exe`，`92,424,836` bytes
- SHA-256：`0245dbfae10ead22138d86b9671255669bfc0296552aa1eeca5b01121372ebe1`
- 版本与签名：安装器和 staging 桌面主程序 FileVersion/ProductVersion 均为 `1.0.406`；签名 `NotSigned`
- staging：经典/智能画布缓存均为 `1.0.406`；高级控制按钮、四套预设、实时处理函数与弹窗 CSS 均存在
- 隔离冻结后端：启动 `2328ms`，`version=1.0.406`、`runtime_mode=desktop`、数据库正常、画布页可访问、8 套复刻模板与人物深度状态接口正常；验证后进程已退出
- 验证记录：`.build/packaged-406-verification.json`；后端日志 `.build/packaged-406-backend.log`；构建日志 `.build/build-1.0.406.log`
- 最近 Git commit：`2af3700 docs: 完成深度图高级控制任务记录`

---

## 任务目标

将当前工作树构建为 Windows x64 安装包 `SHIYIN-AI-Setup-1.0.406.exe`，确保安装内容包含深度图节点高级控制、双图实时预览、预设与精确参数、非破坏式原始深度保存和调整结果持久化，并完成版本、大小、SHA-256、签名、staging 内容与冻结运行时验证。

## 当前项目现状

- 标准版本脚本为 `tools/increment-version.ps1`，会同步 9 个版本源。
- 标准构建入口为 `tools/build-installer.ps1`，包含 PyInstaller、Tailwind、Tauri release、staging 复制、Web 缓存盖章和 Inno Setup。
- 当前桌面正式版占用本机后端，不能执行会要求关闭现有实例的标准桌面 smoke。
- 上一版本使用 `-SkipRuntimeSmoke` 后单独启动冻结后端，验证健康、版本、关键接口与并发预览。
- 大型 person-depth 组件继续保存在用户数据目录，不进入安装包。

## 技术方案

- 使用 `tools/increment-version.ps1 -Version 1.0.406` 同步版本。
- 更新 `release-notes/current.md` 标题与新增功能项；在 `static/update-notes.json` 顶部增加对应 feature/improve/safe/test 条目。
- 执行 `python -m pytest -q`、`cargo test --manifest-path src-tauri/Cargo.toml` 与版本同步检查。
- 执行 `tools/build-installer.ps1 -SkipRuntimeSmoke`，保留用户当前运行的正式版。
- 使用独立端口启动 staging 冻结后端，验证健康、版本、深度图高级控制静态资源和关键 API。
- 使用 `tools/verify-installer-artifact.ps1 -Version 1.0.406` 生成/核对 SHA-256、FileVersion、ProductVersion 与签名状态。

## 文件 / 模块清单

预计修改：

- `VERSION`
- `package.json`
- `package-lock.json`
- `src-tauri/tauri.conf.json`
- `src-tauri/Cargo.toml`
- `src-tauri/Cargo.lock`
- `main.py`（仅版本字段）
- `release-notes/current.md`
- `static/update-notes.json`
- `开发任务/074-编译安装包1.0.406.md`

预计生成：

- `dist/installer/SHIYIN-AI-Setup-1.0.406.exe`
- `dist/installer/SHIYIN-AI-Setup-1.0.406.exe.sha256`
- `.build/build-1.0.406.log`

## 开发阶段

- [x] 阶段 1：版本与更新说明
- [x] 阶段 2：完整回归
- [x] 阶段 3：完整构建与产物校验
- [ ] 阶段 4：文档、Git 提交与远程备份

## 验收标准

1. 九个版本源一致为 `1.0.406`。
2. 完整 Python 与 Rust 测试通过。
3. PyInstaller、Tailwind、Tauri release 与 Inno Setup 构建成功。
4. 安装器和桌面主程序 FileVersion/ProductVersion 为 `1.0.406`。
5. staging 包含深度图“高级控制”按钮、双图弹窗、全部参数、预设与 `depth-controls` 缓存标记。
6. staging 冻结后端能在隔离端口启动，健康和版本接口正确。
7. 安装包大小、SHA-256 和签名状态有真实记录。
8. 用户正在运行的正式版和无关未提交文件保持原状态。

## 已完成内容

- 完成构建基线、版本、工具链、磁盘、运行进程和上一安装包核验。
- 确定保留运行中正式版，采用跳过桌面 smoke 加隔离冻结后端验证方案。
- 九个版本源同步到 1.0.406，更新发布说明与软件内更新说明。
- 完整 Python 1242 项与 Rust 10 项测试通过。
- 完成 PyInstaller、Tailwind、Tauri release、staging 和 Inno Setup 全链构建。
- 完成安装器版本、大小、SHA-256、签名、深度高级控制静态资源和隔离冻结后端验证。

## 当前关键修改

- 九个版本源由 `1.0.405` 更新为 `1.0.406`。
- 发布说明新增深度图高级控制、实时预览、四套预设、七项参数、非破坏式保存与不重复推理说明。
- 生成 `dist/installer/SHIYIN-AI-Setup-1.0.406.exe` 与对应 `.sha256` 文件。

## 已知问题

- 当前安装版正在运行，不能执行标准桌面壳双实例、父进程退出和窗口 smoke；冻结后端与 staging 仍会独立验证。
- 安装包未配置 Authenticode 代码签名，实际签名状态为 `NotSigned`。
- 本次按脏工作树构建，会包含现有未提交源码；Git 提交必须严格限制到版本和构建记录。

## 开发日志

- 2026-09-05 15:12：启动 1.0.406 安装包任务，完成基线、运行状态和构建方案确认。
- 2026-09-05 15:14：完成版本同步、更新说明、Python 1242 项和 Rust 10 项回归，进入正式构建。
- 2026-09-05 15:18：PyInstaller、Tailwind、Tauri release、staging 和 Inno Setup 成功，生成 1.0.406 安装包。
- 2026-09-05 15:21：安装包版本、SHA-256、签名、staging 高级控制和隔离冻结后端验证通过；等待 Git 收尾。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

新会话启动：

1. 阅读项目规则、README 和本任务文档。
2. 检查当前 Git branch 与 `git status`，保留用户已有修改。
3. 根据 `VERSION` 和实际产物确认任务阶段，避免重复递增版本。
4. 从“下一步”继续，不停止用户正在运行的正式版。
5. 不自动安装、不发布 GitHub Release、不提交用户无关修改。
6. 每完成一个阶段执行对应验证并更新同一任务文档。
