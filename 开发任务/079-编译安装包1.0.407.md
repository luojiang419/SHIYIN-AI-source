# 编译安装包 1.0.407

状态：已完成并发布
当前阶段：4/4
最后更新：2026-09-05 17:41

## 当前状态

Windows x64 安装包 `SHIYIN-AI-Setup-1.0.407.exe` 已构建完成。PyInstaller、Tailwind、Tauri release、17 个 Web 文件缓存盖章、staging 和 Inno Setup 全部成功。

安装器与 staging 桌面程序版本均为 `1.0.407`；安装器大小 `92,430,750` bytes，SHA-256 为 `71d9b763e2ae8b092e567cd2a9d688dce0518c007e18f9677bb00588138ec4da`，签名状态 `NotSigned`。

隔离冻结后端已通过健康、版本、desktop runtime、数据库、画布静态页、8 个 `pose-replicate.v3.1` 模板、person-depth 状态以及设置页 30000 字符/即时保存/自定义提交检查；验证后隔离进程已停止。原安装版后端与人物深度 worker 未被中断。

构建元数据已提交为 `5563a45 chore: build 1.0.407 installer`，构建验证记录提交为 `6321dd9`。GitHub Release `v1.0.407` 已正式发布并成为 latest，用户端更新器可从公开 Release 读取并下载新安装器。

`pose-replicate.v3.1` 及一键复刻设置页自由编辑功能已提交并推送；本次安装包必须包含八组合 v3.1 模板、30000 字符完整编辑、即时生效、持久化和单组合恢复默认能力。

已安装版后端与人物深度 worker 正在运行。本轮不停止用户实例，使用 `tools/build-installer.ps1 -SkipRuntimeSmoke` 完成标准构建，再以隔离端口验证 staging 冻结后端。

当前工作树存在其他任务的未提交修改。构建按当前工作树打包，因此这些源码会进入安装包；Git 收尾仅提交版本源、发布说明和本任务文档，不混入其他未提交修改。

## 下一步

当前任务已完成。用户可由软件更新器升级，也可从 `https://github.com/luojiang419/SHIYIN-AI/releases/tag/v1.0.407` 手动下载安装。

## 当前 TODO

- [x] 核对项目规则、Git、版本、工具链、磁盘与运行实例
- [x] 同步 1.0.407 版本源
- [x] 更新发布说明
- [x] 执行 Python 与 Rust 回归
- [x] 完成 PyInstaller、Tailwind、Tauri release、staging 与 Inno Setup 构建
- [x] 验证安装器、staging 和隔离冻结后端
- [x] 更新任务文档、提交并推送
- [x] 发布 GitHub Release `v1.0.407`
- [x] 验证用户端 latest 更新契约和下载资产

## 最近验证状态

- 版本一致性：九个版本源一致为 `1.0.407`
- 工具链：Python、Node、npm、Cargo、Inno Setup 可用
- 磁盘：E 盘约 286GB、D 盘约 281GB 可用
- 上一安装包：`dist/installer/SHIYIN-AI-Setup-1.0.406.exe` 存在
- 当前运行：安装版 `canvas-backend` 与 `person-depth-worker` 保持运行
- 版本后回归：Python `1254 passed`；Rust `10 passed`
- 静态检查：更新说明 JSON、版本一致性、相关 JavaScript 语法和 `git diff --check` 通过
- 构建：`tools/build-installer.ps1 -SkipRuntimeSmoke` 退出码 0；PyInstaller、Tailwind、Tauri release、17 个 Web 文件缓存盖章、staging 与 Inno Setup 成功
- 安装包：`dist/installer/SHIYIN-AI-Setup-1.0.407.exe`，`92,430,750` bytes
- SHA-256：`71d9b763e2ae8b092e567cd2a9d688dce0518c007e18f9677bb00588138ec4da`
- 版本与签名：安装器和 staging 桌面主程序 FileVersion/ProductVersion 均为 `1.0.407`；签名 `NotSigned`
- staging：画布缓存盖章为 `1.0.407`；设置页包含 `pose-replicate.v3.1`、30000 字编辑、输入即时持久化和自定义模板提交逻辑
- 隔离冻结后端：`health=ok`、`version=1.0.407`、`runtime_mode=desktop`、数据库 `WAL/schema4`、8 个 `pose-replicate.v3.1` 模板与人物深度状态接口正常；验证后进程已退出
- 验证记录：`.build/packaged-407-verification.json`；后端日志 `.build/packaged-407-backend.log`；构建日志 `.build/build-1.0.407.log`
- 构建 Git commit：`5563a45 chore: build 1.0.407 installer`
- 发布前检查：GitHub CLI 已登录 `luojiang419`；源分支与远程同步；`v1.0.407` 尚不存在；发布源提交为 `6321dd9`
- GitHub Release：`https://github.com/luojiang419/SHIYIN-AI/releases/tag/v1.0.407`，正式版、非草稿、非预发布并已成为 latest
- 公开安装器：`https://github.com/luojiang419/SHIYIN-AI/releases/download/v1.0.407/SHIYIN-AI-Setup-1.0.407.exe`，HTTP 200，远端大小 `92,430,750` bytes
- 远端 digest：`sha256:71d9b763e2ae8b092e567cd2a9d688dce0518c007e18f9677bb00588138ec4da`，与本地完全一致
- 公开校验文件：`SHIYIN-AI-Setup-1.0.407.exe.sha256`，HTTP 200，大小 94 bytes
- 发布清单：`releases/v1.0.407.json` 记录源仓库提交 `6321dd9`、资产名、大小、SHA-256 与 `NotSigned` 状态
- 发布仓库 `main` 与 tag `v1.0.407` 均指向清单提交 `3e2741adaea6d032a9dde5868fae8111608f3c54`

---

## 任务目标

生成并验证 Windows x64 安装包 `SHIYIN-AI-Setup-1.0.407.exe`，确保其中实际包含 `pose-replicate.v3.1` 和一键复刻设置页自由编辑生效能力。

## 当前项目现状

- 标准版本同步脚本：`tools/increment-version.ps1`
- 标准构建入口：`tools/build-installer.ps1`
- 安装器校验入口：`tools/verify-installer-artifact.ps1`
- person-depth 大型组件继续保存在用户数据目录，不进入安装包
- 当前工作树较脏，构建产物会反映当前源码，提交必须严格限定文件

## 技术方案

- 使用项目版本脚本将版本同步至 `1.0.407`。
- 更新 `release-notes/current.md` 与 `static/update-notes.json`。
- 执行完整 Python 测试和 Tauri Rust 测试。
- 使用 `-SkipRuntimeSmoke` 保留当前安装实例，构建完成后单独启动 staging 后端进行健康、版本、模板接口和静态资源验证。
- 校验安装器和桌面程序 FileVersion/ProductVersion、大小、SHA-256 与签名状态。

## 文件 / 模块清单

预计修改：九个版本源、`release-notes/current.md`、`static/update-notes.json` 与本任务文档。

预计生成：

- `dist/installer/SHIYIN-AI-Setup-1.0.407.exe`
- `dist/installer/SHIYIN-AI-Setup-1.0.407.exe.sha256`
- `.build/build-1.0.407.log`

## 开发阶段

- [x] 阶段 1：版本与更新说明
- [x] 阶段 2：完整回归
- [x] 阶段 3：完整构建与产物验证
- [x] 阶段 4：文档、Git 提交与远程备份

## 验收标准

1. 九个版本源一致为 `1.0.407`。
2. Python 与 Rust 完整测试通过。
3. PyInstaller、Tailwind、Tauri release 和 Inno Setup 构建成功。
4. 安装器与桌面程序版本均为 `1.0.407`。
5. staging 返回 `pose-replicate.v3.1` 的 8 个模板，并包含设置页 30000 字符自由编辑与自定义模板提交逻辑。
6. staging 冻结后端在隔离端口正常启动并通过健康检查。
7. 安装包大小、SHA-256 和签名状态有真实记录。
8. 当前安装实例和其他未提交修改保持原状态。

## 已完成内容

- 完成构建基线、版本、工具链、磁盘和运行实例核验。
- 完成九个版本源、发布说明和软件内更新说明同步。
- 完成 Python 1254 项、Rust 10 项和静态检查。
- 完成 PyInstaller、Tailwind、Tauri release、staging 与 Inno Setup 全链构建。
- 完成安装器版本、大小、SHA-256、签名、v3.1 模板/设置页和隔离冻结后端验证。

## 当前关键修改

- `开发任务/079-编译安装包1.0.407.md`：记录本次构建状态、验证与接力信息。
- 九个版本源由 `1.0.406` 更新为 `1.0.407`。
- `release-notes/current.md`、`static/update-notes.json`：新增 v3.1 场景融合与提示词设置自由编辑发布说明。
- `dist/installer/SHIYIN-AI-Setup-1.0.407.exe` 及对应 `.sha256`：本次可交付构建产物。

## 已知问题

- 当前安装版正在运行，标准桌面双实例 smoke 不执行；使用隔离冻结后端替代验证。
- 安装包未配置 Authenticode 代码签名，实际状态为 `NotSigned`。
- 构建会包含当前工作树中的其他未提交源码，Git 提交必须限制范围。

## 开发日志

- 2026-09-05 17:27：开始 1.0.407 安装包任务，完成构建前核验。
- 2026-09-05 17:32：完成版本与更新说明同步；Python、Rust 和静态检查通过，进入正式构建。
- 2026-09-05 17:37：PyInstaller、Tailwind、Tauri release、staging 和 Inno Setup 成功，生成 1.0.407 安装包。
- 2026-09-05 17:39：完成安装器、staging 与隔离冻结后端验证；原安装实例保持运行，进入 Git 收尾。
- 2026-09-05 17:41：构建元数据提交为 `5563a45`，完成任务文档与远程推送收尾。
- 2026-09-05：用户要求发布更新包；完成 GitHub 登录、远程同步、产物与既有 Release 冲突检查，进入 `v1.0.407` 正式发布。
- 2026-09-05：`v1.0.407` 正式发布为 latest；公开安装器、校验文件、远端大小/digest、清单和 tag 指向全部验证通过。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

新会话启动时阅读项目规则和本任务文档，检查当前版本、Git 状态与实际产物，从“下一步”继续；不要重复递增版本，不停止用户正在运行的安装实例，不提交其他任务修改。
