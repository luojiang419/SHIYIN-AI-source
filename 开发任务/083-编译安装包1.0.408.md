# 编译安装包 1.0.408

状态：已完成
当前阶段：4/4
最后更新：2026-09-06 09:51

## 当前状态

Windows x64 安装包 `SHIYIN-AI-Setup-1.0.408.exe` 已构建完成。PyInstaller、Tailwind、Tauri release、18 个 Web 文件缓存盖章、staging 和 Inno Setup 全部成功。

安装器与 staging 桌面程序版本均为 `1.0.408`；安装器大小 `92,467,738` bytes，SHA-256 为 `5e94f79ffc9c808ade43732ec0be06d1a0eaf4bd365fb7e7c918e6ec84d15cc9`，签名状态 `NotSigned`。

构建前 Python `1272 passed`、Rust `10 passed`，九个版本源、五个相关 JavaScript 和更新说明 JSON 检查通过。

staging 已确认包含全能/平面模式、拼图、结果对比节点、手柄拖拽修复、人物/专业深度模式和集成调参页；`video-depth-lab` 未进入安装包。

隔离冻结后端已通过健康、版本、desktop runtime、数据库、深度设置字段、人物/专业深度状态和静态功能标识验证，验证后正常停止。原安装版 `canvas-backend` PID 25704 全程保持运行。

当前工作区位于独立技术验证分支 `feat/video-depth-lab`，并包含多个任务的未提交内容。标准安装脚本只打包主软件 `static`、后端、既有 skills、connectors 和 licenses，不会打包 `tools/video-depth-lab/`。

当前无阻塞问题。本次仅编译安装包，未发布 GitHub Release。

## 下一步

当前任务已完成。安装包可直接交付测试；如需正式发布，再执行项目 GitHub Release 流程。

## 当前 TODO

- [x] 核对项目规则、版本、Git、工具链、磁盘和运行实例
- [x] 确认独立 `video-depth-lab` 不进入主安装包
- [x] 同步版本为 1.0.408
- [x] 更新版本说明
- [x] 执行构建前回归
- [x] 完成 PyInstaller、Tailwind、Tauri release、staging 与 Inno Setup 构建
- [x] 验证安装器、staging 与隔离冻结后端
- [x] 更新任务文档
- [x] 使用隔离 Git index 记录版本与构建元数据

## 最近验证状态

- 版本一致性：九个版本源一致为 `1.0.408`
- Python：`3.12.7`
- Node：`v24.15.0`
- npm：`11.12.1`
- Cargo：`1.94.1`
- Inno Setup：可用
- E 盘可用空间：约 244GB
- Python：`1272 passed in 20.42s`
- Rust：`10 passed`
- 静态检查：五个相关 JavaScript、更新说明 JSON、版本同步和 `git diff --check` 通过
- 构建：`tools/build-installer.ps1 -SkipRuntimeSmoke` 退出码 0；PyInstaller、Tailwind、Tauri release、18 个 Web 文件缓存盖章、staging 和 Inno Setup 成功
- 安装包：`dist/installer/SHIYIN-AI-Setup-1.0.408.exe`，`92,467,738` bytes
- SHA-256：`5e94f79ffc9c808ade43732ec0be06d1a0eaf4bd365fb7e7c918e6ec84d15cc9`
- 版本与签名：安装器和 staging 桌面程序 FileVersion/ProductVersion 均为 `1.0.408`；签名 `NotSigned`
- staging 功能：模式切换、结果对比、手柄拖拽、深度模式、集成调参页、拼图均存在；`video_depth_lab_packaged=false`
- 隔离冻结后端：`health=ok`、`version=1.0.408`、`runtime_mode=desktop`、数据库 `wal/schema4`、新增深度设置和两类深度状态接口正常；进程已停止
- 验证记录：`.build/packaged-408-verification.json`
- 构建日志：`.build/build-1.0.408.log`
- Git：当前工作区为 `feat/video-depth-lab`；主功能已推送 `feat/canvas-design-mode-compare-depth`

---

## 任务目标

生成并验证 Windows x64 安装包 `SHIYIN-AI-Setup-1.0.408.exe`，确保主软件包含无限画布全能/平面模式、拼图更名、结果对比节点和快捷入口、划像手柄拖拽修复、人物/专业深度图模式及软件内调参器，同时不包含独立技术验证项目 `tools/video-depth-lab/`。

## 当前项目现状

- 标准版本同步：`tools/increment-version.ps1`
- 标准构建入口：`tools/build-installer.ps1`
- 安装器校验：`tools/verify-installer-artifact.ps1`
- 安装脚本只复制 `static`、冻结后端、指定 skills/connectors/licenses，不复制整个 `tools/`
- 当前安装版后端运行中，构建阶段不能中断
- 工作树包含多个任务改动，构建反映当前主软件源码；Git 提交必须严格限定版本和构建记录

## 技术方案

- 由构建脚本 `-IncrementVersion` 将九个版本源同步为 `1.0.408`。
- 构建前执行版本一致性、Python 全量测试、相关 JavaScript 检查和 Rust 测试。
- 使用 `-SkipRuntimeSmoke` 完成 PyInstaller、Tailwind、Tauri release、staging 和 Inno Setup。
- 对安装器和 staging 桌面程序执行版本、签名、大小、SHA-256 与静态功能标识检查。
- 在独立端口启动 staging 冻结后端，验证健康、版本、设置字段、画布/调参静态页和深度状态接口，完成后停止隔离进程。

## 文件 / 模块清单

预计修改：九个版本源、`release-notes/current.md`、`static/update-notes.json`、本任务文档。

预计生成：

- `dist/installer/SHIYIN-AI-Setup-1.0.408.exe`
- `dist/installer/SHIYIN-AI-Setup-1.0.408.exe.sha256`
- `.build/build-1.0.408.log`
- `.build/packaged-408-verification.json`

## 开发阶段

- [x] 阶段 1：版本与发布说明
- [x] 阶段 2：构建前回归
- [x] 阶段 3：完整构建与产物验证
- [x] 阶段 4：文档与交付

## 验收标准

1. 九个版本源一致为 `1.0.408`。
2. Python、Rust 和相关 JavaScript 检查通过。
3. PyInstaller、Tailwind、Tauri release、staging 和 Inno Setup 构建成功。
4. 安装器与 staging 桌面程序版本均为 `1.0.408`。
5. staging 包含结果对比、平面模式、专业深度模式和集成调参页，不包含 `video-depth-lab`。
6. 隔离冻结后端返回健康、版本和新增深度设置字段。
7. 安装包大小、SHA-256 与签名状态有真实记录。
8. 用户当前安装实例和其他未提交内容保持原样；不发布 GitHub Release。

## 已完成内容

- 2026-09-06：完成构建基线、工具链、磁盘和运行实例核验，确认技术验证项目不进入主安装包。
- 2026-09-06：同步 1.0.408 版本源与更新说明，完成 Python、Rust 和静态门禁。
- 2026-09-06：完成 PyInstaller、Tailwind、Tauri release、staging 和 Inno Setup 全链构建。
- 2026-09-06：完成安装器、staging 功能和隔离冻结后端验证；原安装实例保持运行。

## 当前关键修改

- 九个版本源：统一更新为 `1.0.408`。
- `release-notes/current.md`、`static/update-notes.json`：加入本轮功能说明。
- `开发任务/083-编译安装包1.0.408.md`：记录完整构建和验证结果。
- `dist/installer/SHIYIN-AI-Setup-1.0.408.exe` 与 `.sha256`：本次交付产物。

## 已知问题

- 安装包预计仍为 `NotSigned`，需按真实签名状态记录。
- 当前工作树较脏；Git 收尾必须继续使用隔离 index，不得混入其他任务文件。

## 开发日志

- 2026-09-06 09:45：启动 1.0.408 构建任务，完成构建前核验。
- 2026-09-06 09:46：版本和更新说明同步完成；Python 1272 项、Rust 10 项和静态检查通过。
- 2026-09-06 09:48：完整安装包构建成功。
- 2026-09-06 09:51：安装器校验、staging 功能核验和隔离冻结后端验证通过。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

新会话启动时读取本任务文档，检查版本、实际产物、Git branch 和 `git status`，从“下一步”继续；不要重复递增版本，不停止正在运行的安装版后端，不发布 GitHub Release，不提交其它任务改动。
