# 编译并发布安装包 1.0.409

状态：已完成并发布
当前阶段：4/4
最后更新：2026-09-06 11:20

## 当前状态

GitHub Release `v1.0.409` 已正式发布到 `luojiang419/SHIYIN-AI` 并成为 latest。公开安装器和校验文件均返回 HTTP 200，远端大小与 digest 和本地完全一致。

当前分支为 `feat/ecommerce-batch-outfit-depth-grid`，批量换款自动深度、多服装并发和格子视觉提交已推送。工作树还包含 1.0.408 深度调参、版本元数据及其他任务的未提交修改；构建会按当前工作树生成主软件，Git 收尾必须使用精确暂存，不能提交输出、研究资料或其他无关文件。

GitHub CLI 已登录 `luojiang419`，具备 `repo` 权限；Inno Setup、Python、Node、npm、Cargo 可用。3000 端口的安装版后端正在运行，本轮不停止用户实例，构建使用 `-SkipRuntimeSmoke`，完成后以隔离端口验证 staging 冻结后端。

九个版本源已同步为 `1.0.409`，桌面和软件内更新说明已加入批量换款自动深度、多服装并发、格子比例和叠卡视觉。当前进入构建前回归。

构建前门禁已通过：Python 1273 项、Rust 10 项、五个相关 JavaScript、更新说明 JSON、九处版本一致性和 `git diff --check` 均正常。当前进入正式构建。

Windows x64 安装包 `SHIYIN-AI-Setup-1.0.409.exe` 已构建完成。PyInstaller、Tailwind、Tauri release、18 个 Web 文件缓存盖章、staging 和 Inno Setup 全部成功。

安装器与 staging 桌面程序版本均为 `1.0.409`；安装器大小 `92,453,973` bytes，SHA-256 为 `8f615f5377d9ab035b8006f5e9d2f3e5807b3ad6adcc02ef48bc11bb043b7f44`，签名状态 `NotSigned`。

staging 已确认包含批量换款自动人物深度、最多 20 张服装参考、并发任务、格子比例、完整图片显示与叠卡视觉；`video-depth-lab` 未进入安装包。隔离冻结后端已通过健康、1.0.409、desktop runtime、WAL/schema4、深度设置与人物/专业深度状态验证，验证后已停止。

构建元数据提交 `4c2be86` 已推送至 `origin/feat/ecommerce-batch-outfit-depth-grid`。发布仓库 `main`、tag `v1.0.409` 与清单提交均指向 `af57e551cfad156c4526c8aa4e629a11289aee5d`；发布清单记录源提交 `4c2be86a1433866d12545e6ffe77cd27e82a2c6f`。

## 下一步

当前任务已完成。用户端更新器可读取 latest `v1.0.409`，也可从 Release 页面直接下载安装。

## 当前 TODO

- [x] 核对项目规则、版本、Git、工具链、磁盘和运行实例
- [x] 确认 `v1.0.409` 尚未存在且 GitHub CLI 已登录
- [x] 同步版本与发布说明
- [x] 执行构建前回归
- [x] 完成 PyInstaller、Tailwind、Tauri release、staging 与 Inno Setup
- [x] 验证安装器、staging 与隔离冻结后端
- [x] 精确提交版本和构建记录并推送源分支
- [x] 发布 GitHub Release `v1.0.409`
- [x] 验证 latest、公开资产、SHA-256 和发布清单

## 最近验证状态

- 当前版本：`1.0.409`
- 目标版本：`1.0.409`
- Python：`3.12.7`
- Node：`v24.15.0`
- npm：`11.12.1`
- Cargo：`1.94.1`
- GitHub CLI：已登录 `luojiang419`
- Inno Setup：可用
- 磁盘：E 盘约 232.6GB，D 盘约 261.9GB
- 当前运行：构建前检测到 3000 端口 PID 25704；本轮未向其发送停止命令，发布后复核时该实例已不在运行
- GitHub Release：`https://github.com/luojiang419/SHIYIN-AI/releases/tag/v1.0.409`，正式版、非草稿、非预发布且为 latest
- 当前 branch：`feat/ecommerce-batch-outfit-depth-grid`
- Python：`1273 passed in 21.02s`
- Rust：`10 passed`
- 静态门禁：五个相关 JavaScript、更新说明 JSON、九处版本源与 `git diff --check` 通过
- 构建：`tools/build-installer.ps1 -SkipRuntimeSmoke` 退出码 0
- 安装包：`dist/installer/SHIYIN-AI-Setup-1.0.409.exe`，`92,453,973` bytes
- SHA-256：`8f615f5377d9ab035b8006f5e9d2f3e5807b3ad6adcc02ef48bc11bb043b7f44`
- 版本与签名：安装器和 staging 桌面程序均为 `1.0.409`；签名 `NotSigned`
- staging 功能：批量自动深度、多服装、并发、格子比例、图片完整显示和叠卡标识全部存在；`video_depth_lab_packaged=false`
- 隔离冻结后端：`health=ok`、`version=1.0.409`、`runtime_mode=desktop`、数据库 `wal/schema4`、人物/专业深度状态正常；验证后已停止
- 验证记录：`.build/packaged-409-verification.json`
- 构建日志：`.build/build-1.0.409.log`
- 构建 Git commit：`4c2be86 chore: build 1.0.409 installer`，已推送
- 公开安装器：`https://github.com/luojiang419/SHIYIN-AI/releases/download/v1.0.409/SHIYIN-AI-Setup-1.0.409.exe`，HTTP 200，远端大小 `92,453,973` bytes
- 远端 digest：`sha256:8f615f5377d9ab035b8006f5e9d2f3e5807b3ad6adcc02ef48bc11bb043b7f44`
- 公开校验文件：`SHIYIN-AI-Setup-1.0.409.exe.sha256`，HTTP 200，内容与本地一致
- 发布清单：源提交 `4c2be86`、安装器名称/大小/SHA-256/NotSigned 状态一致；更新仓库 main/tag/清单提交均为 `af57e55`

---

## 任务目标

生成并验证 Windows x64 安装包 `SHIYIN-AI-Setup-1.0.409.exe`，确保包含本轮批量换款自动高精度深度图、多服装并发、可变格子比例和叠卡视觉，并发布为 GitHub 正式 latest 更新。

## 当前项目现状

- 标准版本同步：`tools/increment-version.ps1`
- 标准构建入口：`tools/build-installer.ps1`
- 安装器校验：`tools/verify-installer-artifact.ps1`
- GitHub 发布：`tools/publish-release.ps1`
- 高精度人物深度组件继续存放于用户数据目录，不进入安装包
- 当前运行实例不能中断；采用跳过桌面双实例 smoke、隔离验证 staging 的方式

## 技术方案

- 显式执行 `increment-version.ps1 -Version 1.0.409`，避免重复递增。
- 更新 `release-notes/current.md` 和 `static/update-notes.json` 的当前版本说明。
- 完整执行 Python 与 Rust 测试，并检查相关 JavaScript、JSON、版本一致性和 diff。
- 使用 `build-installer.ps1 -SkipRuntimeSmoke` 完成全链构建。
- 校验安装器/桌面程序版本、大小、SHA-256、签名状态和批量换款静态功能标识。
- 隔离启动 staging 后端验证健康、版本、运行模式、数据库及深度接口，随后停止隔离进程。
- 精确提交版本源、发布说明和本任务文档，推送当前功能分支，再调用标准发布脚本创建正式 Release。

## 文件 / 模块清单

预计修改：九个版本源、`release-notes/current.md`、`static/update-notes.json`、本任务文档。

预计生成：

- `dist/installer/SHIYIN-AI-Setup-1.0.409.exe`
- `dist/installer/SHIYIN-AI-Setup-1.0.409.exe.sha256`
- `.build/build-1.0.409.log`
- `.build/packaged-409-verification.json`

## 开发阶段

- [x] 阶段 1：版本与发布说明
- [x] 阶段 2：构建前回归
- [x] 阶段 3：完整构建与产物验证
- [x] 阶段 4：Git 提交、GitHub Release 与公开更新验证

## 验收标准

1. 九个版本源一致为 `1.0.409`。
2. Python、Rust、相关 JavaScript 和 JSON 检查通过。
3. PyInstaller、Tailwind、Tauri release、staging 和 Inno Setup 构建成功。
4. 安装器与 staging 桌面程序版本均为 `1.0.409`。
5. staging 包含批量换款自动人物深度、多服装并发、格子比例和叠卡视觉。
6. 隔离冻结后端返回健康、版本、desktop runtime、数据库和深度状态。
7. 安装包大小、SHA-256 与签名状态有真实记录。
8. 源分支提交推送完成，GitHub `v1.0.409` 为正式 latest，公开安装器和校验文件可下载且 digest 一致。
9. 用户当前安装实例和其他未提交内容保持原样。

## 已知问题

- 安装包预计仍为 `NotSigned`，按真实结果记录。
- 当前工作树较脏；Git 提交必须精确隔离，发布产物仍按当前工作树构建。

## 开发日志

- 2026-09-06 11:10：启动 1.0.409 构建发布任务，完成版本、工具链、磁盘、运行实例和远端 Release 门禁。
- 2026-09-06 11:12：九处版本源和发布说明同步完成；Python、Rust、JavaScript、JSON 与版本门禁通过。
- 2026-09-06 11:17：PyInstaller、Tailwind、Tauri release、staging 和 Inno Setup 成功；安装器与隔离冻结后端验证通过。
- 2026-09-06 11:20：构建提交 `4c2be86` 已推送；`v1.0.409` 发布为 latest，公开资产、digest、清单与 tag/main 指向全部验证通过。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

新会话启动时读取项目规则和本任务文档，检查版本、Git 状态、实际产物与 Release 状态，从“下一步”继续；不要重复递增版本，不停止用户正在运行的安装版后端，不提交无关输出或其他任务文件。
