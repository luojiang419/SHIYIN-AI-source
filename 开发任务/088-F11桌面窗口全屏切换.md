# F11 桌面窗口全屏切换

状态：已完成
当前阶段：3/3
最后更新：2026-09-06 14:17

## 当前状态

桌面壳已为所有主窗口页面统一注入 F11 捕获器，并通过 Rust 原生窗口 API 在全屏与普通窗口之间切换。

快捷键仅响应无修饰键且非自动重复的 F11；捕获阶段监听确保输入框获得焦点时也能响应。浏览器方式访问本地服务不加载桌面注入脚本，因此不受影响。

Rust 11 项单元测试、JavaScript 事件契约模拟和差异检查均通过。全量 `cargo fmt --check` 仍命中 HEAD 已存在的 `ensure_port_available` 排版差异，本任务没有修改该无关逻辑；本次新增代码已符合 rustfmt 输出。

功能提交 `90d265d` 已推送至 `origin/feat/ecommerce-batch-outfit-depth-grid`。当前无阻塞问题。

## 下一步

当前任务已完成。下一次桌面构建或运行时，可补充人工按键验收，但不阻塞本次代码交付。

## 当前 TODO

- [x] 阅读项目规则、README、Git 状态和桌面窗口实现
- [x] 确认快捷键影响范围与实现方案
- [x] 实现 F11 原生全屏切换
- [x] 补充回归测试
- [x] 完成验证
- [x] 更新文档并精确提交、推送

## 最近验证状态

- 静态检查：`git diff --check` 通过；初始化脚本 JavaScript 事件契约模拟通过
- 格式检查：本次新增代码符合 rustfmt；全量检查被 HEAD 既有 `ensure_port_available` 排版差异阻断
- 单元测试：`cargo test --lib`，11 passed，0 failed
- 编译：测试构建通过
- 运行测试：未启动桌面窗口，避免中断当前占用 3000 端口的开发服务
- 功能 Git commit：`90d265d`
- 当前 branch：`feat/ecommerce-batch-outfit-depth-grid`
- Push：功能提交已推送至 `origin/feat/ecommerce-batch-outfit-depth-grid`

---

## 任务目标

在 Windows 桌面软件获得焦点时，用户按下 F11 可进入全屏，再次按下 F11 可退出全屏，使标题栏和系统窗口边框不占用创作空间。

## 当前项目现状

- 桌面端使用 Tauri `2.11.5`
- 主窗口由 `WebviewWindowBuilder` 动态创建
- 软件包含多个 HTML 页面并在同一主窗口内导航
- 页面中存在媒体预览级全屏功能，但没有整个软件窗口级快捷键
- 当前工作区包含其他任务的未提交修改，本任务必须精确隔离提交

## 技术方案

1. 新增 Tauri command，读取主窗口当前全屏状态并调用 `set_fullscreen` 切换。
2. 通过 `WebviewWindowBuilder::initialization_script` 为每个导航后的页面统一注册捕获阶段 `keydown` 监听。
3. 仅处理无 Ctrl、Alt、Shift、Meta 修饰键且非长按重复的 F11，并阻止页面默认行为及重复监听。
4. 为初始化脚本关键契约补充 Rust 单元测试，由 Cargo 编译同时校验原生窗口 API。

## 文件 / 模块清单

- 修改：`src-tauri/src/lib.rs`
- 新增：`开发任务/088-F11桌面窗口全屏切换.md`

## 开发阶段

- [x] 阶段 1：现状分析与方案确认
- [x] 阶段 2：原生全屏切换实现与局部测试
- [x] 阶段 3：整体验证、文档与 Git 交付

## 验收标准

1. 桌面软件任意页面按一次 F11 后，主窗口进入全屏。
2. 再按一次 F11 后，主窗口恢复非全屏状态。
3. 输入框、文本域等控件获得焦点时快捷键仍有效。
4. 长按 F11 不因键盘自动重复而快速来回切换。
5. 浏览器访问模式不受影响。
6. Rust 单元测试、格式检查和差异检查通过。

## 已完成内容

- 完成桌面窗口创建路径、Tauri 版本和现有页面级全屏能力检查。
- 确认使用桌面壳统一注入方案，避免多页面重复实现。
- 新增调用方窗口级 `toggle_fullscreen` command，返回切换后的状态并透传原生错误。
- 新增全页面初始化快捷键脚本，处理普通 F11、屏蔽默认行为、忽略修饰键和自动重复。
- 完成 Rust 单元测试、JavaScript 事件契约模拟和差异检查。

## 当前关键修改

- `src-tauri/src/lib.rs`：新增全屏切换 command、统一初始化脚本、command 注册和契约测试。
- `开发任务/088-F11桌面窗口全屏切换.md`：记录方案、验收和验证结果。

## 已知问题

- 当前 3000 端口已有开发服务运行，未启动会创建并管理独立后端进程的 Tauri 桌面壳；实际 Windows 窗口按键仍需在下一次桌面构建/运行时复核。
- HEAD 中 `ensure_port_available` 存在既有 rustfmt 排版差异，导致全文件 `cargo fmt --check` 非零退出；与本功能无关。

## 开发日志

- 2026-09-06 14:08：启动任务，完成现状分析与实现方案确认。
- 2026-09-06 14:12：完成原生全屏切换、统一 F11 监听和回归测试。
- 2026-09-06 14:14：Rust 11 项测试、JavaScript 事件契约模拟与差异检查通过，进入 Git 精确交付。
- 2026-09-06 14:17：功能提交 `90d265d` 已推送至当前远程功能分支，任务完成。

## Git 交付

- branch：`feat/ecommerce-batch-outfit-depth-grid`
- 功能 commit：`90d265d`
- remote：已推送至 `origin/feat/ecommerce-batch-outfit-depth-grid`
- 其他工作区修改：保持原样，未混入本任务提交

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

新会话启动：

1. 阅读项目规则、README 和本任务文档。
2. 检查当前 Git branch 与 `git status`。
3. 以当前源代码和 Git 状态校正文档。
4. 本任务已完成；除非出现实际回归，不重复实现。
5. 若继续人工验收，启动桌面应用后在普通页面和输入框聚焦状态各按两次 F11，确认进入与退出全屏。
