# 编译安装包 1.0.413

状态：已完成并发布
当前阶段：4/4
最后更新：2026-09-06 19:25

## 当前状态

本轮已将 MiniMax H3 采样步数解除限制的当前工作树构建为 Windows x64 安装包 `1.0.413`，并发布到公开更新仓库 `luojiang419/SHIYIN-AI`，作为 latest Release。`1.0.412` 已在本次改动前构建，本轮已递增版本并重新生成。

工作区已有其他任务的未提交修改；构建使用当前工作树，不清理或覆盖无关改动。3000 端口已有服务，桌面 smoke 使用独立端口。

## 下一步

当前任务已完成。用户端更新器可读取 latest `v1.0.413`，安装包位于 `dist/installer/SHIYIN-AI-Setup-1.0.413.exe`。

## 当前 TODO

- [x] 版本递增与版本说明同步
- [x] 构建 PyInstaller、Tauri、staging 和 Inno Setup
- [x] 完成桌面 smoke 与安装器校验
- [x] 更新构建记录

## 最近验证状态

- 构建前相关 H3/视频测试：57 passed
- 版本同步：9 个版本源均为 `1.0.413`
- 构建与 smoke：`tools/build-installer.ps1 -SmokePort 3120` 成功；health `ok`、startup `5867ms`、内存 `218.8MB/250MB`、单实例保护和 sidecar 清理通过
- 安装器校验：ProductVersion `1.0.413`、`92,484,807` bytes、SHA-256 `62b13bb065cc37f7a2b2ab00a1fd30526c3eae30b7a7dc37d564b7b8a9621a6a`、签名 `NotSigned`
- GitHub Release：`v1.0.413` 已正式发布并为 latest；EXE 与 `.sha256` 均 HTTP 200，远端 EXE 大小和 digest 与本地一致
- 公开地址：`https://github.com/luojiang419/SHIYIN-AI/releases/tag/v1.0.413`
- DWPose 实人 smoke：未提供输入，按脚本提示跳过
- Git：工作区原有大量未提交改动，本轮不创建提交

## 发布记录

- Release：`https://github.com/luojiang419/SHIYIN-AI/releases/tag/v1.0.413`
- 安装器：`https://github.com/luojiang419/SHIYIN-AI/releases/download/v1.0.413/SHIYIN-AI-Setup-1.0.413.exe`
- 校验文件：`https://github.com/luojiang419/SHIYIN-AI/releases/download/v1.0.413/SHIYIN-AI-Setup-1.0.413.exe.sha256`
- 发布清单：`releases/v1.0.413.json`，记录源仓库 `luojiang419/SHIYIN-AI-source`、源提交 `3c52f4e365467ba41dd55f899eccb50c65dfb70d`、资产大小和 SHA-256

## 任务目标

生成并验证包含当前源码和 MiniMax H3 采样步数解除限制的 `SHIYIN-AI-Setup-1.0.413.exe`。

## 验收标准

- 所有版本源同步为 `1.0.413`。
- PyInstaller、Tailwind、Tauri、staging、桌面 smoke 和 Inno Setup 成功。
- 安装器 ProductVersion、文件大小、SHA-256 和签名状态有真实记录。
- 3000 端口服务不被停止，隔离 smoke 端口构建后释放。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]
