# 两端安装目录自动更新与版本同步

状态：已完成
当前阶段：4/4
最后更新：2026-09-07

## 当前状态

两端均已自动更新并从安装目录启动：SHIYIN 1.0.414 / film 1.1.0.373。窗口标题、EXE、内置版本与 Windows 卸载记录一致。用户无需手动安装，两个进程 Responding=True。

film 的 117 个载荷文件 SHA-256 一致；旧文件备份 dist/local-update-backup/1.1.0.373-20260907-074615。SHIYIN 静默安装退出码 0，1007 个载荷文件与 staging 哈希一致，升级前记录的 4 个数据库/配置文件哈希未变；旧安装应用备份 .build/local-install-backup/1.0.411-to-1.0.414。

用户明确授权重新构建 SHIYIN 安装包并更新两端已安装目录，film 直接部署到 D:/Program Files/filmstoryboard，无需用户手动安装。已检查无运行中的两端进程，当前会话有管理员权限。

版本基线：SHIYIN 源码/远端 latest 1.0.413，已安装 1.0.411；film 源码/已安装 1.1.0.372，远端 latest 1.0.0.361。目标分别为 1.0.414、1.1.0.373。

## 下一步

当前任务已完成。两端保持运行，可直接使用。此次仅更新本机并保存安装器，未发布 GitHub Release。

## 当前 TODO

- [x] 读取构建/安装脚本和版本基线
- [x] 版本同步与两端构建
- [x] 更新本机两端目录
- [x] 版本、载荷、启动与桥接验证
- [x] 精确暂存版本修改、更新最终记录（Git 提交见本文件历史）

## 技术方案 / 文件

SHIYIN 使用 tools/increment-version.ps1、build-installer.ps1 和 Inno Setup 静默升级，保留 data；film 使用 scripts/prepare_release.py 和 update_local_installation.ps1，逐文件备份、SHA-256 校验、同步卸载注册表。只修改版本源、更新说明和本任务文档；当前工作树原有功能修改保留并进入本机构建，不冒充所有内容已提交。

## 验证状态

前轮：SHIYIN 27 项回归、9 项浏览器流程、film 7 项测试通过。

本轮：

- SHIYIN：9 个版本源一致；PyInstaller / Tauri / Inno Setup 全部成功。
- 桌面 smoke：health=ok，启动 2883ms，进程树 177.24MB；单实例保护、父进程退出时清理 sidecar 通过。
- 打包后端、实际安装后端分别运行 9 项浏览器检查全部通过，涵盖独立画板、重复发送、列表实时刷新、ZIP 下载/导入、图片字节、错误与静默落盘。测试使用独立临时数据，无用户测试工程残留。
- 实际安装后端健康接口返回 1.0.414；3000 端口桥接 capabilities 返回 direct_receive / automatic_receive=true。
- film：Release 构建成功（43.2 秒），相关静态检查通过，桥接 7 项回归通过。运行中的正式应用占用 3210，已将测试接收器改用系统分配端口，避免测试冲突。
- 两端 EXE / Windows 注册表 / 窗口版本核验通过，进程均 Responding=True。

## 安装器与验证记录

- SHIYIN 安装器：dist/installer/SHIYIN-AI-Setup-1.0.414.exe，92,497,028 bytes。
- SHA-256：9f0cd3def57ffcd54d4d54b99a7a6f627ed6015c014166b449e5f98376ab0868。
- 安装目录：D:/Program Files/SHIYIN AI；D:/Program Files/filmstoryboard。
- 构建日志：.build/build-installer-1.0.414.log；安装日志：.build/install-local-1.0.414.log。
- 安装校验：.build/installed-1.0.414-verification.json。
- 安装版浏览器验证产物：C:/Users/jiang/AppData/Local/Temp/canvas-package-browser-ockNmH。

## 遗留问题

无影响本次交付的问题。既有工作树其他修改保留；本机构建包含当前工作树，未伪造对应完整已提交源码的公开 Release。

## 验收标准

- 安装目录、EXE、内置更新版本和注册表同步到目标版本。
- 两端可从安装目录启动，无需手动安装。
- SHIYIN 安装后端具有按来源画板创建/更新工程的修复，网页具有导出进度与实时刷新。
- 旧应用文件可回退，用户工程、数据库和模型保留。

## Git

SHIYIN fix/canvas-package-film-link，起始 38364e9；film fix/shiyin-board-project-link，起始 58e5e93。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

读取本文件及任务 092，检查构建进程、两端 Git 和安装版本，从下一步继续。
