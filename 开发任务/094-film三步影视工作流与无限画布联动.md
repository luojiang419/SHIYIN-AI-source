# film 三步影视工作流与无限画布联动

状态：源码修复完成（回归、film Windows 构建、两端提交推送完成）
当前阶段：4/4
最后更新：2026-09-08 09:24

## 当前状态
2026-09-08 后续交付：已按用户要求生成并验证 SHIYIN 1.0.422 / film 1.1.0.375 两端安装器，详见 [任务 118](118-film与SHIYIN后台直连双端安装包.md)。以下“未发布/未覆盖安装客户端”指尚未安装到本机或发布远程 Release。

按用户最新要求，两入口已改为完全后台直连，不打开浏览器或资源管理器。导出页发送画板后，SHIYIN 先持久化工程、图片组与三步节点，再直接调用 film 创建/同步脚本并把快照写入三步节点；整个过程不依赖画布页面。故事板入口直接创建/更新对应画布并成组保存图片。服务离线时保留工程、明确返回初始化失败，重发补齐原工程。

连接修复：film 3211–3219 本机端口避让、启动失败定时恢复、启动/关闭竞争保护；SHIYIN 按源项目发现服务；画布仅对 sync/snapshot 做断线重试。分组切换恢复各自 project/script，普通故事板导出同样携带源项目 ID。导出回执校验工程、图片组、图片数量、三步节点和后台初始化结果。

未覆盖两端已安装客户端，未发布安装包。当前工作区既有无关修改保持不变，仅提交本轮差异。

## 下一步
本次源码修复已完成。实际使用需同时运行更新后的 SHIYIN 与 film，film 打开源项目；后台直连不要求打开画布网页。安装版升级需同时包含这次两端修复；本次未修改安装目录或发布安装包。

## 当前 TODO
- [x] 连接发现、启动恢复及只读同步重试
- [x] 两入口移除自动网页/目录打开，补齐导出回执校验
- [x] 后端直接创建脚本并持久化三步节点快照
- [x] 两端实际 HTTP 传输与异常恢复回归
- [x] 最终 Windows 构建确认、文档、精准提交推送

## 最近验证状态
- SHIYIN：65 项回归通过；真实 main 接口覆盖无网页初始化三步快照、失败保留工程、重发补齐、媒体字节与工程包往返。
- JavaScript：语法检查通过；图行为单测 9 项通过，含延迟启动、断线取消重试、不重放生成、重开失败工程、跨组项目恢复。
- Chromium：9 项实际画布操作通过，首次 503 后自动恢复；截图及结果在 C:/Users/jiang/AppData/Local/Temp/film-bridge-fix-20260908/browser。
- film：相关 Dart analyze 无问题；桥接 14 项通过，包含真实 Dart→隔离 SHIYIN HTTP→Dart 服务双向联调，无需打开页面即验证数据库三步快照。
- Windows Release：最终后台直连版构建成功（39.5 秒）。产物 E:/APP/film/filmstoryboard/build/windows/x64/runner/Release/filmstoryboard.exe，需与同目录依赖及 data 一起使用。未递增正式版本，未发布新安装包。
- SHIYIN branch：feat/film-workflow-canvas；film branch：feat/infinite-canvas-workflow。
- Git：SHIYIN 修复 49542af / film 修复 8a91426，均已正常 push 到上述 feature 分支。main.py/canvas.html 根据工作前基线提取本轮补丁，未混入已有未提交修改。新增并发输入变更保护后，工作流 8 项回归再次通过。

## 任务目标
在 film 导出页选定画板，点击左下角“导出到无限画布”，以画板创建/更新工程，包含源画板组与准备资产、确认镜头、视频生成三个功能组。任意图片组连入准备资产自动形成脚本，逐阶段连线传递；确认镜头直连视频节点才启用列表，允许单镜与一键全部生成。

## 技术方案
- 扩展现有 manifest.canvas 工作流元数据，保持 schema v2 与旧导入兼容。
- 后端 receive-direct 完成脚本初始化与三步快照保存；返回 workflow_node_ids/workflow_ready/workflow_warning。两入口均不调用网页/目录打开工具。
- 新建独立 canvas-film-workflow 模块承载图、状态、两栏 UI；通过经典画布公开生命周期钩子集成，保持已有 film-video 普通生成行为。
- Flutter Widget 无法直接在 Web 内运行。画布采用 Web 面板，复用 film 的真实业务控制器、模型目录、资产绑定和生成服务，通过仅本机 bridge 请求调用。film 需运行并打开相应源项目；连接仅同步数据，不自动消费生成 API。
- 以 project/board/group 稳定身份维护脚本；保留镜头手改、资产绑定与生成结果，避免重复建脚本。
- 三步只传可恢复的数据快照与稳定脚本 ID，持久化到画布；运行任务显式触发，失败可重试。视频列表状态由实际直接输入边推导，不能由历史 flag 单独启用。
- 本机服务请求限定操作类型及数据范围；媒体通过受控文件/URL解析传输，避免任意文件读取。

## 文件 / 模块清单
SHIYIN：canvas_core/bridge_workflow.py、bridge_sync.py、独立 workflow 服务与 API、static/js/canvas-film-workflow.js、对应 CSS、canvas.js/canvas.html 最小钩子、专项测试。
film：bridge 画板发送服务与工作流 handler、loopback receiver/client、exporter_page.dart、原故事板发送入口复用、专项测试。

## 开发阶段
- [x] 1. 导出与图初始化
- [x] 2. 脚本/控制器业务桥接
- [x] 3. 节点 UI 与连接行为
- [x] 4. 整体回归、Windows 构建、提交与推送

## 验收标准
- 选中 A/B 分别导出到独立工程，重发 A 不重复节点或脚本，保留位置与参数。
- 工程包含源画板组和三个有输入输出端口的功能组。
- 图片组连入准备资产自动建立有序脚本，更新/重连不丢手改。
- 准备资产/确认镜头/视频节点的参数与动作调用 film 同源控制器，并显示成功/失败状态。
- 确认镜头直连时视频显示列表；普通输入/断开后恢复单任务模式，单项与全部生成使用明确镜头范围。
- 保存重开、删除/断开/重连、原图片桥接、旧影视节点、工程导入导出回归通过。
- 真实执行静态检查、相关测试与 film Windows 构建；不运行付费生成进行验收。

## 已知问题
运行条件：film 必须运行并打开对应源项目（本机 3211–3219 工作流桥接）。工程创建和脚本初始化通过后端完成，不需要打开网页。没有覆盖已安装软件或发布安装包；未执行真实付费生成。若本机打开多个 film 项目且普通图片组没有来源身份，要求明确唯一目标，避免误写其他项目。

## 开发日志
- 2026-09-08：根据实际反馈修复端口绑定与连线失败恢复、导出回执校验；按用户明确偏好移除两入口自动打开网页，补齐后端脚本初始化，完成无页面双向 HTTP 落盘与失败恢复验证。
- 2026-09-07：完成方案、两端导出与图初始化、控制器桥接、Web 节点 UI。
- 2026-09-07：修复重复同步覆盖描述、跨图片组素材 ID 冲突、普通影视节点输出被初始化覆盖；对应回归已补充。
- 2026-09-07：完成端到端 HTTP/工程包/浏览器回归、暂存源码独立浏览器复核、两次 Windows 构建；完成两端功能提交与 push。
- 本地避坑指南已记录脚本内容覆盖与跨组 ID 冲突；该目录沿用项目 .gitignore，不纳入本次提交。

## 接力信息
[CODEX_LONG_TASK_CONTINUE_V3]
读取两仓库规则、本任务文档，检查两个 branch/status，从“下一步”继续。跨项目只维护此任务状态，不重复创建功能快照。无关已有修改不能纳入本任务提交。
