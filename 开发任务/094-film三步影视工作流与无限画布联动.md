# film 三步影视工作流与无限画布联动

状态：验证通过，待提交推送（导入排布与沉浸编辑修复）
当前阶段：3/3
最后更新：2026-09-08

## 本轮当前状态与下一步
- 用户反馈：导入图片重叠；准备资产/确认镜头不能滚动；需要右上角全屏编辑；取消这两个节点的自动组背景。
- 方案：后端按实际节点下限排布，前端复用组整理完成测量并兼容旧工程；仅移除明确自动生成的两类包装组；全屏复用同一业务面板与请求，保留滚动、编辑、分页和任务状态。
- [x] 导入排布与旧自动包装组兼容
- [x] 滚动与全屏编辑及状态恢复
- [ ] Python/JS/真实 Chromium 回归、精准提交推送
- 验收：41 帧横竖混合不重叠、重导不重置手工位置；两节点左右栏可滚动且不缩放画布；全屏所有控件调用同一业务链、退出/重开保留编辑；原图片组与视频生成组保留。
- 文件：bridge_sync.py、bridge_workflow.py、canvas.js、canvas-film-workflow.js/css、canvas.html、专项测试。
- 当前分支 feat/film-workflow-canvas；基线 98f7d90。工作区已有其他任务修改，基线保存在系统 Temp/film-ui-fix-20260908，仅提交本轮差异。
- 下一步：检查暂存 diff，仅提交本轮源码、测试与文档，正常 push；本轮未要求安装包，不递增正式版本或覆盖安装目录。
- Python：工作流、桥接直传/包导入、媒体、manifest、端口、影视与特殊节点共 108 项通过。
- JavaScript：语法检查通过、工作流行为 9 项通过；Python 修改文件 py_compile 通过。
- Chromium：原工作流回归，以及双节点左右滚轮/全屏编辑/分页/退出未失焦保存/异步任务轮询/资产下拉与拖拽绑定/41帧横竖混排/工作流避让/手工位置重开保留/720px 深色适配通过。暂存版 canvas.js/html 精准回放也通过。
- 证据：C:/Users/jiang/AppData/Local/Temp/film-ui-fix-20260908/staged-browser；浏览器使用隔离内存服务和 film HTTP fixture，无付费生成，无日常工程写入。
- 实现：后端按节点下限网格排布，新增/删除/尺寸变更标记重排；前端按真实尺寸整理并记录版本，旧工程一次迁移。只移除明确自动创建且未被用户扩展/连接的两类背景组。全屏使用原请求链，保留滚动、草稿、分页；编辑串行保存，生成连点不重复排队。
- 已知问题：本轮源码回归无失败；尚未构建新安装器或更新 D:/Program Files 中的安装版。下列“已安装”及旧分组数量均为上轮记录。

## 上一轮实装与联通记录（本轮状态以上方为准）
最终实装结果：已更新 D:/Program Files 下两端程序并重启，实际监听为 SHIYIN 3000、film 工作流 3211，无 3210 回传服务。用真实 film 项目中原先失败的 41 帧画板调用与导出按钮相同的发送服务，两端安装版直接完成导出与脚本初始化；创建独立工程 7da13f0b347c4698ab3a6b3e6cb6537d，41 图片 + 3 功能节点 + 4 分组 = 48 节点，3 连线，三个功能节点均含 41 镜头。重复导出保持同一工程/组；单次导出及重发前后 GP01 payload SHA-256 完全一致。film 原项目仍为原来的两个画板，ID 未变化。

安装版后端实际工程列表已渲染五个画布，新画布名称为 SMTING 24SS 梦游都市…视频解析故事板；图片 HTTP 读取成功。验收截图及 JSON 保存在 测试/film联通实装验收-20260908/，不作为源码提交。Windows 原生截图接口返回“不支持此接口”、坐标输入不可用，故通过原生工程参数打开 film，读取可访问性界面，并以真实安装版 HTTP、只读数据库和 headless Chromium 渲染验收，没有声称点击过无法操作的原生导出按钮。

备份：D:/Program Files/SHIYIN AI/data/backups/film-link-repair-20260908-100903 保存 SHIYIN、film 全局及源项目 SQLite 一致性备份；film 程序备份在 E:/APP/film/filmstoryboard/dist/local-update-backup/1.1.0.376-20260908-101252。GP01 与备份间曾有正常节点尺寸和工作流刷新字段变化，未回滚这些改动；后续单独核验导出前后数据完全一致。

本轮已完成：专属导出身份与历史混合工程隔离；film 成功提示实际工程名；新旧接收端能力握手；删除 film 回传接收器与 UI，移除无限画布回传操作；两端新构建、安装与真实数据验收。未执行付费生成，未发布 GitHub Release。

以下保留诊断及上一轮背景：
本轮实装诊断：D:/Program Files/SHIYIN AI 1.0.422 与 D:/Program Files/filmstoryboard 1.1.0.375 均正确安装。实际数据库中 GP01（7cb95071d8874f1f864a769e72068b32）包含 222 个节点，以及本次 film 导出组和三步已初始化脚本；导出 API 日志为 200。根因是 find_bridge_target 仅按任意节点 bridgeId 匹配，历史混合画布被误认成画板专属工程。旧 mock 测试仅覆盖“无相同 bridge 的无关画布”，没有覆盖真实 GP01 这类历史混合工程。

本轮方案：引入绑定当前 canvas_id 的专属导出身份，自动导出只匹配专属工程；旧独立导出可安全识别迁移，混合工程不改动。回执显示实际工程名/创建更新状态，后端日志记录目标。按用户新要求，移除 film 接收回传入口与 3210 接收器启动，同时移除无限画布发送回传入口；影视工作流业务 RPC 保留。

本轮诊断所列事项均已完成，结果以上方实装结论为准。

2026-09-08 后续交付：已按用户要求生成并验证 SHIYIN 1.0.422 / film 1.1.0.375 两端安装器，详见 [任务 118](118-film与SHIYIN后台直连双端安装包.md)。以下“未发布/未覆盖安装客户端”指尚未安装到本机或发布远程 Release。

按用户最新要求，两入口已改为完全后台直连，不打开浏览器或资源管理器。导出页发送画板后，SHIYIN 先持久化工程、图片组与三步节点，再直接调用 film 创建/同步脚本并把快照写入三步节点；整个过程不依赖画布页面。故事板入口直接创建/更新对应画布并成组保存图片。服务离线时保留工程、明确返回初始化失败，重发补齐原工程。

连接修复：film 3211–3219 本机端口避让、启动失败定时恢复、启动/关闭竞争保护；SHIYIN 按源项目发现服务；画布仅对 sync/snapshot 做断线重试。分组切换恢复各自 project/script，普通故事板导出同样携带源项目 ID。导出回执校验工程、图片组、图片数量、三步节点和后台初始化结果。

未覆盖两端已安装客户端，未发布安装包。当前工作区既有无关修改保持不变，仅提交本轮差异。

## 下一步
当前任务已完成。用户可在无限画布“默认项目”打开新建的 SMTING 24SS 梦游都市…画布；后续同源画板导出只更新该独立工程。两个新版程序已安装，无需用户再次安装。

本次源码修复已完成。实际使用需同时运行更新后的 SHIYIN 与 film，film 打开源项目；后台直连不要求打开画布网页。安装版升级需同时包含这次两端修复；本次未修改安装目录或发布安装包。

## 当前 TODO
- [x] 连接发现、启动恢复及只读同步重试
- [x] 两入口移除自动网页/目录打开，补齐导出回执校验
- [x] 后端直接创建脚本并持久化三步节点快照
- [x] 两端实际 HTTP 传输与异常恢复回归
- [x] 最终 Windows 构建确认、文档、精准提交推送

## 最近验证状态
- 本轮 Git：SHIYIN 57d8813（feat/film-workflow-canvas），film c825feb（feat/infinite-canvas-workflow），均已 push。共享文件仅暂存本轮差异，其他任务的未提交改动保留。
- 本轮：23 项 Python 桥接/工程回归通过；JavaScript 语法通过；Dart analyze 无问题，常规桥接 12 项通过，两个显式外部环境测试默认跳过；真实安装环境导出测试已单独运行两轮通过。
- 本轮实装：新工程 48 节点/41 图片/4 组/3 连接；三步各 41 镜头；重导幂等、GP01 导出前后不变、film 原画板 ID 不变、真实列表显示、图片读取通过。
- 本轮构建：SHIYIN 1.0.423 安装器 93,615,110 bytes，SHA-256 33f8babf34f22bd491dd6345ca6c01710d97250c97c341802cc559cff716982e；film 1.1.0.376 安装器 1,702,855,648 bytes，SHA-256 716b5354da9369d9b2a081c386298d8fdd842bc3ceb8f2ec528d084dcaee7c85。均已校验，NotSigned。
- 下列为前次源码回归记录，本轮实装结论优先。
- SHIYIN：65 项回归通过；真实 main 接口覆盖无网页初始化三步快照、失败保留工程、重发补齐、媒体字节与工程包往返。
- JavaScript：语法检查通过；图行为单测 9 项通过，含延迟启动、断线取消重试、不重放生成、重开失败工程、跨组项目恢复。
- Chromium：9 项实际画布操作通过，首次 503 后自动恢复；截图及结果在 C:/Users/jiang/AppData/Local/Temp/film-bridge-fix-20260908/browser。
- film：相关 Dart analyze 无问题；桥接 14 项通过，包含真实 Dart→隔离 SHIYIN HTTP→Dart 服务双向联调，无需打开页面即验证数据库三步快照。
- Windows Release：最终后台直连版构建成功（39.5 秒）。产物 E:/APP/film/filmstoryboard/build/windows/x64/runner/Release/filmstoryboard.exe，需与同目录依赖及 data 一起使用。未递增正式版本，未发布新安装包。
- SHIYIN branch：feat/film-workflow-canvas；film branch：feat/infinite-canvas-workflow。
- Git：SHIYIN 修复 49542af / film 修复 8a91426，均已正常 push 到上述 feature 分支。main.py/canvas.html 根据工作前基线提取本轮补丁，未混入已有未提交修改。新增并发输入变更保护后，工作流 8 项回归再次通过。

## 任务目标
在 film 导出页选定画板，点击左下角“导出到无限画布”，以画板创建/更新工程，包含源画板组与准备资产、确认镜头、视频生成三个功能组。任意图片组连入准备资产自动形成脚本，逐阶段连线传递；确认镜头直连视频节点才启用列表，允许单镜与一键全部生成。

## 技术方案
- 扩展现有 manifest.canvas 工作流元数据，保持 schema v2 与旧导入兼容。
- 后端 receive-direct 完成脚本初始化与三步快照保存；返回 workflow_node_ids/workflow_ready/workflow_warning。两入口均不调用网页/目录打开工具。
- 新建独立 canvas-film-workflow 模块承载图、状态、两栏 UI；通过经典画布公开生命周期钩子集成，保持已有 film-video 普通生成行为。
- Flutter Widget 无法直接在 Web 内运行。画布采用 Web 面板，复用 film 的真实业务控制器、模型目录、资产绑定和生成服务，通过仅本机 bridge 请求调用。film 需运行并打开相应源项目；连接仅同步数据，不自动消费生成 API。
- 以 project/board/group 稳定身份维护脚本；保留镜头手改、资产绑定与生成结果，避免重复建脚本。
- 三步只传可恢复的数据快照与稳定脚本 ID，持久化到画布；运行任务显式触发，失败可重试。视频列表状态由实际直接输入边推导，不能由历史 flag 单独启用。
- 本机服务请求限定操作类型及数据范围；媒体通过受控文件/URL解析传输，避免任意文件读取。

## 文件 / 模块清单
SHIYIN：canvas_core/bridge_workflow.py、bridge_sync.py、独立 workflow 服务与 API、static/js/canvas-film-workflow.js、对应 CSS、canvas.js/canvas.html 最小钩子、专项测试。
film：bridge 画板发送服务与工作流 handler、loopback receiver/client、exporter_page.dart、原故事板发送入口复用、专项测试。

## 开发阶段
- [x] 1. 导出与图初始化
- [x] 2. 脚本/控制器业务桥接
- [x] 3. 节点 UI 与连接行为
- [x] 4. 整体回归、Windows 构建、提交与推送

## 验收标准
- 选中 A/B 分别导出到独立工程，重发 A 不重复节点或脚本，保留位置与参数。
- 工程包含源画板组和三个有输入输出端口的功能组。
- 图片组连入准备资产自动建立有序脚本，更新/重连不丢手改。
- 准备资产/确认镜头/视频节点的参数与动作调用 film 同源控制器，并显示成功/失败状态。
- 确认镜头直连时视频显示列表；普通输入/断开后恢复单任务模式，单项与全部生成使用明确镜头范围。
- 保存重开、删除/断开/重连、原图片桥接、旧影视节点、工程导入导出回归通过。
- 真实执行静态检查、相关测试与 film Windows 构建；不运行付费生成进行验收。

## 已知问题
本轮联通无遗留失败。原生 UI 自动化限制已在验收方法说明，未以该限制跳过真实安装环境的数据与界面验证。film 回传功能已按用户要求关闭，只有影视工作流业务 RPC 保留。

旧记录的“未覆盖安装目录”仅适用于此前版本，本轮已完成安装更新。
运行条件：film 必须运行并打开对应源项目（本机 3211–3219 工作流桥接）。工程创建和脚本初始化通过后端完成，不需要打开网页。没有覆盖已安装软件或发布安装包；未执行真实付费生成。若本机打开多个 film 项目且普通图片组没有来源身份，要求明确唯一目标，避免误写其他项目。

## 开发日志
- 2026-09-08：根据实际反馈修复端口绑定与连线失败恢复、导出回执校验；按用户明确偏好移除两入口自动打开网页，补齐后端脚本初始化，完成无页面双向 HTTP 落盘与失败恢复验证。
- 2026-09-07：完成方案、两端导出与图初始化、控制器桥接、Web 节点 UI。
- 2026-09-07：修复重复同步覆盖描述、跨图片组素材 ID 冲突、普通影视节点输出被初始化覆盖；对应回归已补充。
- 2026-09-07：完成端到端 HTTP/工程包/浏览器回归、暂存源码独立浏览器复核、两次 Windows 构建；完成两端功能提交与 push。
- 本地避坑指南已记录脚本内容覆盖与跨组 ID 冲突；该目录沿用项目 .gitignore，不纳入本次提交。

## 接力信息
[CODEX_LONG_TASK_CONTINUE_V3]
读取两仓库规则、本任务文档，检查两个 branch/status，从“下一步”继续。跨项目只维护此任务状态，不重复创建功能快照。无关已有修改不能纳入本任务提交。
