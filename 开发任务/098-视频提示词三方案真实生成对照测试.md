# 视频提示词三方案真实生成对照测试

状态：已完成
当前阶段：3/3
最后更新：2026-09-07

## 当前状态

已完成两轮 18 项真实主流程测试及 2 项压缩补测，实际 25 个模型调用阶段、28 次上游请求尝试。C 成功 4/6、A/B 各成功 6/6；A/B 无搜索工具或事件。成功结果引用和长度全部通过，但 A 一次压缩及 B 补测丢失 retention_analysis；B 补测还将英文正文改为中文。各组自动解析存在方向误判，联网 C 也出现，不能声称免搜索后语义完全正确。实测报告与全部原始记录已保存。

## 下一步

当前测试任务已完成。等待用户决定是否将“默认免搜索 + 压缩结构/语言校验”落地到产品；尚未修改业务代码或安装版。

## 当前 TODO

- [x] 核对当前模型、源码版本、安装版与参考素材。
- [x] 完成真实调用和阶段日志。
- [x] 人工检查实际提示词与参考图一致性，明确记录方向、左右手和压缩问题。
- [x] 汇总耗时、搜索证据与异常，形成实测结论。

## 最近验证状态

- 模型：已配置 gpt-5.6-terra；不切换其他模型。
- 测试：18 项主流程 + 2 项压缩补测已完成；脚本 py_compile、无搜索断言、引用长度检查与密钥扫描通过；结构检查明确检出两项缺字段，不视为质量通过。
- Git：feat/film-workflow-canvas；起始 HEAD cbcdd1a；存在用户既有未提交修改。

## 技术方案

C：原流程，搜索开启、完整规则、原推理默认配置。
A：关闭搜索，清理自动解析中遗留的必须联网指令，完整规则。
B 实验配置：A + reasoning.effort=low + 超长压缩仅携带文字。保留完整规则，不启用缓存和按模式裁剪。本轮测量 B 的可独立验证部分，不宣称完成全部产品化优化。

通过独立脚本在进程内包装已有请求函数记录真实请求体摘要、SSE 事件、首字、阶段和总耗时、usage、搜索调用证据及输出文本。API Key 仅在内存中读取与使用，不写入结果或日志。源数据库以只读方式读取。

## 验收标准

- 三组均执行真实模型调用，不以 mock 代替生成结果。
- A/B 请求无搜索 tools、无遗留强制搜索指令；核查回执中有无 web_search_call。
- 实际图片数、引用覆盖、生成正文、超长处理和最终状态均可核对。
- 记录失败和重试，不删除失败样本；明确测试样本量和未验证范围。
- 只汇总解析/润色的时间，不误称已渲染最终视频。

## 已知限制

使用当前 1.0.416 源码构建测试流程，不替换正在运行的 1.0.414 安装版。没有测试缓存命中、所有 provider 或最终视频模型成片效果。

## 交付

- 脚本：tools/benchmark-video-prompt-real.py。
- 本地实测报告：输出/分析报告/视频提示词三方案实测-20260907/实测报告.md。
- 同目录包含 summary.json、events.jsonl、任务 JSON/TXT、脱敏请求文本和素材校验值。
- 初次分析报告已加实测报告入口。
- 测试数据与素材保留在本地；Git 仅提交测试脚本和本任务状态文档，不发布用户素材和生成正文。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

从“下一步”恢复，先查看输出/分析报告/视频提示词三方案实测-20260907/中的结果和脚本运行状态，避免重复计费。
