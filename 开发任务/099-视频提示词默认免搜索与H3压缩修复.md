# 视频提示词默认免搜索与 H3 压缩修复

状态：实现与验收完成，待 Git 交付
当前阶段：3/3
最后更新：2026-09-07

## 当前状态

正式代码已默认免搜索，新增节点级可选联网增强；搜索独立限时 30 秒，失败后继续解析并记录提示。H3 按模式检查三段/六段字段，压缩仅重写主要描述段，本地保留其余字段及正文，检查明显中文翻译、引用和长度，最多两次内容校正后明确报错。当前模型推理设置与完整导演规则未削减。当前工作区有既有未提交改动，提交时仅包含本任务差异。

## 下一步

1. 将本任务差异提交并正常推送。
2. 当前安装版仍为先前版本，正式安装版生效需后续打包升级；本次未关闭或覆盖正在运行的安装版。

## 当前 TODO

- [x] 后端默认免搜索、搜索失败降级和阶段计时。
- [x] H3 必需字段、章节顺序、英文正文检查；压缩保留其他段落，不硬截断结构。
- [x] 前端显式关闭默认搜索，提供可选联网增强。
- [x] 回归测试与真实调用验证。
- [ ] 文档、Git 提交和正常推送。

## 最近验证状态

- 起始 branch：feat/film-workflow-canvas。
- 起始 commit：05a7b40。
- 实施前依据：098 的 18 项主流程 + 2 项压缩实测。
- 回归：114 passed；main.py、质量模块与验证脚本 py_compile 通过；相关三份前端脚本 node --check 通过。
- 浏览器：普通视频/影视节点实际点击与设置持久化通过，7 项请求检查，pageerror=[]；旧智能画布现已重定向，针对保留的旧请求函数做了兼容性测试，没有恢复已下线入口。
- 实际 API：P 模式直接执行正式函数，不替换提示词或推理参数。单图 9.54 秒、七图解析 26.63 秒、七图润色 26.73 秒；原 7,126 字符超长样例 20.87 秒压缩至 6,432 字符。四项均无搜索，长度、引用与 H3 字段检查通过；非描述段与原文一致。
- 可选搜索真实补测：主动开启后在 30.01 秒取消超时搜索，随后视觉阶段 10.36 秒成功，总耗时 40.37 秒；返回搜索已跳过提示，没有使整项任务失败。
- 暂存核验：10 个目标类/函数的 AST 与实际验收工作区一致；其他原有 main.py 与画布修改未暂存。
- 本地证据：输出/分析报告/视频提示词修复验收-20260907/verification.json 与对应 JSON/TXT；.codex-artifacts/video-prompt-ui/results.json 与截图。

## 已知边界

本次修复的是搜索依赖和可确定校验的 H3 结构/明显语言退化，不宣称解决模型所有左右方向或语义判断错误。语言检测保留带引号的原语言对白/歌词，属于明显中日韩正文翻译的规则检测，不是通用语言鉴定。固定字段自身已经占满限长时明确要求精简，不能靠截断交付。

## 方案与文件

- 新增 canvas_core/video_prompt_quality.py：H3 三段/六段结构、章节正文语言退化检测、主要描述段压缩并保留其他字段。
- 修改 main.py：默认 web_search=False，移除自动解析必须搜索指令；可选搜索限时和降级；压缩接入质量检查；任务阶段计时。
- 修改相关前端：节点 promptWebSearch 明确布尔值，默认关闭并持久化选项。
- 新增/更新相关测试：默认分支、可选搜索失败、结构/语言退化、真实超长样例回归。

## 验收标准

- 默认按钮请求没有搜索工具，直接进入视觉生成；可选搜索故障不阻断解析。
- H3 Ref2VA 六段和基础模式三段完整、顺序正确；正文不能整体翻译成中文，对话/歌词原语言可保留。
- 超长 H3 提示词压缩后保留非主要描述段、全部原引用标签且不超过模型限长；不能用硬截断伪造成功。
- 普通、影视、智能画布都显式传递搜索开关；未设置的旧节点默认关闭。
- 相关自动回归与实际 API 验证完成，记录真实耗时和结果。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

先检查本任务与 Git 状态，沿“下一步”继续。生产安装版版本与测试运行版本应分别说明，不把源码修复等同于安装版已更新。
