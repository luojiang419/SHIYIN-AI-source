# 其他电脑深度组件连接失败恢复

状态：代理与断点续传源码完成，待故障电脑验收
当前阶段：3/3（源码）
最后更新：2026-09-07

## 当前状态

截图中的 Failed to fetch、0%、无重试按钮对应前端 status 请求 catch：它把通信失败覆盖为安装失败并停止轮询。尚无故障电脑日志，不能断言其模型下载或 worker 的实际状态。内置正式清单有效，但四个分包仅配置 GitHub，总下载量约 5.98GB。

组件下载现已自动读取 Windows 系统代理，GitHub 官方源优先走系统代理，失败后回退直连。每个分包保留 `.part`，连接中断、超时、408、429、5xx 或大小异常时在当前线路最多尝试 3 次，间隔 2 秒、5 秒；续传响应必须返回匹配的 `Content-Range`，避免错误拼接。已完成且 SHA-256 正确的分包不会重复下载。

## 下一步

1. 将本次源码修复纳入后续安装包构建。
2. 在故障电脑复验系统代理识别、下载中断恢复、四个 GitHub 分包校验及模型推理；如仍失败，读取该电脑 data 下服务日志定位。

## 当前 TODO

- [x] 核对截图、请求链、组件清单与既有安装任务
- [x] 修复连接异常后的自动恢复和手动重连
- [x] 验证状态恢复、安装请求去重及失败停止重装
- [x] 更新记录并提交、推送
- [x] 自动识别系统代理并优先用于 GitHub 下载
- [x] 增加单包断点续传自动重试及响应范围校验

## 最近验证状态

- 静态检查：node --check 与 git diff --check 通过
- 行为测试：Node VM 断线/恢复/进度保留/POST 丢响应/真实失败场景通过；相关 53 项通过，覆盖系统代理优先、暂时性连接中断续传、无效 Content-Range 拒绝、永久 HTTP 错误不重试。
- 安装版/故障电脑：尚未验证
- Git：feat/film-workflow-canvas；有大量既有未提交改动，仅提交本任务增量

## 任务目标与技术方案

本地状态请求失败时显示连接中，保留已下载量与来源，每 5 秒重查并提供手动重连。真实安装失败仍由服务端状态决定，不自动循环安装。成功恢复后沿用既有自动安装及 ready 后推理流程。HTTP 错误保留服务端提示。

## 文件与验收

- static/js/canvas-special-nodes.js：共享状态轮询和两类节点的重连入口。
- tests/support/person_depth_connection_check.cjs：模拟断线、恢复、POST 响应丢失和真实安装失败。
- 验收：通信故障不归零、不隐藏恢复入口；恢复后继续查询；真实失败不重复 POST。

## 已知问题

当前缺少故障电脑的服务日志和网络条件，GitHub 可达性及该电脑模型推理需现场复验。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

阅读本任务文档，检查 Git 与当前源码，从“下一步”继续；不要提交既有无关修改。

## 提交与发布状态

源码修复提交：d21a585；已推送 origin/feat/film-workflow-canvas。首次直连 TLS 失败，使用本机 127.0.0.1:7890 代理推送成功。本轮未构建或发布安装包，未验证故障电脑实际下载与推理，不标记整体故障验收完成。

代理、自动重试与断点续传提交：76d571e；记录提交：5dda63f；已推送 `origin/feat/film-workflow-canvas`。本轮仍未构建安装包。
