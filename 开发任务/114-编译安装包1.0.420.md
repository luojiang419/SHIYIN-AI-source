# 编译安装包 1.0.420

状态：已完成
当前阶段：3/3
最后更新：2026-09-07 23:43

## 当前状态

版本源已同步为 `1.0.420`。已完成 PyInstaller 后端冻结、Tailwind 静态资源构建、Tauri release 编译、安装 staging 和 Inno Setup 打包，生成并校验 Windows x64 安装器。

工作区含用户尚未提交的功能改动，本次仅基于当前工作树生成安装器，不覆盖、清理或提交这些改动。已安装的正式版 `SHIYIN AI` 与其 `canvas-backend` 在构建前后均保持运行。

## 下一步

当前任务已完成。交付 `dist/installer/SHIYIN-AI-Setup-1.0.420.exe` 及相邻 SHA-256 文件；未安装到本机，未发布 GitHub Release。

## 当前 TODO

- [x] 确认版本、构建脚本和运行中应用状态
- [x] 构建 Windows x64 安装器
- [x] 校验安装产物
- [x] 更新构建记录

## 最近验证状态

- 版本同步：9 个版本源均为 `1.0.420`
- 编译：PyInstaller、Tailwind、Tauri release、Inno Setup 均成功
- 安装器校验：通过，`92,616,280` bytes，产品/文件版本 `1.0.420`，签名 `NotSigned`
- SHA-256：`336099f6a09bad5e1a8b1438c7a8ccbe79d3d11c8909ce6691cb78f8512895ee`
- Git branch：`feat/film-workflow-canvas`
- 最近 Git commit：`8581e9c docs: 完成视频模型尺寸适配记录`

---

## 任务目标

生成当前工作树对应的 SHIYIN AI `1.0.420` Windows x64 安装器及 SHA-256 校验文件。

## 技术方案

调用 `tools/build-installer.ps1` 完成 PyInstaller 后端冻结、Tauri release 编译、安装 staging 和 Inno Setup 打包。正式版正在运行时使用 `-SkipRuntimeSmoke`，避免干预用户进程；之后调用 `tools/verify-installer-artifact.ps1` 生成校验文件并确认版本信息。

## 验收标准

- 生成 `dist/installer/SHIYIN-AI-Setup-1.0.420.exe`。
- 生成同名 `.sha256` 文件。
- 校验脚本确认文件版本为 `1.0.420`、安装器大小合理、签名状态有效或未签名。
- 不终止运行中的正式版或修改无关工作树内容。

## 已完成内容

- 使用 `tools/build-installer.ps1 -SkipRuntimeSmoke` 成功生成安装器；跳过运行态冒烟是为了不干预正在运行的用户正式版。
- 安装器 staging 已由构建脚本完成静态资源、提示词 skill 与人物深度组件完整性检查，并对 62 个网页文件完成版本缓存戳扫描，修改 18 处。
- 使用 `tools/verify-installer-artifact.ps1 -Version 1.0.420` 校验文件大小与安装器版本，并生成 `.sha256`。

## 已知问题

- 安装器尚未进行代码签名，校验状态为 `NotSigned`。
- 未执行完整桌面运行态冒烟或安装覆盖验证，原因是正式安装版正在运行；本次未擅自终止用户进程。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

新会话启动后先阅读本文件，检查 Git 状态及构建进程，从“下一步”继续。
