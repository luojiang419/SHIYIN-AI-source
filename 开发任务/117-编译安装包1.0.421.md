# 编译安装包 1.0.421

状态：已完成
当前阶段：3/3
最后更新：2026-09-08 08:26

## 当前状态

已完成 `1.0.421` Windows x64 安装包构建及校验。PyInstaller、Tailwind、Tauri release、staging、完整 runtime smoke 和 Inno Setup 均成功，安装 staging 已确认包含经典、影视和智能三套 H3 比例同步修复。

当前工作区包含其它功能的既有未提交改动，本次按用户要求基于当前工作树构建，不清理或覆盖这些改动。

## 下一步

当前任务已完成。交付 `dist/installer/SHIYIN-AI-Setup-1.0.421.exe` 及相邻 SHA-256 文件；未安装到本机，未发布 GitHub Release。

## 当前 TODO

- [x] 检查版本、Git 状态、构建环境和运行进程
- [x] 执行构建前相关回归
- [x] 构建 Windows x64 安装器
- [x] 校验安装器产物
- [x] 更新任务文档

## 最近验证状态

- 构建前测试：152 passed
- 版本同步：9 个版本源均为 `1.0.421`
- 目标版本：`1.0.421`
- 编译：PyInstaller、Tailwind、Tauri release、Inno Setup 均成功
- runtime smoke：health ok，启动 7247ms，内存 216.39MB/250MB，单实例与 sidecar 清理通过
- 安装器：`92,610,652` bytes，ProductVersion/FileVersion `1.0.421`，签名 `NotSigned`
- SHA-256：`dd59bd6230f759215b17709f875bcc0e610da8cb7ca49ca0d6f6211ce7143cc2`
- Git branch：`feat/film-workflow-canvas`
- 最近 Git commit：`7f6cd26 docs: complete h3 aspect ratio fix`

---

## 任务目标

生成并验证包含当前工作树和 H3 视频比例修复的 `SHIYIN-AI-Setup-1.0.421.exe` Windows x64 安装器。

## 当前项目现状

- 上一安装包版本为 `1.0.420`。
- H3 比例修复提交为 `60a4e2f`，完成文档提交为 `7f6cd26`。
- 工作区另有其它任务的未提交改动，构建会包含当前工作树实际内容。
- 当前无运行中的正式版，可执行完整 runtime smoke。

## 技术方案

调用 `tools/build-installer.ps1 -IncrementVersion -SmokePort 3121` 完成版本递增、后端冻结、桌面构建、staging、runtime smoke 和 Inno Setup；随后调用 `tools/verify-installer-artifact.ps1 -Version 1.0.421` 生成 SHA-256 并核对版本。

## 文件 / 模块清单

- 版本源文件（由递增脚本统一维护）
- `dist/installer/SHIYIN-AI-Setup-1.0.421.exe`
- `dist/installer/SHIYIN-AI-Setup-1.0.421.exe.sha256`
- 本任务文档

## 开发阶段

- [x] 阶段 1：构建前检查
- [x] 阶段 2：完整安装包构建
- [x] 阶段 3：产物验证与交付

## 验收标准

- 所有版本源同步为 `1.0.421`。
- PyInstaller、Tailwind、Tauri、runtime smoke 和 Inno Setup 成功。
- 安装器 ProductVersion 为 `1.0.421`，大小不少于 5MB。
- SHA-256 文件生成，签名状态为 Valid 或 NotSigned。

## 已完成内容

- 已完成构建环境、运行进程和相关测试检查。
- 已同步所有版本源到 `1.0.421`。
- 已完成完整安装包构建和 runtime smoke。
- 已确认 staging 中包含三套 H3 比例同步实现。
- 已生成并验证安装器与 SHA-256 文件。

## 当前关键修改

- 版本源由 `tools/increment-version.ps1` 从 `1.0.420` 统一更新到 `1.0.421`。
- 构建产物位于 `dist/installer/`。

## 已知问题

- 未提供 DWPose 实人测试输入，构建脚本可能按既有规则跳过该专项 smoke。
- 安装器未进行代码签名，状态为 `NotSigned`。

## 开发日志

- 2026-09-08：启动 1.0.421 安装包构建任务。
- 2026-09-08：完成完整构建、runtime smoke、staging 功能检查和安装器校验。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

新会话启动后先阅读本文件，检查 Git 状态和构建进程，从“下一步”继续。
