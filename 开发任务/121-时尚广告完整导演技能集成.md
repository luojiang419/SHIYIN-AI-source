# 时尚广告完整导演技能集成

状态：开发中
当前阶段：3/4
最后更新：2026-09-09

## 当前状态

已完成完整技能读取与打包、双层输出/分格规划、商品主角和独立动作/神态/镜头控制、专项质检及修复。独立多图先生成内部联系表再重建，不增加交付数量。已修复 AIReference 丢失 lookbook_role、参考图静默过滤、完整方案再次提交超过旧 20KB 上限的问题。当前分支 feat/fashion-director-full-integration。工作区原有其他任务修改，仅提交本任务增量。

## 下一步

1. 等待 .codex-artifacts/fashion-director-real-v3 的真实参考图测试结果，查看图片并核对报告。
2. 根据实图修复实际失败层；完成文档与 Git 提交推送。

## 当前 TODO

- [x] 完整技能运行时加载与打包资源声明
- [x] 导演规划、独立图片/九宫格生成与修复链路
- [x] 产品主角与风格专项质检
- [ ] 单元及集成回归、实际生图
- [ ] 文档、Git diff、提交与推送

## 最近验证状态

- 已复现旧函数把 24mm 全身镜头覆盖为腰上/胸上双人模板。
- 21 项新导演测试通过；Lookbook 全套 143 项通过、1 项已知基线失败单独排除。
- 电商与画布电商回归：148 通过、1 项原有预设名单断言失败。
- 基线证据：HEAD 的 canvas.html 已在 v 参数前加入 arrange-spacing，旧缓存测试正则不能匹配；HEAD 的 default_api_providers 已含 linkfox，旧测试期待名单缺此项。二者均非本任务引入，不修改无关业务/断言。
- Python 编译检查通过；技能仓库原文与本次安装的技能 SHA-256 相同。
- 首轮有效执行的 v2 验证未正确传入参考图：脚本使用了不受支持的 reference role，旧归一化静默过滤，生成结果不作为质量通过证据；现已修复脚本并添加生产层防丢失检查。
- v3 已核对 2 张真实参考图进入正式 prepare_ecommerce_request，正在调用安装版 AI助手与拾影 gemini-3-pro-image-preview 完成完整生产链路。

## 任务目标与方案

将 skills/fashion-editorial-sequence-director/SKILL.md 全文作为导演规划及生成依据，取消仅依赖摘要的运行方式。新增 canvas_core/fashion_director.py 隔离时尚广告逻辑，现有风格保留原行为。每个输出可以含一个或多个有独立动作、构图、产品展示任务的 panel；单幅系列共享联合视觉基准。不得强制统一晴天、暖肤色、九格、夸张机位或牛仔裤题材，遵守用户明确要求和参考事实。

## 文件 / 模块清单

- canvas_core/fashion_director.py：完整技能、规划校验和最终提示词。
- canvas_core/lookbook_story.py、main.py：定向接入、生成、质检与缓存。
- canvas_core/ecommerce.py：移除时尚广告残余冲突默认。
- canvas-backend.spec：技能资源打包。
- tests/test_fashion_director.py：数量、景别保留、连续性、产品及回归。

## 验收标准

- 一张九宫格产生九个独立镜头计划，一次图像调用输出完整九格。
- 大胆低角度及完整裤型构图不被通用模板覆盖。
- 不同题材可自适应，不继承固定双人或牛仔裤模板。
- 完整技能出现在规划和实际生图请求中；打包包含相同资源。
- 旧风格回归通过，失败规划明确报错，不静默退回旧模板。
- 真实调用核对最终图，不能用单元测试替代视觉质量验收。

## 已知问题

待 v3 实图验证；源码功能完成不代表当前安装版已替换。完整技能放入导演系统上下文，避免 CanvasLLMRequest.message 的 20000 字符上限；最终生图保留完整技能和具体镜头计划。质检与自动修复仍遵守节点开关；不暗中启用付费重试。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

阅读本任务、检查 Git 与源码，从下一步继续；不覆盖其他未提交任务，不重复已验证阶段。
