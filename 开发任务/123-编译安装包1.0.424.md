# 编译安装包 1.0.424

状态：已完成并发布 GitHub 更新
当前阶段：构建、发布与远程验证完成
最后更新：2026-09-08

## 当前状态

已构建并验证 Windows x64 安装包 1.0.424，包含独立 Node 和两区可灵 CLI，以及当前工作树实际内容。PyInstaller、Tailwind、Tauri、完整 runtime smoke、Inno Setup、版本与 SHA-256 校验均通过。

分支 fix/kling-cli-portable-auth，版本提交 b544dc4 已推送。按任务前快照三方合并，仅提交版本/说明增量；其他既有工作区修改保留。用户已退出原程序，运行验证使用隔离目录，未覆盖本机安装或用户数据。

## 下一步

当前任务已完成。已按用户追加授权将本任务验证过的 1.0.424 安装器发布到 luojiang419/SHIYIN-AI 并设为 Latest，旧版用户可通过软件检查更新下载安装。未覆盖本机安装。

发布页：https://github.com/luojiang419/SHIYIN-AI/releases/tag/v1.0.424

## 当前 TODO

- [x] 检查版本、Git、依赖、运行实例
- [x] 同步版本与编译
- [x] 运行验证、安装器校验
- [x] 文档、提交推送与交付
- [x] 发布 GitHub Release v1.0.424 并设为最新更新
- [x] 验证公开下载与自动更新契约

## 最近验证状态

- 上一源码任务：54 项专项测试、空 PATH 两区 CLI、Chrome 授权流程通过；全量 7 项失败均在任务前快照复现。
- 本次专项：40 passed（可灵免 npm 组件与桌面更新契约）
- 本次版本同步：9 个版本源一致，均为 1.0.424
- PyInstaller/Tailwind/Tauri/Inno Setup：成功；安装器压缩 54.313 秒
- Runtime smoke：health ok、启动 6251ms、内存 212.48MB/250MB，单实例与 sidecar 退出清理通过
- 冻结后端：空 PATH 下通过两区组件启用、区域持久化、全新凭据需要授权、授权状态/备用浏览器 API；未发起真人 OAuth 或付费生成
- 安装器：115,172,059 bytes，ProductVersion 1.0.424，签名 NotSigned
- SHA-256：1ad7646b97dbb980e1918d2f4bc2d062bd602fafd0e8b78110ef2efa6a4ada39
- 构建日志：.build/installer-1.0.424-task/build.log
- 冻结后端报告：.build/installer-1.0.424-task/packaged-kling-smoke.json
- 远程发布：正式版 v1.0.424、非草稿/非预发布，Latest 接口已指向此版本
- 远程附件：EXE 115,172,059 字节，GitHub SHA-256 digest 与本地完全一致；公开 .sha256 内容匹配；EXE Range 下载 HTTP 206、文件头 MZ 正确
- 更新契约：公开 releases/latest 返回准确版本和 EXE/.sha256 配对，通过
- 发布日志与远程验证：.build/installer-1.0.424-task/publish.log、remote-verification.json

## 任务目标与技术方案

使用现有 increment-version.ps1、build-installer.ps1、verify-installer-artifact.ps1 生成 Windows x64 安装器。构建阶段包含独立 Node 22.23.2 和可灵中国区/海外区 CLI 0.1.3；校验安装版不需要用户 Node/npm。

## 文件 / 模块清单

- 项目现有九处版本源、发布说明
- dist/installer/SHIYIN-AI-Setup-1.0.424.exe 及 .sha256
- 本任务文档

## 验收标准

- 版本源一致，完整后端/桌面/安装器构建成功。
- runtime smoke 通过，内置两区 CLI 空 PATH 执行成功。
- EXE ProductVersion 1.0.424，大小/签名状态/SHA-256 检查通过。
- 不覆盖现有安装与用户数据；按追加授权发布 GitHub Release 并验证更新入口。

## 已知问题

无构建、发布或产物校验遗留失败。未执行与本次可灵修复无关的 DWPose 真人模型烟测。

附加烟测首轮把“清空 PATH”误当成完全隐藏 ProgramFiles，实际 Windows 子进程仍恢复 ProgramFiles 并兼容发现开发机旧 CLI。已修正验证目标为：清空 PATH 后显式启用安装包内置的两个区域组件，检查持久化区域与冻结 API；随后全部通过。没有删除或修改系统 Node/npm。

## Git 状态

- branch：fix/kling-cli-portable-auth
- 版本及发布说明：b544dc4，已正常推送 origin
- 工作区既有修改保留，构建产物按现有规则不提交 Git
- Release manifest/tag commit：096df9dbf409617b5a13d1e2971cc0e717965ad8（发布仓库 main）
- 发布源码基线：b544dc4d9b2f56443e7ae76017da88f7cd43428e；Release 说明明确标注构建包含本地工作树既有修改，未声称二进制只来自该 commit

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

读取本任务和 Git 状态，不重复递增版本。构建基于当前工作树，提交仅包含本任务增量。
