# 分镜合成深度参考与批量模式

状态：开发中
当前阶段：场景匹配扩展 3/3
最后更新：2026-09-09

## 当前状态

上一阶段批量合成已提交 ce6efc6 / 95e6a71。本轮场景一致性扩展已实现并验证：用户场景提供空间、材质与固定建筑特征；线稿/深度提供机位和布局；原始参考图提供光线与色彩。多张用户背景作为候选，生成前由 AI 快速分析并逐镜选择背景及对应位置，然后按匹配关系派发并发任务。

沿用原任务文档，不重复规划已完成部分。本轮基线保存于系统临时目录 shiyin-storyboard-scene-127，继续隔离工作区原有改动。

## 下一步

提交已验证的本次增量并推送；完成最终 Git 记录。本轮未调用真实模型生成图片，不以模拟回归替代画质验收。

## 当前 TODO

- [x] 原单图/批量、深度输入、并发输出与恢复已完成
- [x] 单场景自动定位、多场景逐镜匹配
- [x] 场景结构锁定与参考色光迁移生成约束
- [x] 匹配分组展示、失败保留与重试
- [ ] 本轮测试、增量提交和推送

## 最近验证状态

- 静态检查：六个相关 JavaScript 脚本语法检查通过；diff 空白检查通过
- 运行测试：JavaScript 运行时测试 14/14 通过（含场景匹配扩展），相关 Python 测试 91/91 通过；最终全量 1563 passed / 4 failed，四项均已在修改前基线上复现
- 浏览器：当前工作区与本轮独立提交候选的双画布回归均通过，包含场景单图复用、逐镜候选路由、场景分组展开、非法编号阻断；编组+直连、并发乱序、提交/生成失败、深度预处理、单槽重试、单图数量、保存与刷新恢复，页面脚本错误 0、控件无溢出、端口误差 <0.02px
- Git：实现提交 ce6efc6 已正常推送至 origin/feat/fashion-director-full-integration；其他任务的既有工作区修改保留。本文最终验收记录随后单独提交。

## 任务目标

单图保留当前合成方式；批量模式按输入分镜逐张独立生成，共用演员、服装、道具与场景资产。参考照片自动生成深度约束，并仅提供构图、色彩和光线证据，要求重新生成真实清晰场景，禁止模糊底图扩图。

## 技术方案

- 保留 sketch 端口，追加 depth（已有深度图）与 reference（原始参考图）。模式默认 single，batch 每镜一张。
- 批量默认源画幅，可指定统一画幅；遵守画布现有单输出 96 图容量，一批超过 96 镜明确提示分批，禁止静默丢图。
- 批量按端口内连线/编组顺序展开；显式线稿/深度作为镜头列表，参考图一张可共用或等量逐一配对，不等量明确报错；仅参考图时逐图自动提深度。
- 共享模块负责不可变任务快照、资产角色、输入上限检查和提示词；两种画布复用现有持久化图片任务和输出槽。
- 复用一键复刻的深度模型及全局深度处理参数；同批相同参考仅预处理一次。失败独立显示，成功结果按镜头顺序保留。

## 文件 / 模块清单

- 新增 static/js/canvas-film-storyboard.js、tests/js/film_storyboard_runtime.test.cjs
- 修改 canvas-film-nodes.js、canvas-special-nodes.js、canvas.js、smart-canvas.js
- 修改两种画布 HTML 入口及 canvas-film-nodes.css
- 更新相关测试

## 验收标准

- 旧画布默认单图，演员资产及连线不丢失。
- 直接多连线和编组多图都生成等量独立镜头，共享资产不串图。
- 深度端口不再次提深度；参考端口自动提深度，原图明确仅供构图色光参考。
- 单项失败不取消其他任务，输出槽与来源对应，已提交任务保存后可恢复。
- 运行时测试覆盖计划/深度/并发/失败/两种画布，界面无明显溢出。
- 实际模型生成质量依赖上游能力；未真实出图前不宣称任意图片完美还原。

## 已知问题

全量初次回归 1561 passed / 6 failed，其中两项为本次改变的静态断言，已更新为新语义并增加运行时验证。另四项在修改前文件基线上同样失败：启动脚本顺序、旧可灵视频参考限制、平台默认列表、智能画布旧可灵模型限制。它们不属于本次范围，不修改。

未调用付费模型真实出图；浏览器使用隔离服务与模拟生成/深度响应，不能据此宣称任意参考图达到像素级完美还原。当前不创建安装包、不递增正式版本。

## 开发日志

- 2026-09-09：完成需求与现有链路检查，建立任务文档。
- 2026-09-09：完成共享计划、双画布执行与错误恢复；修复端口对齐和失败槽保留，通过隔离浏览器回归。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

阅读项目规则和本文，检查 branch/status，以当前代码为准从“下一步”继续；不要覆盖或提交其他任务的既有修改。

## 已完成内容

- 新增深度图与参考图端口，旧画布默认单图；单图生成数量保持，批量每镜一张。
- 图片直连、编组及组内输出图片自动展开，按镜头共用演员、服装、道具与场景。
- 参考图自动提深度，同批复用相同参考预处理；已有线稿/深度不重复推理。
- 明确姿势、演员、服化道与色光职责，禁止模糊原图底板扩图，要求从零重建清晰实景。
- 默认源画幅、输入数量摘要、错配/超限提示、独立失败槽、顺序保留及已提交任务刷新恢复。
- 两种画布完整隔离浏览器回归通过；仅本次增量的提交候选也通过同一回归。

## 验证产物

- `测试/分镜合成-127/report.json`：当前工作区双画布回归。
- `测试/分镜合成-127/提交候选/report.json`：独立提交候选回归。
- 两目录均有节点截图；本地测试产物未提交。
- 实现 commit：`ce6efc6`；未提交密钥、配置、缓存、输出素材或其他任务改动。

## 场景匹配扩展方案与验收

- 复用 /api/canvas-llm 与现有视觉模型配置；只传镜头结构/色光图和候选背景，演员服装不进入背景分析。
- 同一镜头的多张单图输出复用一次匹配；批量多个镜头分组解析，单次最多 20 张视觉输入，候选超限分批比较和复选，不静默截断。
- 验证 AI 返回的镜头/场景 ID 及位置、机位、色光文字；匹配失败不盲目派发生成任务。
- 每个生成任务只携带选中的场景。固定场景建筑、空间布局、材质与地标，在对应位置依参考图光线色彩重新渲染；不能复制原始参考背景替代用户场景。
- UI 显示场景分组和镜头对应关系；输出仍保持原镜头顺序。
- 验收：单场景定位、不同镜头不同场景、多图分批与重复输出缓存、错误 ID/缺项、请求资产与提示词角色、双画布集成。

## 本轮场景匹配验证

- `tests/js/film_storyboard_runtime.test.cjs`：14 passed；含单图 3 个输出只解析一次、逐镜不同背景、25 张候选全量比较/复选、非法返回拒绝、已选场景重试固定。
- `tests/support/film_storyboard_ui_check.cjs`：双画布通过，当前工作区与独立 HEAD 提交候选均验证，脚本错误 0。每个任务只带选中场景，先匹配再派发，组内位置说明可展开查看。
- 相关 Python 测试：91 passed；全量：1563 passed、4 个已知基线失败，无新增失败。
- 产物：`测试/分镜合成-127/场景匹配/` 与 `测试/分镜合成-127/场景匹配提交候选/`。
- 场景描述和区域匹配来自已配置视觉模型；尚未使用用户实际图片检验匹配精度及生成画质。
