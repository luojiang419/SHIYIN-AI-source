# Lookbook 两阶段故事方案与真实案例报告

状态：已完成（方案、HTML与真实案例；生产功能未实施）
当前阶段：3/3
最后更新：2026-09-10

## 当前状态

用户最新要求为先出设计方案、详细 HTML 报告及配置 API 的真实案例；本轮不实施生产功能。
已完成详细单文件 HTML、两次独立解析与一次四格生图。采用最近 Lookbook 任务的一张人物参考（两人及相机），空广告需求测试。
报告：输出/Lookbook两阶段方案-20260910/Lookbook两阶段故事设计与实测报告.html（约6.25MB，原图及返回内嵌）。
模型：ecommerce-vision / gpt-5.6-terra；shiying / gemini-3-pro-image-preview。
结果：API链路成功，视觉部分达标；报告保留复古相机即时回放逻辑矛盾、首尾同机位、衣着结构漂移等真实问题，并据此补充语义校验方案。
工作分支：feat/fashion-director-full-integration。已有其他任务修改，不纳入本任务。

## 下一步

当前报告任务已完成。本轮不进入生产功能实施。后续按用户审阅反馈调整方案或正式实施两阶段节点流程。

## 当前 TODO

- [x] 调用链与现状核查
- [x] 确认报告优先，不改生产代码
- [x] 两阶段真实 API 请求及图片演示
- [x] HTML 报告与显示验证
- [x] Git 检查、提交与推送

## 最近验证状态

- 静态检查：两份 Python 脚本 py_compile、git diff --check 通过。
- API：故事42.257秒；提示词96.546秒；图片64.114秒；合计202.917秒。两轮重新附同图，messages为空，故事逐字交接通过。1张896×1200四格图片。
- HTML：Edge / Playwright，1440×1000、390×844无横向溢出；2张图片、4局部、6组完整证据、回填演示及图片放大通过；页面错误0。已人工查看桌面与手机截图。
- 截图与运行结果：.codex-artifacts/lookbook-two-stage-report/verification.json。
- 最近 Git commit：报告与脚本45b4afc，已推送origin/feat/fashion-director-full-integration；本完成状态记录随后归档。

## 任务目标

解释问题成因，设计“综合参考 → 故事与立意回填 → 独立提示词请求 → 生图”的可实现方案，并给出一次真实案例证据。

## 当前项目现状

main.py 的 prepare_lookbook_creation 并行执行参数理解与可见事实分析；storyboard 合并创意和分镜，自动风格另有 plan。当前不是仅一次 API，而是缺少独立的完整故事交付阶段。canvas.js 将摘要保存到 lookbookStorySummary，没有回填 lookbookPrompt。

## 技术方案

默认无搜索成功路径两次 LLM 请求，生图单独计数。第一阶段综合所有有效图片、用户文字与节点约束，输出参考事实、故事、立意、动作因果与镜头意图；第二阶段新会话再次提供原图、冻结故事和最终参数，输出连续性与机位分镜及完整提示词。保存原始需求与生成故事的来源，采用请求版本防止过期回填。严格区分 30° 相邻机位差、180° 轴线与单纯裁切。

## 文件 / 模块清单

本轮新增：本任务文档、独立实验脚本、HTML 模板与报告输出。生产 main.py、canvas.js、canvas-lookbook-node.js 本轮只读。

## 开发阶段

- [x] 现状与方案
- [x] 真实案例与视觉复查
- [x] 报告验证与交付

## 验收标准

- HTML 可离线查看，真实图片与返回内容完整可见，无密钥。
- 两阶段为独立 API 调用，第二阶段输入可追溯到第一阶段原文。
- 生图为真实已配置 API 返回，展示实测时间与数量，不把提示词中的机位角度当成实图测量。
- 说明单案例无对照不能证明普遍提升，不声称生产功能已实现。

## 已知问题

无交付阻塞。单样本、无对照、联合预览不能证明独立生图普遍提升；生产回填尚未实施。用户未指定案例素材，复用最近安装版 Lookbook 任务已有参考，不扩大到其他私人素材。

## 已完成内容与重要记录

- 实验环境隔离于.codex-artifacts，只读安装版数据库；密钥仅内存使用，底层调试日志不落盘。
- 第一轮产生《光落在快门之前》，第二轮保留完整故事并生成4镜头与拼图提示词；最终输出未经人工修图。
- 报告提供事实/推断区分、30°与180°来源、数据契约、模块改造清单、参数与缓存兼容、异常恢复、验收矩阵及失败复盘。
- 通过fashion-editorial-sequence-director的事件/角色/连续性/联合预览方法辅助方案；用户目标优先，不将联系表改成节点默认输出。
- 重跑demo脚本发现已成功报告会直接退出，不重复调用付费API。build脚本只生成HTML，无网络调用。
- Git归档包含自包含HTML（明确指定单文件加入，未改变输出目录的忽略规则）；原始素材/响应已内嵌，实验散文件与运行缓存留在本地。未提交配置、密钥、安装包及其他任务改动。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

阅读本任务和 Git 状态，从下一步继续；先完成报告与案例，不实施 Lookbook 功能。
