# 编译安装包 1.0.432

状态：已完成
当前阶段：3/3
最后更新：2026-09-10

## 当前状态

从已验证提交 `6889f63` 创建隔离 worktree `E:/APP/SHIYIN-AI-release-1.0.432`，分支 `release/1.0.432`，只包含已提交的媒体架构修复。复用主工作区 node_modules 和 Rust target 构建缓存，不带入主工作区未提交 Lookbook 等源码修改。

安装器及 SHA-256 已生成并复制到主工作区 `dist/installer/`。九处版本、完整构建、产物元数据、staging 媒体 UI 与冻结后端两账号 smoke 均通过。

## 下一步

交付 `E:/APP/SHIYIN-AI/dist/installer/SHIYIN-AI-Setup-1.0.432.exe` 及同名 `.sha256`。当前任务已完成；用户可自行运行安装器升级。

## 当前 TODO

- [x] 检查规则、Git、工具链、磁盘及运行中实例
- [x] 创建隔离构建工作树
- [x] 版本与更新说明
- [x] 完整构建
- [x] 产物及运行校验
- [x] 文档、提交推送和交付

## 最近验证状态

- 上轮源码隔离验证：73 passed
- 工具链：Python、npm、Cargo、Inno Setup 可用，E 盘约 207GiB 空闲
- 构建：PyInstaller 约 42 秒，Tauri release 1 分 49 秒，Inno Setup 50.141 秒；Tailwind、67 页面/18 处缓存盖章、Kling 双区空 PATH 独立运行时校验通过。
- 安装器：115,236,418 bytes，版本 1.0.432，签名状态 NotSigned。
- SHA-256：`fdd6247b9490527c9a03e3a111109f44f2e80d1fc67c43d36f6e30d0a168973f`；主工作区复制件哈希一致。
- 冻结后端：health ok，启动 1656ms；两个普通账号上传、保存画布、作品列表、图片预览、304、跨账号拒绝、坏图片拒绝和研究网页排除均通过；测试后端已停止。
- 冻结归档：确认包含 media_integrity、works_snapshot 和 database；staging 的 works.js 与源码字节一致，包含新会话缓存。
- 基线 commit：6889f63；修复 commit：56cc3b8
- 构建 commit：6280c3e，已推送 `origin/release/1.0.432`，主分支 `fix/media-architecture-isolation-performance` 已快进同步到 1.0.432；原有 main.py 未提交差异逐行比较一致。

## 任务目标

生成 SHIYIN AI 1.0.432 Windows x64 安装器与 SHA-256，确保媒体完整性、缓存账号隔离和无损分页修复进入产物。

## 技术方案与文件清单

使用标准 `tools/increment-version.ps1` 和 `tools/build-installer.ps1`，同步 VERSION、package/package-lock、Cargo/Cargo.lock、tauri.conf、main.py 和更新说明。构建日志位于隔离 worktree `.build/installer-1.0.432-task/`，产物完成后复制至主工作区 `dist/installer/`。

## 开发阶段与验收标准

- [x] 九处版本同步且来源明确
- [x] 后端、桌面和安装器完整编译通过
- [x] PE 版本、文件大小、SHA-256、staging 资源和冻结媒体模块校验通过
- [x] 隔离冻结后端 health、普通账号上传/画布/作品/预览行为通过
- [x] 提交推送且用户原有改动保留

## 已知限制

已安装 SHIYIN AI 实例正在运行。标准桌面 smoke 明确要求关闭全部现有实例；本轮使用 `-SkipRuntimeSmoke` 构建并补充独立冻结后端 smoke，不关闭现有软件、不覆盖安装、不发布 GitHub Release。桌面整机生命周期及 DWPose 实图 smoke 未执行。

## 开发日志

- 2026-09-10：建立隔离发布分支，确认目标版本、现有运行实例和构建工具。
- 2026-09-10：完成完整构建、冻结媒体专项和安装器校验，复制交付文件到主工作区。
- 2026-09-10：发布源码提交推送并同步主工作区版本，已验证原有用户修改保留。没有覆盖安装或发布 GitHub Release。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

阅读本任务文档、检查发布分支和 Git 状态，从“下一步”继续。主工作区仍有用户原有修改，禁止直接替换用户源码或混入当前安装包。
