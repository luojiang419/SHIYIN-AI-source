# 安装后创作空间启动卡死修复

状态：源码及本机安装文件修复完成，已提交推送；待用户重开确认
当前阶段：3/3
最后更新：2026-09-10

## 当前状态

已修复安装版 1.0.434 启动卡死：`AccountIdentity.public()` 返回 `account_id`，新增 StudioPageState 原先只接受 `id/user_id`，导致 shell restore 永远等待 accountReady。旧浏览器 fixture 使用了错误的 `id` 字段，遗漏真实接口契约。现已接受真实字段，增加存储超时降级、异常处理和启动锁释放，并同步本机安装目录两个前端文件。

工作区分支 `feat/workspace-state-restore`，起始提交 `53cc467`；已有 Lookbook 等无关修改，全部保留。

## 下一步

1. 用户完全退出并重开已安装软件，使当前窗口加载修复文件；已发出重开后的确认请求。
2. 后续重新打包时必须包含本修复；旧 1.0.434 安装器未重建。

## 当前 TODO

- [x] 安装态日志、账号契约与启动调用链核对
- [x] 修复与回归测试
- [x] 安装态静态资源修复与浏览器验证
- [x] 提交与推送

## 最近验证状态

- 安装版版本：1.0.434；安装路径：D:/Program Files/SHIYIN AI。
- 后端启动正常；最后两次主页请求后账号接口均 200，未进入业务 iframe 请求。
- 原安装文件 + 真实后端账号结构：浏览器启动等待 8 秒超时，稳定复现原故障。
- 源码启动行为测试：1 passed（覆盖 8 个浏览器场景，包括管理员/用户启动与跨账号冷恢复、IndexedDB open/事务卡住、存储访问拒绝、恢复异常、失败重试、缺少账号标识降级）。
- 已安装文件执行同一测试：1 passed。
- 启动架构与桌面 bootstrap 回归：14 passed。
- 全工作台浏览器回归：公共层、六组表单/列表、存储回退、关闭浏览器进程后恢复、电商、深度、画布、8 个主页面热驻留及最后画布恢复全部通过。纠正 fixture 字段后同步修正了等待 `currentStudioAccount.id` 的旧断言。
- 源码与安装 HTML 各 3 段内联 JS 语法检查通过，状态脚本和新浏览器脚本语法检查通过，git diff --check 通过。
- 安装目录仅更新 `app/web/js/studio-page-state.js` 和 `index.html` 的 restoreActivePage 函数；状态脚本与源码字节一致。原文件备份：`.build/task150-installed-before/`。
- 不涉及编译型代码，本次使用静态资源修复，没有重新构建安装器或强制关闭现有桌面/后端进程。
- 源码提交 `36f94c5` 已推送 `origin/feat/workspace-state-restore`。本轮只提交 6 个相关文件，其他工作区修改保留；隔离测试服务已停止。

## 技术方案与文件清单

- static/js/studio-page-state.js：优先读取 account_id，保留兼容字段；无法使用账号缓存或 IndexedDB 时有界降级。
- static/index.html：状态恢复失败仍允许进入业务页，启动异常显示重试，并可靠释放启动锁。
- tests/support/workspace_state_restore_check.cjs：纠正账号 fixture。
- tests/：以后端真实账号公开结构验证公共状态层和主框架启动，覆盖超时/异常。

## 验收标准

- 真实管理员与普通账号均可越过加载页，缓存按 account_id 隔离。
- 存储无法打开或事务卡住不会无限阻塞；启动异常可以重试。
- 本机安装文件应用同一修复，不修改账号、画布、媒体等用户数据。
- 检查并提交自身差异，完成正常 push。

## 已知问题

此前 1.0.434 安装器仍包含此错误，不应再次覆盖安装；本次已修复源码和本机安装文件，尚待用户重开当前窗口确认。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

阅读项目规则和本文，核对 Git 与源码，从“下一步”继续；不要混入已有未提交改动。
