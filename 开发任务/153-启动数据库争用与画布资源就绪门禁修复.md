# 启动数据库争用与画布资源就绪门禁修复

状态：已完成，源码已提交推送
当前阶段：3/3
最后更新：2026-09-10

## 当前状态

已完成会话心跳写锁容错、读锁503提示、列表有限自动重试，以及动态节点资源门禁。必要配置和资源检查前置，图片解码、视频可播放及低清缓存完成后才进入；缺失/失败资源保留加载页并允许重试或返回列表。进入后内存整理保留已有像素，避免降采样请求再次造成空白。

## 下一步

源码提交 59df715 已推送 origin/fix/canvas-resource-readiness。当前源码任务已完成；本次未重新编译安装包，已安装的1.0.435不包含这些修复。

## TODO

- [x] 现状和调用链检查
- [x] 数据库锁容错与列表自动重试
- [x] 当前资源动态就绪门禁
- [x] 延迟/重绘/失败/数据库并发回归
- [x] Git 隔离提交推送

## 验收标准

暂时读锁返回可重试响应，非关键会话心跳写锁不影响已验证身份；前端暂时失败自动恢复。遮罩不因旧 DOM 移除关闭，图片实际完成解码、视频首帧可用后再进入，必要配置不在进入后重绘。失败不自动进入未完成画布。

## 文件清单

canvas_core/accounts.py、main.py、static/js/canvas-list.js、canvas.js、独立资源等待模块及相关 HTML、tests。

## 最近验证状态

数据库写锁不影响有效会话、读锁返回503；动态DOM替换、图片解码、视频canplay及失败取消测试通过；实际页面自动重试、替换后的普通图片等待、失败保持遮罩及重试成功通过；301节点独立/主框架驻留恢复均通过，重新进入工程与媒体请求为0。38 项相关测试通过。新增低清缓存预热与保留旧像素保护后，301 节点独立列表和主框架完整驻留回归均通过（最后一次运行约1497/1551ms，进入期间等待就绪，工程与媒体重复请求均为0）；包含保存中退出、旧GET/PUT保护和LRU保护。分支 fix/canvas-resource-readiness，起始 7e8e587。主工作区既有修改已保留基线用于隔离暂存。

## Git 与交付

- 源码提交：59df715，已正常推送。
- JavaScript语法、git diff --cached --check通过；main.py及canvas.js既有未提交修改经逐块比对保持完整，未混入本次提交。
- 未改动用户画布数据、未替换已安装软件。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

读取本文和 Git，从下一步继续。
