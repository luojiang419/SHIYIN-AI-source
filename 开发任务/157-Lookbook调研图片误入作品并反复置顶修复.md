# Lookbook 调研图片误入作品并反复置顶修复

状态：源码修复完成，验证通过
当前阶段：2/2
最后更新：2026-09-11

## 当前状态

只读检查安装目录主数据库后定位：Lookbook 节点 lookbookResearchImages[0].image_url 中的外部蕾丝服装调研图被递归资产扫描收录。该节点没有 created_at，extract_canvas_assets 使用画布 updated_at 回退；远程图片没有本地 mtime 覆盖，每次保存画布都会更新其作品排序时间。000001 是排序后的展示编号，不是该图片的实际文件名。

## 下一步与 TODO

- [x] 核对源码及实际数据库，未修改用户数据
- [x] 在作品适配层排除调研来源，保留 Lookbook 参考数据及资产扫描
- [x] 缺少节点时间时回退固定画布创建时间，不使用画布修改时间
- [x] 回归与隔离提交范围检查

下一步：本源码修复完成；安装版需要构建更新，当前未替换安装程序。提交与推送记录以 Git 为准。

## 技术方案与文件

- main.py：canvas_generated_work_items 排除 lookbookResearchImages 路径；extract_canvas_assets 采用稳定时间回退，作品适配层同样不回退 canvas_updated_at。
- tests/test_works_research_order.py：调研图排除、同节点真实输出保留、旧远程图随保存不变新、生成作品仍排在前面。
- 去重时优先正式图片/输出用途：同 URL 在调研记录和普通节点同时出现，无论节点顺序如何都保留为作品。
- 不改显示编号体系，不改本地作品 mtime 排序策略，不删除任何作品文件或参考数据。

## 验证与验收

- 修改前新增测试 3 失败/1 通过，确定性复现调研图误入与保存时间漂移。
- 最终作品、媒体索引与视频展示相关回归 69 项通过。
- 安装目录主数据库只读回放：实际问题画布有 1 项 research-only 资产，修复后作品适配返回空；内存模拟次日保存，全部资产 created_at 不变。未修改用户数据库或图片。
- 缺少节点独立时间的历史远程资产使用画布创建时间作为保守回退，不声称它是精确生成时间。拥有本地媒体的作品仍沿用原有 mtime 逻辑。

## Git

分支 fix/canvas-resource-readiness。main.py 已有其他修改，只提交本任务差异。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

核对本文和 Git，从 TODO 继续。安装包需单独构建，本任务不替换运行中的软件。
