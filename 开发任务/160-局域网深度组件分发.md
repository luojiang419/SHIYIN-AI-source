# 局域网深度组件分发

状态：已完成
下一步：在另一台局域网电脑用安装版触发人物深度组件安装，观察实际吞吐与防火墙可达性
TODO：源码与本机服务完成；待另一台真实电脑验收
最近验证：35 项定向测试通过；真实服务、6105 文件清单、HTTP Range、设置页窄窗口均通过
branch/commit：fix/canvas-cache-fallback / 未提交
阻塞：无

## 目标与验收

在软件设置中配置人物深度组件局域网服务器与客户端下载地址。本机默认监听 `192.168.0.24:3011`，按文件直接传输已安装且验证通过的组件；其他电脑并发下载并完成大小、SHA-256、路径和 worker 冒烟校验，局域网不可用时回退原有公网源。

验收：设置可持久化；服务提供健康状态、清单和 Range 下载；客户端优先使用局域网并保留公网回退；非法地址、越权路径和损坏包被拒绝。

## 完成情况

- 设置页新增服务器启用、地址、端口、客户端下载地址、保存和生成文件清单控件。
- 本机配置为 `http://192.168.0.24:3011`；服务直接传输 6105 个文件，总计 5,982,828,177 字节，不压缩、不解压。
- 客户端使用 8 路并发、Range 断点续传与逐文件 SHA-256，required paths 和 worker smoke 仍在激活前执行；局域网失败后回退原有公网源。
- Windows 防火墙新增 `SHIYIN AI Person Depth LAN 3011`，仅 Private/LocalSubnet/TCP 3011。
- `python -m pytest tests/test_app_config.py tests/test_person_depth_components.py tests/test_person_depth_lan.py -q`：35 passed。
- `py_compile`、`node --check`、`git diff --check` 通过；浏览器 744px 宽度实测无重叠或截断。

[CODEX_LONG_TASK_CONTINUE_V3]
