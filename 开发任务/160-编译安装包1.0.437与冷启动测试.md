# 编译安装包1.0.437与冷启动测试

状态：构建与测试通过
当前阶段：3/3
最后更新：2026-09-11

## 当前状态

基于 59446de 创建隔离工作树 E:/APP/SHIYIN-AI-release-1.0.437，分支 release/1.0.437。包含 f29c153 冷启动入口阻塞及失败预取修复，以及已提交的画布首屏和后台图片加载优化。主工作区未提交修改不纳入安装包。版本按项目规则递增至1.0.437。

## 下一步

安装包交付至 E:/APP/SHIYIN-AI/dist/installer/SHIYIN-AI-Setup-1.0.437.exe，附.sha256。构建与隔离测试已完成；当前用户实例未退出、未覆盖安装。下一步可使用此包进行实际用户环境升级验收。

## 当前 TODO

- [x] 源码、环境、版本与隔离工作树
- [x] 完整构建与产物校验
- [x] 冻结后端冷启动与画布页面验证
- [x] 交付与版本同步；提交推送记录以 Git 为准

## 最近验证状态

9个版本来源同步检查通过；隔离源码30项回归通过。PyInstaller、Tailwind、Tauri release、Inno Setup完整构建成功；Tauri约2分01秒，安装包压缩53.828秒。Kling双区域空PATH测试通过，72个web资源检查、18个版本改写。

冻结后端及真实浏览器7轮通过：新数据首次进入、3次重启、1.0.436创建的数据在1.0.437下3次重启。每轮301节点、提示词数据一致，图像像素可见，JS错误为0。冻结进程启动1156–2266ms；列表页面220–315ms，从列表导航到画布完成检查1337–1746ms。使用300个引用同一张256×160 PNG的图片节点和1个文本节点，是合成回归工程，不代表实际大型混合媒体工程的耗时。数据升级测试为旧/新冻结后端复用独立数据目录，不是执行安装器覆盖升级。

双账号媒体上传/画布/作品/预览、304、无效图片拒绝、HTML排除、账号隔离全部通过。所有本次测试后端均已退出；用户原PID 12100/35244仍运行。未执行要求关闭全部实例的桌面单实例生命周期测试、实际覆盖安装或DWPose实图验证。

EXE 115259645 bytes，版本1.0.437，NotSigned。SHA-256：ab77149e98a68614023d8422cd2e89f347ad11c0d8fd0b36ac3e2370a8ef4561。日志、JSON报告及截图位于隔离工作树 .build/installer-1.0.437-task；已查看首次进入截图确认图片可见。

## 任务目标与验收标准

产出包含本次修复的1.0.437安装包；完整构建成功、版本一致、哈希可验证；冻结后端能首次启动并读取列表/节点，重启后数据可恢复；包内实际浏览器可进入画布。不得以源码测试替代冻结产物测试。

## 文件 / 模块清单

现有9个版本来源、本文、必要的冷启动测试脚本。产物与日志仅保存在 dist 和 .build。

## 已知问题

不执行会强制关闭用户软件或改写现有安装注册的安装脚本；实际用户数据量及当前安装覆盖升级另需验收。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

读取本文和Git，从下一步继续；构建日志位于隔离工作树 .build/installer-1.0.437-task。
