# 编译安装包1.0.438与单次复刻规则验证

状态：构建中
当前阶段：1/3
最后更新：2026-09-11

## 当前状态

用户确认只更新一键复刻提示词、每张仍一次生图，随后要求编译安装包。v3.4八组模板回归105项通过，前端语法和diff检查通过。采用已验证提交创建隔离工作树构建，避免混入主工作区其他未完成任务。

## 下一步

1. 完成1.0.438版本同步和规则提交。
2. 创建隔离构建目录，完整构建PyInstaller、Tauri、Inno Setup。
3. 启动冻结后端，验证8组v3.4提示词、前端版本、媒体API和画布启动，校验安装包哈希后交付。

## TODO

- [x] v3.4规则相关回归105项
- [ ] 版本同步、提交与隔离源码
- [ ] 完整构建
- [ ] 冻结产物验证
- [ ] 交付EXE、SHA256及提交推送

## 验证与边界

不增加自动第二阶段校色，不重复付费生图。用户自定义提示词仍保留；默认模板更新不等于覆盖自定义规则。保留当前安装版运行实例，不执行需要关闭全部实例的桌面单实例测试，使用独立数据和端口验证冻结后端。不会自动覆盖安装。

## 文件清单

9个版本来源、发布说明、v3.4模板和相关前端/测试、161及162任务文档。构建产物放dist/installer，日志放隔离工作树.build。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

检查Git与本文，从下一步继续；勿重复递增版本或覆盖其他任务改动。
