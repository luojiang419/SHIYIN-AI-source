# 编译安装包1.0.438与单次复刻规则验证

状态：构建与产物验证完成
当前阶段：3/3
最后更新：2026-09-11

## 当前状态

用户确认只更新一键复刻提示词、每张仍一次生图，随后要求编译安装包。基于906d240在E:/APP/SHIYIN-AI-release-1.0.438（release/1.0.438）完成PyInstaller、Tauri和Inno Setup完整构建。包内8组v3.4模板与源码逐字一致，媒体与画布冻结后端测试通过。主工作区其他未提交任务未混入。

## 下一步

当前构建任务已完成。交付 E:/APP/SHIYIN-AI/dist/installer/SHIYIN-AI-Setup-1.0.438.exe 及同名.sha256；未覆盖安装，后续由用户安装升级。

## TODO

- [x] v3.4规则相关回归105项
- [x] 版本同步、提交与隔离源码
- [x] 完整构建
- [x] 冻结产物验证
- [x] 交付EXE、SHA256及源码提交推送（906d240）

## 最近验证结果

- 主工作区与隔离源码105项相关测试均通过；JavaScript语法和diff检查通过；9个版本来源同步至1.0.438。
- PyInstaller、Tauri release、Inno Setup成功；安装器压缩66.235秒。
- 冻结后端8组模板与pose_replicate_template_catalog逐项完全相同，模板ID为pose-replicate.v3.4；经典/智能画布与电商资源访问正常，前端版本一致；未调用付费生成API。
- 双账号媒体上传、画布、作品、预览、304、非法图片拒绝、HTML排除和账号隔离通过。
- 首次进入、重启、1.0.437创建的数据由1.0.438读取三轮浏览器测试通过：301节点恢复，图片像素可见，JS错误0。冻结启动1156～2781ms，画布进入1331～1544ms。为合成301节点夹具，不代表用户复杂工程性能。
- EXE：115251446字节，PE ProductVersion=1.0.438；构建副本与交付副本SHA256一致：4dc4adc2a27f0f5fe99da67a9f7c2777b5e1ffdeddb04fdfd1a36d05255f4338。
- 日志和JSON报告：隔离工作树.build/installer-1.0.438-task。各测试后端均已停止。

## 验证与边界

不增加自动第二阶段校色，不重复付费生图。用户自定义提示词仍保留；默认模板更新不等于覆盖自定义规则。保留当前安装版运行实例，不执行需要关闭全部实例的桌面单实例测试，使用独立数据和端口验证冻结后端。不会自动覆盖安装。

## 文件清单

9个版本来源、发布说明、v3.4模板和相关前端/测试、161及162任务文档。构建产物放dist/installer，日志放隔离工作树.build。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

检查Git与本文，从下一步继续；勿重复递增版本或覆盖其他任务改动。
