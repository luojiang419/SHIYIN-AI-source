# 输入组尺寸与 Web 浏览器下载修复

- 当前状态：按用户最新要求改为保留节点自适应尺寸并自动整理；实现与验证完成。
- 下一步：部署更新后的静态资源；桌面安装包本次未重新构建。
- TODO：无待实现项。
- 最近验证：更新后的 Edge 浏览器行为回归通过，覆盖延迟竖图加载、节点变高、顺序/间距/组边界及无循环渲染；Web/桌面下载矩阵继续通过。初版 27 项相关 pytest 通过；调整后布局与组整理 39 项 pytest 通过，`node --check static/js/canvas.js` 通过。
- 分支：`fix/input-group-web-download`；初版提交 `ee45c95` 已推送，本次在同分支追加调整。
- 已有修改：工作区包含用户其他任务修改，尤其 `canvas.js`；仅提交本任务差异。

## 验收
输出转换或复制为输入组后，保留竖图自适应尺寸与悬浮面板布局；按输出顺序整理，节点不重叠、保持组间距，组背景包住全部节点，媒体延迟加载或尺寸变化后再次整理。Web 顶层页和 iframe 中单个、批量下载均走浏览器，不向快捷保存接口 POST；桌面快捷保存保持原行为。

[CODEX_LONG_TASK_CONTINUE_V3]

## 实现与验证范围
- `static/js/canvas.js`：撤回初版固定尺寸标记，转换/复制生成的组持久化 `autoArrangeInputGroup`。复用组整理功能，按 `group.items` 顺序及真实节点尺寸排列；测量到组成员尺寸变化后再次整理。自动整理合并调度，不增加撤销记录；成员位置改变也触发重绘，组删除后旧回调不再生效。
- `static/js/quick-save.js`：沿同源父窗口检测 Tauri 原生桥。Web 不读取服务器快捷配置、不响应静默保存设置广播；单项保留浏览器链接下载，批量使用浏览器下载并支持 Blob。桌面仍读取配置并保留静默保存和手动目录分支。
- 11 个 HTML 入口更新共享下载脚本缓存参数，画布额外更新尺寸修复脚本参数。
- 行为回归覆盖 Web/模拟桌面 × 顶层/iframe、单项/批量、服务器静默设置广播、延迟竖图尺寸读取及 JSON 标记恢复、后续节点变高、原顺序/间距/组边界、稳定后不重复渲染。
- 桌面原生桥与接口响应由浏览器测试模拟；未实际启动已安装桌面软件，未连接真实服务器目录执行保存。
