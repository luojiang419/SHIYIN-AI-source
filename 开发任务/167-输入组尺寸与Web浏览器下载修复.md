# 输入组尺寸与 Web 浏览器下载修复

- 当前状态：实现与验证完成，准备提交推送。
- 下一步：部署更新后的静态资源；桌面安装包本次未重新构建。
- TODO：无待实现项。
- 最近验证：Edge 无头浏览器执行 `node tests/support/input_group_web_download_check.cjs` 通过；27 项相关 pytest 通过；两个修改脚本 `node --check` 通过。
- 分支：`fix/input-group-web-download`。
- 已有修改：工作区包含用户其他任务修改，尤其 `canvas.js`；仅提交本任务差异。

## 验收
输出转换或复制为输入组后，图片加载及重绘不扩大既定尺寸、不超出组。Web 顶层页和 iframe 中单个、批量下载均走浏览器，不向快捷保存接口 POST；桌面快捷保存保持原行为。

[CODEX_LONG_TASK_CONTINUE_V3]

## 实现与验证范围
- `static/js/canvas.js`：转换/复制输入组的图片持久化保留尺寸标记，避免异步媒体方向检测和重绘把 260×336 扩为 520 宽。普通竖图布局仍保留。
- `static/js/quick-save.js`：沿同源父窗口检测 Tauri 原生桥。Web 不读取服务器快捷配置、不响应静默保存设置广播；单项保留浏览器链接下载，批量使用浏览器下载并支持 Blob。桌面仍读取配置并保留静默保存和手动目录分支。
- 11 个 HTML 入口更新共享下载脚本缓存参数，画布额外更新尺寸修复脚本参数。
- 行为回归覆盖 Web/模拟桌面 × 顶层/iframe、单项/批量、服务器静默设置广播、竖图尺寸读取及 JSON 恢复、组边界、普通竖图不回归。
- 桌面原生桥与接口响应由浏览器测试模拟；未实际启动已安装桌面软件，未连接真实服务器目录执行保存。
