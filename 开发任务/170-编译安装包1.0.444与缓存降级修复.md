# 编译安装包1.0.444与缓存降级修复

状态：当前443前端已热修复；444安装包构建、验证、交付及 GitHub 正式发布全部完成，用户实际刷新反馈尚未收到。
下一步：用户刷新当前软件或下载安装444后反馈实际情况。
TODO：[x] 缓存异常不阻塞入口；[x] 新旧快照隔离与旧记录导出验证；[x] 当前443前端热修复；[x] 444完整构建及包内检查；[x] Git提交与推送；[x] GitHub正式发布。
Git：修复19a2bf2，安装包源码6f339e4，分支fix/canvas-cache-fallback；发布分支release/1.0.444；更新清单提交2ff6276。
最近验证：10类真实浏览器场景及6项相关pytest通过；缓存不可读时能进入、保存新编辑、重启不套用旧脏快照且旧记录完整导出；远端 Latest、资产大小与 SHA-256 复核通过。
阻塞：无。用户实际安装版等待刷新反馈；不影响继续构建。

## 当前安装版处理

确认D:/Program Files/SHIYIN AI运行1.0.443。对照原443安装包确认文件未被另改后，备份并替换studio-page-state.js，更新3个入口HTML缓存参数。备份位于主工作区.codex-tmp/installed-cache-hotfix-443，含补丁文件哈希。未清理IndexedDB、未修改工程、未关闭用户软件。用户需刷新整个页面加载新脚本。

## 验收

可选恢复缓存失败不能使整个工程停留在加载层；新快照使用持久化新槽位，旧记录保留可导出。444版本同步、完整构建与冻结测试通过，安装包校验后交付；不执行覆盖安装。

[CODEX_LONG_TASK_CONTINUE_V3]

从下一步继续，不重复递增444版本。

## 最终交付与验证

- PyInstaller、Tailwind、Tauri、Inno Setup完成；新增旧配置保护后重新构建桌面程序并重新封装，最终桌面exe及恢复模块与staging哈希一致。
- 包内前端注入缓存不可读场景：进入、保存、重新加载不套用旧脏记录、旧记录导出全部通过；正常脏快照恢复通过，页面错误0。
- 444冻结后端首次进入、重启和443测试数据兼容性恢复共3轮通过，301节点可见，无页面错误。此为合成工程和独立目录测试，不代表实际覆盖安装或用户故障环境已验收。
- 发现旧WebView配置自动清理会丢弃IndexedDB，增加包含IndexedDB目录即保留的规则。两个Rust函数级测试通过；完整桌面程序重新编译通过。未关闭用户实例执行桌面外壳烟雾测试。
- 安装包：E:/APP/SHIYIN-AI/dist/installer/SHIYIN-AI-Setup-1.0.444.exe；同目录.exe.sha256，115266416字节，PE版本444，NotSigned。
- SHA256：b7185275b408c992eb0dd33287f1f37a782827fa5a6528f4249f8744bdc6e0cd；交付副本校验一致。
- GitHub Release：https://github.com/luojiang419/SHIYIN-AI/releases/tag/v1.0.444；正式、非预发布并设为 Latest，EXE 与 SHA-256 校验文件均已上传。
- 构建日志、包内回归及冻结报告位于隔离工作树.build/installer-1.0.444-task。测试不读写用户工程；当前安装热修复的四个前端文件有完整备份，未删除恢复缓存。
