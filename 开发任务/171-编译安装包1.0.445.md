# 编译安装包1.0.445

状态：1.0.445 安装包构建与产物验证完成，等待 Git 交付
下一步：提交并推送版本源、发布说明和本构建记录。
TODO：[x] 版本同步；[x] 完整构建；[x] 冻结后端烟雾；[x] 安装包版本与 SHA-256 验证；[ ] Git 交付。
Git：当前分支 `fix/canvas-cache-fallback`，起点 `f5844ae`；工作区包含用户现有未提交功能，将按当前代码构建。
最近验证：九处版本源同步；PyInstaller、Tailwind、Tauri release、staging 和 Inno Setup 通过；冻结后端媒体烟雾通过；安装包 PE 版本、大小、SHA-256 与三项新功能包内代码检查通过。
阻塞：桌面外壳烟雾因用户正在运行 SHIYIN AI（PID 2740）未执行；未影响安装包生成，冻结后端已用隔离数据完成替代烟雾。

[CODEX_LONG_TASK_CONTINUE_V3]

## 目标

将当前工作区功能编译为 Windows `1.0.445` 安装包，完成版本同步、完整构建、打包态烟雾和最终文件校验。不覆盖安装，不发布 GitHub Release。

## 验收标准

- 所有正式版本来源统一为 `1.0.445`。
- PyInstaller、Tailwind、Tauri 与 Inno Setup 成功。
- staging 资源检查和打包态运行烟雾通过。
- 输出 `SHIYIN-AI-Setup-1.0.445.exe` 及 SHA-256 校验文件，并核对 PE 版本和文件大小。
- 仅提交版本文件、构建记录和必要的构建生成配置，不提交构建目录或无关用户改动。

## 构建结果

- 版本：`1.0.445`，九处版本来源同步通过。
- PyInstaller 后端冻结成功；Tailwind 构建成功；Tauri `release` 编译约 1 分 50 秒；Inno Setup 封装约 49 秒。
- staging 扫描 72 个 Web 文件并更新 18 个缓存版本；Kling 中国区与国际区均通过空 `PATH` 检查。
- 冻结后端隔离烟雾：健康启动约 2281ms，双账号隔离、上传、画布、作品预览、非法图片拒绝和 `304` 缓存通过，退出后进程停止。
- 包内确认包含 `export-depth-map`、`data-batch-adjust-depth` 及最新作品倒序渲染逻辑。
- 安装包：`E:/APP/SHIYIN-AI/dist/installer/SHIYIN-AI-Setup-1.0.445.exe`
- 大小：`115279719` 字节；PE ProductVersion/FileVersion：`1.0.445`；签名：`NotSigned`。
- SHA-256：`53fc92e02dc6f1ffbb697e3d3e6b98da8d7d20b90d00a5b7238d9ba294ce5404`；同目录校验文件已生成。
- 未覆盖安装，未创建或发布 GitHub Release。
