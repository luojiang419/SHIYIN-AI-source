# 独立局域网分发服务与控制面板

## 当前任务：重建2.0.0基准发布（2026-09-16）

状态：2.0.0全量安装包和完整热更新已构建、发布并完成下载验证，旧软件发布已清空。用户明确授权抛弃1.0.x旧引导路线。
下一步：其他用户在局域网下载2.0.0基准安装包覆盖升级；已有2.0.0客户端应用20260916175144热更新。正式客户端覆盖安装和业务体验由实际应用后验收。
TODO：[x] 构建和隔离启动验证；[x] 分发入口改为2.0.0基线；[x] 清理旧软件发布；[x] 发布及签名/下载验证；[x] Git交付（见本轮提交）。
范围：保留固定模型、模型运行时、用户数据、签名身份、设置和客户端记录。旧软件种类hot/hot-bootstrap/hot-updater/full均清理。1.0.x用户通过2.0.0全量安装包覆盖升级，不再走旧引导链。
构建来源：当前工作区a9b709a2，加已有main.py与canvas-list.js工程包导出修复；不递增正式版本。保留用户未提交源码状态，不混入本任务提交。
最近验证：分发服务/桌面/流量23项及清理保护1项通过；工程包6项通过；冻结后端首次启动和两次重启、301节点工程读取通过；真实Rust更新CLI下载和校验解包返回ok:true。branch：release/2.0.0-baseline。阻塞：无。

### 清理与发布结果

- 已移除32条hot/hot-bootstrap/hot-updater历史软件发布（含published/archived/paused），服务器此前没有full发布；删除373个独占文件，共1,627,003,557字节。540个与模型发布共用的blob完整保留，person-depth、video-depth、video-depth-runtime三条发布及fixed-models.json清理前后哈希一致，所有模型清单文件存在且大小匹配。
- 清理工具`tools/reset-distribution-software.py`默认只预览，apply要求公开服务暂停且无导入/下载；备份数据库后删除软件记录，只删除被旧软件引用且没有其他发布引用的blob，并校验删除路径。数据库备份`.build/200-baseline/distribution-before-reset.db`，源码/基线文件备份`backup/v207`。未删除用户账号、媒体、工程、签名密钥或分发设置。
- 分发中心已重新构建部署。首页改为基准安装包下载；旧迁移工具返回410，旧bootstrap/updater导入被拒绝。1.0.x的catalog返回release:null，让支持全量回退的客户端读取全量包；其他旧客户端可直接从首页下载安装。2.0.0具备package-v3/fast-extract-v1且满足最低版本时返回签名热更新，不再经过旧引导链。
- 全量包`full-2.0.0`：`dist/installer/SHIYIN-AI-Setup-2.0.0.exe`，115,523,186字节，SHA-256 `6f6d9a74a311b60eafd2334b56cf7749b5858807d2da0c959811a99cf53a4f6e`。内置热更新基线20260916174851。Authenticode状态NotSigned，与现有构建方式一致。
- 完整热更新`hot-20260916175144`：1009文件，包123,777,829字节，SHA-256 `4d4a20775161476fc4456272e0f4c7ec0dab24b125059d46946499f4a2436c2b`，最低桌面2.0.0，含当前宿主/后端/前端。
- 服务端当前仅保留上述两条软件发布，均published；下载入口`http://192.168.0.24:3011/`，全量下载`/update/files/SHIYIN-AI-Setup-2.0.0.exe`。全量通过局域网完整下载校验；公开catalog/plan使用客户端内置Ed25519公钥验证通过；编译的真实2.0.0 EXE在`.build/200-baseline/updater-check`独立目录执行prepare-hot-update，成功下载校验解包到20260916175144，未应用到正式安装。

### 验证口径与源码

全量构建使用build-installer.ps1 -SkipRuntimeSmoke（正式软件正在运行，默认桌面smoke要求全部退出）；另运行smoke-canvas-cold-packaged.py隔离后端与浏览器，首次健康2766ms，重启1172/1156ms，301节点和媒体读取通过，JS错误0。未关闭用户正式客户端、未执行实际覆盖安装，也未宣称现场冷启动问题已经完全解决。

构建包含用户指定的当前工作区main.py/canvas-list.js工程包修复；这两文件保持原有未提交状态，本任务Git只交付分发路线、维护工具、测试、基线时间戳和发布文档。完整日志、清理结果与验签报告在`.build/200-baseline/`。
[CODEX_LONG_TASK_CONTINUE_V3]

## 生成节点输出端口快捷菜单热更新（2026-09-17）

纯前端热更新 `20260917095026` 已从提交 `1131470a` 的隔离工作树发布。范围仅 `app/web` 的 155 个文件；未重新打包桌面宿主、后端或固定模型，也未纳入主工作区其他任务的未提交改动。该版本后来由补漏版 `20260917095809` 替代。

公开清单和补齐计划的 Ed25519 签名与客户端内置公钥核验通过；实际下载包 23,534,207 字节，SHA-256 `bf0880d4a9eabe228eef8699c7d26ed0aa24a5257f0084d3746112d6855fbf02` 与发布清单一致。客户端应用后的界面验收仍待进行；详细记录见 `开发任务/180-生成节点输出端口快捷菜单.md`。

截图所示 `VIDEO` 素材卡使用 `type:'image'`，前版菜单仅覆盖 `type:'video'` 生成节点。补漏提交 `b76fb3bd` 已从隔离工作树发布纯前端热更新 `20260917095809`；此版本后来由图片素材卡补漏版 `20260917100917` 替代。包 155 个文件、23,534,242 字节，SHA-256 `9c992cb56e502f6d9add985d927032adc1b2af7fdd3b1ea4bc108a38de85a909`；公开签名和实际整包下载校验通过。

截图所示 `IMAGE` 素材卡也使用 `type:'image'`，前版八项菜单仅覆盖 `type:'generator'` 图片生成控制节点。修复提交 `504ce481` 从隔离工作树发布纯前端热更新 `20260917100917`；分发中心公开目录和签名补齐计划均指向此版本，`20260917095809` 已归档。新包 155 个文件、23,534,085 字节，SHA-256 `5185c335ca363e11d93daa6a5fc03f41cdd15748160f729c3f416d47b5e6999d`；公开签名和实际整包下载校验通过。真实客户端应用后的视频与图片素材卡拖线验收仍待进行。

[CODEX_LONG_TASK_CONTINUE_V3]

以下为历史记录，旧1.0.x发布路线已被本任务替代。

状态：落后客户端自动补齐机制已开发并发布；引导 `20260915124957`、更新器修复包 `20260915125114`、完整累积包 `20260915125127` 均为published，分发中心已部署签名计划与待补齐监控
下一步：真实用户首次确认更新后观察跨重启自动执行后续步骤并在达到目标时清除 `catchup.json`
TODO：真实用户端一次确认自动补齐验收；另一台长期离线电脑恢复启动验收；跨网卡/IP与非管理员UAC分支实机验证
最近验证：Python39项、Rust15项通过；浏览器模拟已有计划自动执行check→download→apply；最终Rust CLI接受生产签名计划并完成123.70MB/1007文件；控制面板显示落后状态
branch/commit：`fix/lan-single-package-update`；本轮提交见Git最新记录，工作区其他任务改动未纳入
阻塞：暂无；完全未运行的软件无法被服务端主动唤醒，按设计在下一次启动时检测并补齐，不安装常驻系统代理

## 发布规则

用户明确要求：后续默认热更新，除非明确要求全量编译。已写入根目录 `AGENTS.md` 和 `package.json`，`desktop:build` 默认指向 `update:publish`。Windows/macOS全量构建工作流已取消push自动触发，只允许手动勾选full_build运行。此前分发中心阶段未构建或发布全量安装包；本轮用户明确要求1.0.447全量基线安装包，执行结果见文末。

默认命令：`npm run update:publish`；带说明时使用 `python tools/build-hot-update.py --publish --notes "更新说明"`。构建脚本按源码指纹决定是否重编 Rust 宿主/Python sidecar；热更新文件逐项校验，客户端跳过相同哈希文件。只有纯前端改动才使用 `--web-only`。

## 已部署结果

2026-09-14 固定模型发布确认：用户要求深度视频和深度图模型作为固定资源。核对发现两项已经发布，保留现有不可变发布副本，不重复导入或生成新版本。

| 固定资源 | 模型版本 | 发布序号 | 文件数 | 字节数 |
| --- | --- | --- | --- | --- |
| 深度图（人物深度组件） | 1.0.0 | 20260914153204 | 6106 | 5982828973 |
| 深度视频模型 | 1.0.0 | 20260914153334 | 2 | 458247408 |

本机源文件与分发仓库文件全部逐项通过 SHA-256 和大小核对；两项模型的实际文件 Range 下载返回206且字节内容匹配。固定版本记录保存至 `D:\SHIYIN-Distribution\fixed-models.json`，核验副本在 `.build/175/fixed-models-report.json`。根目录AGENTS.md已明确：不随软件热更新重新打包/发布，只有用户明确要求替换模型时变更。当前固定约定为发布策略与版本记录，不是禁止管理员手动操作的界面锁。

- 独立控制面板：`D:\Program Files\SHIYIN Distribution Center\SHIYIN-Distribution-Center.exe`。
- 桌面快捷方式：`SHIYIN 分发中心.lnk`；登录自启动现在使用 HKCU Run 的 `SHIYINDistributionCenter` 项，运行 `--startup`；旧Startup快捷方式已停用备份。
- 服务数据：`D:\SHIYIN-Distribution`。SQLite 保存发布、客户端和日志；blobs 按 SHA-256 去重。签名私钥/admin-token 只存本机，不进入 Git 或更新包。
- 下载入口：`http://192.168.0.24:3011/`。迁移工具：`http://192.168.0.24:3011/SHIYIN-Hot-Update.exe`（约6MB）。
- 管理 API：`127.0.0.1:3013`，Bearer token；桌面控制面板自动认证。对外接口不能调用管理 API。
- 防火墙：Private/LocalSubnet 范围的 TCP 3011、UDP 3012。原内置服务通过安装版设置 API 停用，客户端源码已移除服务生命周期与管理操作。
- 已发布热更新：`20260914154010`；本机 `D:\Program Files\SHIYIN AI` 已应用并自动重启到此序号，桌面基线仍为 `1.0.446`。
- 人物深度组件与深度视频模型已从本机已安装目录实际导入独立仓库；不依赖画布软件运行。

## 实现范围与技术选择

控制面板采用 HTML/CSS/JS + pywebview/WebView2 独立 EXE，后端为同一 EXE 的 `--service` 后台进程。关闭 GUI 不终止分发。当前通过可选的 Windows 登录自启动运行，没有注册 SCM Windows Service，也没有实现登录前运行。

服务端同时广播 mDNS 和 UDP；当前 Windows 客户端使用内置/缓存地址优先、连接失败后 UDP 发现，发现结果必须匹配内置公钥。mDNS 广播用于网络可发现性，客户端专门的 mDNS 解析与多网卡无感切换尚未实机验收。初始下载地址内置当前管理电脑 IP，普通客户端不显示服务器监听地址。

热更新采用 Ed25519 签名清单 + 逐文件 SHA-256；白名单为 `app/web`、`app/backend/canvas-backend`、`app/skills` 和精确的 `SHIYIN AI.exe`。只在用户确认后下载，通过独立 helper 等待旧进程退出、记录替换日志、备份并应用，然后自动启动新版。新进程的后端与窗口就绪后写 ready 标记，90 秒未就绪则恢复旧版；异常中断日志在下次启动时先恢复。该 ready 标记不代表所有业务功能端到端自检。

普通安装目录不可写时 helper 通过系统 UAC 请求管理员权限；取消时主程序保持运行。此分支源码已接入，本机管理员环境没有覆盖非管理员 UAC 实测。

默认不悄悄回退到 GitHub 全量安装包。管理员手动导入的局域网全量包继续沿用既有安装器流程。模型沿用组件清单/Range/逐文件哈希及现有 worker 激活验证，当前不宣称模型下载清单采用客户端强制签名验证。

历史发布允许暂停/恢复；重新指向旧热更新不会让已安装更高序号的客户端降级。要回退业务内容，应将旧内容作为新的热更新序号发布。

## 同事首次接入

1. 保存并退出 SHIYIN AI。
2. 在同一局域网打开下载入口，运行 `SHIYIN-Hot-Update.exe`，选择包含 `SHIYIN AI.exe` 的安装目录。
3. 工具下载受信任的签名热更新，启动独立更新器并自动重启软件。
4. 后续直接在软件内收到热更新提示；点击“立即更新”后完成下载、安装和重启。暂缓不会下载或安装。

旧更新器不认识签名协议，因此旧 `/hot-update/manifest.json` 返回 409，避免静默应用新快照。本机已通过相同签名下载/独立会话完成首次迁移，不需要全量安装器。

## 验证记录

- `python -m pytest tests/test_distribution_service.py tests/test_person_depth_lan.py tests/test_person_depth_components.py tests/test_desktop_updater_contract.py -q`：48 passed。
- `cargo test --manifest-path src-tauri/Cargo.toml updater:: --lib`：11 passed。包含文件替换、清理、回滚、中断日志恢复和不受信任路径/清单拒绝。
- 实际 Rust CLI 从独立服务器下载并验证签名清单，独立更新窗口完成隔离副本更新，后端启动在3195，ready与applied写入成功，用户数据标记保留。
- 本机安装版通过独立更新器应用 `20260914154010`，后端3000与分发服务3011健康检查均200；客户端设置API返回 `role=client, connected=true`，服务器开关和监听地址已从实际安装版HTML移除。
- 迁移工具实际Range响应206，文件头MZ；目录下载入口200。
- 真实控制面板浏览器验收：停止→状态已停止；启动→状态运行中；真实资源列表、导入窗口、设置保存正常，检查期间无控制面板JS错误。
- `tests/support/desktop_hot_updater_ui.html` 引用真实前端更新脚本、原生命令使用测试桩：检查后只调用check；稍后提醒不下载；点击立即更新后按download_update→apply_downloaded_update顺序执行。
- JS/Python语法检查、diff检查通过。构建期间ONNX可选GPU库提示由现有CPU-only spec过滤，不影响后端启动。
- 本地报告：`.build/175/smoke-report.json`、`.build/175/installed-report.json`；实际更新日志保存在安装目录 `data/update/updater.log`。

## 主要文件

`distribution/` 包含独立服务、GUI与运行说明；`src-tauri/src/hot_updater.rs` 负责签名热更新；`updater.rs/lib.rs` 接入原有更新命令和启动恢复；`canvas_core/distribution_client.py` 负责组件源发现与连接状态；`main.py` 移除内置服务；`static/js/desktop-updater.js` 提供统一提示和确认后下载；`tools/build-hot-update.py` 为默认发布流水线。

构建与部署中心：`tools/build-distribution-center.ps1`、`tools/deploy-distribution-center.ps1`。每次控制面板构建输出至不同版本目录，避免覆盖正在运行的EXE。

本任务开始前已有大量Lookbook/深度视频改动。必要文件已备份在 `backup/v175-lan-distribution`。Git只提交本任务内容，`main.py`按分发相关hunk暂存，其他任务的未提交改动保持原样。前一轮原型保留在 `static/prototypes/lan-control-center.html`，实际运行使用 `distribution/panel.html`。

## 控制面板外观与后台增强（2026-09-14）

- `theme` 支持system/dark/light，默认跟随系统；页面CSS与Windows原生标题栏同步。原生窗口使用64位HWND与DWM属性20（旧系统回退19），每秒检查Windows应用主题，不修改系统主题。页面监听prefers-color-scheme变更。
- `background_on_close` 控制关闭窗口是否收至系统托盘；顶部“后台运行”和托盘菜单可隐藏/恢复面板。关闭此选项时退出面板，独立下载服务仍保留，可用服务启停控制下载。
- `launch_at_login` 控制当前用户登录自启动，实际设置/移除HKCU Run；`start_hidden` 控制登录启动时是否隐藏到托盘。部署脚本尊重已保存开关，不再每次强制开启。
- 迁移旧的正常/乱码Startup快捷方式到 `.lnk.disabled`，避免自启动关不掉或重复运行。Windows PowerShell部署脚本改用UTF-8 BOM，修复中文快捷方式乱码。
- 控制面板增加单实例保护；重复启动会通过本机认证管理API恢复已有窗口。托盘无法初始化时保留可见窗口，不隐藏成无法恢复的进程。
- 新增 `distribution/desktop_settings.py`，修改 launcher/service/panel 和部署脚本，增加 `tests/test_distribution_desktop.py`。只升级分发中心组件，未编译SHIYIN AI全量安装包，也未重新发布固定模型。
- 验证：`pytest tests/test_distribution_desktop.py tests/test_distribution_service.py -q` 为13 passed；JS/Python语法和diff检查通过。
- 桌面实测：浅色DWM读取0、深色读取1；`--startup`启动隐藏且托盘存在；显示/关闭收至托盘正常，后台进程保持；关闭托盘选项后关闭面板会退出；自启动实际关闭/开启均核对系统状态；下载健康接口持续200。
- 本机报告：`.build/175/desktop-verification.json`。当前保留theme=dark、background_on_close=true、launch_at_login=true、start_hidden=true。系统主题变化的选择逻辑有测试，未为测试改变用户Windows系统主题或重启电脑。

[CODEX_LONG_TASK_CONTINUE_V3]

## 下载监控补齐（2026-09-14 已部署）

原型的下载数、实时速度、今日流量与峰值均为示例，正式版此前未实现。此次基于服务器实际成功写出的文件响应字节采集，覆盖热更新、固定模型、全量包下载和迁移工具。按 IP 聚合客户端，显示当前文件/Range 请求进度与发送速度，不将文件进度冒充整次更新或安装进度。每日流量按服务器本地日期保存至 SQLite，重启保留；最近速度曲线及请求记录为进程内数据。客户端状态区分正在下载、近期连接与暂无活动，不宣称已实现客户端心跳。

验收：并发下载/Range/中断统计准确、停止后速度归零、跨日与重启保留每日统计、管理接口保持认证隔离、正式面板实际运行验证、仅构建部署独立分发中心。固定模型与软件热更新发布内容不变。

- 新增 `distribution/traffic.py`：线程安全采集成功socket发送字节，近3秒平均速度、1秒峰值、60秒曲线、活动请求及最近40条完成/中断记录；SQLite `traffic_daily` 按本地日期/IP累计。统计保存异常保留待写数据并重试，不中断文件传输；正常传输约每秒落盘并在请求结束刷新，异常进程退出可能丢失最后约1秒未落盘数据，存储故障期间除外。
- `service.py` 覆盖全部文件下载入口，记录版本时不被无版本的下载请求覆盖。热更新blob通过缓存清单显示文件名与版本。没有新增客户端心跳或整包安装回执。
- `panel.html/js/css` 增加真实下载数、下载客户端数、总速度、今日流量/峰值、曲线、文件进度及每IP汇总；每秒刷新，连接失败提示显示的是上次数据。当前请求进度不表示客户端已接收/校验/安装。
- 部署脚本在存在活动下载时拒绝部署，防止打断下载。已构建至 `dist/distribution-builds/20260914161646` 并部署既有安装目录；没有全量安装包构建，没有重新发布模型或热更新。
- `python -m pytest tests/test_distribution_traffic.py tests/test_distribution_service.py tests/test_distribution_desktop.py -q`：18 passed。包括16路Range请求累计、RST中断清理、跨日与重新实例化恢复、存储故障恢复、公共接口隔离；JS语法和diff检查通过。
- 真实已部署服务：读取深度视频模型前32MB，逐字节摘要与本地发布blob核对一致；期间界面可见1个下载、非零速度、63.3%/85.9%进度，结束为0 B/s与32MB；峰值约5.8MB/s。SQLite只读核对已持久化33554432字节。该测试流量保留在今日统计中；升级前没有历史采集，无法补算旧流量。
- 实测证据 `.build/175/traffic-verification.json`、`.build/175/traffic-persistence-verification.json`；浏览器总览与客户端截图已人工检查、无脚本错误。额外的已部署服务进程重启验证及恢复托盘启动命令被自动审批以“blocked by policy”拦截，未给出具体原因，未执行；不影响已部署服务。需要打开桌面面板时由用户点击现有快捷方式。

## 正式面板视觉还原（2026-09-14 已部署）

用户指出正式版明显粗糙。以 `static/prototypes/lan-control-center.html` 为唯一视觉基准，复用232px侧栏、品牌标识、Lucide图标、68px顶栏、四列指标、发布表格与活动双栏、资源行及客户端卡片。保留真实监控与服务设置，不引入示例设备或示例统计。验收为同尺寸截图对照、真实接口操作、深浅主题及窄窗口检查，部署独立面板并提交。

- 直接复用Demo的配色、字号、232px侧栏、品牌与34px图标容器、68px顶栏、四列指标、双栏发布表格与最近活动。恢复资源图标行、客户端信息网格、开关设置、导入弹窗，补充浅色主题。使用本地Lucide源图标子集，离线可用。
- 保留真实数据和原管理接口。总览新增的流量曲线、活动请求与最近记录放到发布区域下方；客户端保留速度、峰值、今日流量与请求进度。原型演示的版本/设备/假流量没有带入正式版。成功连接提示移到侧栏，操作结果使用短暂通知，连接故障保持明显提示。
- 已将 `distribution/panel.html`、`panel.css`、`panel.js` 部署至安装目录 `_internal/distribution`。仅更新界面资源，下载服务进程持续运行，无全量安装包构建、无固定模型发布。部署前的三个文件保留在 `.build/175/panel-before-visual/`。
- 浏览器实测：总览与Demo同尺寸拼接对照、资源列表、客户端、深浅主题切换并恢复dark、导入窗口打开/关闭通过。820×600检查发现四列指标过挤，修复为两列并复查通过；JS语法与diff检查通过。截图见 `.build/175/visual-comparison.png`、`visual-overview.png`、`visual-820.png`。
- 桌面核对发现已有WebView仍缓存旧页面，已通过现有desktop-action close关闭面板（分发服务保持运行）。重新打开阶段用户按Esc停止Computer Use，未继续桌面操作；用户需要从现有快捷方式重开。原生截图接口另有 `SetIsBorderRequired / 0x80004002` 兼容故障，未宣称原生窗口视觉验收完成。

## 1.0.447局域网更新基线安装包（已完成）

用户明确要求全量编译。使用当前工作区源码构建，保护既有未提交业务修改。内置独立分发地址与受信公钥，新增编译基线序号，避免新装或覆盖安装后应用旧热更新。构建入口build-installer.ps1，正式版本递增至1.0.447。现有SHIYIN AI正在运行，使用隔离验证而不关闭用户软件；固定模型不重新发布。

- 版本统一为1.0.447（9处版本源核对通过）；编译内置基线序号20260914164209。新增 `src-tauri/distribution-baseline.txt`，更新检查、待安装包验证和实际应用阶段均拒绝不晚于已安装基线的热更新。覆盖安装保留更高的已应用序号。全量构建生成基线序号，热更新构建指纹包含此文件。
- 内置地址与两个客户端公钥已同实际分发中心核对，服务器20260914154010清单签名验证通过。新编译EXE在隔离目录忽略此旧序号；通过仅绑定本机的临时测试服务器下载合法签名新序号20260914164210并核对SHA-256，无效签名被拒绝，没有变更生产发布目录。
- 自动化：`pytest tests/test_desktop_updater_contract.py tests/test_distribution_service.py -q` 29 passed；`cargo test --manifest-path src-tauri/Cargo.toml updater:: --lib` 12 passed。报告 `.build/175/baseline-update-verification/report.json`。
- 打包后端使用独立端口、数据目录启动，health返回1.0.447，首页更新脚本存在，分发连接返回role=client、connected=true；测试进程通过关闭接口退出。报告 `.build/175/baseline-backend-data-v2/report.json`。初次验证脚本日志放置位置与请求路径不合适，修正验证脚本后通过，无产品源码回归。
- 正常桌面smoke要求关闭所有已有SHIYIN AI实例，本轮使用SkipRuntimeSmoke并执行上述隔离后端与原生更新CLI验证；没有替换正在运行的用户安装，没有执行会覆盖卸载注册信息的静默安装测试。另一台全新电脑上的完整安装/更新重启与非管理员UAC分支仍待实机验收。

- 安装包：`dist/installer/SHIYIN-AI-Setup-1.0.447.exe`，1743614824字节（约1.62GiB），SHA-256：`e7a844ac4bdea6f8a1c81dafb09c930ddef1e98cb8dd835fd1e8c0acc53805e4`。校验文件及中文安装说明已放同目录。ISCC完整构建退出0、版本/大小/签名状态门禁通过，Authenticode为NotSigned（未做Windows代码签名）。7-Zip不支持读取此Inno安装包，未将其报错视为解包验证通过。没有执行实际覆盖安装。
- 完整日志 `.build/175/baseline-build.log`；产物检查 `.build/175/baseline-artifact.json`。构建含当前工作区已有业务变更，Git仅提交本次更新器保护、版本元数据与记录；main.py仅暂存版本行，其他业务改动保留。未向分发中心导入全量包，也未修改当前热更新发布。


## 2026-09-15 Lookbook 默认时尚流程热更新

- 发布序号：20260915110530，已发布到独立分发中心；最低桌面基线1.0.447。本机已检测为1.0.447。
- 来源：隔离Git工作副本，仅包含Lookbook修复及必要的原基线代码块/视频接线恢复，不含其他任务的新参数与灯箱改动。
- 热更新1005文件、275111688字节；逐文件SHA-256/大小、Ed25519签名、局域网入口目录及前后端Range下载均已核验。
- 干净源码194项测试通过；打包后端健康/风格接口200。两项固定模型发布记录和fixed-models.json哈希前后一致，未重新导入模型。
- 未构建全量安装包，未强制关闭或重启用户软件；客户端按现有确认更新流程应用。
- 构建与发布证据：.codex-artifacts/lookbook-packaged-smoke/verification.json、publication.json；快照位于.codex-artifacts/lookbook-publish-20260915-1029/dist/hot-update/20260915110530。

## 2026-09-15 单包增量更新重构

用户软件持续出现 `下载失败：io: Peer disconnected`。现场确认独立服务进程曾异常退出但托盘面板仍存活，两台客户端各收到约1.36MB后中断；恢复服务后双路各1005文件、275111688字节的旧清单压力下载虽能完成，但耗时623秒并产生2010次文件请求，散文件形态仍会放大短时断线风险。故障散文件发布 `20260915114558` 已暂停。

- 默认热更新升级为协议v3：构建单个 `.shiyin-update` 数据包，内部ZIP压缩；签名清单包含包名、包大小、包SHA-256及完整目标文件列表。没有使用自解压EXE，避免新增SmartScreen、杀毒软件、UAC和逐版本代码签名风险。
- 客户端发送 `X-Shiyin-Capabilities: package-v3`，对整包执行最多5次自动重试、Range断点续传与SHA-256校验；解包拒绝目录、重复/越界/清单外路径，逐文件核对大小和SHA-256后才保存pending，应用阶段沿用备份、事务替换、ready确认与失败回滚。
- 服务端同时维护 `hot-bootstrap` 和 `hot` 发布线：无能力头的旧客户端只收到协议v2单EXE引导；新版客户端只收到协议v3正式包。服务端导入v3时先核对整包及ZIP内全部文件，公共blob仍支持Range。
- 分发面板连续3次无法访问服务后自动拉起后台服务，15秒限频；主服务异常退出会写 `service-error.json`。实机精确终止服务PID后6秒内3011/3013恢复，局域网health返回协议3。
- 已发布引导 `hot-bootstrap-20260915121422`：6413824字节，仅 `SHIYIN AI.exe`。安装目录中的真实旧版EXE在隔离根目录下载成功并返回 `ok:true`。
- 已发布正式包 `hot-20260915121532`：1007文件，展开275223214字节，单包123693410字节，压缩率44.9%。Ed25519签名、1MB Range响应与完整隔离下载/安全解包通过；预置10MB残片后服务端记录offset=10485760，仅发送剩余113207650字节并完成。
- 自动化：`python -m pytest tests/test_desktop_updater_contract.py tests/test_distribution_service.py tests/test_distribution_desktop.py tests/test_distribution_traffic.py -q` 为39 passed；`cargo test --manifest-path src-tauri/Cargo.toml updater:: --lib` 为14 passed。未构建全量安装包，固定模型未变更。
- 验证目录：`.build/175/legacy-bootstrap-20260915121422`、`.build/175/package-resume-20260915121532`。用户端需先完成引导重启，再接受第二次正式增量更新提示。

### 下载完成后长时间处理修复

用户截图显示按钮停在“正在下载并验证更新”。服务端记录实际在0.93秒内完成123693410字节发送；客户端安装目录现场检查在约2分钟后只解出463/1007个文件，最终约3分钟才完成并于12:23:57自动重启。根因是v3首版无条件解出全部文件，且每个小文件都调用 `sync_all` 强制刷盘，在Program Files及端点安全扫描环境下被显著放大。

- 新更新器先对比安装目录文件大小与SHA-256，内容相同则不写暂存；变化文件完成哈希校验后直接关闭文件，不逐文件强制刷盘。pending仍只在全包处理完成后写入，异常中断不会进入应用阶段。
- 弹窗按钮显示“正在处理…N秒”，正文明确包含下载、解包和本地校验，避免网络已完成后界面看似失去响应。
- 新增 `--updater-only` 小包模式，只包含桌面EXE、更新弹窗脚本和15个HTML缓存入口，无backend、无prune。正式小包 `hot-updater-20260915123215` 为17文件、2772750字节。
- 为防止小包版本序号让未安装业务包的客户端跳过完整更新，目录改为三阶段能力路由：无能力头→`hot-bootstrap-20260915123104`；仅 `package-v3`→更新器小包；同时有 `fast-extract-v1`→`hot-20260915123226` 完整包。
- 最终完整包1007文件、123693907字节。真实旧v3 EXE隔离获取小包成功，最终快速EXE隔离完成完整包下载与处理；两者均在24秒检查点前返回 `ok:true`。原始客户端此前同类处理约3分钟。
- 本轮仍未构建全量安装包，固定模型未变更。验证目录：`.build/175/slow-v3-20260915123215`、`.build/175/fast-v3-20260915123226`。

## 2026-09-15 落后客户端自动补齐

目标是客户端错过一个或多个发布后，在下次启动时可靠补到最新，而不是依赖用户逐次打开软件和逐版确认。不增加软件关闭期间运行的Windows常驻代理；完全离线或关机设备无法被服务器主动更新。

- 分发目录在原发布清单之外增加独立签名的 `plan_payload/plan_signature`，内容为协议版本和当前最终完整业务包序号。客户端使用同一内置公钥验证，拒绝无效签名、非法序号或早于当前步骤的目标；`X-Shiyin-Plan-Target` 仅供诊断，不参与信任决策。
- 用户首次点击立即更新时，客户端创建 `data/update/catchup.json`，保存最终目标和最近步骤。下载失败、应用回滚、软件退出或电脑重启后计划仍保留；`applied_release >= target_version` 后自动删除。后续目标升高时可扩展计划，不能被较旧目标覆盖。
- 前端检查结果包含 `continuation/planTarget`。已有计划时隐藏关闭与稍后按钮，自动执行下载和应用；错误后恢复关闭与重试入口。automatic策略按原逻辑检查，manual策略仅在已有计划时启动续跑，disabled明确暂停。
- 发布保持三阶段兼容：无能力头→`hot-bootstrap-20260915124957`（1文件）；旧v3→`hot-updater-20260915125114`（17文件，2776862字节）；`fast-extract-v1`→最新累积包 `hot-20260915125127`（1007文件，123698004字节）。三条目录响应的签名目标均为 `20260915125127`。
- 最新 `hot` 是累积快照，落后客户端具备快速更新器后直接比较本地哈希并补齐变化文件，不顺序安装已归档业务版本。未来引入新能力时可增加迁移发布线，持久化目标会跨重启继续。
- 分发中心客户端状态新增 `target_version/update_state`，从 `X-Shiyin-Version` 上报的已应用序号判断 `current/outdated/unknown`，正式面板显示“已是最新/待补齐/未报告”。此状态用于监控，不替代客户端签名校验。
- 验证：Python更新/分发39项通过；Rust更新器15项通过，新增计划创建、目标不降级、达到目标清理及非法计划拒绝；本机Edge测试普通manual不自动安装，continuation模式自动调用 `check_for_update → download_update → apply_downloaded_update`；最终Rust CLI验证生产计划签名并完成隔离下载。
- 未构建全量安装包，未重新发布固定模型。验证目录 `.build/175/catchup-plan-20260915125127`。

## 2026-09-16 统一画布渲染链路热更新

任务 178 从提交 6e4aa56 的独立干净工作区发布纯前端更新 `20260916163603`，155 文件、23,534,010 字节。分发中心原未运行，已通过已部署 EXE 的 --startup 启动后导入；状态 published，公开目录 Ed25519 签名校验通过。未混入其他任务未提交改动，未更新宿主/后端或固定模型，未构建全量安装器。客户端尚需应用；详细验证与包哈希记录在任务 178。

## 2026-09-16 冷启动画布交互修复热更新

任务182按用户要求，从源码2bed94f3独立工作树发布纯前端更新`20260916174158`，155文件、23,532,828字节，状态published。移除全工作台后台预加载造成的画布主线程阻塞，保留已访问页面状态和未同步编辑保护。公开catalog及更新计划签名、局域网整包下载及逐文件哈希验证通过；prune_roots仅app/web，fixed-models.json哈希不变。未包含其他任务未提交改动，未构建全量安装器。客户端尚需应用；包哈希和验证记录见任务182。

## 2026-09-16 画布缓存阻塞修复发布

任务182（`开发任务/182-旧画布启动缓存阻塞排查与修复.md`）纯前端热更新 `20260916171750` 已发布，源码 `d2bf610`。155文件，仅app/web，23,534,411字节；公开catalog签名验证通过，管理端published且target_version一致。正式客户端尚未应用本次修复，820-GP更新后体验待验收。固定模型保持不变。

## 设备信息分组列表（2026-09-16）

已完成并部署：BUG反馈设备信息由JSON文本改为系统、CPU、内存、磁盘、显示适配器及NVIDIA显卡分组字段列表，中文标签、容量换算、数量标记和空状态；防止快速切换客户端时旧请求覆盖当前设备。变更文件为distribution/panel.html、panel.css、panel.js，已同步既有安装目录并核对SHA-256一致。已打开面板需刷新页面后显示。
验证：node语法检查通过；Edge无头实际渲染检查15个字段、容量、空数据和600px浅色布局通过，无JS异常；截图.build/hardware-list-preview.png已检查。未重启下载服务。当前分支release/2.0.0-baseline，仅提交本次文件；保留其他任务工作区改动。

## 客户端真实用户名与去重（2026-09-16，已部署并发布）

目标：客户端上报计算机登录用户名与计算机名；分发中心按计算机用户合并历史客户端ID，仅显示最近IP和在线状态，保留历史日志及设备查看。旧客户端未上报真实名称时明确提示，不把随机ID当用户名。
已完成：客户端身份上报、服务端兼容存储、列表聚合；分发中心重新构建部署至既有安装目录。Python 19项测试通过，Edge验证重装ID、IP变化、不同电脑同名用户、应用账号重复、旧数据及HTML安全渲染；安装版实际28条设备历史合并为3条。旧记录按IP归并，只有该IP已知身份唯一时并入对应用户；真实用户名必须等客户端上报，不根据IP编造。保护main.py等其他任务未提交改动，客户端从隔离工作树构建。
当前分支release/2.0.0-baseline；[CODEX_LONG_TASK_CONTINUE_V3]

客户端热更新20260916181620从adf7aad7构建发布，1007文件、123754229字节，SHA-256 834cfa2db8f3f30e85d6e9dc109a0b10add9bf80b7fa2d392b3471b32f92a381；签名清单/更新计划和局域网整包下载校验通过，fixed-models.json哈希未变。未构建全量安装包。编译后端隔离运行health=ok，真实上报jiang / DESKTOP-G7KA5NK / 192.168.0.24成功；未替换或重启正式客户端，其他电脑需应用更新后上报用户名。验证文件.build/client-identity-published.json、.build/client-identity-smoke-data/verification.json和.build/client-identity-installed.png。
初次隔离运行因验证脚本将打开的日志放入迁移目录导致WinError32；日志移至数据目录外后通过，无产品修改。当前实现提交adf7aad7已推送origin/release/2.0.0-baseline；下一步为客户端应用已发布热更新。
