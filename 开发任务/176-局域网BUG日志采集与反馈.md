# 局域网 BUG 日志采集与反馈

状态：设备信息采集、服务端存储和 BUG 反馈展示已实现并部署；客户端热更新 `20260915150624` 已发布
下一步：观察真实用户升级后的账号心跳和机器快照；复现深度视频错误并核对显卡架构/驱动信息
TODO：真实用户设备升级及报错验收；Git精确拆分（共享文件有其他任务未提交改动）
最近验证：定向 pytest 21 passed；`node --check distribution/panel.js` 和 `py_compile` 通过；本机采集实际返回 CPU/内存/磁盘/显示适配器/NVIDIA 显卡；服务端模拟快照落盘/管理接口/页面验收通过；客户端采集器实际自动上报并服务端接收到 GPU 驱动与计算能力；分发目录目标 `20260915150624` / 1005文件
branch/commit：`fix/lan-single-package-update`；未提交
阻塞：无发布阻塞；真实用户设备尚未完成更新，因此不能宣称其硬件或报错已采集。此前 GUI sidecar 独立启动检查受自动审批拦截，控制台同等打包入口已运行通过

[CODEX_LONG_TASK_CONTINUE_V3]

## 目标与验收

- 分发中心从客户端接收受限大小的运行日志与错误报告，按客户端稳定标识分目录保存，拒绝路径越界。
- 本机控制面板左侧有“BUG反馈”，可实时查看在线客户端和最近错误，按用户查看日志详情。
- 客户端周期性发送运行状态；深度视频任务失败自动发送异常与必要的 CUDA 环境信息，网络暂断时保留本地日志并重试。
- 实际验证接口、页面和客户端上报后，部署分发中心并发布局域网热更新；不变更固定模型资源。

## 本次发布

- 服务端：`D:\Program Files\SHIYIN Distribution Center` 已部署；`D:\SHIYIN-Distribution\bug-logs\账号ID\客户端ID` 保存 JSON 报告。
- 客户端：从隔离工作树 `E:\APP\SHIYIN-AI-bug-release-176` 构建，使用当前生产安装目录的前端，避免带入主工作区其他未提交前端改动；修正版 `20260915145804` 已 published，取代初版 `20260915144916`。修复后台线程读取未写完队列文件的竞态。
- 客户端按一分钟心跳推送运行状态；后端 WARNING/ERROR 和深度视频失败上报。失败包含 worker stderr、PyTorch/CUDA 版本（worker 可返回时）及 `nvidia-smi` 设备信息。网络断开时本地 `bug-reports/pending` 重试。
- 固定深度模型资源未改动。真实用户升级与其显卡架构尚未得到实机数据。

## 设备信息扩展

用户追加要求：BUG 反馈页面直接查看用户机器详细信息，特别是硬件与显卡驱动，供错误分析。客户端启动及每30分钟采集系统、CPU、内存、磁盘、显示适配器、NVIDIA 驱动/显存/计算能力；每分钟已登录账号心跳携带缓存的机器快照。服务端按账号和客户端保存最新 `device.json`，提供仅本机管理接口读取；页面选择客户端后展示完整 JSON。避免采集序列号、MAC、用户目录和密钥。服务端已重新部署，客户端 `20260915150624` 已发布并通过本机实际自动上报验收。
