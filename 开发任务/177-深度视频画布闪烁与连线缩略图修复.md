# 深度视频画布闪烁与连线缩略图修复

状态：修复与局域网热更新已完成。
下一步：客户端接受 `20260915154430` 后，在真实深度视频任务上观察长时间播放与连接预览；本次代码和发布无需继续操作。
TODO：实现、专项回归、页面检查、发布核验、Git 提交与推送均完成。
最近验证：浏览器连续状态更新保持两路视频元素、75%进度和1项视频缩略图；专项 Python 7 项通过；JS 语法与 diff 检查通过。
branch/commit：`fix/lan-single-package-update` / `0d51413`，已推送 `origin`。
阻塞：无。

## 目标与验收

1. 深度视频任务进度轮询不重建输入/输出 `<video>`，播放画面和状态稳定。
2. 深度视频连到视频生成节点后，视频缩略图保持可见，连线和生成输入一致。
3. 发布只含本次 Web 修复及已部署基线，不发布其他任务的未提交工作区改动；固定模型不变。

## 结果与发布

- `static/js/canvas.js` 对深度视频任务状态采用原 DOM 局部更新；媒体 URL 或手动输入来源变化才替换节点。视频生成节点输入视图保留图片、视频、音频引用。
- `static/js/canvas-special-nodes.js` 为深度视频模板增加媒体 URL 与进度标记，供局部更新判断。
- Playwright 在隔离画布页面验证连续进度 25→50→75 后两路视频元素身份不变、状态/进度正确、缩略图与连接各 1 项；同一检查针对待发布快照也通过。
- 最新已发布基线 `20260915150624` 的整包 Web 文件逐项提取并校验，叠加本次修复前后只有上述两个脚本的哈希变化。
- `20260915154430` 已作为 `hot` 在独立分发中心发布；清单 `prune_roots=['app/web']`，154 文件，包大小 23475765 字节。没有编译全量安装包、backend/桌面宿主或重新发布固定模型。
- `pytest tests/test_canvas_depth_video.py tests/test_depth_video_controls.py -q`：7 passed。扩展布局测试 10 passed、1 failed；失败断言要求历史 `batchGenerator` 节点常量，现有代码在本次修改前已不满足。
- 浏览器专项使用静态测试页和模拟工程 API；真实用户 GPU 深度视频任务的长时间 WebView2 验收仍需客户端接受更新后观察，不冒充已实测。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

恢复时先读本文件、核对 Git 状态，再从“下一步”继续。
