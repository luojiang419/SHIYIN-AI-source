# 直接移植新无限画布引擎

状态：新引擎节点资源加载卡住的 P0 止血已完成
下一步：在干净工作区或明确打包范围后发布网页热更新，并在客户端复测 GP01、820-GP 首屏媒体是否从“资源加载中”转为真实像素
TODO：授权原文归档位置；安装环境热更新验收；发布不包含无关 `static/js/canvas-list.js` 改动的热更新包
最近验证：`node --check static/js/canvas.js` 通过；`python -m pytest -q tests/test_canvas_initial_load_performance.py tests/test_canvas_media_queue_runtime.py` 18 passed；`git diff --check -- static/js/canvas.js static/canvas.html tests/test_canvas_initial_load_performance.py` 通过。未发布热更新，因为当前工作区还有无关 `static/js/canvas-list.js`、`main.py` 和多项删除改动，`tools/build-hot-update.py` 会复制整个 `static/`。
branch/commit：`feat/infinite-canvas-engine-migration` / `26a5ebb fix: wake canvas media queue for upstream nodes`；已推送 `origin/feat/infinite-canvas-engine-migration`
阻塞：用户于 2026-09-16 本次会话确认已取得允许保留现有许可并通过局域网热更新分发的上游书面授权，原文位置尚未记录。当前工作区存在其他未提交改动，不能直接用默认热更新脚本发布本轮修复，否则可能混入无关静态资源变化。

## 2026-09-16 新引擎资源加载卡住修复

用户现场截图显示进入画布已经很快，但所有图片节点长期停留在“资源加载中”。根因锁定在新引擎动态挂载/卸载节点后，旧媒体空间索引没有失效，媒体队列没有被立即唤醒；节点 DOM 已经出现并被 `CanvasResourceReady.monitor()` 标记为 pending，但队列候选仍可能来自旧索引，导致可见媒体不启动加载。

本轮修改 `static/js/canvas.js` 的 `CanvasEngineBridge.onNodeMount/onNodeUnmount`：节点挂载和卸载时将 `classicMediaSpatialGridEpoch` 置为失效并调用 `scheduleClassicMediaQueue()`，让新进入视口或重新挂载的节点立刻进入媒体队列。`static/canvas.html` 同步给 `canvas.js` 增加 `resource-loading=20260916.1` 缓存标记，避免 WebView 沿用旧脚本。新增 `tests/test_canvas_initial_load_performance.py::test_upstream_node_mount_wakes_media_queue` 锁定新引擎挂载生命周期必须唤醒媒体队列。

## 2026-09-16 首屏媒体稳定缓存

本阶段给本地媒体缩略图 URL 增加由源 URL 派生的稳定 `rev`，继续复用已有的账号隔离 Service Worker Cache Storage。编辑器驻留被回收后重新创建 iframe 时，不再向后端重复读取已缓存缩略图。媒体队列按媒体元素的屏幕矩形排序，进入阶段只启动实际可见项；节点可见但节点内部滚动区外的媒体不再一次性抢占连接。

一键复刻的目标图、服装参考和内部控制图，以及影视节点的资产映射和深度预览已接入统一缩略图函数。它们现在受总并发 6、视频并发 2、驻留 72 项和 32M 像素预算约束。本地预览 404 后不再回退请求必然同样缺失的原文件；远程资源仍保留原有回退路径。

验证使用隔离数据库，并把隔离媒体目录只读映射到正式数据目录；所有画布写入仍落在隔离库。测量仅统计编辑器 iframe 发起的媒体请求，排除了主工作台中素材、电商等常驻 iframe 的独立请求。GP01 冷开 13 个预览请求，820-GP 采样首批 12 个请求，均为稳定缩略图 URL、无失败和重复；驻留复入不重新请求已加载媒体，销毁编辑器后的新 iframe 全部命中 Cache Storage。隔离目录联接已在验证后移除。

## 新窗口接续

当前正式安装版已验证新引擎实际启用：820-GP 452 节点仅挂载 31 个，GP01 328 节点仅挂载 25 个；引擎核心 render 约 63-64ms。用户感知的“进入画布加载页等待”来自 `prepareCanvasEntry()` 的旧媒体门禁：列表点击进入编辑器后，即使新引擎已挂载可见节点，仍等待视口缩略图/解码。源码已在 `17041f8` 将新引擎路径 `CanvasResourceReady.wait` 预算从 800ms 降到 120ms，媒体转为进入后后台加载；当前手动安装的 2.0.0 尚未包含此修复。

新窗口执行顺序：

1. 运行 `npm run installer:build` 重新生成 2.0.0，确认 stage 的 `canvas.js` 与安装器 bundle 包含 120ms 门禁。
2. 不覆盖安装前先保留 `dist/installer/SHIYIN-AI-Setup-2.0.0.exe` 旧包和新包哈希，安装到用户确认的测试环境。
3. 从画布管理页点击 `820-GP`，记录点击到 iframe、首个可见节点、`canvas-entry-progress` 消失、首次可操作四个时间点；再测 `GP01`。
4. 若仍慢，继续拆分配置读取、恢复记录、媒体请求和解码时间；不要再用直接 `canvas.html?id=...` 导航替代真实列表点击路径。

## 2026-09-16 入口直达调整

用户要求对齐 Infinite Canvas 的进入方式：点击后直接切到画布文档，可见节点挂载即允许操作，不显示工程进入等待动画，也不让媒体解码、远端版本检查或运行时同步挡住画布。正常进入不再创建全屏进度层；宿主只在页面长时间无法挂载时显示错误重试。React 视口引擎完成同步挂载后立即解除 `shell` 的交互锁，资源监控、媒体队列、缩略图解码、能力查询和二级启动任务继续在后台运行。驻留实例恢复同样直接显示，远端版本与配置刷新在后台完成。

数据安全边界保持不变：工程结构和本地未同步快照仍需完成裁决后才开放编辑；服务端工程读取失败继续显示错误与返回入口。此次移除的是工程已经可挂载之后的旧全局门禁及动画，不把读取失败伪装为成功。

## 目标与约束

用户要求直接使用 `https://github.com/tigerowo/infinite-canvas` 的画布引擎，弃用现有卡顿引擎；保留页面布局和所有节点功能。修改运行源码前创建必要源码备份，以便恢复。默认不构建全量安装包。

## 当前证据

- 上游 `f30b8d7` 的 `infinite-canvas.tsx` 管理视口变换、平移、缩放和网格；编辑页 `canvas-client-page.tsx` 按视口筛选节点，节点通过 React 组件渲染，连线仍是 SVG。
- 本项目经典/智能画布是原生 DOM + SVG；业务事件、端口定位、媒体生命周期和节点模板直接依赖现有 DOM。只替换坐标变换无法获得上游的千节点性能。
- 旧性能审计中，每节点控件 DOM 膨胀和媒体请求洪峰是主要瓶颈；迁移验收需覆盖真实混合节点与媒体，不能只测空节点。
- 上游许可证 AGPL-3.0，本项目 `LICENSE` 包含商业使用限制；复制上游代码前需要确定分发许可是否兼容。

## 直接移植边界

上游没有独立的 npm 引擎包。可移植单元是 `infinite-canvas.tsx` 的视口容器、编辑页的可见节点筛选、`canvas-node.tsx` 的 React 节点挂载与 `canvas-connections.tsx` 的连线。只把容器嵌入当前页面，旧 `render()` 仍会全量重建 DOM，因此不符合用户要求。直接移植需把 React 渲染层作为唯一画布核心，停止经典/智能 `render()` 和 CSS transform 引擎直接操作画布根节点。

原有页面 shell、工具栏和浮层布局不迁移。现有工程数据仍是唯一持久化格式；React 层读取现有 `nodes`、`connections`、`viewport`，旧节点业务逻辑通过逐类型适配进入 React 节点组件。特殊节点的媒体、3D 舞台、提示词输入和端口必须在组件挂载/卸载生命周期中保留状态。布局 CSS 和 DOM 锚点通过截图及功能测试逐一核对。项目现已统一进入 `canvas.html`，历史智能画布 URL 重定向，旧数据由经典画布迁移；无需再替换已下线的独立智能编辑器入口。

## 实施顺序

1. 固定上游 commit、许可证和依赖清单；同条件测试上游与本项目的混合节点、真实媒体和连线，记录收益上限。
2. 接入上游 React 核心与构建管线，在隔离入口加载现有工程；保留页面 shell 与现有数据格式。安装或分发上游代码前先解决许可证兼容性。
3. 适配统一画布所有节点类型、端口、拖动、缩放、连线、选择、多选、撤销、生成、媒体与保存，关闭旧核心的 DOM 写入；验证旧智能工程的现有迁移入口。
4. 跑旧工程迁移、布局截图、全节点交互和 500/1000 混合节点性能回归；达标后切换默认入口。保留 `backup/v202` 和可回退 Git commit，热更新仅在验收完成后发布。

恢复：`backup/v202` 保留实施前的必要源码，不覆盖用户原有未提交文件；Git 分支将记录本任务改动。发生回归时先回退本任务入口切换，工程数据不做不可逆迁移。

上游授权说明见 `https://github.com/tigerowo/infinite-canvas/blob/f30b8d7f8a01573d0332c98f214f4bf4d1e3de14/docs/business/license.md`；上游 README 提供联系人邮箱 `yhb293933@gmail.com`。本轮未联系维护者。需取得明确覆盖修改、保留本项目现有许可、源代码推送、桌面与局域网热更新分发的书面许可，并保存授权文本后再执行分发。

## 源码桌面运行补齐

`npm run desktop:dev` 现在调用 `tools/start-desktop-dev.ps1`：按内置 `python/python.exe`、`CANVAS_PYTHON_EXECUTABLE`、PATH 顺序查找 Python，验证 FastAPI/Uvicorn，设置 `CANVAS_DEV_ROOT` 和独立的 `%LOCALAPPDATA%/SHIYIN-AI/source-dev` 数据目录；默认端口冲突时选择 13170-13220 的空闲端口。Tauri 在没有已打包 Sidecar 时以仓库根目录作为 `--app-root`，让源码后端读取 `static/`；已打包 Sidecar 继续使用 `app/`。`-PrepareOnly` 可检查环境与端口而不打开桌面窗口。

## 无感切换契约

现有按钮、菜单、快捷键和手势保持相同：选择/移动模式、空格暂时切换、缩放中心、拖拽与多选、节点缩放、端口连接与断开、右键菜单、节点创建、整理、小地图定位、复制粘贴、撤销重做、保存与导入导出。相同工程在切换前后打开时，节点坐标、尺寸、层级、端口位置、连线走向、菜单与工具栏位置保持一致；不能把上游节点卡片或页面样式带进本项目。

视口卸载是渲染生命周期变化，不是删除节点：工程数据、选中集、撤销栈、生成任务与结果、媒体引用仍完整。输入焦点/中文输入法合成、运行中的节点、视频播放、3D 交互和正在连接/拖动的节点需要固定挂载或转移状态；回到视口后恢复可见控件。对每类节点分别验证离开视口、回来、离开页面再回来和重启后的行为。

回归按现有 `tests/test_canvas_tool_modes.py`、`test_canvas_connection_double_click.py`、`test_canvas_multi_file_drop_layout.py`、`test_canvas_project_package_frontend.py`、`test_canvas_session_cache_runtime.py`、`test_canvas_media_queue_runtime.py`、`test_canvas_special_nodes.py`、`test_canvas_depth_video.py` 等专项扩展，补充新引擎的实际浏览器交互与桌面宿主检查。默认入口切换前，旧/新引擎在同一份只读工程副本上对照；自动化通过且人工检查关键复杂节点、中文输入和媒体恢复后才视为无感达成。

## 验收标准

- 现有页面工具栏、菜单、小地图和节点外观截图无非预期变化。
- 所有旧工程无需数据转换即可打开、编辑、保存、导入导出；经典和智能节点类型、端口与生成流程保持可用。
- 500/1000 混合节点及真实媒体场景，比较冷/热首屏、可交互时间、平移 FPS、长任务、预览请求和内存；性能收益来自新引擎接入且无功能回退。
- 实际运行桌面/浏览器界面并检查主要工作流；完成后仅提交本任务文件，记录 commit/push 状态。

[CODEX_LONG_TASK_CONTINUE_V3]
