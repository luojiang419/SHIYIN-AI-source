# 2.0.0 全量安装包

状态：2.0.0 全量安装包已本地编译、验收并提交；尚未推送、发布或覆盖安装
下一步：取得上游另行授权后再决定分发，实际覆盖安装另行验收
TODO：上游另行授权及后续分发；真实用户数据上的覆盖安装验收
最近验证：`backup/v204` 14 个修改前必要文件核对；版本门禁 2.0.0（9 个引用源）；PyInstaller、Tauri release 与 ISCC 完整构建退出 0，Inno 566.734s；安装器 ProductVersion 2.0.0、1,743,675,469 字节、SHA-256 `f442bbe9e83547d5767e6fa91fd13042e3d2aa81097887f64894f5dc7720c966`、NotSigned；独立打包桌面 health/stage 均 2.0.0，双实例/退出清理通过；画布 bundle stage 哈希一致且无固定模型权重；Python 25 passed、Rust 22 passed
branch/commit：`feat/infinite-canvas-engine-migration` / 本轮本地提交 `b129832`，未推送
阻塞：上游画布代码的另行分发授权尚未取得；允许本地编译验收，不推送源码、不向 GitHub 或局域网分发中心发布安装包

## 目标与约束

用户明确要求全量安装包，作为 2.0.0 大版本更新。版本必须准确为 `2.0.0`，不执行自动补丁递增，不覆盖已安装软件或用户数据。沿用独立局域网分发中心；person-depth 与 video-depth 模型权重保持固定资源，不进入安装包。

## 方案

1. 沿用项目 `tools/increment-version.ps1 -Version 2.0.0` 同步 VERSION、npm、Cargo、Tauri、后端、发行说明和更新 JSON，再运行 `assert-version-sync.ps1`。
2. 在本地 `build-installer.ps1` 构建桌面宿主、backend sidecar、视频深度 worker 与 Inno Setup 安装包；确保上游画布 bundle 在复制 `static/` 前重新生成并在 stage 中校验。
3. 运行项目自带的安装态 smoke 与 `verify-installer-artifact.ps1`；核对产物版本、大小、哈希、签名状态、画布资源以及无模型权重。记录未覆盖的真实用户工程/API 功能。

## 验收标准

- 10 处版本源一致，安装包、桌面宿主和 backend health 都报告 `2.0.0`。
- 本地 `dist/installer/SHIYIN-AI-Setup-2.0.0.exe` 存在并通过项目验证；SHA-256 校验文件同目录。
- stage 含新画布 bundle、必要 Sidecar 和运行时，不含固定模型权重；源码后端及客户端原有数据不被覆盖。
- 只提交本任务源码、版本和任务文档；授权前不 push、发布或进行覆盖安装。

## 本地结果

- 安装器：`dist/installer/SHIYIN-AI-Setup-2.0.0.exe`；同目录 `.sha256` 已由 `verify-installer-artifact.ps1` 写入。
- 桌面宿主：`src-tauri/target/release/SHIYIN-AI.exe` ProductVersion 2.0.0；基线序号 `20260916104542`。
- stage：`app/VERSION` 2.0.0，`app/web/vendor/js/infinite-canvas-engine.js` 与源码同 SHA-256，`canvas.html` 引擎/Topaz缓存键均为 2.0.0；视频深度运行时及 person-depth 路径没有 `.pth/.onnx/.safetensors/.ckpt` 固定模型权重。
- 运行：构建时默认 desktop smoke 返回 health ok；补充版本断言后隔离复测返回 `health_version=expected_version=2.0.0`，第二实例退出 0，主实例保持运行，关闭后 Sidecar 停止。软内存目标两次观测分别约 162 MB 与 272 MB，后一次进程列表包含约 80 MB PowerShell，不能宣称稳定低于 250 MB。
- 本轮安装器未做代码签名，Authenticode 为 NotSigned；没有实际覆盖安装。上游另行授权未取得，不能上传、推送或发布含画布代码的本地 2.0.0 构建。

[CODEX_LONG_TASK_CONTINUE_V3]
