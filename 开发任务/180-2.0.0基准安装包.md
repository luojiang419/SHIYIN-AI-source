# 2.0.0 基准安装包

状态：2.0.0 基准包已重编、安装并验证画布首次进入；下方保留旧包记录
下一步：观察用户实际点击与 RTX 5080 客户机更新结果；组件迁移进度见同号深度运行时任务
TODO：[x] 新基线编译与安装；[x] 旧数据升级烟测；[x] 本机画布菜单检查；[ ] RTX 5080 客户机真实整段生成验收
最近验证：新包装有画布 HTML/脚本与接口恢复修复；安装器校验通过；冻结包新装、重启、旧 2.0.0 数据升级三场景 301 节点和可见媒体通过，页面错误 0；桌面 smoke 和正式安装后菜单检查通过。
branch/commit：`feat/infinite-canvas-engine-migration`；源码修复 `11f05c7`、`0764609`，基线记录提交见 Git

## 2026-09-16 画布首次进入修复后的基准重编

- 从隔离工作树 `E:/APP/SHIYIN-AI-canvas-menu-build-20260916` 完整重编画布引擎、backend sidecar、桌面宿主与 Inno 安装器，未纳入主工作区未提交的工程包导出和其他改动。
- 新基线序号 `20260916154805`，晚于已发布的网页热更新 `20260916153439`；新装或覆盖安装后不会回灌旧网页包。版本同步仍为 2.0.0（9 个版本源）。
- 新包：`dist/installer/SHIYIN-AI-Setup-2.0.0.exe`，115483243 字节，SHA-256 `134035A858FC8F9F327BD3EF04E0E5E35BC8C688A3993D823F7F66D6C018CC07`。签名状态 `NotSigned`，沿用项目现有配置。此前 SHA-256 为 `c78099f068fda702b3c73294792ce2d3bfd7fa21679b4776adc63beebd97fe20` 的旧包仍保存在主工作区，未覆盖。
- 新包 staging 的 `canvas-list.html`、`index.html` 和 `canvas-list.js` 均包含首次 HTML/脚本失败恢复及暂时性接口持续重试；未打包可选 `app/runtime/video-depth`，固定模型保持分发中心独立资源。
- 冻结包隔离验证：新装首次列表/工程可操作/首媒体约 242/602/821 ms，重启约 288/573/791 ms，旧 2.0.0 数据升级后约 608/674/996 ms；三次均加载 301 节点、可见媒体有像素、页面错误 0。烟测已按新入口契约分别等待可操作和首媒体，原脚本把二者当作同一时点的断言已修正。
- 桌面 smoke：health `ok`、版本 2.0.0、启动约 10039 ms、进程树私有内存约 132.11 MB；第二实例保护和退出清理通过。
- 覆盖正式 `D:/Program Files/SHIYIN AI` 安装退出码 0，安装日志确认成功；安装前对 `data/database/canvas.db` 作 SQLite 在线备份到隔离工作树 `.build/preinstall-backup-20260916/canvas.db`，备份与安装后数据库的 `integrity_check` 均为 `ok`。安装器只替换 `app`，数据文件大小和修改时间未变化。
- 正式版重新启动后 `/api/health` 为 `ok`/2.0.0；全新浏览器会话对当前账号从左侧菜单进入，约 1198 ms 显示 5 个工程，未出现列表错误或页面脚本异常。此检查为同一正式 backend 的浏览器路径，不等同于人工操作 WebView2 窗口的长期稳定性验收。

## 本次编译

- `VERSION` 与 9 个版本源均为 `2.0.0`
- 使用 `tools/build-installer.ps1 -SkipRuntimeSmoke`
- 产物：`dist/installer/SHIYIN-AI-Setup-2.0.0.exe`
- 大小：115511824 bytes
- SHA-256：`c78099f068fda702b3c73294792ce2d3bfd7fa21679b4776adc63beebd97fe20`
- 基线序号：`20260916134723`
- 签名：`NotSigned`（沿用当前项目配置）

## 重要限制

当前 2.0.0 包内仍是 `video-depth-runtime 1.0.0`、`video-depth 1.0.0`、`person-depth 1.0.0` manifest。包已具备 5080 的 `cuda128` variant 定义，但尚未把已经安装的错误组件通过版本/SHA 迁移自动修复。后续热更新必须先补齐迁移逻辑和修正版资源清单，再作为 2.0.0 之后的第一版发布。

## 验证

- `tools/assert-version-sync.ps1`：通过
- `tools/verify-installer-artifact.ps1 -Version 2.0.0`：通过
- `npm run canvas:engine-build`：通过
- Tauri release host build：通过
- Inno Setup compile：通过
- packaged desktop runtime smoke：未执行，原因是现有正式版进程正在运行
