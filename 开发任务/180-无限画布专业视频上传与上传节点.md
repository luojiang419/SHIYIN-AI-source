# 状态

- 已完成：画布视频流式上传、专业格式播放代理、上传节点媒体类型文案；局域网热更新 `20260917133159` 已发布。
- 下一步：客户端应用更新后，用真实专业素材复核导入、播放和编辑体验。
- TODO：无代码待办。
- 最近验证：`pytest` 针对性 23 条通过；99 MB 分块上传与 MPEG-2 转 H.264 测试通过；`node --check`、`py_compile` 通过；Playwright 在静态服务中确认快捷栏显示“上传”。更新包签名、更新计划签名及公开下载 SHA-256 均通过；冻结版后端隔离启动 `/api/health` 返回 200。独立后端的编辑页受登录门禁限制，未完成登录态浏览器验收。
- Branch：`feat/output-port-quick-menus`；功能 commit：`f4bf4517`；阻塞：端到端页面验收需要客户端登录态。

[CODEX_LONG_TASK_CONTINUE_V3]

## 目标与验收

- 大于 50 MB 的视频可上传，上传过程不按文件大小一次性占用内存。
- 专业容器或浏览器不兼容编码的视频可在节点和剪辑预览中播放，剪辑源仍为原文件。
- 底部入口显示“上传”；上传后节点标明图片、视频或音频。

## 实现说明

- `/api/ai/upload` 仅对视频改为 1 MB 分块写入，不限制视频体积；图片和其他文件保留原有 50 MB 策略。
- FFprobe 检查容器和编码；浏览器不兼容时由 FFmpeg 生成 MP4 播放副本。节点源地址始终指向原视频，剪辑、截图和下载使用原始文件。
- 本机独立服务地址 `http://127.0.0.1:8766`；编辑页需登录。静态 UI 验证使用的临时服务已停止。
- 现有 `test_canvas_video_clip_editor.py` 和 `test_canvas_media_quick_toolbar.py` 各有一条旧源码字符串断言失败；对应字符串在 HEAD 版本中已不存在，本任务未修改对应逻辑。

## 热更新发布

- 从 `f4bf4517` 的隔离工作树 `.build/180-video-upload-release` 构建并发布版本 `20260917133159`，未混入主工作区其他未提交改动，未构建全量安装包，未更换固定模型。
- 分发中心 `hot-20260917133159` 状态 `published`；客户端目录目标版本一致。清单 1007 文件，`prune_roots` 仅 `app/web`、`app/backend/canvas-backend`。
- 更新包 123,771,796 字节，SHA-256 `959813680c609446ba29fd2e0f46dda6dcbfb6e9f6a913fd68c1b8d1919e1919`；公开下载完整读取后哈希一致，目录和计划 Ed25519 签名验证通过。
- 本次打包的 `canvas-backend.exe` 使用隔离数据目录启动，`/api/health` 返回 200、`runtime_mode=packaged`、`database=ok`；测试进程已停止，未替换或重启已安装客户端。
