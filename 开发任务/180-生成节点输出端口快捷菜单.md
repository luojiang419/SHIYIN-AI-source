# 生成节点输出端口快捷菜单

状态：图片素材卡菜单补漏已发布为纯前端热更新 `20260917100917`。
下一步：客户端应用更新后按截图中的 `IMAGE` 素材卡实际拖线验收。
TODO：真实客户端应用后的界面验收。
最近验证：`node --check`、`canvas_output_port_menu_check.cjs`（图片/视频素材卡真实端口拖线、菜单、节点连接和工具入口）、`canvas_ad_menu_check.cjs`（影视制作二级菜单及撤销）、`tests.test_canvas_menu_performance` 通过。公开目录与补齐计划签名有效；实际下载包 23,534,085 字节，SHA-256 `5185c335ca363e11d93daa6a5fc03f41cdd15748160f729c3f416d47b5e6999d` 与清单一致。
Branch：`feat/output-port-quick-menus`；图片素材卡修复提交 `504ce481` 已推送；隔离构建来源 `504ce481`；阻塞：无。

## 目标与验收

- 视频生成输出端口拖线后显示视频截取、视频抽帧、深度视频、截图、TOPAZ 视频超分。
- 图片生成输出端口拖线后显示图片生成、视频生成、lookbook、深度图、一键复刻、拼图、批量处理、AI助手，影视制作可展开二级菜单。
- 节点类操作创建后自动连接；视频编辑类操作完成后生成结果节点并保留来源连接。
- Topaz 节点中的“高清放大”文案改为“视频超分”。

## 实现

- 图片/视频生成节点的输出端口拖线后显示指定菜单；图片菜单保留可展开的影视制作二级项。
- 视频截取、截图、抽帧继续使用现有编辑器；读取生成节点的最新视频，完成后生成派生节点与来源连线。
- 拼图与 AI 助手读取图片生成结果；相关连线及视频派生连线可在连接清理后保留。

## 视频素材卡补漏

用户实际拖拽的是 `type:'image'` 且 `mediaKind:'video'` 的视频素材卡。原改动仅覆盖 `type:'video'` 的生成控制节点，视频素材卡继续显示旧的通用菜单。两类视频输出现在共用五项快捷菜单。

修复包从 `b76fb3bd` 的隔离工作树使用 `--web-only` 发布，范围仅 `app/web` 155 个文件；未重新打包宿主、后端或固定模型，未纳入主工作区其他任务的未提交改动。分发中心已将上一版 `20260917095026` 归档，并以 `20260917095809` 为当前目标版本。

## 图片素材卡补漏

截图中的 `IMAGE` 也是 `type:'image'` 素材卡；前版八项菜单仅覆盖 `type:'generator'` 图片生成控制节点。现将普通图片素材卡接入相同八项菜单，`lookbook` 改为顶层入口，原平面广告/影视广告分组收敛为可展开的“影视制作”二级菜单；视频素材卡继续显示五项视频操作。

图片素材卡补漏从 `504ce481` 的隔离工作树使用 `--web-only` 发布，范围仅 `app/web` 155 个文件；未重新打包宿主、后端或固定模型，也未纳入主工作区其他任务的未提交改动。分发中心已将上一版 `20260917095809` 归档，并以 `20260917100917` 为当前目标版本。

[CODEX_LONG_TASK_CONTINUE_V3]
