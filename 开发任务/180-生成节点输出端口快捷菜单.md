# 生成节点输出端口快捷菜单

状态：已发布局域网热更新 `20260917095026`，纯前端包 155 个文件。
下一步：客户端应用更新后现场核对输出端口菜单与自动连线。
TODO：真实客户端应用后的界面验收。
最近验证：`node --check` 两个修改脚本通过；`canvas_ad_menu_check.cjs`、`canvas_output_port_menu_check.cjs`、`tests.test_canvas_menu_performance` 通过。公开 catalog 和补齐计划签名有效；下载包 23,534,207 字节，SHA-256 `bf0880d4a9eabe228eef8699c7d26ed0aa24a5257f0084d3746112d6855fbf02` 与清单一致。`tests.test_canvas_special_nodes` 的 3 项历史源码字面断言失败，与本次行为无关。
Branch：`feat/output-port-quick-menus`；功能提交 `e2cf0de2`，隔离构建来源 `1131470a`；发布记录提交见 Git；阻塞：无。

## 目标与验收

- 视频生成输出端口拖线后显示视频截取、视频抽帧、深度视频、截图、TOPAZ 视频超分。
- 图片生成输出端口拖线后显示图片生成、视频生成、lookbook、深度图、一键复刻、拼图、批量处理、AI助手，影视制作可展开二级菜单。
- 节点类操作创建后自动连接；视频编辑类操作完成后生成结果节点并保留来源连接。
- Topaz 节点中的“高清放大”文案改为“视频超分”。

## 实现

- 图片/视频生成节点的输出端口拖线后显示指定菜单；图片菜单保留可展开的影视制作二级项。
- 视频截取、截图、抽帧继续使用现有编辑器；读取生成节点的最新视频，完成后生成派生节点与来源连线。
- 拼图与 AI 助手读取图片生成结果；相关连线及视频派生连线可在连接清理后保留。

## 热更新发布

- 从 `1131470a` 建立隔离工作树构建并发布 `--web-only`，避免纳入主工作区其他任务的未提交改动。发布范围仅 `app/web`；不含桌面宿主、后端和固定模型。
- 分发中心 `http://192.168.0.24:3011/` 当前热更新目标为 `20260917095026`，上一热更新 `20260917091915` 已归档。
- 公开清单、补齐计划的 Ed25519 签名与客户端内置公钥核验通过；实际下载的单包大小和 SHA-256 与本地发布清单一致。

[CODEX_LONG_TASK_CONTINUE_V3]
