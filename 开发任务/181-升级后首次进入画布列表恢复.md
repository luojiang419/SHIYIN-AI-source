# 升级后首次进入画布列表恢复

状态：源码修复与隔离浏览器验证完成；安装版尚未更新
下一步：取得上游另行授权后纳入局域网热更新，在实际覆盖升级后的第一次启动复测左侧菜单
TODO：[x] 列表 iframe 失败可恢复；[x] 故障注入验证；[x] Git 源码交付；[ ] 安装环境覆盖升级验收
最近验证：首次列表 HTML/脚本 503 的预加载与当前页四种组合均在第二次请求恢复；连续三次失败后再次点菜单第四次恢复；编辑器预热脚本首次失败后正常导航恢复。冷启动专项 10 场景、相关 pytest 30 项、普通极速进入及 301 节点双入口驻留回归通过；后者复入未重复读取工程和媒体。JS 语法与暂存 diff 检查通过。
branch/commit：`feat/infinite-canvas-engine-migration` / `11f05c7`（源码）；文档提交见 Git。分支含尚未获得上游另行授权的引擎源码，不推送或发布。
阻塞：正式安装版覆盖升级尚未复现；本轮以已复现的源码入口故障修复并做隔离验证。

## 目标与方案

升级后首次启动时，左侧“无限画布”按钮应能在当前应用会话中打开工程列表。列表 HTML 或脚本首次加载失败时，不把 iframe 的 `load` 事件误当成功；用户点击和当前激活页的失败处理应自动重试，有界失败后保留可再次点击的恢复入口。启动恢复期间有遮罩，正常鼠标无法点击菜单，故不修改其路由逻辑。

涉及 `static/index.html` 的 iframe 就绪和重试、`static/js/canvas-session-host.js` 的宿主等待与编辑器预热回退，以及 HTML 中宿主脚本缓存键。使用隔离画布 fixture 与真实浏览器注入首次失败，不读写用户工程。验收包括首次预加载失败后点击恢复、激活页首次失败后自动恢复、脚本加载失败后恢复、重试耗尽后的再次点击，以及常规入口和驻留回归。保护现有工作区未提交修改。

[CODEX_LONG_TASK_CONTINUE_V3]
