# 182-旧画布启动缓存阻塞排查与修复

状态：缓存阻塞修复、回归、Git推送及热更新发布完成；正式客户端体验待更新后复测。
下一步：正式客户端应用20260916171750并重启后复测820-GP；若仍卡顿，继续采集正式WebView2启动请求时序。
TODO：[x] 820-GP真实数据隔离复测；[x] 桌面现状检查；[x] 缓存超时降级；[x] 回归；[x] 发布；[ ] 正式客户端更新后体验验收。
最近验证：820-GP 452节点，独立列表入口472ms可操作、首屏图片8.67s；完整工作台首轮323ms可操作、首图4.50s、图片6.06s。缓存已有时完整入口218ms可操作、图片1.00s。未复现用户报告的一分钟，不能把上述数字当成正式WebView2验收。
branch/commit：fix/canvas-startup-cache-stall / d2bf610，已推送origin。
阻塞：正式客户端无远程调试端口；Windows截图接口返回SetIsBorderRequired 0x80004002，可访问性树可读取。

## 现场证据与范围

用户指定820-GP。当前客户端可访问性树显示该工程452节点、60 FPS，多张图片持续显示资源加载中。只读检查客户端CacheStorage约296MB、916文件；已安装canvas.js与当前源码仅列表URL一行差异。没有删除缓存、重启客户端或修改正式工程。

`static/media-cache-sw.js` 对脚本与媒体的 caches.open/cache.match 无时间上限，静态脚本更是等待 cache.put 完成才返回已收到的网络响应。缓存I/O缓慢会让网络资源被可选缓存挡住，冷启动缓存繁忙时尤其明显。此为已确认的无界等待路径，尚未证明是本次一分钟的唯一原因。修复需保留账号隔离、正常缓存命中、媒体版本键和用户数据，仅让缓存异常或超时回退正常服务器请求。

复测副本和采样脚本位于 `.build/182-startup`，复用任务178的只读媒体映射。所有测试写入隔离数据库；工作区原有main.py、canvas-list.js、178文档及删除文件不纳入本任务。

[CODEX_LONG_TASK_CONTINUE_V3]

## 已完成修复与验证（2026-09-16）

缓存读取总预算250ms，打开/读取失败或超时后30秒内跳过CacheStorage，直接请求现有本地服务；静态文件缓存写入改由Service Worker事件后台维护。未更改账号鉴权、缓存分区、版本键或媒体内容，未清理用户缓存。

验证：
- `node --test tests/js/media_cache_startup.test.cjs`：11 passed，包含缓存open/match/put永久挂起、配额错误、存储不可用和等待期间账号切换。
- `node tests/support/media_cache_isolation.cjs`：账号切换、退出、缓存命中、并发响应隔离通过。
- `node tests/support/media_cache_startup_browser.cjs`：真实Edge Service Worker五场景通过，读取永久挂起时脚本和图片272–307ms可用，写入挂起/错误9–13ms；将同一测试指向HEAD旧脚本，首场景在2500ms超时，证明回归测试能检出原阻塞。
- `python -m pytest tests/test_generated_image_browser_cache.py tests/test_media_cache_isolation.py tests/test_web_cache_policy.py -q`：8 passed。
- 820-GP真实工程隔离副本、完整工作台入口、实际Service Worker中注入caches.open永不返回：452节点，222ms可操作、338ms首图、879ms当前视口19张图完成；失败图片0、脚本异常0、往返后空白0。磁盘缩略图缓存已有，不是物理冷磁盘结果。证据保存在`.build/182-startup/results.json`。

正式客户端仍需应用热更新后重新启动复测；本轮修复解决经故障注入确认的缓存无界等待，不能声称已复现并彻底消除用户现场一分钟卡顿。

## 发布核验

独立工作树.build/182-cache-release基于d2bf610发布纯前端热更新20260916171750，155文件、23,534,411字节，SHA-256 f956f921c68a7ced18e0c6df773c37930e8d88aa8602ef273697883b3e1bcd67。管理API确认published、job无错误、target_version一致；公开catalog使用客户端内置公钥验证Ed25519签名通过，prune_roots仅app/web。未构建全量安装包、未替换固定模型、未包含其他任务未提交改动。

## 2026-09-16 移除旧恢复记录提示

按用户截图要求移除studio-page-state.js创建的恢复提示浮窗及其中导出/关闭按钮；不改变恢复槽位轮换、旧记录归档和脏快照保护。同步调整已有浏览器回归：缓存失败和重载均无提示，新编辑可保存、旧脏快照不反向覆盖、旧记录在IndexedDB完整保留。cache-error专项通过（410ms、脚本错误0），JS语法检查通过。首次额外运行dirty-cache场景时静态HTTP服务缺少fixture.png，导致等待图片超时，与本次弹窗修改无关。
