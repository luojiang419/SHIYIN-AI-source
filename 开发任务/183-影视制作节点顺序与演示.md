# 影视制作节点顺序与演示

状态：已按用户示例将二级菜单改为四个制作阶段的分区布局，并更新 HTML 演示；等待用户确认排版。
下一步：用户确认后决定是否发布纯前端热更新。
TODO：[x] 梳理制作流程；[x] 重排创建节点菜单；[x] 重排图片输出连线子菜单；[x] 制作 HTML 演示；[ ] 用户确认；[ ] 按确认结果发布。
最近验证：`node --check static/js/canvas.js`、`git diff --check` 通过；51 项相关 Python 测试通过；`canvas_ad_menu_check.cjs` 浏览器回归通过，含 11 个图片端口影视选项的连接、撤销和菜单边界；正式创建菜单含 4 个分区、16 个按钮；演示页 1180/500/390/320px 无横向溢出。
branch/commit：`feat/output-port-quick-menus` / 本次提交见 `git log -1`。
阻塞：无。

目标：影视制作相关节点按照前期准备、分镜预演、镜头生成、后期处理排序；不改变节点创建与连接行为。演示文件为 `static/prototypes/film-production-node-order.html`。

验收：创建节点菜单包含原有全部影视节点且各出现一次，按四阶段分区；图片输出连线子菜单仅展示可连接阶段和节点，保留输入端口角色；两个菜单共同节点的相对顺序一致；HTML 演示能在桌面和移动宽度正常显示。

排序：工作流快捷入口置顶；准备资产、取景/导演预演、三视图与姿态参考、确认镜头、线稿与分镜、两种视频生成、深度视频与超分。正式二级菜单按阶段展示简短流程说明和节点按钮；手机宽度改为单列。

[CODEX_LONG_TASK_CONTINUE_V3]
