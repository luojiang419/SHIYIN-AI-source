# 影视制作节点顺序与演示

状态：用户已确认五阶段演示；正式创建菜单、图片输出连线子菜单和两个演示页已同步，验证通过，等待提交并发布热更新。
下一步：仅提交本任务文件并从隔离工作树发布纯前端热更新。
TODO：[x] 梳理制作流程；[x] 重排创建节点菜单；[x] 重排图片输出连线子菜单；[x] 制作 HTML 演示；[x] 用户确认；[x] 完成验证；[ ] 发布。
最近验证：`node --check static/js/canvas.js`、`git diff --check` 通过；59 项影视与菜单相关 Python 测试通过；`canvas_ad_menu_check.cjs` 浏览器回归通过，含 11 个图片端口影视选项的连接、撤销、重做和菜单边界；正式创建菜单含 5 个分区、15 个节点和 1 个工作流入口，1180px 与 390px 均无横向或按钮文字溢出。扩大到 69 项时有 1 项原有图片提示面板懒加载断言失败，现有代码缺少测试要求的 `nodesEl?.addEventListener('pointerover'`，与本次菜单修改无关。
branch/commit：`feat/output-port-quick-menus` / 本次提交见 `git log -1`。
阻塞：无。

目标：影视制作相关节点按照资产筹备、镜头预演、分镜定稿、镜头生成、后期处理排序；不改变节点创建与连接行为。演示文件为 `static/prototypes/film-production-node-order.html`。

验收：创建节点菜单包含原有全部影视节点且各出现一次，按五阶段分区；图片输出连线子菜单仅展示可连接阶段和节点，保留输入端口角色；两个菜单共同节点的相对顺序一致；HTML 演示能在桌面和移动宽度正常显示。

排序：工作流快捷入口置顶；资产与三视图、取景/导演/动作预演、线稿与正式分镜、确认镜头、两种视频生成、深度视频与超分。正式二级菜单按阶段展示简短流程说明和带图标、边框的节点按钮；手机宽度改为单列。

[CODEX_LONG_TASK_CONTINUE_V3]
