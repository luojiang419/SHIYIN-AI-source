# 深度视频对比播放与进度控制

状态：空格键优化已验证、提交、推送并发布纯前端热更新。
下一步：客户端接收 `20260917105125` 后，用真实深度视频观察空格键和长时间同步播放。
TODO：本次代码、验证、提交、推送和发布均完成；保留真实素材客户端观察项。
最近验证：JS 语法通过；深度视频专项 7 项通过；Playwright 验证空格键暂停/播放、进度条焦点下空格键切换、划像与同步拖动均通过；`git diff --check` 通过。
branch/commit：`feat/output-port-quick-menus` / `14a9a20c`，已推送 `origin`。
阻塞：无。

## 目标与验收

1. 深度视频节点移除“离线导入”，改为“对比播放”。
2. 对比窗口以深度视频为底、源视频为上层，支持实时划像、同步播放和同步进度拖动。
3. 节点内源视频与深度视频都提供播放进度条和时间显示。
4. 经典画布与智能画布共用实现，任务轮询局部更新不重建正在播放的视频。

## 已完成实现

- 节点工具栏以“对比播放”替换“离线导入”，仅在源视频和深度视频均就绪时启用。
- 对比窗口下层显示应用当前调校参数的深度视频，上层显示源视频，拖动竖向分割线实时改变源视频可见范围。
- 对比窗口统一控制两路播放、暂停和进度，播放期间校正时间偏差。
- 节点内源视频与深度视频分别增加可拖动进度条和当前时间/总时长。
- 对比弹窗打开时聚焦播放按钮；按空格键切换两路视频播放/暂停，并阻止页面滚动及按键连发。

## 验证与发布

- `node --check static/js/canvas-special-nodes.js`：通过。
- `pytest tests/test_canvas_depth_video.py tests/test_depth_video_controls.py -q`：7 passed。
- `node tests/support/depth_video_compare_check.cjs`：两路节点进度条、50% 划像、同步播放、72% 同步定位、关闭停止均通过。
- 空格键优化后再次运行同一 Playwright 检查：真实键盘输入可暂停/播放两路视频，进度条获得焦点时仍有效。
- 在固定到 `06d5a003` 的隔离工作树发布纯前端热更新 `20260917104659`；清单 `prune_roots=['app/web']`，155 个文件，包大小 23536612 字节，SHA-256 `c4d18eb05df37185e04195198fa5328ddafd064f6e593c2e8385e8cf637cc491`。
- 分发中心 `/api/status` 核验该版本状态为 `published`；未编译全量安装包，未发布 backend、桌面宿主或固定深度模型。
- 空格键优化从固定提交 `14a9a20c` 的隔离工作树发布纯前端热更新 `20260917105125`；清单仅含 `app/web`，155 个文件，包大小 23536675 字节，SHA-256 `0ee5f22dc210c52829e75adb7caeeb0ac808f0645df4b3745a8ee5c6c4e500d4`。分发中心确认状态为 `published`。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

恢复时先读本文件并核对 Git 状态，再从“下一步”继续。
