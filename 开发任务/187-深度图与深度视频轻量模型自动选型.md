# 深度图与深度视频轻量模型自动选型

状态：全部完成；自有 ModelScope 源热更新 `20260917172803` 已发布
下一步：观察非局域网客户机首次下载速度与官方源回退情况
TODO：[x] 统一硬件分档；[x] 轻量模型接入；[x] 固定资源发布；[x] 设置三档选择；[x] 图片与视频共同路由；[x] 自有 ModelScope 上传；[x] 匿名下载和真实首装；[x] 客户端源地址热更新
最近验证：自有仓库三文件 SHA-256、匿名 Range、图片模型隔离首装、公开更新清单和冻结 backend 均通过
branch/commit：`feat/output-port-quick-menus`；`95b46eb7`、`585f91f3`、`51c7b2a6`
阻塞：暂无

## 目标

- 普通配置首次使用无需下载数百 MB 至数 GB 的高质量模型。
- CUDA 显存不少于 8GB 的设备自动使用现有高质量模型，其余设备自动使用轻量模型。
- 深度图轻量档复用现有 Depth Anything V2 Small ONNX；深度视频轻量档使用官方 Video Depth Anything Small。
- 状态接口和画布节点显示实际选择的模型档位，保留现有高质量模型及已安装资源兼容性。

## 验收标准

1. CPU、无 NVIDIA GPU 或显存小于 8GB 时选择轻量档。
2. 兼容 CUDA 且显存不少于 8GB 时选择高质量档。
3. 深度图轻量档不触发 person-depth 5.98GB 组件下载。
4. 深度视频仅下载所选档位权重，并向 worker 传入对应模型 key。
5. 分档逻辑、组件变体和任务调用测试通过。

## 实现结果

- 自动分档阈值为 8GB NVIDIA CUDA 显存；达到阈值选择质量档，其余设备选择轻量档。
- 支持 `SHIYIN_DEPTH_MODEL_TIER=lite|quality` 强制档位，供客服排障和质量对比。
- 图片轻量档复用现有约 50MB Depth Anything V2 Small ONNX，不再触发约 5.98GB 的人物深度组件下载；质量档继续使用 Large + BiRefNet。
- 视频轻量档使用 Video Depth Anything Small `vits` 权重，大小 116,440,756 字节；质量档继续使用 VDA Base。
- 视频模型清单升级到 schema v2，只下载当前机器对应权重；节点和任务状态回显实际模型。
- 分发中心兼容 `video-depth` schema v1/v2；v2 清单签名后按 package id 提供 Range 下载。
- 软件设置新增自动、大模型、小模型三档，持久化字段为 `depth_model_preference`。
- 自动档按 8GB NVIDIA 显存阈值选择；手动档不受显存阈值限制，深度图、深度视频和一键复刻共享选择。
- 设置变化会更新画布配置签名，避免切换模型后继续复用上一档结果；视频组件下载器同步切换对应 package。

## 验证结果

- `python -m pytest tests/test_depth_model_policy.py tests/test_adaptive_depth_runtime.py tests/test_video_depth_components.py tests/test_canvas_depth_video.py tests/test_person_depth_components.py tests/test_person_depth_worker.py tests/test_depth_reference.py tests/test_depth_video_controls.py -q`：59 passed。
- VDA Small 真实推理：320×240、8 FPS、8 帧，约 4.917 秒完成；峰值分配/保留显存约 1.04/1.32GB，结束后分配显存约 8.1MB。
- 真实 116,440,756 字节权重通过 SHA-256，并经组件管理器完成 schema v2 选择、校验、原子激活和 ready 状态验证。
- Python 编译、JavaScript 语法和 `git diff --check` 通过。
- ModelScope 固定提交 `8dd3558f4d407363cc1a113b81e89f16a0d51e5c`，权重 SHA-256 为 `13379300b739e659f076a59d52e9801bd8d38c541a7e71f73bbca4dcfb013609`。
- 分发中心构建 `20260917164627` 已部署；固定资源 `video-depth 2.0.0` 已发布，公开签名清单验证通过。
- 公开 Small/Base package 的 1MiB Range 请求均返回 206，总大小分别为 116,440,756 和 458,247,082 字节。
- 应用热更新 `20260917164910` 从提交 `c1bfa7fc` 的隔离工作树构建，1007 个文件、123,778,349 字节，SHA-256 `6470b4e1e763000c720c61a438bdf8386566411821f9aa75d3afbeac605c76c4`。
- 公开 hot catalog 与补齐目标均为 `20260917164910`，Ed25519 签名和 1MiB Range 下载验证通过；冻结 backend 隔离启动后 `/api/health` 返回 200。
- 固定资源台账 `D:\SHIYIN-Distribution\fixed-models.json` 已更新为 `video-depth 2.0.0`，总权重 574,687,838 字节。
- 本机 16GB 显存真实 API 验证：自动档与大模型档均选择 `quality`，小模型档选择 `lite`；图片与视频返回的模型名称一致。
- 设置页面静态资源加载与 1440px 桌面布局检查通过，下拉框包含自动、大模型、小模型三项。
- `python -m pytest tests/test_app_config.py tests/test_depth_model_policy.py tests/test_canvas_design_compare_depth_settings.py tests/test_adaptive_depth_runtime.py tests/test_video_depth_components.py tests/test_canvas_depth_video.py tests/test_person_depth_components.py tests/test_person_depth_worker.py tests/test_depth_reference.py tests/test_depth_video_controls.py -q`：86 passed。
- 三档设置热更新 `20260917171303` 从提交 `51c7b2a6` 的隔离工作树构建，1007 个文件、123,779,280 字节，SHA-256 `b2c3493d73273a0eb2492e37a04ea9e1185e3dd610791b84aeb969b323137119`。
- 公开 catalog 签名、补齐目标 `20260917171303`、1MiB Range 下载和冻结 backend `/api/health` 200 均验证通过。
- 自有 ModelScope 仓库：[jiangjiang419/shiyin-depth-lite-models](https://www.modelscope.cn/models/jiangjiang419/shiyin-depth-lite-models)，固定提交 `a1a14f2b20152dd7f2978d32bea5cad74a5a6e02`。
- 仓库包含图片 ONNX 两文件共 50,572,535 字节及视频 VDA Small 116,440,756 字节；三文件远端 SHA-256 与客户端固定值一致，匿名 1KiB Range 均返回 206。
- 图片小模型在全新临时目录从自有 ModelScope 源下载、校验并激活，最终 `source=domestic`、`ready=true`；ModelScope 失败会自动回退 Hugging Face 官方源。
- 自有源热更新 `20260917172803` 从提交 `dc3c3d9d` 的隔离工作树构建，1007 个文件、123,779,898 字节，SHA-256 `da2de0c3a0162090dc1cf0f024e7cb06fd0c4babfeeb8f1d41b6bdf51601255d`。
- 公开 catalog 签名、补齐目标 `20260917172803`、1MiB Range 下载和冻结 backend `/api/health` 200 均验证通过。

## 已知取舍

- 图片轻量档不运行 BiRefNet，因此人物模式会保留完整画面深度；8GB 以上质量档仍会输出经过人像分割的纯人物深度。节点会显示实际模型，避免将两种结果混淆。

[CODEX_LONG_TASK_CONTINUE_V3]
