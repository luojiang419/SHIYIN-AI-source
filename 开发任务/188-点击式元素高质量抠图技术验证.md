# 188 点击式元素高质量抠图技术验证

[CODEX_LONG_TASK_CONTINUE_V3]

- 当前状态：热更新 `20260917195929` 已发布，公开签名、整包下载与真实客户端隔离解包验证全部通过
- 下一步：用户在软件内检查更新并应用，验证实际画布中的抠像与保存返回体验
- TODO：开发、发布及发布验证均已完成；现场应用后的用户验收待客户端更新
- 最近验证：9 项单元测试通过；真实浏览器分割、保存、再次编辑、下游更新、刷新恢复及嵌套全屏/返回/失败保护通过；冻结后端 GPU 与禁用 CUDA 的 CPU 离线分割/导出均通过，保持 `1112×869` RGBA、alpha `0–255`
- branch/commit：`feat/fabric-detail-enhancement`；发布源 `9c9a3b54`，发布记录见本文件最近 Git 历史
- 阻塞：无

## 目标

开发一个与主应用隔离、后续可集成的点击式元素抠图验证工具。点击裤子、衣服、鞋、手等区域后自动生成蒙版；`Ctrl+左键`增加前景点，`Alt+左键`增加背景点；导出结果保持原图分辨率和透明边缘质量。

## 方案

- 独立目录 `tools/click-segmentation-lab/`，FastAPI 提供图片会话、点提示分割和导出接口。
- 使用 SAM 点提示模型；同一图片缓存图像 embedding，后续增减提示点只运行轻量 mask decoder。
- 前端 Canvas 负责等比显示、坐标回映射、正负点反馈、蒙版叠加和快捷键。
- 点击完成后默认实时显示透明结果；相同提示点下调整阈值和羽化复用原始蒙版，只执行轻量后处理。
- 后端在原图尺寸上做连通域约束、孔洞修复和可调羽化，导出透明 PNG 与 8 位灰度蒙版。

## 验收标准

- 导入常见 JPEG/PNG/WebP 后可点击获得目标蒙版。
- 普通左键替换选择，`Ctrl+左键`加选，`Alt+左键`减选，撤销和清空可用。
- 预览可切换原图、蒙版叠加、透明结果，并可调阈值和边缘羽化。
- 透明 PNG 与蒙版 PNG 的像素尺寸等于输入图像尺寸。
- 自动化测试覆盖点操作语义、路径隔离、原尺寸 alpha 合成与接口错误处理。

## 实际验证

- `python -m pytest -q tools/click-segmentation-lab/tests/test_segmenter.py`：`4 passed`。
- `python -m py_compile tools/click-segmentation-lab/server.py tools/click-segmentation-lab/segmenter.py`：通过。
- `node --check tools/click-segmentation-lab/frontend/app.js`：通过。
- Playwright 桌面视口：页面宽高等于 `1440×900` 视口，无溢出；上传、点击、加载状态和导出按钮状态通过。
- 真实推理：输入 `1112×869` PNG，透明导出仍为 `1112×869` RGBA，alpha 范围 `0–255`。
- 质量限制：用于自动测试的单色深度人偶图缺少真实纹理，模型倾向选择大面积区域；真实服装图片仍需人工评估裤装、鞋、手和发丝边缘。
- 2026-09-17 修复：`post_process_masks` 改为 `binarize=False` 并显式 sigmoid，避免把二值结果误当概率；候选排序使用正负点置信度，负点命中的候选受到强惩罚。

## 交付文件

- `tools/click-segmentation-lab/server.py`：本地 API 与会话管理。
- `tools/click-segmentation-lab/segmenter.py`：SAM 推理、候选选择、蒙版细化和 PNG 合成。
- `tools/click-segmentation-lab/frontend/`：交互界面。
- `tools/click-segmentation-lab/tests/`：单元测试、真实推理和浏览器 smoke。

## 实时预览根因修复

此前将提前二值化认定为整幅覆盖的根因不准确。直接根因是前端把灰度 PNG 的原始 alpha（全为 255）用于 source-in，未将灰度值写入透明通道。
本轮在合成前将灰度写入 alpha；无蒙版时展示原图，结果解码与绘制完成后才结束等待状态，并更新脚本缓存版本。
验证：真实 Chromium 加载应用，拦截分割响应为已知黑白蒙版；点击后背景 alpha=0、前景 alpha=255、底层 alpha=0，导出请求次数=0。该测试验证预览合成，不评价模型分割质量。

## 背景压暗辅助加减选

按用户最新要求，选区预览保留原图背景并覆盖 65% 黑色，选中元素按蒙版恢复正常亮度；无选区时显示完整原图。预览按钮改为“选区预览”，PNG 导出逻辑不变。
验证：Chromium 实际合成像素测试通过，未选区域红通道从 239 降到 83，选中区域保持 239；背景仍可见、无导出请求。脚本缓存键更新为 dim-preview-3。

## 导入后隐藏空白页提示

修复 empty-state 的 display:grid 覆盖 hidden 属性：增加 .empty-state[hidden] 隐藏规则，并更新 CSS 缓存版本。
Chromium 验证导入后提示区域不可见，背景压暗与目标正常亮度回归通过。

## 边缘精修滑块

增加边缘扩展/收缩滑块（-20 至 +20 原图像素，默认 0）。后端在羽化前对二值边界做椭圆核腐蚀或膨胀；预览和导出共用参数，旧请求默认不偏移。
验证：6 项后处理测试通过；Chromium 验证滑块数值与 edge_shift 请求同步，压暗预览回归通过。服务已重启。

## 滚轮缩放和平移浏览

增加以鼠标位置为中心的滚轮缩放（适应窗口比例的 10%–1200%）、空格+左键和中键平移、适应窗口重置。导入新图片重置视图。两层画布统一变换，点击通过实际显示矩形回映射原图。
Chromium 验证通过：缩放锚点稳定、放大后点击原图坐标为 (40,40)、空格拖动位移正确且无分割请求、重置回到 100%。预览像素回归通过。

## 可见操作滑块

顶部增加缩放滑块，与滚轮及百分比同步。修复小于 900px 时隐藏参数面板的问题，窄窗口改为底部可滚动面板，保留阈值、羽化和边缘扩展控件。
验证：Chromium 缩放滑块设置 200% 生效；760×700 窗口缩放与边缘滑块可见，滚轮/平移回归通过。

## 自动抠像节点验收
图片悬浮菜单创建并连接自动抠像节点；点击图片打开编辑器，提供进度条、全屏和保存；保留原图及提示点，保存生成持久 PNG，下游仅使用已保存结果，再次修改同步更新引用。

## 主项目节点集成交付

- canvas.js 注册 autoCutout，图片悬浮菜单创建节点并走既有连线事务。输入/输出端口、特殊节点媒体引用、生成器输入与 Output 更新接通。
- static/js/canvas-cutout-node.js 提供节点缩略图、全屏编辑弹窗、同源消息校验、保存及原图/参数保留。
- static/cutout-editor 继承验证工具交互，增加进度弹窗和不确定进度条、保存到节点，复用主应用账号素材上传。
- canvas_core/cutout_api.py 与 cutout_engine.py 提供主服务推理，最多 8 个会话、账号隔离、关闭释放会话。main.py API 挂载行已被同期其他任务的 2093a501 提交收录，本次不重复提交 main.py 的其他改动。
- 参数修改只在保存后更新下游；生成器刷新输入引用，不自动消耗 API 重生成历史结果。
- 测试：8 项 Python 测试通过；真实主服务 8792 隔离数据测试通过创建、图片菜单、SAM 分割、透明 PNG 保存、再次编辑边缘参数、下游旧引用替换、刷新恢复。源码语法与 diff 检查通过。
- 浏览器测试：tests/support/cutout_node_browser_check.cjs。测试服务使用 .codex-tmp/cutout-integration 数据，未修改用户已有画布。
- 部署限制：canvas-backend.spec 排除 torch/transformers；当前仅源码环境具备模型运行依赖，未构建或发布热更新，不宣称安装版开箱可用。

## 主题适配与全屏控件

节点背景、棋盘格、边框和弹窗改用项目主题变量；宿主监听主题 class/style 变化，通过同源消息同步 iframe 颜色。关闭编辑器时清理观察器。
修正 iframe flex 高度与全屏内部布局，参数栏保留滚动、窄窗口底部排列。真实 Chromium 验证全屏阈值/羽化/边缘/缩放/保存可见，深浅主题实时同步、再次保存下游替换和刷新恢复通过。

## 安装版异常与全屏保存返回修复（2026-09-17）

- 安装版日志确认 `/api/cutout/api/segment` 和导出均返回 500；原打包配置明确排除 `torch`、`transformers`，与当前 SAM 推理实现冲突。此前源码集成通过不能代表安装版可用。
- 后端打包纳入 SAM 所需依赖与固定 revision `70c1a07f894ebb5b307fd9eaaee97b9dfc16068f` 的权重/配置；构建环境可通过 `SHIYIN_SAM_MODEL_DIR` 指定模型目录，否则读取该 revision 的本地缓存。安装版从 `_internal/models/sam-vit-base` 离线加载，不依赖开发机缓存。深度图、深度视频固定资源未变更。
- 点击节点图片或重新编辑均直接覆盖整个同源应用窗口；嵌套画布将编辑器挂在顶层，维护原画布窗口的消息来源校验。右上角提供“返回画布（Esc）”和“保存并返回”；返回不提交未保存修改，成功保存才更新节点与下游。
- 分割失败清除有效蒙版并禁用保存；保存参数与已完成预览必须一致。导出/上传显示服务端具体错误，失败保持编辑器打开。关闭时释放会话；禁止处理中撤销和替换原图，避免保存参数竞态。
- 新增 `tests/support/cutout_editor_flow_check.cjs`：嵌套全屏尺寸、返回按钮、Esc、分割失败、导出失败留页、保存返回、会话释放均通过。原真实浏览器集成的分割/保存/再次保存/下游替换/刷新恢复通过。
- 后处理/API 单元测试 9 项通过；新引擎真实 SAM 推理得到原尺寸 `1112×869` RGBA，alpha 范围 `0–255`。
- 新增 `tests/support/cutout_packaged_check.py`：使用独立 8793 端口及 `.codex-tmp/cutout-frozen-data`，禁用 Hugging Face 联网，验证冻结后端的真实分割和透明导出。
- 冻结后端实际验证已通过，分别运行默认 GPU 和 `CUDA_VISIBLE_DEVICES=-1` 的 CPU 路径，均得到原尺寸 RGBA 与完整透明通道。测试进程已退出。
- 打包排除与 SAM 无关的训练/音视频模块，特别是 `torchcodec`：此前其模块被收集但元数据未被收集，导致 Transformers 图片处理初始化也失败。保留 `console=True` 标准流，宿主已有 `CREATE_NO_WINDOW` 与日志重定向，不弹额外窗口。
- 隔离后端产物位于 `.codex-tmp/cutout-packaged/canvas-backend`，约 4.53 GB（未压缩，含本机构建环境的 CUDA PyTorch 与 SAM 权重）。后续正式发布应从干净提交构建；不能把本次工作区中的其他任务改动一并发布。
- 本次仅构建隔离后端验证产物，不运行全量安装包构建，不覆盖正在运行的软件；热更新尚未发布。

## 局域网热更新发布（2026-09-17）

用户随后明确要求发布，现已完成：

- 发布序号 `20260917195929`，最低桌面 `2.0.1`，分发地址 `http://192.168.0.24:3011`。
- 从 `9c9a3b54` 的干净工作树 `D:/SHIYIN-Builds/cutout-release-9c9a3b54` 构建，包含重新编译的后端与前端。桌面宿主源码指纹及二进制 SHA-256 均与上一发布一致，复用已验证宿主。
- 更新包 8081 个文件、2,952,356,568 字节（约 2.95 GB），SHA-256 `6d8f8224047094c9c8944f2607812680e6af77da474e14d31f0ce31de60da135`。
- 发布后端 GPU/CPU 离线分割及原尺寸透明 PNG 导出通过；公开清单/补齐计划 Ed25519 签名、局域网整包下载 SHA-256 均通过。
- 真实客户端 EXE 在该工作树 `.build/updater-smoke` 独立目录执行 `--prepare-hot-update`，返回 `ok:true`、版本 `20260917195929`；完成下载、验签、逐文件校验和解包，未应用到当前用户安装目录。
- `fixed-models.json` 发布前后 SHA-256 一致，深度图、深度视频固定资源未修改。主工作区其他未提交改动未纳入发布。
- 验证报告：该工作树 `dist/hot-update/20260917195929/verification.json`；总发布记录同时追加至任务 175，并单独暂存本次新增段落，保护其已有未提交修改。
