# 一键复刻紧凑平台菜单与深度控制

状态：已完成并发布 `20260917224212`。分支 `fix/cutout-pose-reference`，源码提交 `75db0c92` 已推送。
下一步：用户在软件中检查并应用更新，使用新布局。
TODO：[x] 实现；[x] 运行与浏览器验证；[x] 提交推送；[x] 隔离发布与校验。
阻塞：无。

用户已确认 HTML 示意布局：合并平台和模型为“平台”按钮，两级菜单选择；模式下拉右侧增加“深度控制”。复用真实高级深度控制面板，节点独立参数需持久化并进入生成输入，骨架模式禁用深度按钮。经典与智能画布共用实现。保护其他任务未提交改动；仅从干净提交发布，不构建全量安装包，不改固定模型。

[CODEX_LONG_TASK_CONTINUE_V3]

发布：干净工作树 `D:/SHIYIN-Builds/pose-controls-75db0c92`；纯前端包 160 文件、23,557,314 字节，SHA-256 `bbd9b6b1eee21d289c8910a134ff5982976de09817af82ed2b919dd2e1134da2`。公开目录和补齐计划签名、整包下载与逐文件校验通过；发布 JS/CSS 与源提交一致；固定模型哈希未变。报告位于工作树 `dist/hot-update/20260917224212/verification.json`。未代替用户应用更新。

实现：`canvas-special-nodes.js` 合并平台/模型为固定定位两级菜单，支持键盘、越界约束和节点卸载清理；模式右侧深度控制复用已有高级面板。`poseDepthControls` 保存节点独立参数，生成前保证读取调整后的 PNG；目标图片切换时取消旧面板的延迟写入。样式位于 `pose-replicate-node.css`。

验证：JS 语法与 diff 检查通过；现有并发复刻运行时测试通过；新增 `tests/js/pose_replicate_controls.browser.cjs` 使用真实共享节点代码和 Edge 浏览器，隔离模拟上传/生成接口，验证平台模型选择、PNG 像素实际变化、生成输入引用、JSON 重载、智能画布适配、恢复默认、骨架禁用、窄窗口菜单和 Esc，页面 JS 错误为 0。已查看浏览器截图确认控件顺序。44 项 Python 定向测试中 41 通过、3 失败；在修改前干净工作树 `a087a8ca` 重跑，同样的 3 项失败，属于现有静态契约测试失配（经典连线、深度节点、旧四端口描述），未新增回归。未调用收费生成接口。
