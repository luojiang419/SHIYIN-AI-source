# 热更新遗漏版本自动补齐

[CODEX_LONG_TASK_CONTINUE_V3]

状态：已完成并发布
下一步：客户端在软件内检查更新并应用 `20260918084551`；后续纯前端更新发布后，遗漏版本的客户端会先自动应用最近完整检查点，再跨重启追到目标版本。
TODO：[x] 服务端按客户端序号选择完整检查点；[x] 回归测试；[x] 构建并部署分发中心；[x] 发布完整热更新检查点；[x] 公开目录与客户端补齐验证；[x] Git 交付。
最近验证：分发服务 20 项测试通过；已部署分发中心并验证旧基线先收完整检查点、检查点后收目标、目标版本无重复更新。完整检查点 `20260918084551` 共 8081 文件、2,952,361,797 字节，SHA-256 `cb0457e2591c24bb686eb80f8a743a509560d1719d1669f37bedb4bbb36e9764`，包含桌面、后端和前端；公开清单与补齐计划签名、整包下载哈希通过，真实客户端隔离准备返回 `ok:true`。branch/commit：`fix/hot-update-catchup` / `ff1e4093`。阻塞：无。

## 目标

2.0.0 及后续客户端即使错过若干热更新，也能从软件内发起一次更新流程，先自动应用最近的完整热更新检查点，再跨重启追到当前目标版本。不得恢复旧 hot-bootstrap/hot-updater 路线，不构建全量安装包，不改动固定模型。

## 验收标准

- 早于完整检查点的客户端先收到最新完整检查点，补齐桌面宿主、后端和当时的完整前端。
- 已达到完整检查点但落后于目标的客户端直接收到最新包。
- 已是目标版本的客户端不收到重复更新。
- 清单签名、补齐计划签名、最低桌面版本和能力门禁继续生效。
- 当前分发中心部署新逻辑，并发布新的完整热更新检查点以修复已经跳过旧完整包但版本号较新的客户端。
