# 批量换款与一键复刻执行一致性实测

状态：执行链路与作品预览偏差已修复，真实批量页面任务已完成；2K 生成画面保真仍不稳定，不能宣称每次生成均完全正确。
下一步：已安装 2.0.1 尚未应用本次源码；热更新需隔离工作区中其他任务的未提交改动。
TODO：差异定位 [x]；实现修复 [x]；页面与接口回归 [x]；真实生成和视觉检查 [x]；Git 精确提交 [x]；推送 [x]。
最近验证：相关 pytest 97 passed；隔离 Chromium 批量页回归通过，覆盖人物/专业深度切换、逐款细节参考数量、分组生成、作品完整预览和响应式；最新前端/批量契约 61 passed。已安装 2.0.1 的 A002 实际生成从白裤换成橙裤并归档；当前源码页面完成真实 4K 换款，见下文视觉结论。
branch/commit：`fix/hot-update-catchup` / `1c89a0e0`、`9c129488`；已推送至 `origin/fix/hot-update-catchup`，其他任务改动未暂存。
阻塞：无代码阻塞；模型随机输出的保真度无法仅凭共用接口保证，2K 本例出现明显重画。

[CODEX_LONG_TASK_CONTINUE_V3]

## 目标

批量换款作为一键复刻的批量操作页面，使用相同的深度图模式、调节算法、提示词编译和生成接口；按当前服装逐件绑定面料细节，且所选模型须支持实际参考图数量。通过真实生成检查换装结果，而非仅验证接口返回成功。

## 验收

- 与节点一致地读取软件深度模式与全局参数，并在模式变化后刷新控制图。
- 深度图调节在相同输入和参数下与节点产生相同像素结果。
- 模型参考图上限包含面料细节，不能静默选到容量不足的模型。
- 实际批量换款生成有可见服装变化，任务审计确认复刻模板、参考角色和结果归档。

## 本轮结果

批量页面现在读取 `/api/app-settings` 中的深度模式、模型档位和全局深度控制值。旧缓存或设置变化时重新准备控制图；人物模式使用 `/api/person-depth/estimate`，专业模式使用 `/api/depth/estimate`。批量深度调整的灰度/LUT、gamma、边界填充与节点算法一致。后端复刻入口也按当前专业/人物质量档选择就绪组件，不再无条件要求高精度人物组件。逐款带面料细节时，模型选择计入第 4 张参考图。

当前源码真实任务：2K `canvas_img_3261697ecc4a463d97965baa8323385e`，4K `canvas_img_9494605833cd423384827d44cad5d791`。两次均为 `pose-replicate.v3.5` 内置模板、同一目标/深度/服装/细节输入，服装和细节以原像素发送并成功按测试款号归档。2K 出现人物和街景整体重画，未满足目标图保留要求；4K 保留了目标图的姿态、墙面、手提包和鞋，换成棕色参考裤，达到本次视觉验收。2K 与旧节点 4K 样本的提示词、目标和深度哈希相同，因此本例质量差异属于生成分辨率/模型随机输出，不是批量页使用另一套提示词。

本轮隔离结果位于 `.codex-artifacts/batch-outfit-real-20260918/` 与 `.codex-artifacts/batch-outfit-real-4k-20260918/`，仅供本机检查，不提交图片和密钥。3000 端口是已安装 2.0.1，本次源码修改尚未进入该客户端。

## 当前源码页面实测

在隔离的 3015 源码服务上，从批量页面新建 `BATCH-UI-LIVE-192`，通过页面上传 `目标图 (2).jpg`、`服装参考 (2).jpg` 和逐款 `腰头细节.jpg`，选择 Shiying `gemini-3-pro-image-preview`、`3:4`、`4K`、高质量后点击生成。页面先显示占位，完成后回填 1 张作品。`generation_history` 的 operation 为 `pose_replicate`，任务携带目标、控制图、服装、细节四张参考图，尺寸为 `2448x3264`；结果 `fabric_enhancement.status=applied`，按款号保存为 `.codex-artifacts/batch-ui-live-runtime/data/exports/batch-outfit/BATCH-UI-LIVE-192/BATCH-UI-LIVE-192-1a32affa90-01.png`。目视确认保留目标墙面、动作、手提包和鞋，蓝裤替换为棕裤。

这次页面实测还发现右侧主预览的纵图在 298px 舞台内被布局成 528px 高并裁切。`static/css/ecommerce.css` 已将图片绝对定位到固定大小的预览按钮内，`object-fit:contain` 完整显示；真实页面重载后整张人物可见，隔离 Chromium 增加了图片高度等于预览按钮高度的回归断言。源码服务仅监听 `127.0.0.1:3015`，使用独立数据目录，未替换 3000 安装版。
