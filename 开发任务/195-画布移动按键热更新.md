# 画布移动按键热更新

状态：已完成并发布。  
下一步：用户在客户端检查并应用更新。  
TODO：无。  
最近验证：`node --check static/js/canvas.js`、`npm run canvas:engine-build`、`git diff --check` 通过；公开目录与补齐计划 Ed25519 签名验证通过，局域网包 Range 下载返回 206。  
branch/commit：`fix/hot-update-catchup` / `3f51e6e4`（发布源）。  
阻塞：无。  

目标：画布选择工具长按后允许用户选择用左键或中键拖动画布，保留不同操作习惯。

发布：纯前端热更新 `20260919110324`，只包含 `app/web`，未重编桌面宿主、后端或固定模型。发布包 `SHIYIN-Hot-Update-20260919110324.shiyin-update` 为 23,559,676 字节，SHA-256 `b0fab642760d43f23f2901f42485044e941991e2f54a35a8f0f231ea92fc3677`。固定模型清单 SHA-256 保持 `b6af134be3a3e5f18f07d05dfc00bb9fcfbc05301858c9cbf80ee79affdb68a8`。

[CODEX_LONG_TASK_CONTINUE_V3]
