# 重置软件发布并建立 2.0.2 基准

状态：完整热更新已发布；全量安装器校验失败，尚未发布。

下一步：修复全量安装器 Windows 产品版本资源后重新构建、校验并导入 `full-2.0.2`。

分支/提交：`fix/hot-update-catchup` / `3e5a05f0`（全能模式参数与动作布局修复）。

最近验证：`python -m pytest tests/test_ecommerce.py -q -k "large_universal_reference_analysis or ecommerce_analysis_preview_keeps_user_type_and_role_order"`（3 passed）；`node tests/support/ecommerce_universal_action_layout_check.cjs`（passed）。

TODO：

- [x] 清理历史软件热更新与旧全量安装包，保留固定模型、签名密钥和分发配置。
- [ ] 从当前工作区构建并验证新的全量安装器。
- [x] 构建并发布完整热更新，使已安装客户端收到更新。
- [x] 验证公开目录、签名、下载、客户端更新准备和缓存上限。

验收标准：分发中心只保留新的 `full` 基准包和对应的完整 `hot` 检查点；新安装/覆盖安装可下载全量包，已有客户端检查更新可获取完整检查点；固定模型文件未变。

[CODEX_LONG_TASK_CONTINUE_V3]

## 本轮修复（2026-09-19）

- 全能模式分析预览接口沿用生成接口的 512 KiB 逐图分析参数上限，避免分析结果回传时被旧的 20 KiB 通用限制拒绝为“功能参数过大”。
- 素材坞默认将“开始生成”与添加素材按钮垂直居中对齐；只有错误或分析预览可见时才采用上方信息、下方按钮的两行布局。

## 发布记录（2026-09-19）

- 已删除 `full-2.0.0` 与 40 个历史 `hot` 发布，删除 41 个软件 blob，共 31,448,183,386 字节；备份为 `.build/195-baseline/distribution-before-reset.db`。固定模型发布和 5,164 个共享 blob 未变。
- 完整热更新 `20260919181105` 已发布：协议 3、最低桌面版本 `2.0.0`、8,083 个文件、包大小 2,952,399,925 字节、SHA-256 `db4c46ecc9977527bf1e76e850897ff769a3968e48f3d44d0d00618cf8d3bfaa`。
- 使用 `X-Shiyin-Version: 2.0.0 / 20260918135459` 和 `package-v3,fast-extract-v1` 实测 `/v1/catalog` 返回该检查点；分发中心导入状态为“校验与发布完成”。
- `SHIYIN-AI-Setup-2.0.2.exe` 已生成但 Windows `ProductVersion` 为空，`verify-installer-artifact.ps1` 未通过，故未导入为全量包。
- 完整热更新 `20260919191706` 已发布：协议 3、最低桌面版本 `2.0.2`、8,083 个文件、展开 `4,565,458,931` 字节、包大小 `2,952,400,273` 字节、SHA-256 `5d001f2142d0e74473d9e4076fcf3a82e33e89bc7902b563f590cba63aef5ad9`。更新说明为“修复全能模式素材卡片下方被裁切的面料细节入口”。
- 使用 `X-Shiyin-Version: 2.0.2 / 20260919181105` 及 `package-v3,fast-extract-v1` 验证 `/v1/catalog` 返回新序号，Ed25519 签名校验通过；更新包 `Range: bytes=0-1048575` 返回 `206`、`1,048,576` 字节。固定模型未参与本次发布；未构建全量安装器。`.build` 为 0.24 GiB，`.codex-tmp` 为 0 GiB，均低于缓存上限。
- 纯前端热更新 `20260919212437` 已发布：仅 `app/web`，160 个文件，包大小 `23,564,149` 字节，SHA-256 `f6ad77907459519d95c5b70a34281a4f605af4e9c37367a6e119ecc20d59f152`。内容为移除全能模式细节图在唯一归属商品时重复显示的下拉项；多商品时仍保留归属选择。使用 `20260919191706` 客户端版本头验证目录签名、包哈希及 1 MiB Range 下载通过；未重新打包桌面宿主、后端或固定模型。
- 完整热更新 `20260919231539` 已发布：协议 3、最低桌面版本 `2.0.2`、8,083 个文件、展开 `4,565,459,046` 字节、包大小 `2,952,399,952` 字节、SHA-256 `0ae9f18c248166cab08d4ab066199698a3e5bdb674f69292d5a826fbfd4e9d27`。包含全能模式逐图分析参数上限修复及当前工作区已授权的桌面宿主、后端和前端更新。分发中心状态为 `published`；使用 `2.0.2 / 20260919212437` 客户端版本头验证公开 catalog 与补齐计划 Ed25519 签名，1 MiB Range 下载返回 `206`。固定模型未参与本次发布；未构建全量安装器。`.build` 为 0.24 GiB，`.codex-tmp` 为 0 GiB。
