# 全能模式分析链路稳定化

状态：已完成。

下一步：无。

TODO：

- [x] 生成请求不再接收 `reference_analysis`。
- [x] 预览与任务通过服务端素材指纹缓存复用逐图分析。
- [x] 覆盖缓存命中、缓存缺失回退与参数边界的自动化测试。
- [x] 用多张真实参考图完成一次全能模式生成并核验结果。

最近验证：`python -m pytest tests/test_ecommerce.py -q` 为 138 passed、2 项既有失败（Lookbook 计数优先级与默认平台列表）；`python -m py_compile main.py` 通过。真实六参考图任务 `ecommerce_c2b5601b92fd42c1add3a19f2bcab11f` 成功，Shiying Gemini 3 Pro Image 生成 `1696x2528` 图片；AI 助手上游空 502 时已验证规则回退不阻断生成。

分支/提交：`fix/hot-update-catchup` / `b2763edd`。

验收标准：前端任务请求不包含逐图分析正文；任意数量的合法参考图不因分析正文体积失败；预览后生成命中同一份服务端分析，未预览也能由任务自行完成分析并生成。

[CODEX_LONG_TASK_CONTINUE_V3]

## 方案

逐图分析是服务器基于上传文件与角色配置得出的派生数据，不属于浏览器提交的用户输入。预览接口调用分析器后写入按文件身份、提示词和模型版本确定的有界 LRU 缓存；生成任务重新调用同一分析器，命中缓存即复用，失效或缺失则重新分析。浏览器只消费用于显示的预览摘要，任务请求仅保留原始参考图和轻量配置。

## 真实验证

- 使用人物、连衣裙、鞋、项链、姿势、门店场景共 6 张参考图；类型所有权、顺序和无冲突计划均已写入任务结果。
- 视觉分析上游当时对 6 项请求均返回空 502。任务没有向界面抛出生成错误，而是按用户明确的角色映射、素材与固定约束继续生成，最终由 `gemini-3-pro-image-preview` 成功返回图片。
- 成图保存在 `案例/全能模式自动提示词/20260920-012845/生成图/全能模式自动提示词生成图.jpg`，测试报告保存于同目录。

## 热更新发布（2026-09-20）

- 完整热更新检查点 `20260920015928` 已发布，最低桌面版本 `2.0.2`，包内宿主 `ProductVersion/FileVersion` 均为 `2.0.2`；包含桌面宿主、后端与前端，不是全量安装包。
- 包含 8,081 个文件，包大小 `2,952,375,239` 字节，SHA-256 为 `65801d31dcb6cae0aa7ed876ff5f73a915c1bab7a160087088dd866446e999c0`。公开目录和补齐计划 Ed25519 签名均已验证；以 `2.0.2 / 20260919231539` 客户端版本请求目录，1 MiB Range 下载返回 `206` 且内容范围与包大小一致。
- 发布从隔离工作树的 `b2763edd` 代码修复构建；为保持项目 `2.0.2` 分发基线，复用已核验的 `2.0.2` 桌面宿主，未携带主工作区其他未提交功能。固定模型未参与此次发布。`.build` 为 `0.218 GiB`，`.codex-tmp` 为 `0 GiB`。
