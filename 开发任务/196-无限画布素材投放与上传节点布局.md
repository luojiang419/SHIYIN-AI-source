# 无限画布素材投放与上传节点布局

状态：已完成并发布，待客户端页面复核。  
下一步：客户端应用 `20260919143036` 后拖拽素材库资源到图片节点、电商节点或专用图片上传格复核。  
TODO：[x] 素材库投放到图片上传节点；[x] 素材库投放到节点图片上传格；[x] 上传节点默认尺寸与竖图预览调整；[x] 定向验证。  
最近验证：`node --check static/js/canvas.js`、`python -m pytest -q tests/test_canvas_portrait_media_node_layout.py tests/test_canvas_asset_library_drop_targets.py`（8 passed）、`git diff --check` 通过；公开目录与补齐计划 Ed25519 验签通过，公开整包下载 SHA-256 一致。隔离 fixture 的 Playwright 入口脚本在 `page.goto(..., waitUntil=load)` 30 秒超时，未获得浏览器截图；此失败未涉及本次逻辑断言。  
branch/commit：`fix/hot-update-catchup` / `aeea258e`。  
阻塞：无。  

[CODEX_LONG_TASK_CONTINUE_V3]

目标：素材库资源拖到画布具体图片接收位置时直接替换或填入该位置，不显示模糊的全画布拖放遮罩；未投到接收位置时保留创建素材节点的现有行为。上传节点默认宽度与选中后的编辑浮层一致，底部编辑面板完整显示；9:16 素材仍在 16:9 预览框内以 `contain` 完整显示。

发布：纯前端热更新 `20260919143036` 已从当前工作区直接发布，未创建隔离工作树，包含 160 个 `app/web` 文件，包 `SHIYIN-Hot-Update-20260919143036.shiyin-update` 为 23,563,380 字节，SHA-256 `e57e9aaad0c87c36132d4a4ff884cbe15e7f0948d4fe90115b961d892acb9a3f`。对已到完整检查点 `20260919134114` 的客户端，`/v1/catalog` 返回本版本；公开 Range 请求为 206，完整下载哈希一致，目录与补齐计划 Ed25519 签名有效。未重编桌面宿主或后端，未变更固定模型。用户已要求后续变更直接在当前工作区自动发布热更新，禁止创建隔离工作树。
