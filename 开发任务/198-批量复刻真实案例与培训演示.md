# 批量复刻真实案例与培训演示

状态：真实页面完成 A 三款、B 一款与画布 B 对照，共 5 张；六标签 HTML 和 56 秒操作视频完成。用户指出脚踝溢纹后，源码边界修复与同底图复测完成，报告已加入修正版和失败证据。
下一步：续验已确认纽扣周围纹理覆盖不连续，质量验收仍未通过。需修正材质覆盖与重复增强，部署后再做真实软件验收；本轮未发布、未替换已安装客户端。新增报告板块的浏览器显示复查因本地 URL 安全策略拒绝而未完成。
TODO：真实页面操作 [x]；结果审阅 [x]；HTML 培训与汇报 [x]；动态演示 [x]；脚踝溢纹修复 [x]；验证交付 [x]。
最近验证：面料/细节专项 39 passed；批量与复刻运行 15 passed；Python 编译通过。六标签窄屏无页面横向溢出、图片放大 650 原像素、Esc 关闭、播放/暂停/逐步通过；页面无控制台错误。MP4 为 56 秒 / H.264 / 1600×1000。缓存 .build 0.239 GiB、.codex-tmp 1.043 GiB。
branch：feat/universal-product-lookbook（工作区有其他任务修改，仅新增本任务文件）。
限制：案例纹理可见，但裤长、特殊腰头与牛仔下摆仍有设计偏差，不宣称全部商品准确。软件修复尚未部署；交付修正版是直接重放已修复的软件增强函数，不冒充安装客户端新生成。

[CODEX_LONG_TASK_CONTINUE_V3]

## 2026-09-20 续验结果（以本节为最新状态）

真实软件原图通过 generation_history 对应到 SHIYIN-001403-20260920.jpg，最终输出为 fabric_0c419eb12a2e4b5c89dd04913deab09e.png。记录确认旧安装版增强执行两次。模型原图腰头已经偏软，最终输出在纽扣上方及周围保留一片未增强布面，相邻布面增强后放大了清晰度落差。不是整个三角布片均未增强，也不能说后处理把纽扣突然模糊化。

裁片坐标 (1500,850,2050,1250)：纽扣上方 50×30 布面框变化为 0，相邻 100×60 布面框变化比例 95.37%，右扣中心 20×20 保持不变。变化图不等于内部掩膜，零变化也可能含增强信号恰为零的像素。复现工具 tools/verify-training-button-coverage.py 只读数据库和图片，生成 evidence/button-coverage-audit.json 与原像素对照。报告美工验收板块和 ZIP 已更新，没有外部修图冒充软件结果。

本轮验证：test_fabric_enhancement.py、test_pose_replicate_fabric_detail.py、test_pose_replicate_runtime.py、test_batch_outfit.py 共 54 passed in 3.43s；HTML 本地资源和 ZIP testzip 通过。浏览器拒绝 file URL 访问，未绕过策略，新增板块显示复查未完成。测试通过不等于材质验收合格，颜色/质感与纹理连续性仍未通过。下一步为修复并部署后的真实流程验收。

## 目标
使用 D:/data/图片/案例/A 与 b 原始素材，在软件真实批量换款页面生成。A 为同一街拍目标换三款上衣，A 产品细节仅绑定 A 产品；B 为街拍目标换棕裤并绑定腰头/面料细节。自动选择 4K、高质量及适合原图的画幅。模型输出不可承诺完美，保留实际质量审阅与证据。

交付为本地可携带 HTML：领导汇报、销售速用、美工复核、真实案例与细节对比、动态操作演示。面向使用者讲操作，不讲算法。动画区分真实页面截图与教学演示，不能伪称连续录屏。

## 真实执行

3000 客户端的批量页新建 `培训-A-上衣三款-0920`、`培训-B-棕裤细节-0920`，保留 A001/A002；每组按钮实际提交，未点击会重跑旧任务的一键生成全部。A 三款为一次组内批量提交。平台 shiying / gemini-3-pro-image-preview，4K、高、跟随原图。A 返回 3584×4800；B 返回 3712×4608；单任务模型记录耗时 47.290、48.431、60.835、53.836 秒。B 绑定腰头细节；A 细节只属于 A 产品。

画布 `培训演示-一键复刻-0920`（ID `5cb247a0e8aa42dbbf0b71953c85c32f`）实际创建节点、上传 B 三图并生成。首轮补充要求被助手增量身份校验拒绝，未产生生图任务 ID；清空补充后内置模板成功，界面用时约 64.4 秒，原生模型图 `SHIYIN-001401-20260920.jpg`。

## 用户指出脚踝纹理溢出后的修复

旧图 `assets/b-node-result.png` 的裤脚下皮肤阴影被颜色粗掩膜选中，并叠加了细节图和整裤参考两层织纹。模型原图没有该织纹，确认是本地后处理问题。

`canvas_core/fabric_enhancement.py` 新增基于当前生成图的边界细化：衣片内部作为前景种子，不确定边缘需具有内部色度支持；GrabCut 后只在旧候选范围内收缩，并留窄边保护，无法可靠细化则跳过。没有使用本例手工轮廓、固定裤脚高度或固定肤色。`main.py` 的复刻分支优先只使用专属面料细节，没有细节才回退整衣，避免双重叠加。

同两张模型原图重放新函数，产物 `assets/b-node-fixed.png` 与 `assets/b-product-fixed.png`。节点旧图两处紧贴裤脚的皮肤验证框改变 370 / 180 像素；修复后均为 0，双脚验证框与新掩膜外也为 0。验证框仅用于检查，不参与掩膜构造。局部人工复查确认纹理未再落在脚踝阴影，仍不能将该验证外推成所有肤色/服装都准确。

## 交付与复现

- `案例/批量复刻培训-20260920/index.html`：七板块报告、原像素查看、案例对照、错误与修复对照、讲稿及练习。
- `操作演示.mp4`：真实素材与截图编排的 56 秒教学动画，无音轨；HTML 内另有交互播放、逐步与倍速。
- `evidence/generation-records.json`、`node-record.json`、`boundary-fix-audit.json`：生成和修复分层审计，无平台凭据。
- 收集：`python tools/collect-batch-training-case.py`；重放边界修复：`python tools/verify-batch-training-boundary.py`；构建报告：`python tools/build-batch-training-report.py`；视频：`python tools/render-batch-training-video.py`。
- 原图、生成图和视频仅本地交付，不提交远程。源码、复现工具与本任务文档精确提交；未全量编译、未发布热更新。

## 文案调整

按用户澄清，采用语序正常、表达明确的书面中文，保持培训汇报语气，不改成聊天口吻。已调整七个板块中的标题、说明、图注与教学步骤，重新生成 HTML 和 56 秒视频，浏览器核对一键复刻标题及正文已更新，ZIP 同步重打包。

## 总览轮播与主题统一

已删除汇报讲稿板块；总览改为五张真实成图自动轮播，提供缩略图、上一张/下一张、暂停/播放及原图查看。切离总览、后台页面或打开大图时暂停计时，尊重减少动态效果设置。明暗主题采用软件 studio-unified.css 的暖米色/炭黑色值，支持手动切换和本地记忆；教学视频同步为软件暖色配色。

验证：浏览器观察到自动轮播由 02 切至 04；手动切图、暂停、原图查看、主题切换和刷新记忆正常。六个板块在 390 宽视口均为 clientWidth=scrollWidth=375，无页面横向溢出；轮播图片无损坏，控制台无错误。JS 语法检查通过。报告及 ZIP 已同步更新。

## 统一图片浏览操作

全部案例、轮播及动态演示图片使用统一全屏浏览器：滚轮以鼠标位置为中心缩放、拖拽平移、左右键在当前板块内切图、380ms 叠化、原图大小、适应窗口、双击重置和 Esc 关闭。新图解码后才切换，快速切图丢弃过期加载结果，减少动态效果偏好下取消动画。

验证：浏览器真实滚轮操作将比例从 0.171 增至 0.312，拖拽改变平移坐标；右键方向键将总览第 5 张切到第 1 张且页面仍为总览；案例图打开后显示 1 / 12。JS 语法检查通过，交付 HTML 与 ZIP 已更新。

## 100% 与特色设计对照

新增独立 100% 细节对比标签，七组十八张原像素裁片：裤装斜纹、双扣腰耳、皮牌扣件、后袋车线、豹纹绒感、牛仔胸袋和毛边下摆。裤装并排展示实拍、批量生成修复版、一键复刻修复版；明确拍摄比例不同。提供 100% / 适应窗口切换、完整原图下载及统一全屏查看。裁切坐标记录于 evidence/detail-comparison-crops.json。

重要纠正：重新检查 a-style-c.jpg 完整实拍，原款确有毛边。此前“新增毛边”判断错误，旧的下摆裁片实际取到门襟；已修正裁切、正文及轮播标签，并重新渲染视频。历史记录中相反说法以本次复核为准。

验证：18 张裁片逐像素等于原文件对应区域，无缩放修饰；浏览器测得全部裁片 CSS 宽度与自然宽度一致，适应窗口切换正常；全屏打开成功，390 宽视口无页面横向溢出，控制台无错误。

## 裤装颜色与质感验收更新

用户指出 compare-2-2.png 与实拍颜色质感不符。复核为生成偏红偏灰、纹理更细更平，未达到原款材质一致性要求。已在总览、案例、轮播和细节对比中明确标注未通过；边界溢纹修复不等于面料还原合格。本次只更新真实验收结论，没有声称修好了颜色或重新生成图片。

## 真实软件重新生成（完成两次，材质验收未通过）

用户明确要求仅使用真实软件流程。此前误用 imgx 的两张图片已移至 案例/非软件流程-imgx-不用于培训-20260920，不纳入培训或软件验收。

已通过安装软件 3000 端口批量页原 B 组点击生成，模型 shiying/gemini-3-pro-image-preview，4K/高/跟随原图。新任务 batch_outfit_585dcea76c27485fbee5b505658fd08c，模型记录 51.469 秒，输出 fabric_a58a364c05a24625928bae776733306d.png，保存为 assets/b-software-rerun.png。安装版后处理仍叠加两次。裤长改善，但颜色仍偏红、纹理平铺感明显，未通过。

随后通过培训画布 5cb247a0e8aa42dbbf0b71953c85c32f 的一键复刻节点，在补充要求中填写颜色与真实织物要求，已点击提交，等待结果。保留软件截图与软件任务 JSON 证据，不在图外修图冒充软件结果。

节点补充要求生成完成，runMs=90913，输出 fabric_0c419eb12a2e4b5c89dd04913deab09e.png，保存为 assets/b-software-node-rerun.png。对照腰头原像素仍偏红、织纹平铺感明显，未通过。报告新增两次真实软件重生与实拍并排对比。后续需要修正软件材质流程并在实际运行版本验证，不能靠外部增强或反复抽图宣称可靠。
