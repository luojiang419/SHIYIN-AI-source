# 视频生成培训教程

状态：已完成。视频页已改为真实案例瀑布流，补齐视频节点自动解析/润色、端口含义、深度视频和快速操作教程，离线套件与 ZIP 已重建。
下一步：使用 `案例/SHIYING培训教程-20260920/index.html` 的左侧“视频生成”入口演示；案例卡片可点击播放。
TODO：案例瀑布流 [x]；H3/可灵对比 [x]；深度视频实片 [x]；自动解析/润色 [x]；端口说明 [x]；imgx 配图 [x]；套件重建 [x]；浏览器验证 [x]。
最近验证：新增 imgx 文生图 1/1 成功（16:9、2K、text-to-image）；`python -m py_compile`、本地资源校验和 ZIP 完整性通过。Playwright 检查桌面与移动端：六个页签和 14 个视频入口正常，原位播放、全屏和切换自动暂停通过。正式局域网服务的 Range 请求返回 `206` 与精确 `Content-Range`，浏览器从视频开头跳转至 75% 时间点成功，无横向溢出、脚本错误或资源请求失败。
branch/commit：`feat/universal-product-lookbook` / `0c22f77c`（功能提交已推送；工作区其他任务改动未纳入）。

[CODEX_LONG_TASK_CONTINUE_V3]

## 目标

在现有“批量换款 / 全能模式”离线培训套件中增加“视频生成”菜单和页面。使用已完成的“豹纹”画布工程说明复杂视频制作链路：生成线稿分镜、生成资产图、分镜合成画面、按组生成视频；用 SVG 动画解释深度视频如何提供逐帧空间约束，并面向销售与视频制作人员说明视频生成节点、生成视频节点和无限画布常规视频生成的选择方法。

## 交付内容

- 新增 `案例/视频生成培训-20260921/index.html` 构建源，引用用户提供的豹纹工程全景截图与项目卡片截图。
- 增加两段内嵌 SVG 动画：四段式影视流程、原视频到相对深度视频再到下游生成约束。
- 侧栏菜单扩展为“批量换款 / 全能模式 / 视频生成”，套件构建后第三页位于 `案例/SHIYING培训教程-20260920/视频生成/index.html`。
- 深度视频说明明确它约束逐帧空间关系和遮挡连续性，并保留模型不能保证逐像素、逐帧完全相同的使用边界。

## 2026-09-21 案例与节点教程增强

- 删除截图标注的“销售怎么讲 / 制作怎么用”卡片及文字，将“豹纹案例”标签改为“案例”。
- 删除“案例”页顶部“不同入口，解决不同的视频任务”说明区，页签打开后直接呈现案例瀑布流。
- 全部案例视频改为点击封面后在卡片内原位播放，播放器保留原生控制条并提供独立全屏按钮；打开新视频时自动暂停上一条。
- 深度视频页新增 `D:/data/图片/案例视频/深度视频2` 案例二，展示原视频、深度视频和复刻视频三列对照，沿用原位播放与全屏交互。
- 局域网教程服务改用 `tools/serve-training-tutorial.py`，支持 HTTP `Range`/`206 Partial Content`，修复视频无法拖拽进度条快速预览的问题。
- 使用 `D:/data/图片/案例视频` 中标注的文件和文件夹整理 11 个可播放视频入口；超大豹纹广告仅对教程副本进行 H.264 网页转码，源文件不变。
- 案例页采用响应式瀑布流，包含三个通用案例、完整豹纹广告、H3 本地预览与可灵高质量成对对比、单图自由创作输入/输出。
- 深度视频页展示源视频、提取的深度序列和两个复刻结果，可分别点击播放。
- 按当前实现说明自动解析与自动润色：素材顺序解析、模型规则适配、时长/画幅/分辨率联动、全部引用覆盖校验、外接提示词合并和可选联网增强。
- 端口说明覆盖分镜图、演员、服装、道具和提示词，并区分常规“视频生成节点”与影视“生成视频节点”的使用场景。
- 新增 6 步快速操作和问题定位顺序；新增 imgx 配图保存于 `generated-images/20260921-video-tutorial-intelligence`，并复制到教程资产目录。
