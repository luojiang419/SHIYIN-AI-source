# 移除画布旧状态缓存防回滚

状态：已完成，已提交并推送
当前阶段：3/3 验证与交付
最后更新：2026-09-21

## 当前状态

已确认 `static/js/canvas.js` 会并发读取服务端工程和 IndexedDB 的 `canvas:<id>` 快照；快照晚到时仍可执行 `restoreCanvasPage`，以旧节点、连线、视口和撤销栈覆盖当前状态。本任务已移除画布持久快照读写与恢复，并清理历史记录。

## 下一步

无。

## TODO

- [x] 定位旧状态覆盖链路
- [x] 移除画布持久快照机制
- [x] 静态、单元和浏览器验证
- [x] Git 提交
- [x] Git 推送

## 验收标准

- 打开画布仅以当前服务端工程初始化，不读取或应用本地画布快照。
- 任何已有 `canvas:*` 快照和恢复指针会被清理。
- 旧 IndexedDB 记录存在时，浏览器实际打开仍显示服务端最新内容。
- 正常画布保存、远端同步和同一页面内驻留不回归。

## 已完成内容

- `canvas.js` 已删除画布快照捕获、IndexedDB 读取、`restoreCanvasPage` 覆盖分支、离开/点击 checkpoint 及宿主对应接口；打开画布只以本次服务端响应初始化。
- `studio-page-state.js` 加入非阻塞历史清理：删除 IndexedDB `pages` 中所有 `:canvas:` 键以及对应的恢复指针，普通页面状态缓存不受影响。
- 新增真实浏览器反向用例：先写入 `cold-test:canvas:cold` 的旧状态，再进入画布，确认界面保持服务端 `server text` 且旧记录被删除。

## 最近验证

- `node --check static/js/canvas.js static/js/studio-page-state.js static/js/canvas-session-host.js tests/support/canvas_cold_dispatch_check.cjs tests/support/studio_canvas_input_check.cjs tests/support/workspace_state_restore_check.cjs` 通过。
- `python -m pytest -q tests/test_canvas_initial_load_performance.py tests/test_canvas_session_cache_runtime.py tests/test_canvas_startup_runtime.py`：15 passed。
- `CANVAS_TEST_SCENARIOS=stale-canvas-snapshot node --unhandled-rejections=strict tests/support/canvas_cold_dispatch_check.cjs http://127.0.0.1:3042`：通过，306ms，页面脚本错误 0。
- `node --unhandled-rejections=strict tests/support/studio_canvas_input_check.cjs http://127.0.0.1:3042`：通过，首开可拖动/平移/缩放，切换后活实例保持，页面脚本错误 0。
- `git diff --check` 通过；`.build` 0.46 GiB、`.codex-tmp` 1.77 GiB，均低于 20 GiB 上限。
- branch：`feat/universal-product-lookbook`；源码 commit：`c7c6f033 fix(canvas): remove stale state snapshots`，任务记录 `483e0291`；已推送 `origin/feat/universal-product-lookbook`。

## 热更新发布

- 已从干净工作树 `D:/data/codex/work/canvas-cache-hotfix/SHIYIN-AI` 发布纯前端热更新 `20260921130622`，说明为“修复画布旧状态缓存覆盖问题”。
- 包含 160 个 `app/web` 文件，23,566,104 字节，SHA-256 `e26def5ca56cfa25a1a2247f8672dd5682250c78a2b1e8e6753c784959474808`；未重编桌面宿主、后端或固定模型。
- 分发中心目录与补齐计划 Ed25519 签名验证通过。已达到完整检查点 `20260921115901` 的 2.0.2 客户端会直接取得该版本；较旧客户端按既有补齐策略先应用完整检查点，再自动跟进此包。
- 公共下载包大小、SHA-256 以及 `canvas.js`、`studio-page-state.js`、`canvas-session-host.js` 的包内哈希均与干净源码一致。

[CODEX_LONG_TASK_CONTINUE_V3]
