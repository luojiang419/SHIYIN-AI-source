# 可灵 Chrome 参考视频自动填充

- 当前状态：2026-09-22 继续实现桌面/Web 无限画布自动交接、填充与单次生成；插件安装及手动填充已由用户确认成功。
- 本轮方案：每个任务独立短期凭据，通过 Chrome 交接页自动接收；无需共享桌面登录 Cookie 或手动连接；参数校验后取得原子提交许可，仅点击一次，回执不明禁止自动重试。
- 下一步：Web画布真实生成已提交成功，等待成片；桌面窗口内同流程和正式安装版热更新仍未验收。
- TODO：代码和免连接填充链路已完成；实际付费生成与成片回收不计为已验证，成片回收不在本轮范围。
- 最近验证：12 项后端测试、7 项后台/画布运行用例、4 项提交保护用例通过；真实 Chrome 自动交接回传 filled，真实网页参数配置及提交前检查通过；桌面 13171 健康检查 runtime_mode=desktop，Web 13170 健康检查通过。
- branch：`feat/kling-web-autofill`；自动填充基线 commit：`feede188`；0.2.0 变更见本任务最新提交。
- 约束：用户已授权实现自动生成；用户已授权覆盖人工草稿，保护其他任务改动，不重复点击、不误报生成完成；不改动 3000 端口。

## 验收

画布视频节点能发送提示词、图片及视频参考；插件能复用或打开可灵 Omni 页面；上传完成才写入提示词，失败明确反馈且不重复上传；0.2.0 在画布明确点击生成后自动提交；只填充模式保留为内部联调用途。本轮不承诺自动下载生成结果。

真实素材：`D:/data/图片/案例视频/深度视频案例/源视频提取的深度视频.mp4`（4,201,813 字节）、`D:/data/图片/案例视频/SHIYIN-000091-20260921.png`（18,925,335 字节）。

[CODEX_LONG_TASK_CONTINUE_V3]

## 实现与实测记录

- `canvas_core/kling_web_bridge.py`：现有账号会话鉴权、隔离草稿队列、原子领取、回执、取消、超时；不调用生成接口。
- `main.py`：挂载新路由；`static/js/canvas.js`：普通视频/影视视频增加“填充到可灵（不生成）”，收集连线素材与提示词并等待回执。
- `tools/chrome-kling-bridge/`：独立 Manifest V3 扩展；分块传文件，复用/打开 Omni，等待上传缩略图，使用原生 `@` 菜单绑定引用，连续草稿复用同一批素材，保护人工草稿。
- 浏览器工具禁止访问 `chrome://extensions`，没有尝试绕过。用户已选择先完成代码和页面实测。因此真实页面测试使用工具允许的 CDP 开发调试接口加载**同一份 adapter.js**；扩展后台与画布交互通过独立运行测试，未声称已安装扩展或完成真实后台链路。
- 完整适配流程 `run(full-flow-1)`：两份原始文件分块传输→依次上传→缩略图就绪→提示词写入；结果 `filled`、2 张素材卡、原生标签 `[视频1, 图片1]`。
- `run(full-flow-2)`：同素材、更换提示词与引用顺序；结果 `filled`、仍为 2 张素材卡、标签 `[图片1, 视频1]`，没有重复上传。
- `run(full-flow-3)`：带 `@视频1` 与画布 `<<<image_1>>>` 格式、多行文本；结果 `filled`、标签 `[视频1, 图片1]`。可灵简化编辑器将正文换行折为空格，文字完整。
- 当前可灵页面保留第三份草稿；视频标签 DOM 确认 `data-type=video` 且有 `data-video-url`，图片标签 `data-type=image`；从未点击生成。
- 补充原有可灵账号回归：3 passed、1 failed；失败为 `tests/test_kling_remote_web_access.py:112` 对旧智能画布字符串的断言。其要求的字符串在当前 HEAD 和工作区原文件均不存在，本次未修改 `smart-canvas.js`，属于已有测试失配。
- 截图捕获接口超时，未生成截图文件；实测结论来自真实 DOM 和适配器回执。
- 未执行安装包/热更新构建，未发布客户端版本。


## 0.2.0 自动交接与提交（2026-09-22）

- 用户已刷新扩展至 0.2.0。原生 Chrome 交接页接收任务后自动关闭，填充结果成功回传；不依赖 Chrome 中的拾影 Cookie，桌面/Web 各自端口自动随任务传递。
- 后端 task ticket 限定单任务领取、素材读取与回执，生成前原子许可只能领取一次。普通状态查询不返回凭据，媒体读取使用所属账号上下文。
- 真实页面确认参数选择器、数量固定 1、音画同步状态、生成按钮；只有匹配提示词的新历史任务才回传 submitted，超时回传 unknown。
- 普通视频/影视视频节点的 Omni 生成共用网页入口；保留非 Omni 的原 CLI 生成。新参数控件不依赖 CLI 登录。
- 当前开发后端 PID 47184、端口 13170；源码桌面宿主 PID 48424、后端 PID 28708、端口 13171（PID 仅记录本轮，不可直接用于以后终止进程）。未操作 3000。
- 已实际启动源码桌面宿主并确认 desktop 模式健康检查；桌面窗口内点击完整生成尚未实测。Web 画布界面已出现新参数与“发送到可灵并生成”。
- 此前测试草稿保护生效，清除本轮自建草稿后自动填充成功。无付费生成、未发布安装版热更新。

## 0.3.0 自动覆盖与后台接收（2026-09-22）

- 按用户最新授权，发送任务时使用原生 reset-option 自动清空旧素材/提示词；真实页面自建旧提示词重置后 text 为空、cards=[]。不改变已提交任务历史。
- 连接凭据限定当前账号队列，24 小时有效；后端根据心跳直接等待后台扩展接收，40 秒仍未领取时才尝试唤起 Chrome。连接失效后自动重新交接。
- 扩展复用页面不激活标签，新建标签 active:false；无窗口时 focused:false/state:minimized。冷启动使用最小化参数与 Windows 不激活窗口标志，但首次连接/冷启动完全无闪现尚未验证，不能承诺。
- 验证：13 项后端用例、8 项后台/画布用例、4 项提交保护用例通过；真实可灵原生重置已验证。完整后台上传与付费生成尚未实测。
- Web 开发后端已重启加载新路由（13170，PID 48536）；此前源码桌面宿主已退出，下次启动加载新代码。未变更 3000，未发布安装版热更新。


## 0.3.1 最小真实生成实测修复（2026-09-22）
- 实测画布 d80da77e8d20477ebfe495d1f75fda69，参考图 D:/data/图片/20260911-154935.jpg，720p / 3 秒 / 9:16 / 关闭声音，点击普通视频节点“视频生成”。
- 首次后端拒绝旧模型遗留分辨率；已修复 Omni 参数渲染时规范化节点数据，避免界面显示与实际发送不一致。
- 第二次自动上传图片、绑定图片1、填充提示词和参数成功；可灵报价18积分。提交许可已领取，但点击后30秒无回执，状态 unknown；刷新可灵历史后仍没有舞蹈任务，未重复提交，不能认定真实生成成功。
- 插件0.3.1增加报价稳定1.5秒后才提交（最多等待15秒），兼容“18 生成”按钮文本。需要用户刷新扩展再继续实测；早点击是待实测确认的原因。
- 验证：JS语法检查通过，4项提交保护和8项运行用例通过；新报价等待逻辑尚未完成真实页面验收。
- 下一步：刷新插件到0.3.1，确认历史无同一任务后，从保留的测试画布继续一次最低配置生成；不得直接重复 unknown 任务。

## 0.3.1 真实提交成功
- 用户刷新后从测试画布普通“视频生成”按钮发起，任务 q-vqebfpN3T-Z8WG8ks5BPB_ 自动上传图片与提示词，720p / 3秒 / 9:16 / 无声音 / 单条，页面报价18积分。
- 可灵明确显示“任务提交成功，生成中”，新增任务322027034213673；后台submit-permit、result均200，画布结束轮询并保存。随后页面显示创作中，预计等待30秒。此次仅提交一次。

## 0.4.0 安装引导开发
- 当前状态：开发普通用户商店安装引导、版本检测、安装后原任务续接和HTTPS站点显式授权。商店ID待上架配置，不伪造安装成功。
- 待完成：交接页、安装后重新注入、版本心跳、画布等待恢复、企业部署材料。普通用户保留Chrome安装确认。
- 验证约束：本轮用户要求开发，未授权新增付费生成；不触发真实生成测试，不发布正式热更新。

- 0.4.0代码已落地：商店ID环境配置、安装交接页、安装后重注入、原任务恢复、15分钟取消、版本拦截与HTTPS按站点授权。企业策略示例和隐私说明草案已准备。
- 本轮只检查diff和实现，未运行测试、未触发生成、未重启用户现有服务；商店上架/真实安装验收/正式热更新尚未完成。下一步取得商店ID及发布主体资料，完成发行验收后发布含后端热更新。

## 内置扩展热更新发布
- 用户改为不上架商店，扩展文件夹和ZIP内置于app/web/extensions；交接页提供下载和Chrome开发者模式加载说明。首次需用户加载，更新需刷新扩展，不声称普通Chrome静默安装。
- 正在构建包含后端和前端的热更新，不构建全量安装器。
