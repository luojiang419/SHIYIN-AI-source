# 可灵 Chrome 参考视频自动填充

- 当前状态：代码及真实页面适配实测完成；用户选择“先完成代码和页面实测”，本轮不安装插件。
- 下一步：Git 交付；后续手动加载扩展、重启拾影后端、登录同一地址的拾影账号并连接扩展，即可使用画布入口。
- TODO：本轮只剩 Git 交付；安装后的画布→扩展完整联调留待插件安装后执行。
- 最近验证：9 项后端测试、5 项 JavaScript 运行用例、4 个 JavaScript 文件语法检查通过；真实可灵上传原始 MP4/PNG 并连续填充三份草稿成功。
- branch：`feat/kling-web-autofill`；commit：待提交。
- 约束：本次测试只填充，不提交生成；保护工作区其他任务改动。

## 验收

画布视频节点能发送提示词、图片及视频参考；插件能复用或打开可灵 Omni 页面；上传完成才写入提示词，失败明确反馈且不重复上传；填充操作绝不点击生成。用户后续可以在网页人工确认生成。本轮不承诺自动下载生成结果。

真实素材：`D:/data/图片/案例视频/深度视频案例/源视频提取的深度视频.mp4`（4,201,813 字节）、`D:/data/图片/案例视频/SHIYIN-000091-20260921.png`（18,925,335 字节）。

[CODEX_LONG_TASK_CONTINUE_V3]

## 实现与实测记录

- `canvas_core/kling_web_bridge.py`：现有账号会话鉴权、隔离草稿队列、原子领取、回执、取消、超时；不调用生成接口。
- `main.py`：挂载新路由；`static/js/canvas.js`：普通视频/影视视频增加“填充到可灵（不生成）”，收集连线素材与提示词并等待回执。
- `tools/chrome-kling-bridge/`：独立 Manifest V3 扩展；分块传文件，复用/打开 Omni，等待上传缩略图，使用原生 `@` 菜单绑定引用，连续草稿复用同一批素材，保护人工草稿。
- 浏览器工具禁止访问 `chrome://extensions`，没有尝试绕过。用户已选择先完成代码和页面实测。因此真实页面测试使用工具允许的 CDP 开发调试接口加载**同一份 adapter.js**；扩展后台与画布交互通过独立运行测试，未声称已安装扩展或完成真实后台链路。
- 完整适配流程 `run(full-flow-1)`：两份原始文件分块传输→依次上传→缩略图就绪→提示词写入；结果 `filled`、2 张素材卡、原生标签 `[视频1, 图片1]`。
- `run(full-flow-2)`：同素材、更换提示词与引用顺序；结果 `filled`、仍为 2 张素材卡、标签 `[图片1, 视频1]`，没有重复上传。
- `run(full-flow-3)`：带 `@视频1` 与画布 `<<<image_1>>>` 格式、多行文本；结果 `filled`、标签 `[视频1, 图片1]`。可灵简化编辑器将正文换行折为空格，文字完整。
- 当前可灵页面保留第三份草稿；视频标签 DOM 确认 `data-type=video` 且有 `data-video-url`，图片标签 `data-type=image`；从未点击生成。
- 补充原有可灵账号回归：3 passed、1 failed；失败为 `tests/test_kling_remote_web_access.py:112` 对旧智能画布字符串的断言。其要求的字符串在当前 HEAD 和工作区原文件均不存在，本次未修改 `smart-canvas.js`，属于已有测试失配。
- 截图捕获接口超时，未生成截图文件；实测结论来自真实 DOM 和适配器回执。
- 未执行安装包/热更新构建，未发布客户端版本。
