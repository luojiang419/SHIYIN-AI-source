# 209 - 首次账号设置与持久登录

[CODEX_LONG_TASK_CONTINUE_V3]

- 当前状态：开发与验证完成；本次未构建或发布热更新。
- 下一步：需要分发时按项目约定构建包含后端的热更新，不可使用 `--web-only`。
- TODO：无开发遗留项。
- 最近验证：账号/权限/引导专项 `38 passed`；补充迁移与桥接回归 `16 passed`（与前一组有重叠）；工作流 HTTP 往返 `1 passed`；API 冒烟、真实后端 Playwright 验证、Python/JavaScript 语法检查均通过。
- branch/commit：`feat/first-run-account-setup` / 本任务提交见 Git 历史。
- 阻塞：无；`main.py` 与已有测试文件按本任务补丁暂存，其他任务未提交内容保留。

## 验收与兼容

- 取消内置管理员账号密码，通过注册设置账号，首个本机注册账号为管理员；并发注册不能产生多个管理员，远端不能抢占首次管理员。
- 管理员仍使用既有 `admin` 数据作用域，保留旧版画布、素材与配置；旧普通账号保持原角色及数据目录。旧硬编码管理员会话失效，需首次设置新账号。
- API 配置等敏感接口继续由后端按角色校验，普通账号不能访问。
- 首次账号设置作为已有 API 引导的前置步骤，说明只需首次注册、桌面后续自动登录以及多人数据隔离；注册后直接进入。
- 桌面本机登录令牌使用现有安全存储加密持久化，重启及 Cookie 丢失时恢复；主动退出、改密或禁用后不能恢复旧会话。
- Web 会话仍保留期限，不从桌面保存的凭据恢复。

## 实现与验证记录

- `canvas_core/accounts.py`：持久化账号角色；事务内分配唯一管理员；废弃固定账号密码；迁移保持旧普通用户权限和目录；旧虚拟管理员会话失效；桌面令牌加密保存并可撤销。
- `main.py`：公开首次设置状态接口、统一注册和登录、按本机桌面模式持久化会话、bootstrap 恢复身份；API 配置权限沿用后端角色校验。
- `static/login.html`：首次默认注册、确认密码、管理员说明、首次注册与后续自动登录说明、多人数据隔离说明，完成后衔接原有配置引导。
- `static/admin.html`：展示账号角色，管理员禁止禁用，并正确展示原有管理员数据目录。
- 测试覆盖并发首次注册、远端不能抢占管理员、旧数据库迁移、角色权威来源、Web 过期、后端重新初始化、Cookie 丢失恢复、退出/改密/禁用撤销，以及普通用户的敏感接口拒绝。
- 浏览器验证使用临时数据目录和真实后端；检查深色桌面、390px 窄屏、密码确认错误、注册后配置引导及普通账号权限，无页面 JavaScript 错误。
- 截图位于 `.codex-tmp/account-setup/`，作为本地验证缓存，不提交。缓存检查：`.build` 0.70 GiB，`.codex-tmp` 8.20 GiB，均未超过 20 GiB。
- 依赖旧固定凭据的隔离测试改为首次注册；针对已有服务的手工浏览器检查改为显式提供 `SHIYIN_TEST_ACCOUNT` / `SHIYIN_TEST_PASSWORD`。
