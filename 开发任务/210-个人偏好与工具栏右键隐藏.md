# 210 个人偏好与工具栏右键隐藏

当前状态：热更新 `20260922185923` 已发布，签名与整包下载哈希校验通过。
下一步：2.0.5 及以上客户端可通过检查更新应用；不强制重启用户正在使用的软件。
TODO：无。
最近验证：11 项 Python 测试通过；Playwright 页面与节点运行测试通过；JS 语法及 Git diff 检查通过。
branch：feat/personal-generation-preferences；工作区其他任务改动保留，使用原始差异从暂存区剔除，仅交付本任务差异。
阻塞：无。

## 验收

- 底部可选按钮右键隐藏，设置中可恢复，重载后保留。
- 设置按钮上方提供所有账号可用的个人偏好页面，适配浅色、深色、纯白及窄屏。
- 图片、视频按平台分别保存模型和参数，新建节点及切换平台应用对应偏好。
- 快捷保存方式与目录全局生效，偏好按账号隔离，不改动已有节点。

[CODEX_LONG_TASK_CONTINUE_V3]

## 实现与验证记录

- 新增 `personal-preferences.html` 及对应 CSS、状态模块、页面模块，沿用设置主题变量并支持窄屏布局。
- `main.py` 提供账号范围的个人偏好接口，复用原生目录选择与实际下载写入链路；目录保存前校验绝对路径与可写性。普通账号也可使用。
- `canvas.js` 接入图片、批量图片、视频、影视、电商视频和 LinkFox 创建入口及平台切换；图片快捷编辑也使用偏好。工具栏复用现有持久设置，右键隐藏后可在设置中恢复。
- `python -m pytest tests/test_personal_preferences.py tests/test_quick_save.py tests/test_events_preferences.py -q`：11 passed。
- `node tests/support/personal_preferences_runtime.cjs`：节点参数、平台隔离、目录选择响应、保存重载、深色／浅色／纯白、390px 窄屏、右键隐藏通过。页面使用模拟 API，未进行收费生成。
- 页面截图：`.codex-tmp/personal-preferences/`，已查看深色截图。
- 现有 `tests/test_canvas_menu_preferences.py`：8 passed、2 failed；失败检查的旧 pointerover 字符串与工具按钮列表在任务开始时和 HEAD 中均不存在，本次未修改对应逻辑。
- 无需编译静态 JS；未运行安装器构建、未递增正式版本、未发布热更新。

## 安装版实测与热更新

- 已直接更新 `D:\Program Files\SHIYIN AI\app\web` 的 8 个前端文件，备份位于安装目录 `backup/v001-personal-preferences`；未改变宿主、冻结后端和用户工程。
- 旧后端兼容按账号本机持久化，保存目录复用旧接口。通过真实运行的安装版后端验证保存重载、图片 9:16/2k、优云 H3 9 秒节点参数及右键隐藏；测试画布移入回收站，工具栏恢复。未调用收费生成。
- 用户按 Esc 终止 Windows 自动操作，主窗口最终刷新未确认；实际后端与浏览器验证已完成。
- 新后端首次读取空偏好时迁移当前账号本机生成参数，保留原始本机数据；目录沿用软件设置，避免导入失效路径阻塞迁移。
- 按 `175-独立局域网分发服务与控制面板方案.md` 的分发流程构建变更后的宿主与冻结后端；不是全量安装包，也不是纯前端包。正式桌面版本保持 2.0.5。
- 发布：`hot-20260922185923`，分发中心 `http://192.168.0.24:3011`，状态 `published`，共 1027 文件、123998101 字节。
- SHA-256：`146e62980472217a3b76553e9d924713d6f971112d1d89ddd0c54b6fe835c8e6`。
- 验证：11 项 Python 测试、3 项迁移测试、页面运行测试通过；冻结后端隔离实测两账号偏好、普通账号权限和真实静默文件写入通过；客户端目录 Ed25519 签名、升级目标签名、整包下载 SHA-256 通过。
- 快照与证据：`dist/hot-update/20260922185923/`。发布前完成缓存清理及 20 GiB 上限检查；清理的是可再生构建缓存，发布快照与固定模型保留。
- 分发中心仍存在 2.0.1 历史客户端记录，遵循项目现行约定，热更新清单最低版本固定 2.0.5，旧客户端需安装当前唯一全量基准；未恢复旧引导链、未改变固定模型。
- 发布使用当前已部署基线对应的工作区构建，保留其他任务未提交改动；Git 仅提交本任务兼容、迁移、验证脚本与记录。
