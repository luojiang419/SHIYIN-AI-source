# 检查更新版本日志时间轴

状态：已发布纯前端热更新 20260922194541，分发验收通过。
下一步：客户端检查并应用更新，重启后再次检查更新即可浏览时间轴。
TODO：源码与发布无遗留；尚未强制重启或代替用户应用客户端更新。
最近验证：归档与发布门禁 9 项通过；Python/JavaScript 与 PowerShell 语法通过；Playwright 408 条记录、分类筛选、失败基线、组件同号不覆盖、当前版本、深浅主题、390px 窄屏、键盘及错误分支通过。上一阶段更新器合同测试 17 通过、1 失败，失败为安装脚本的既有旧断言，与本次归档功能无关。
branch：feat/update-history-timeline，使用独立索引，仅提交本任务差异，不切换共享工作区。

[CODEX_LONG_TASK_CONTINUE_V3]

## 实现
手动检查无更新时展示本地版本历史；自动检查保持静默，有更新时沿用原流程。
历史来源为 Git 保存的 update-notes.json，按版本去重并剔除更早版本已有条目；2.0.4、2.0.5 根据任务 209 已验证发布记录补齐。未独立记录的版本明确标注，不推测内容。只归档文字，不恢复旧安装包。
以倒序时间轴展示版本、日期、当前版本标记，长说明按四条折叠；请求超时、部分读取失败和空记录均有提示，日志使用 textContent 防止注入。

主要文件：static/js/desktop-updater.js、static/update-history.json、tests/support/update_history_ui_check.cjs。
后续版本独立日志可写入 update-notes.json 的 history；当前累计列表中的新条目会去重展示，长期记录应持续归档到 update-history.json。
界面验证使用真实首页样式与更新器脚本，模拟桌面检查结果；截图在 .codex-tmp/update-history/，未声称实际安装版验证。未修改正式版本、构建安装器或发布。

## 全部日志与基线切换（追加要求）

共 408 条可追溯记录：307 条软件版本、7 条基线事件（含 2.0.2 校验失败尝试）、89 条热更新、5 条组件记录。来源为 Git 历史、分发中心当前数据库、195 基线重置前备份、现存本地清单和明确发布任务记录。原始记录缺失的内容不编造；仅有构建清单的标记为构建记录，不声称已发布。组件与同版本多次基线以独立事件 ID 保存，避免被版本号去重吞掉。

- 新增 tools/update_history.py：合并、原子写入、构建暂存及成功后归档。已有日志损坏时中止，不覆盖历史。
- 新增 tools/backfill-update-history.py：可从只读分发数据库和本地清单重复补录，保留已有记录和发布状态；仅提取版本、日期、说明等字段，不输出路径、令牌或签名密钥。
- tools/build-hot-update.py 在计算清单哈希前写入累计日志，包构建成功后提交记录；不把异步导入请求当作发布成功。
- tools/build-installer.ps1 将新基线日志放入安装暂存目录，安装器构建成功后才归档到源码。包含内置基线序号与当前发布说明，不混入 Markdown 的历史章节。
- UI 增加全部/基线切换/热更新/组件/版本更新筛选，标明完整、纯前端、更新器、历史引导等类型；旧引导仅保留文字，不恢复入口。

验证包括临时目录实际生成纯前端测试 ZIP 并核验日志文件哈希、基线 CLI 暂存与归档、重复构建保留、损坏文件保护。未运行正式全量编译或发布。后续正常构建会携带历史日志；只有清单/数据库确认后才可将构建记录提升为已发布。历史归档在 static/update-history.json，不依赖可清理的 dist 或 .build 快照。

## 2026-09-22 发布验收

纯前端热更新 `20260922194541` 已发布，最低桌面版本 `2.0.5`；173 个文件、23,659,473 字节，SHA-256 `2bde503af21df7652292322ed66768c6d71601e24bd1c02a785dadaf0edab4da`。累计 409 条日志（含本次发布）。

隔离工作区 `D:/SHIYIN-Builds/update-history-78681063`，源提交 `78681063`。以前一已发布 `20260922185923` 的 172 个前端文件为底稿，逐文件校验 SHA-256 后仅叠加时间轴函数和历史日志；保留上一版 updater 的后台延迟通知修复及个人偏好功能。HTML 仅重写资源缓存序号。未混入共享工作区其他改动。

发布前浏览器回归通过；173 个包内文件大小和哈希逐一通过，所有路径均在 app/web，prune_roots 仅 app/web。分发中心 job 完成无错误、target_version 正确；公开 catalog 和补齐计划 Ed25519 签名、局域网整包下载 SHA-256、Range 206/32 字节通过；fixed-models.json 哈希前后一致。未构建全量安装器、未更换后端与固定模型。

产物及验收 JSON：`D:/SHIYIN-Builds/update-history-78681063/dist/hot-update/20260922194541/`。构建执行缓存上限清理检查，隔离工作区缓存低于 20 GiB。发布仅改变服务端可用更新，不宣称客户端已安装。
