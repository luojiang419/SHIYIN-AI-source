# 检查更新版本日志时间轴

状态：实现及浏览器验收完成，未发布热更新。
下一步：需要安装版生效时，将本任务文件随纯前端热更新发布。
TODO：源码无遗留；安装版尚未部署。
最近验证：JavaScript 语法通过；Playwright 313 条历史、当前版本、深浅主题、390px 窄屏、焦点循环/Escape、读取失败及有新更新分支通过。更新器合同测试 17 通过、1 失败；失败为未修改的安装构建脚本缺少旧断言要求的 npm run desktop:host-build，与本次前端无关。
branch：feat/update-history-timeline，使用独立索引，仅提交本任务差异，不切换共享工作区。

[CODEX_LONG_TASK_CONTINUE_V3]

## 实现
手动检查无更新时展示本地版本历史；自动检查保持静默，有更新时沿用原流程。
历史来源为 Git 保存的 update-notes.json，按版本去重并剔除更早版本已有条目；2.0.4、2.0.5 根据任务 209 已验证发布记录补齐。未独立记录的版本明确标注，不推测内容。只归档文字，不恢复旧安装包。
以倒序时间轴展示版本、日期、当前版本标记，长说明按四条折叠；请求超时、部分读取失败和空记录均有提示，日志使用 textContent 防止注入。

主要文件：static/js/desktop-updater.js、static/update-history.json、tests/support/update_history_ui_check.cjs。
后续版本独立日志可写入 update-notes.json 的 history；当前累计列表中的新条目会去重展示，长期记录应持续归档到 update-history.json。
界面验证使用真实首页样式与更新器脚本，模拟桌面检查结果；截图在 .codex-tmp/update-history/，未声称实际安装版验证。未修改正式版本、构建安装器或发布。
