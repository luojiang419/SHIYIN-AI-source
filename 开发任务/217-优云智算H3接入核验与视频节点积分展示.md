# 优云智算 H3 接入核验与视频节点积分展示

[CODEX_LONG_TASK_CONTINUE_V3]

- 状态：源码修复与真实生成已验证；纯前端热更新 `20260924174208` 已发布到局域网分发中心，当前安装版尚未应用。
- 下一步：客户端检查并应用更新，在实际安装版节点切换到优云智算，复核显示的实时余额。
- TODO：客户端实际应用与节点验收。
- 最近验证：2026-09-24 前端切换、960 积分动态刷新、失败回退测试通过；发布包签名和整包 SHA-256 校验通过；此前后端定向测试 `47 passed`，真实优云任务生成成功。
- branch/commit：`feat/youyun-h3-balance` / `769e1a0d`；已推送 `origin/feat/youyun-h3-balance`。
- 阻塞：无。现有工作树有大量其他任务的未提交修改，发布和提交均只纳入本任务改动。

## 目标与验收

- 对照 `G:\app\minimax\desktop-workbench\src-tauri\src\cloud_h3.rs` 的鉴权、提交、查询、积分接口与工作台余额展示，确认当前项目的实际接入状态。
- 经典视频节点、影视视频节点及智能画布视频参数在选择优云智算 H3 时显示实时可用积分；切换到可灵或本地 H3 时不显示优云积分。
- 积分接口响应异常时不把未知余额显示成 0；保留准确的不可用提示。
- 运行相关后端和前端回归，并实际检查页面表现。

## 核验记录

- 当前后端的 `youyun-h3` 有独立平台 ID、密钥、`/minimax/v2/video_generation` 提交、`/query/video_generation/{id}` 轮询和积分查询。
- 工作台使用同一 API 主机、Bearer 鉴权和积分查询；其余额标签仅在云端模式显示。
- 优云官方文档当前列出 `480P`、`768P`、`1080P`、`2K`、`4K` 五档，而当前项目只开放其中三档；本任务聚焦接入核验和积分展示，规格扩展另行处理。
- 真实生成实测：经当前项目的 `generate_youyun_h3_video` 提交 4 秒、768P、16:9 文生视频，云端任务成功，下载到 `data/media/generated/youyun_h3_29e525b488.mp4`。`ffprobe` 确认 H.264 视频 1344×768、AAC 音频、时长 4.458 秒、文件 777496 字节，并目视检查了抽帧。
- 实测前可用积分 1150，实测后 960；同期任务列表还有另一条 15 秒、768P 成功任务。官方当前计价为 768P 每秒 10 积分，因此余额变化与两条任务合计 190 积分相符，但云端任务详情没有单任务积分字段，不能将全部差额归于本次测试。
- 后端对有效积分返回整数校验，未知余额不再显示成 0；余额为 0 时标记不可生成。经典、影视、智能三个视频入口分别只在优云智算选中时展示其余额，切回本地 H3 后不显示。
- 验证：`python -m pytest -q tests/test_minimax_h3_provider_migration.py tests/test_youyun_h3_video.py tests/test_youyun_h3_references.py` 得到 47 passed；`node tests/support/h3_provider_split_runtime.cjs` 通过并检查了截图；JS 语法和 `git diff --check` 通过。

## 运行安装版续查（2026-09-24）

- 当前运行安装版的 `app/web/js/canvas.js`、`smart-canvas.js` 与最新纯前端底包 `20260924142326` 哈希相同，尚无本任务积分显示逻辑。之前的源码与模拟面板验证不代表该安装版已更新。
- 两个源码脚本与安装版脚本的差异，除了本任务积分逻辑，仅有既存的静态缓存版本串；制作发行叠加包时沿用已发布底包的版本串，避免夹带这些差异。
- 刷新异常时前端过去会保留旧 `defaults.available_points`，新余额提示可能误显示旧数字。现已让失败分支清空余额，并让错误信息优先显示；0 积分仍显示为 0。
- 发行底稿使用 `20260924142326` 的已发布安装版前端，仅更改 `js/canvas.js`、`js/smart-canvas.js`；发行包其余差异是 HTML 静态缓存版本串和 `update-history.json`。`20260924174208` 为纯前端热更新，183 文件，23,704,153 字节，SHA-256 `8f598ddc5d3f6c394cf3f837b08271800054f51aabb95b8b2e18d724f3ea4201`；最低桌面版本 2.0.0，当前已联网旧客户端兼容门禁通过。未编译全量安装包或后端，固定模型未变；本次未同步魔搭。
- 局域网客户端目录与计划签名、完整下载哈希均已验证。当前运行中的 2.0.6 安装版 `app/web` 仍是旧脚本，需在客户端应用更新后才会看到节点显示；本轮未绕过软件更新机制直接改写安装目录。
