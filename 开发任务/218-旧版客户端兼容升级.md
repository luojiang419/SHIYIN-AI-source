# 旧版客户端兼容升级

状态：兼容快照 `20260924142326` 已发布至局域网与魔搭，最低桌面版本 `2.0.0`；2.0.6 全量安装基准保持不变。
下一步：在实际 2.0.5 客户端点击检查更新、应用并确认重启；1.0.x 用户仍通过 2.0.6 全量包覆盖安装。
TODO：旧客户端实际应用验收。
最近验证：2.0.5 截图提示“热更新要求桌面基线 2.0.6”。新快照 1,180 文件、236,658,393 字节，SHA-256 `b8e83d08ca436980566fa9f1e51c0dbd92e8c8bca1a2fec3fa4c94d9793a4456`；包内全部文件大小与哈希逐项通过。较源快照仅 HTML 缓存版本与更新历史改变，EXE 和后端字节不变。相关 Python 测试 23 passed，Rust 测试 22 passed。模拟 2.0.0、2.0.1、2.0.5、2.0.6 客户端均可从局域网取得兼容快照；按 2.0.5 请求方式的目录/计划签名及整包下载 SHA-256 通过。魔搭匿名完整回读哈希一致，活动目录/计划签名通过，Range 0-31 返回 206。固定模型台账 SHA-256 发布前后均为 `b6af134be3a3e5f18f07d05dfc00bb9fcfbc05301858c9cbf80ee79affdb68a8`。
branch/commit：`fix/video-depth-worker-compatibility`；源码修复提交 `5d8b26ed`。兼容包已实际发布；本记录与分发记录单独提交，保护共享工作区的其他未提交修改。
阻塞：无法远程操作用户的旧版设备，实际安装与业务功能仍待该设备验收。

目标：让已安装旧 2.0.x 客户端能通过原有局域网更新协议获取包含桌面宿主、后端和前端的完整兼容热更新；具有魔搭回退能力的旧客户端在公网也能更新。1.0.x 不具备当前签名包协议时，继续使用现有 2.0.6 全量安装包覆盖升级并保留 data。

方案：从已发布且验证的 20260924113138 快照重新封装，不采集共享工作区的其他未提交功能；将最低桌面版本设为 2.0.0。另修复当前源码中的热更新预检：遇到不兼容热更新时继续检查全量安装包。该源码修复尚未编入本次沿用的 2.0.6 桌面 EXE；本次客户端恢复更新依靠兼容快照的最低版本调整。2.0.1 发布源码已包含协议 3 单包更新；1.0.x 继续全量覆盖。局域网服务保留归档完整兼容包供未来旧客户端补齐；魔搭静态活动目录今后的发布若提高最低版本，须先保证未迁移旧客户端仍有可用入口。

发布文件：`dist/hot-update/20260924142326/SHIYIN-Hot-Update-20260924142326.shiyin-update`；局域网活动版本 `hot-20260924142326`，魔搭路径 `releases/20260924142326/SHIYIN-Hot-Update-20260924142326.shiyin-update`，活动目录 `public/catalog.json`。当前供 1.0.x 覆盖安装的全量包仍为 `installers/2.0.6/SHIYIN-AI-Setup-2.0.6.exe`。

验收：2.0.5 的检查更新可看到兼容包，完整下载及应用后正常启动；2.0.1 等受支持的旧 2.0.x 版本可安全迁移；局域网和魔搭签名目录、整包回读与 SHA-256 相符；固定模型与用户 data 不被替换。

[CODEX_LONG_TASK_CONTINUE_V3]
