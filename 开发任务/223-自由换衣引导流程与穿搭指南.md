# 自由换衣引导流程与穿搭指南

[CODEX_LONG_TASK_CONTINUE_V3]

- 状态：实现与浏览器演示已完成，待 Git 交付。
- 下一步：检查本任务 diff/status，仅提交本任务文件；如远程可用则 push。
- TODO：Git 交付；使用真实已配置图片平台生成一张指南图进行人工质量验收（本次不能以测试 Mock 冒充真实 API 结果）。
- 最近验证：`node tests/support/ecommerce_tryon_guide_ui_check.cjs` 通过，覆盖上传、步骤门槛、提示词 API、指南任务 API、JPG 导出与 520px 单列；`node --check` 两份脚本通过；`EcommerceContractTests` 两项试穿后端契约通过；`build_prompt('universal', ..., {'prompt_policy':'free'})` 返回原始指南提示词。完整 `tests.test_ecommerce` 中 4 项既有无关断言失败，前端测试类因工作区既有的 `static/gpt-chat.html` 缺失无法初始化。
- branch/commit：`feat/pose-transfer-fidelity`；本任务未提交。工作区有大量其他任务改动，必须保护。
- 阻塞：无。

## 目标与方案

将当前同时铺开的上传面板改为四个可返回的引导步骤：人物、服饰、细节、提示词与生成。人物和服饰是必填门槛，细节（姿势、面料、影棚风格）可跳过。末步调用现有分析 API 读取参考图并生成可审阅的最终提示词，用户可补充生成需求，再走已有试穿生成任务。

右侧保持搭配素材概览，独立提供“生成穿搭指南图”。使用已有 `/api/ecommerce/tasks` 图像生成 API 建立独立任务，提交原始参考图和任务专用排版提示词，展示生成状态与结果，支持导出 PNG。指南任务不能覆盖试穿任务或误称静态搭配概览为真实试穿效果。

## 页面决策与 API 流程

1. 人物：只显示人物参考图。未上传不能进入服饰。
2. 服饰：独立衣物槽，可继续添加参考；至少有一件才可进入细节。
3. 细节：姿势、面料局部与影棚风格均为可选。面料局部继续保留原有归属选择。
4. 生成：`/api/ecommerce/analyze` 返回参考图排序、视觉分析状态与最终提示词预览；用户可补充要求；试穿图继续走原有 `createTask` 链路。需求变更后提示重新规划。
5. 右侧指南：将人物映射为 `model_identity`，其余服饰沿用角色归属，以 `universal + prompt_policy=free` 提交独立图片任务。固定竖版 3:4，提交成功后轮询，完成显示缩略图与 PNG/JPG 格式选择。导出时从实际 API 图片转换，不伪造“已生成”。

页面演示截图位于 `.codex-tmp/ecommerce-tryon-guide-demo.png`，由浏览器测试使用模拟图片 API 制作，只证明界面与 API 请求流程，不能代表真实生成画质。生产页面仍要求已有图片平台可用并配置 API Key。

## 验收

- 每步只显示对应工作区；允许返回编辑，门槛阻止跳过必填步骤，切换标签后状态可恢复。
- 自动规划展示 API 返回的提示词和视觉分析状态；失败有明确反馈，生成仍沿用原有验证。
- 右侧指南调用真实任务 API，完成后显示图片并可下载；缺少参考图、失败和重复提交均有反馈。
- 现有自由换衣生成、其他电商标签与暗色/小屏布局无回归。
