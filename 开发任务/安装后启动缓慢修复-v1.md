# 安装后启动缓慢修复

状态：已完成
当前阶段：3/3
最后更新：2026-08-30

## 当前状态

已确认安装版启动时后端会在导入阶段加载 `cv2`、`numpy` 和 `onnxruntime`，并在窗口创建前同步恢复历史任务，造成桌面窗口出现前的重量级初始化。安装目录文件完整，问题集中在后端启动路径。

## 下一步

当前任务已完成；用户安装新版 EXE 后可复测首次启动时间。

## 当前 TODO

- [x] 定位重量级启动导入
- [x] 将深度推理依赖改为按需导入
- [x] 将历史任务恢复移到服务就绪后的后台任务
- [x] 重新编译安装包
- [x] 完成启动性能与功能回归验证

## 最近验证状态

- 静态检查：`canvas_core/depth_inference.py` 编译通过；模块导入不再加载 `onnxruntime`、`cv2`、`numpy`
- 安装包：`SHIYIN-AI-Setup-1.0.375.exe`，SHA-256 待最终校验
- 桌面冷启动冒烟：通过，`startup_ms=4633`，后端 `64.37 MiB`，进程树 `217.47 MiB`
- 相关测试：35 项通过
- Git commit：待提交

## 任务目标

减少安装版启动时的阻塞初始化时间，同时保持 `/api/depth/estimate` 深度推理功能不变。

## 当前关键修改

- `canvas_core/depth_inference.py`：移除顶层重量级依赖，在首次深度推理时导入。
- `main.py`：历史任务恢复改为健康检查可用后的后台任务，并在退出时取消任务。
- `tests/test_startup_architecture.py`：更新启动关键路径契约测试。

## 验收结果

- 后端启动时不再预加载 `onnxruntime`、`cv2`、`numpy`。
- 深度估计请求首次执行时仍按需加载上述依赖。
- 安装器版本、文件大小、ProductVersion 和 SHA-256 已校验。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]
