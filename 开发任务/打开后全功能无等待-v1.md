# 打开后全功能无等待

状态：已完成
当前阶段：4/4
最后更新：2026-08-30

## 当前状态

已完成首屏 iframe 预加载和驻留升级：预加载改为 3 路受控并行、逐页记录就绪状态，并把带版本号的 JS/CSS/字体/静态图片资源放入 Service Worker 持久缓存，减少后续启动的资源下载。后端冷启动瓶颈已完成导入分析，主要耗时来自 PyInstaller/桌面宿主，未进行高风险运行时重构。

## 下一步

本阶段已重新封装 Windows 安装器并通过桌面运行冒烟。若要进一步压缩 4 秒级桌面冷启动，需要单独拆分 PyInstaller sidecar 或调整 Tauri 窗口创建时序。

## 当前 TODO

- [x] 梳理后端健康检查和前端 iframe 路由启动链路
- [x] 首屏当前页面就绪后再解除启动遮罩
- [x] 后台预加载全部可用页面
- [x] 预加载页面保持驻留，避免再次冷加载
- [x] 受控并行预加载和就绪状态跟踪
- [x] 版本化静态资源持久缓存
- [x] 契约测试与回归验证
- [x] 重新封装并验证 Windows 安装器

## 最近验证状态

- 静态检查：`node --check`（Service Worker、客户端脚本、8 段 index 内联脚本）、`git diff --check` 通过
- 单元测试：相关 16 项通过；完整套件 951 项通过，12 项既有画布资源/节点契约失败，与本次启动缓存修改无关
- 编译：安装器重新封装成功；桌面运行冒烟通过，`startup_ms=4624`，进程树 `223.14 MiB`
- 安装器：`dist/installer/SHIYIN-AI-Setup-1.0.376.exe`，70.84 MiB，SHA-256 `8c3171c0086d4007f70cbfe72330d154d65c3ed57463f3230cb9222e7949f930`
- 最近 Git commit：待提交

---

## 任务目标

让用户打开软件并完成账号校验后，所有本地功能页面均已在后台加载；点击导航时直接显示已初始化页面，避免首次进入等待。

## 技术方案

- 当前 iframe 继续按 `data-src` 管理 URL，但在首屏目标页面设置 `src` 后等待 `load` 或短超时，再解除启动遮罩。
- 首屏完成后使用带时间片的调度器，按导航顺序为其它非管理员页面设置 `src`；管理员账号额外预加载设置页面。
- 所有 iframe 使用 `loading="eager"`，已加载页面不再经过 15 分钟空闲卸载，避免再次触发初始化。
- 预加载采用 3 路受控并行，记录每个 iframe 的 `ready/error` 状态并派发 `studio-all-frames-ready` 事件。
- Service Worker 仅缓存带版本号的同源静态脚本、样式、字体和图片，不缓存 HTML 或 API 响应，避免绕过账号权限和读取陈旧动态数据。
- 预加载失败不阻塞当前页面；用户点击失败页面时仍沿用原有按需加载路径。

## 验收标准

- 当前页面 `load` 事件或就绪超时后才解除启动遮罩，不出现长时间空白。
- 登录后的普通账号后台会为 `ecommerce`、`gpt-chat`、`canvas`、`asset-manager`、`works` 完成预加载；管理员再包含 `api-settings`、`app-settings`。
- 已设置 `src` 的 iframe 不会被空闲回收；切换到已预加载页面不重新创建文档。
- 新增契约测试通过，既有启动相关测试和 JavaScript 语法检查通过。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]
