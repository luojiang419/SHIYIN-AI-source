# 无提示词图片顺序自动导演增强 v1

状态：已完成
当前阶段：2/2
最后更新：2026-08-29

## 当前状态

空提示词自动解析已能进入自主导演模式。本次增强补充图片顺序时间线规则，并在模型漏写规范图片标签时使用同一 provider/model 做一次引用修复，降低仅图片输入时的偶发失败。

## 下一步

自动解析、响应协议和前端语法回归已通过；准备提交并推送本次增强。

## 当前 TODO

- [x] 无提示词时按图片顺序明确开场、发展、变化/揭示、收束职责
- [x] 漏写图片标签时增加一次同模型引用修复
- [x] 回归验证
- [x] Git 提交与推送

## 最近验证状态

- 单元测试：`pytest -q tests/test_video_auto_parse.py tests/test_responses_protocol.py`，35 passed
- 静态检查：`python -m py_compile main.py`、三个 `node --check`、`git diff --check` 均通过
- 最近 Git commit：`378dc28 feat: improve image-only auto parse orchestration`

## 验收标准

- 多张图片无提示词时，请求明确要求保留上传顺序并编排连续故事。
- 模型首次输出漏标签时，自动重试一次格式修复；修复仍失败才返回明确错误。
- 不切换 provider/model，不改变有提示词时的润色流程。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]
