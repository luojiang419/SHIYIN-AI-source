# 连续视觉解析任务 v1

状态：开发中
当前阶段：6/6
最后更新：2026-08-29

## 当前状态

现有视频润色/自动解析已经由前端提交本地后台任务并轮询；电商 Responses 请求使用 SSE。当前补充将 SSE 增量文本写入任务状态，由前端显示阶段状态和生成草稿，成功后用最终提示词覆盖回填。

## 下一步

完成新版编译后，用安装包启动并执行一次 GUI 回归，确认解析中的草稿增量和最终回填。

## 当前 TODO

- [x] Responses SSE 流式请求与统一结果解析
- [x] 连续任务阶段/错误信息记录流式进度
- [x] 测试与真实电商专用 API 验证
- [x] 编译新版安装包并记录校验值
- [x] SSE 增量回调、任务进度状态和三类画布提示词框回填
- [ ] 编译 1.0.373 安装包并完成 GUI 回归

## 最近验证状态

- 静态检查：`python -m py_compile main.py` 通过
- 单元测试：`pytest -q tests/test_responses_protocol.py tests/test_video_auto_parse.py tests/test_ecommerce.py` 165 passed
- 前端语法：三个画布脚本 `node --check` 通过
- 任务进度冒烟：后台任务状态从 running/visual-parse 累积增量并在 completed 保留最终结果
- 真实运行：安装目录配置 `ecommerce-vision + gpt-5.6-sol`，4 图 + 完整导演规则 + 联网搜索通过，返回 7316 字符并覆盖 4 张图
- 原生后台探测：代理对 `background:true` 返回 400 Unsupported parameter；SSE 返回 200 text/event-stream
- 安装包：`dist/installer/SHIYIN-AI-Setup-1.0.372.exe`，73,213,676 bytes，SHA-256 `66172972130FA89242947FCFF0F7D8E5FB754509E8F11B9F36FA1AE4567FAAA7`
- 最近 Git commit：`bf45a46 chore: release continuous visual task build`

## 任务目标

将润色/自动解析改造成类似 CLI agent 的连续后台任务：前端只提交任务并轮询，Responses 上游使用流式连接持续接收事件；任务阶段、provider、model、错误和结果可追踪。完整版导演规则必须始终保留，且严禁错误拉起其它模型。

## 技术方案

- 对 `protocol=responses` 的文本和视觉请求发送 `stream=true`，消费 SSE `response.output_text.delta` 与终态事件。
- 将增量文本聚合成现有 `text_from_llm_response` 可消费的响应结构，保留 `usage`、`id` 和 `output`（如有）。
- 仅在响应尚未形成且上游返回 524/网络错误时重试同一 provider/model/完整请求。
- 保留本地后台任务的 queued、web-search、visual-parse、succeeded、failed 状态；不依赖代理未实现的 `background:true`。

## 验收标准

- Responses 代理返回 SSE 时，视觉任务不再等待单个 HTTP 响应体，能持续接收并完成结果。
- 4 张参考图 + 完整导演规则 + 同模型联网搜索真实任务成功，结果覆盖全部图片标签。
- 所有测试通过，安装包可启动；日志明确 provider/model/protocol/stream 状态。
- 失败时不切换模型、不切换 provider，并能显示具体任务阶段。

## 已完成内容

- `request_llm_json` 对 Responses 自动使用 SSE；非 Responses 协议保持原有 JSON POST。
- 流式事件聚合为兼容 `text_from_llm_response` 的结构，保留 response id、output、usage。
- 真实长任务在视觉阶段持续运行并成功完成，未发生模型或 provider 切换。
- Responses `response.output_text.delta` 通过回调实时更新 `progress_text`；前端轮询显示草稿，完成后统一回填 `result.text`。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

新会话启动：阅读本任务文档、检查 Git 状态，从“下一步”继续；不要重复已完成的同步后台任务实现。
