# 静态资源缓存版本回归修复

状态：已完成
当前阶段：4/4
最后更新：2026-08-31

## 当前状态

已确认 `1.0.375` 安装包正常，375 之后加入 Service Worker 静态资源缓存和 iframe 全量预加载。`/static/*.html` 仅设置 no-store 响应头，未重写页面内的历史 `data-src/src/href` 查询串，导致 iframe 在升级后继续组合旧版脚本、CSS 与节点模块。该路径可解释 AI 助手回到旧模型选择界面、Lookbook 选择弹窗不响应和蓝色旧样式残留。

已在 `VersionedStaticFiles` 对 HTML 响应统一调用 `versioned_static_html`，使源码运行和打包运行都使用当前应用版本的资源键；新增回归测试覆盖 iframe `data-src`。

## 下一步

1. 完成一次完整测试与静态检查。
2. 提交修复，并在发布安装包时执行现有 `stamp-web-cache-version.mjs` 与桌面 smoke。

## 当前 TODO

- [x] 对比 1.0.375 与当前版本的资源加载/缓存链路
- [x] 修复 `/static/*.html` 内嵌资源版本重写缺口
- [x] 增加 `data-src` 版本回归测试
- [x] 执行针对性测试和 Python 编译检查
- [ ] 构建新的安装包并完成用户桌面实例验收

## 最近验证状态

- 静态检查：`python -m py_compile main.py`、`node --check static/js/canvas-lookbook-node.js`、`git diff --check` 已通过
- 单元测试：`27 passed`（启动架构、静态缓存、Lookbook、AI 助手 UI 契约）
- 编译：前端静态资源无需编译
- 运行测试：本地 `1.0.384` 服务确认 AI 助手模型弹层显示“对话/图像”两类模型；重启服务后请求 `/static/gpt-chat.html?v=old` 与 `/static/canvas.html?v=old` 均已重写为 `v=1.0.384.<mtime>`；Lookbook 需在新安装包中再做桌面实例冒烟
- 全量回归：`989 passed, 13 failed`；13 项为任务文档已记录的既有基线断言（影视线稿、旧智能画布、旧主题和启动 profile），本次改动未新增失败
- 最近 Git commit：待提交

## 任务目标

修复 1.0.375 之后因静态资源缓存/版本键不一致造成的页面回退和旧样式残留，确保 AI 助手模型选择、Lookbook 风格弹窗和画布节点逻辑从同一版本资源加载。

## 关键证据

- `9a71d89`：引入 Service Worker 静态资源 cache-first 与 iframe 并行预加载。
- `75deb02` 及后续 Lookbook 提交：新增 Lookbook 节点和风格弹窗；旧 iframe/脚本组合时节点行为缺失或事件契约不匹配。
- 当前 `static/index.html` 的 iframe 使用 `data-src`，而原 `VersionedStaticFiles` 只改响应头，未对 HTML 内容做版本重写。
- 当前修复后，HTML 中的 `data-src="/static/gpt-chat.html?..."` 会被替换为 `v=<当前版本>.<文件修改时间>`，Service Worker 无法再命中旧版资源键。

## 验收标准

- `/static/*.html` 响应内所有静态脚本、样式和 iframe `data-src` 使用当前版本查询串。
- 旧 Service Worker cache 不会把旧版模型选择器、Lookbook 节点和蓝色主题资源重新组合到新版页面。
- 现有启动架构、缓存、Lookbook、AI 助手契约测试继续通过。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]
