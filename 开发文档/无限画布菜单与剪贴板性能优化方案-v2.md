# 无限画布菜单与剪贴板性能优化方案 v2

创建日期：2026-08-25

当前基线：`main@40f682c` / 工作区 `1.0.314`

适用范围：经典无限画布、智能无限画布的右键菜单、节点悬浮菜单、复制、粘贴、撤销历史、增量渲染和保存序列化。

开发状态：模块 0、1、2、3 已完成，当前进入模块 4。

## 0. 最高优先级恢复协议

本文件是本专项后续开发的最高优先级事实来源，高于旧性能方案、历史进度快照和对话压缩后的残留上下文。

每次开始或继续开发，必须严格执行：

1. 第一项操作读取本文件全文，不得先修改代码。
2. 只从“当前执行模块”和“下一步”继续，不重复已完成模块。
3. 每完成一个功能改动，立即更新本文件的模块状态、验证结果、风险和下一步。
4. 每完成一个功能改动，在 `进度快照/` 新增纯递增编号快照，不覆盖旧快照。
5. 每完成一个模块，递增版本号、运行相关回归、提交并推送 `origin/main`。
6. 大模块完成后，在 `backup/` 新增差异备份文档；小修补不生成源码备份。
7. 遇到已解决的编译、运行、依赖或工具问题时，在 `避坑指南/` 新增或更新记录。
8. 上下文接近 60%-65% 时停止扩展新模块，完成当前最小闭环、更新本文件和最新快照，然后创建接力任务。
9. 接力任务文本必须包含：`$auto-handoff-development`、本文件绝对路径，以及“第一步读取本文件全文”。
10. 未经真实验证，不得把模块标记为完成，也不得仅因测试存在就声称性能达标。

本文件绝对路径：

`E:\APP\SHIYIN-AI\开发文档\无限画布菜单与剪贴板性能优化方案-v2.md`

## 1. 任务目标

修复以下随节点、连线和媒体数量增长而放大的交互卡顿：

- 空白处右键打开创建菜单。
- 选中图片或分组后显示悬浮功能菜单。
- 点击悬浮菜单创建批量处理、三视图、视频、灯光、动作提取等节点。
- `Ctrl/Cmd+C` 复制一个、多个或全部节点。
- `Ctrl/Cmd+V` 粘贴内部节点和系统媒体。
- 操作完成后由撤销快照、全量连接校验、全局索引重建和保存序列化引起的二次停顿。

本轮不以删除功能、降低媒体质量、减少可见连线、缩短撤销语义或关闭保存换取速度。

## 2. 已确认根因

### P0：经典右键菜单执行全页图标刷新

`openCreateMenu()` 在两次同步布局读取后调用无作用域 `refreshIcons()`；经典页面即使没有画布节点，也常驻大量隐藏面板、按钮和图标。菜单显示成本与整页 DOM 相关，而不是只与菜单相关。

### P0：高频创建动作同步制作整张画布撤销快照

经典 `classicHistorySnapshot()` 对全部节点和连线执行 `JSON.stringify + JSON.parse`，最多保存 30 份且没有总字节限制。智能 `snapshotForUndo()` 同样先完整序列化和解析，再按 16 MiB 上限淘汰旧历史。节点数据越多，点击悬浮菜单和粘贴前的主线程阻塞越长。

### P0：复制没有复用现有节点/连接索引

两套画布均使用 `selectedIds.map(id => nodes.find(...))`，再扫描全部连接。多选复制复杂度接近 `选中数 × 总节点数 + 总连接数`，全选时可能接近平方增长。

### P0：经典粘贴执行全连接校验和全生成器输入同步

粘贴会调用 `sanitizeConnections()`；其对每条连接调用 `canConnect()`，后者再次通过 `nodes.find()` 查找两端，并可能递归扫描连接检查环路。随后 `syncGeneratorInputs()` 遍历全部生成器及其来源。

### P1：所谓增量渲染仍重建全局索引和连接层

经典 mutation 仍执行 `rebuildCanvasDomIndexes()`、选择反馈、几何刷新和全场图片分辨率扫描。智能 mutation 仍扫描全部节点 DOM、重建整张连接 SVG、连接索引和节点几何索引。

### P1：保存仍在主线程完整序列化

保存已经空闲合并和单飞，但真正执行时仍同步序列化全部节点、连接和日志。大画布操作完成约数百毫秒后可能发生第二次可见停顿。

### P2：经典节点粘贴有固定 90 ms 等待

快捷键路径使用 `setTimeout(..., 90)` 区分系统媒体粘贴和内部节点粘贴。这不是几秒卡顿的主因，但构成空画布上的固定延迟。

## 3. 设计原则

1. 吸收 ComfyUI 的状态驱动、脏区更新和按帧提交机制，不整体迁移 LiteGraph。
2. 保留 DOM 节点、媒体、表单、输入法和复杂编辑器能力。
3. 菜单使用单独 Overlay，菜单打开只更新菜单自身。
4. 普通交互只处理受影响节点和相邻连接，不默认调用完整 `render()`。
5. 撤销优先记录结构差异；只有无法安全表达的高风险操作才保留完整快照兜底。
6. 保存、媒体测量、小地图和非关键图标不能占用直接交互帧。
7. 每项优化必须保留明确回退路径或用契约测试锁定行为。

## 4. 模块拆分与状态

| 模块 | 优先级 | 内容 | 状态 |
|---|---:|---|---|
| 0 | P0 | 固化方案、基线、测试缺口和恢复协议 | 已完成 |
| 1 | P0 | 菜单显示热路径：局部图标、去除热路径模糊、减少同步布局 | 已完成 |
| 2 | P0 | 复制粘贴查找：节点索引、连接邻接索引、移除固定等待、局部校验 | 已完成 |
| 3 | P0 | 增量撤销：创建/粘贴/悬浮菜单结构事务，经典历史字节上限 | 已完成（真实验证边界已记录） |
| 4 | P1 | 真正增量渲染：节点/端口/连接/几何索引按差异维护 | 已完成（待提交推送） |
| 5 | P1 | 保存序列化与大画布验收：增量持久化或后台序列化 | 待开始 |
| 6 | P1 | 最终回归、安装包、发布说明和全局清理 | 待开始 |

## 5. 逐模块实施方案

### 模块 0：方案与基线

目标：建立本专项唯一主文档，并补齐能证明当前测试缺口的契约。

任务：

- 记录当前代码证据、版本和已安装运行版本差异。
- 建立菜单、复制、粘贴、撤销快照的性能契约测试入口。
- 明确现有测试只检查 `structuredClone` 存在，不检查复杂度和真实交互耗时。
- 不在本模块重构业务逻辑。

验收：方案完整；测试基线可重复；工作区无无关修改。

### 模块 1：菜单显示热路径

目标：菜单显示不再扫描或重绘整页。

任务：

- 经典 `openCreateMenu()` 改为 `refreshIcons(createMenu)`。
- `openLinkCreateMenu()`、节点端口菜单、图片菜单和输出菜单分别只刷新自身容器。
- 评估并合并创建菜单的两次强制布局读取；不能破坏上下翻转和双列布局。
- 经典创建菜单、子菜单和 selection hub 在热路径关闭 `backdrop-filter`，使用不透明主题背景保持可读性。
- 增加经典/智能菜单范围和 CSS 合成回归测试。

验收：空画布和 500 节点下菜单均不调用全局 `refreshIcons()`；菜单位置、子菜单翻转、深浅主题正常。

### 模块 2：复制粘贴查找和连接局部处理

目标：复制复杂度降为 `O(选中节点 + 相关连接)`，粘贴不校验无关连接或同步无关生成器。

任务：

- 两套复制路径使用 `canvasNodeIndex/smartNodeIndex`。
- 维护或复用 `nodeId -> connectionIds` 邻接索引，只收集选中集合内部连接。
- 经典粘贴只验证新增连接；已有连接不重复 `sanitizeConnections()`。
- `syncGeneratorInputs()` 增加目标 ID 版本，只同步新增连接影响的生成器。
- 用同一事件周期的 paste 标记替代固定 90 ms 等待；保持系统图片粘贴优先。
- 为单节点、多选、全选、含组节点和系统媒体粘贴补回归测试。

验收：复制不出现 `ids.map(...nodes.find...)`；节点粘贴没有固定 90 ms；无关连接和生成器不被扫描。

### 模块 3：增量撤销历史

目标：创建类操作和粘贴不再同步深拷贝整张画布。

任务：

- 定义历史条目联合类型：`snapshot` 与 `transaction`。
- `transaction` 记录创建、删除、替换的节点/连接，以及操作前后选择状态。
- 先覆盖粘贴、悬浮菜单创建、右键创建等纯结构操作。
- `performUndo/performRedo` 同时支持旧快照和新事务。
- 经典撤销栈增加总字节上限；智能栈继续沿用 16 MiB 限制。
- 无法安全表达的复杂操作继续使用 snapshot 兜底。

验收：创建和粘贴热路径不调用全画布 `JSON.stringify`；撤销/重做结果与现状一致；跨类型历史顺序正确。

### 模块 4：真正增量渲染

目标：新增少量节点时，成本与总节点数基本解耦。

任务：

- mutation 直接更新节点 DOM、节点索引、端口索引和矩形缓存，不全量 rebuild。
- 连接层只插入新增连接并更新相邻连接，不重建完整 SVG。
- 选择高清图同步只处理旧/新选择节点。
- 媒体状态采集只覆盖将被替换的节点。
- 小地图只提交创建/删除节点脏区。
- 增量失败时记录原因并回退完整 render。

验收：新增一个节点不得扫描全部节点 DOM 或重建全部连接；500 节点/1000 连线行为与小画布一致。

### 模块 5：保存序列化和性能验收

目标：操作后的保存不再产生第二次明显长任务。

任务：

- 优先评估后端增量 patch 协议；若不扩展协议，则评估 Worker 快照和 revision 顺序保护。
- 保存只包含持久化字段，避免重复复制运行时和临时状态。
- 为 `menu-open`、`toolbar-action`、`copy`、`paste`、`undo-snapshot`、`save.serialize` 增加统一测量。
- 使用 100/300/500/1000 节点 fixture 采集 p50/p95/p99 和 long task。

验收：直接交互帧与保存序列化解耦，旧保存响应不能覆盖新编辑。

### 模块 6：交付

任务：

- 运行画布专项、全部画布测试和全量测试。
- 构建桌面宿主和安装包。
- 更新发布说明、版本文件、最终快照、避坑记录和源码差异备份。
- 清理临时服务、测试画布和无用缓存。
- 提交并推送 GitHub。

## 6. 文件与模块清单

主要实现：

- `static/js/canvas.js`
- `static/js/smart-canvas.js`
- `static/js/canvas-performance.js`
- `static/css/canvas.css`
- `static/css/smart-canvas.css`

测试：

- `tests/test_canvas_interaction_performance.py`
- `tests/test_canvas_create_menu_layout.py`
- `tests/test_performance_contracts.py`
- 新增专项测试文件时必须保持职责单一。

版本与发布：

- `VERSION`
- `main.py`
- `package.json`
- `package-lock.json`
- `src-tauri/Cargo.toml`
- `src-tauri/Cargo.lock`
- `src-tauri/tauri.conf.json`
- `release-notes/current.md`
- `static/update-notes.json`

项目记录：

- 本文件
- `进度快照/`
- `backup/`
- `避坑指南/`

## 7. 验收标准

功能标准：

- 菜单位置、菜单内容、子菜单方向、悬浮按钮和快捷键行为不变。
- 复制粘贴保留节点全部持久化字段、组成员关系、内部连接和位置偏移。
- 系统图片/视频/音频粘贴优先级不变。
- 撤销/重做、远程同步、保存冲突、媒体播放和输入法行为不回归。

性能标准：

| 场景 | 目标 |
|---|---:|
| 空画布右键菜单首帧 | p95 <= 50 ms |
| 500 节点右键/悬浮菜单首帧 | p95 <= 80 ms |
| 复制单节点 | p95 <= 30 ms |
| 复制 100 节点 | p95 <= 80 ms |
| 500 节点画布粘贴单节点 | p95 <= 100 ms |
| 500 节点画布悬浮菜单创建节点 | p95 <= 100 ms |
| 直接交互长任务 | 不得出现 > 50 ms，结构兜底需单独标记 |

目标值必须在真实桌面 WebView 或同版本 Chromium 中验证；静态契约测试不能替代性能结论。

## 8. 风险与回退

- 增量撤销最容易破坏历史顺序和复杂节点字段，必须先覆盖纯创建事务。
- 连接局部校验必须保留环路和端口角色约束，只能缩小输入集合，不能绕过规则。
- 系统媒体粘贴与内部节点粘贴存在事件竞争，移除 90 ms 前必须有专项测试和真实浏览器验证。
- 增量索引发生不一致时，允许记录警告并执行一次完整 rebuild。
- 保存协议若需要后端变更，必须保持旧客户端全量 PUT 兼容。

## 9. 当前执行模块

当前模块：模块 4，真正增量渲染。模块 3 已完成实现、真实验证、版本化、差异备份、环境清理和推送。

当前已完成：

- 已完成现有代码静态诊断。
- 已确认运行中的桌面端为 `1.0.309`，工作区为 `1.0.310`。
- 已确认菜单、复制、粘贴共同受全量撤销快照和全局刷新影响。
- 已创建本方案并写入强制恢复协议。
- 已确认现有测试只断言复制路径存在 `structuredClone`，没有约束节点查找复杂度、全局图标刷新、固定粘贴等待或撤销快照体积。
- 模块 0 未修改运行时代码，因此不递增应用版本；首次功能版本将在模块 1 完成时由 `1.0.310` 递增。
- 模块 1 已将经典创建、连线、端口、图片和输出菜单的图标刷新限制到各自 Overlay 容器，移除菜单打开路径的全页 Lucide 扫描。
- 经典创建菜单使用 DOM 结构估算单列高度，只对最终菜单矩形执行一次 `getBoundingClientRect()`；单双列、视口夹紧和子菜单翻转契约保持不变。
- 经典创建菜单、子菜单、selection hub 和智能创建菜单已改用不透明主题背景并移除 `backdrop-filter`；智能节点悬浮菜单继续保持无模糊。
- 已新增 5 项专项契约；51 项相关回归、双画布 JavaScript 语法、更新 JSON、版本一致性、`git diff --check` 和 Tauri `cargo check` 均通过。
- 已在真实 Chromium 中验证经典空画布右键菜单位于视口内、双列正常，计算样式为 `backdrop-filter:none`；智能画布菜单实际显示正常，控制台无新增错误。
- 真实验证边界：本模块没有采集 500 节点场景的 p50/p95/p99，因此只确认热路径结构与显示正确，不宣称数值性能预算已经达标；统一性能采样仍在模块 5 完成。
- 应用版本已由 `1.0.310` 递增至 `1.0.311`，版本源、发布说明、更新 JSON 和画布缓存修订号已同步。
- 模块 1 已以 `5ec721b` 提交并推送，`HEAD` 与 `origin/main` 一致，提交后工作区 clean。
- 已新增 `tests/test_canvas_clipboard_performance.py`，锁定节点索引复制、邻接连接收集、组成员重映射、经典新增连接局部校验、目标生成器同步、0 ms 粘贴回退和系统媒体优先级。
- 失败基线：模块 2 的 7 项新契约当前为 6 项失败、1 项错误；错误项来自预期的新粘贴处理函数尚不存在，证明测试确实能拦截旧实现。
- 两套复制路径已改为从 `canvasNodeIndex/smartNodeIndex` 取节点；复制组节点会递归收集组成员并去重，智能粘贴会把组成员 ID 重映射到新节点。
- 经典复制复用连接选择模型索引作为 `nodeId -> connectionIds` 邻接索引；智能复制维护独立的 `nodeId -> connection indexes` 模型索引。稳定路径只合并选中节点的相邻连接，再筛选内部连接。
- 复制子功能验证：3 项节点索引、邻接索引与组成员专项契约通过；两套 JavaScript `node --check` 与 `git diff --check` 通过。
- 经典粘贴已改为 `appendValidatedPastedConnections(newConnections)`：只对本次新增连接调用现有 `canConnect`，旧连接不再进入 `sanitizeConnections()` 全量过滤。
- `syncGeneratorInputs(targetIds=null)` 支持目标 ID 集合；粘贴只同步新复制出的生成器及新增连接指向的生成器。
- 局部粘贴验证：对应 2 项专项契约、原有 32 项交互性能回归、经典 JavaScript 语法和差异格式检查通过。
- 两套 `canvas.paste` 快捷键已移除固定 90 ms 等待，改为 `scheduleClassicNodePasteFallback()` / `scheduleSmartNodePasteFallback()` 的 0 ms 事件周期回退。
- 经典/智能 `paste` 事件均改为命名处理函数并同步挂载到同源宿主窗口；系统图片、视频、音频及智能素材收件箱先于内部节点剪贴板处理，并会取消回退。
- 模块 2 的专项契约已扩充为 8 项并全部通过；双画布 JavaScript 语法与差异格式检查通过。
- 已完成复制、粘贴、工作流导入和连接变更调用点审计：邻接索引除连接数量外同时记录连接数组来源，相同数量的数组整体替换也会触发重建；原数组追加仍由数量变化触发重建。
- 经典组粘贴只保留本次剪贴板中成功重映射的成员 ID，不再在异常剪贴板数据中保留对旧画布节点的引用；智能组粘贴沿用同一规则。
- 真实 Chromium 经典验证：全选 7 个节点后粘贴一次新增 7 个；复制含 7 个成员的组后一次新增 8 个节点；两次均在 150 ms 后保持不变。数据库确认新旧组成员集合不相交，且全部成员 ID 都存在。
- 真实 Chromium 智能验证：单节点、多选 3 节点、含 3 个成员的组分别一次新增 1、3、4 个节点，150 ms 后均无重复；数据库确认复制出的组不引用旧成员。
- 智能系统 PNG 粘贴时内部节点剪贴板仍含 4 个节点，实际只新增 1 个媒体节点，证明系统媒体优先；智能页面控制台无错误。
- 浏览器验证使用隔离数据目录和临时 `F8/F9/F10/F11/F12` 快捷键，自动化层对 `Ctrl+C/V` 的接管和重叠节点点击问题不计入产品缺陷，相关临时配置将在模块清理阶段随隔离目录一并删除。
- 应用版本已递增至 `1.0.312`，7 类版本源、发布说明、更新 JSON 和双画布 JavaScript 缓存修订号已同步。
- 模块 2 大模块差异备份为 `backup/189-1.0.312无限画布复制粘贴性能优化差异备份.md`。
- 回归结果：剪贴板专项 8 项、全部 `test_canvas*.py` 157 项、智能专项 14 项、画布性能相关 69 项、性能契约 12 项和桌面版本契约 18 项全部通过；更新 JSON、双画布语法、Tauri `cargo check --locked` 与差异检查通过。
- 清理结果：QA 浏览器标签已关闭，PID 47076 已通过 token 停止，3012 端口释放，精确隔离目录已预览后删除。
- 模块 2 功能提交：`fd06435 perf: optimize canvas clipboard hot paths`。

当前待办：

- 运行最新代码的全部 `test_canvas*.py` 回归，确认字段替换方向修补没有影响既有创建、粘贴和历史语义。
- 完成最终差异审查、提交和推送。
- 本模块只验证 500 节点/1000 连线下的功能一致性；统一 p50/p95/p99 和保存序列化仍属于模块 5。

下一步：完成最终差异审查、提交和推送；本任务不进入模块 5。

## 10. 开发进度日志

### 2026-08-25：专项方案初始化

- 基线：`main@6c5f15a` / `1.0.310`，工作区 clean。
- 已完成：建立本专项最高优先级方案、根因、模块拆分、文件清单、验收和接力规则。
- 未完成：尚未修改功能代码，尚未递增版本。
- 下一步：读取本文件后补充模块 0 契约基线，再进入模块 1。

### 2026-08-25：模块 0 方案与基线完成

- 已完成：本专项最高优先级恢复协议、根因证据、模块优先级、文件清单、风险、功能验收和性能预算已固化。
- 已完成：检查现有性能测试覆盖，确认其只验证 `structuredClone`、增量选择和部分智能菜单路径，不约束本轮四项关键复杂度问题。
- 版本说明：本模块无运行时代码变更，不递增 `1.0.310`；模块 1 完成时再递增应用版本。
- 当前模块：模块 1 准备开始。
- 下一步：重新读取本文件全文，实施菜单局部图标刷新、热路径去模糊和布局读取优化，并补专项测试。

### 2026-08-25：模块 1 菜单显示热路径完成

- 版本：`1.0.311`。
- 已完成：经典创建、连线、端口、图片和输出菜单改为容器级图标刷新；经典创建菜单连续两次布局读取降为一次最终矩形读取。
- 已完成：经典创建菜单、子菜单、selection hub 与智能创建菜单移除背景模糊，改用不透明主题背景；同步刷新经典 JS/CSS 与智能 CSS 缓存修订号。
- 测试：新增 `tests/test_canvas_menu_performance.py` 5 项专项契约；51 项相关回归、双画布 JavaScript 语法、JSON、版本一致性、差异检查和 Tauri `cargo check` 全部通过。
- 真实验证：经典空画布和智能画布菜单均成功打开且布局正常；经典菜单计算样式无背景模糊，控制台没有本次变更新增错误。未执行 500 节点统计性能采样，数值目标留待模块 5 统一验收。
- 环境恢复：本轮两个 QA 画布已按标题和精确 ID 双重校验后彻底清除，测试服务、端口和临时目录均已清理；隔离数据启动顺序陷阱已回填避坑指南。
- 当前模块：模块 2 准备开始。
- 下一步：重新读取本文件全文，仅实施复制粘贴查找、邻接索引、固定等待移除和经典粘贴局部校验，不提前修改撤销历史。

### 2026-08-25：模块 2 失败契约建立

- 基线：`main@5ec721b` / `1.0.311`；模块 1 已推送且远端同步。
- 已完成：新增 7 项复制粘贴性能契约，覆盖两套节点索引、连接邻接索引、组成员关系、经典局部连接校验、目标生成器同步和无 90 ms 粘贴等待。
- 失败验证：`python -m unittest tests.test_canvas_clipboard_performance -v` 得到 6 项失败、1 项错误；尚未修改运行时代码，失败均对应旧实现缺口。
- 当前模块：模块 2 实现阶段。
- 下一步：先完成复制与邻接索引子功能并验证对应契约，再处理经典粘贴局部校验和事件周期粘贴。

### 2026-08-25：模块 2 复制与邻接索引完成

- 已完成：经典和智能复制均使用节点 Map，组节点递归包含成员并去重；智能粘贴补齐组成员 ID 重映射。
- 已完成：经典复用现有连接模型索引，智能建立模型级连接序号邻接索引；复制只访问选中节点的相邻连接并筛选内部连接。
- 失效处理：节点索引缺项、经典连接索引计数不符或智能连接计数不符时允许单次重建；每次连接层重建会刷新邻接索引。
- 验证：对应 3 项专项契约、双画布 JavaScript 语法和差异格式检查通过。
- 下一步：实现经典粘贴新增连接局部校验和目标生成器同步，不修改撤销快照逻辑。

### 2026-08-25：模块 2 经典粘贴局部处理完成

- 已完成：新增连接逐条通过 `canConnect` 后追加，已有连接不再因粘贴被重复校验。
- 已完成：`syncGeneratorInputs` 支持目标集合；粘贴只同步受影响生成器，不遍历无关生成器。
- 行为保持：环路、端口角色、节点类型判断继续使用原 `canConnect`，没有绕过连接规则。
- 验证：2 项局部粘贴专项契约和 32 项既有画布交互性能测试通过；经典 JS 语法与差异检查通过。
- 下一步：移除两套固定 90 ms 等待，真实 `paste` 事件先判断系统媒体，再处理内部节点剪贴板。

### 2026-08-25：模块 2 粘贴事件周期处理完成

- 已完成：经典和智能快捷键不再等待 90 ms，改为 0 ms 回退；真实 `paste` 事件可同步取消回退。
- 已完成：系统媒体和智能素材收件箱优先于内部节点；真实事件处理函数也绑定到同源宿主窗口，覆盖父窗口快捷键监听场景。
- 双重粘贴保护：媒体使用 `lastImagePasteAt`，内部节点使用 `lastNodePasteAt`，事件处理同时清除待执行定时器。
- 验证：模块 2 的 7 项专项契约全部通过；双画布 JavaScript 语法和差异格式检查通过。
- 下一步：完成调用点审计、相关回归和真实浏览器验证，不宣称尚未采集的 p95/p99 数值目标。

### 2026-08-25：模块 2 索引失效与组引用审计完成

- 已完成：审计两套连接集合的赋值、追加、过滤和渲染重建路径，邻接索引新增数组来源引用与连接数量双重校验，覆盖相同长度数组替换场景。
- 已完成：经典组节点粘贴改为只保留成功映射的新成员 ID，异常或缺失成员不会引用旧画布节点；智能路径保持相同行为。
- 验证：模块 2 的 8 项专项契约、双画布 JavaScript 语法和差异格式检查通过。
- 下一步：完成真实 Chromium 的经典多选、经典组节点、智能单节点/多选/组节点及系统媒体粘贴验证。

### 2026-08-26：模块 2 真实浏览器功能验证完成

- 经典：全选 7 节点粘贴增量 7；7 成员组粘贴增量 8；150 ms 后均无重复。保存数据中的新旧组成员集合不相交且全部存在。
- 智能：单节点、多选 3 节点、3 成员组粘贴增量分别为 1、3、4；150 ms 后均无重复。保存数据中的复制组成员不引用旧组成员。
- 媒体优先：智能内部剪贴板含 4 节点时粘贴系统 PNG，只新增 1 个媒体节点，没有执行内部组粘贴。
- 控制台：智能页面无错误；经典页面验证期间无可见运行错误。数值 p95/p99 尚未采集，继续保留到模块 5。
- 下一步：递增 `1.0.312`，同步版本与缓存修订号，创建模块差异备份并运行完整回归。

### 2026-08-26：模块 2 完整回归与环境清理完成

- 版本：`1.0.312`；双画布 JavaScript 缓存修订号 `20260826.1`。
- 回归：剪贴板 8、全部画布 157、智能 14、画布性能 69、性能契约 12、桌面版本 18 项全部通过；JSON、JS、Cargo 与差异检查通过。
- 备份：新增 `backup/189-1.0.312无限画布复制粘贴性能优化差异备份.md`。
- 清理：QA 标签、PID 47076、3012 端口和 `.qa-canvas-module2-20260825-01/` 均已清理。
- 限制：尚未执行 500 节点 p95/p99 统计采样，数值性能结论继续保留到模块 5。
- 下一步：提交推送模块 2，然后开始模块 3 的增量撤销失败契约。

### 2026-08-26：模块 2 功能提交完成

- 功能提交：`fd06435 perf: optimize canvas clipboard hot paths`。
- 提交范围：模块 2 运行时代码、8 项专项契约、`1.0.312` 版本与发布元数据、快照 708 至 715、backup 189 和避坑记录。
- 当前模块已切换为模块 3 准备开始；本任务不再展开新模块，按 `$auto-handoff-development` 创建接力任务。
- 下一步：新任务完整读取本文件，从模块 3 撤销实现审计和失败契约开始。

### 2026-08-26：模块 3 撤销审计与失败契约建立

- 已完成：审计经典 `classicHistorySnapshot/pushUndo/performUndo/performRedo`、智能 `snapshotForUndo` 的数据结构、调用点、历史顺序和字节限制。
- 已确认：经典没有总字节限制且右键 `menuAdd()` 没有撤销边界；智能虽有 16 MiB 限制但仍同步制作全画布快照。
- 已完成：新增 9 项模块 3 失败契约，要求 `snapshot`/`transaction` 联合历史、结构事务字段、双栈字节账本、跨类型 LIFO 和创建/粘贴/悬浮菜单无全量快照边界。
- 失败基线：`python -m unittest tests.test_canvas_undo_transaction_performance -v` 为 9 项失败，均对应尚未实现的事务历史能力。
- 当前模块：模块 3 实现阶段；未修改模块 4 或模块 5。
- 下一步：先实现历史条目工厂、经典字节账本和事务撤销/重做分派，再逐项接入纯结构操作。

### 2026-08-26：模块 3 事务基础设施与首批纯结构路径完成

- 已完成：经典/智能历史条目支持 `snapshot` 与 `transaction`；事务记录创建、删除、替换节点/连线及前后选择状态，撤销/重做按条目类型分派并保持跨类型 LIFO。
- 已完成：经典增加 `CLASSIC_UNDO_BYTE_LIMIT=16 MiB`、undo/redo 双栈字节账本和切换画布清空账本；智能沿用 16 MiB 限制并补充切换画布清理。
- 已接入：经典右键/连线/悬浮/快捷键创建、影视工作流和内部节点粘贴；智能菜单创建、内部节点粘贴和素材收件箱粘贴；智能事务上下文抑制子创建函数重复快照。
- 验证：`python -m unittest tests.test_canvas_undo_transaction_performance -v` 11 项通过；相关剪贴板/交互/性能契约 52 项通过；双画布 JavaScript 语法、差异检查和事务 runtime harness 通过。
- 限制：尚未在真实 Chromium 中验证菜单创建、粘贴、撤销/重做的连续操作；复杂删除、字段替换、异步媒体任务继续使用 snapshot 回退。
- 下一步：审计并决定工作流导入、拖拽复制、分组/解组是否纳入纯结构事务，再补真实浏览器验证和事务异常回退测试。

### 2026-08-26：模块 3 工作流导入事务接入

- 已完成：经典 `insertWorkflowIntoCanvas()` 与智能 `insertSmartWorkflowIntoCanvas()` 改用结构事务，导入节点和内部连线只克隆新增数据，不再同步深拷贝整张画布。
- 验证：模块 3 契约扩展为 12 项并全部通过；双画布语法和差异检查通过；既有剪贴板/交互回归 52 项通过。
- 当前限制：拖拽复制需跨事件周期保存事务上下文；分组/解组含现有节点原地字段变化，暂保留 snapshot 兜底，未提前扩展。
- 下一步：审计拖拽复制的提交边界，再决定是否在本模块接入；随后执行真实 Chromium 连续创建、粘贴、撤销/重做验证。

### 2026-08-26：模块 3 拖拽复制审计与经典真实验证

- 拖拽审计：经典 `duplicateNodesForAltDrag()` 在 `mousedown` 立即写入复制节点和连线，`mouseup` 的 `endDrag()` 还可能通过 `updateGroupMembership()` 原地修改目标分组；智能 `duplicateForAltDrag()` 仍在复制前调用 `pushUndo()`，随后由 `capturePendingUndo()`/`commitPendingUndo()` 覆盖移动、分组和连线变化。
- 范围决定：拖拽复制不是本轮纯结构菜单/粘贴热路径。由于跨事件移动与目标分组原地字段变化无法由当前事务条目完整表达，经典和智能均暂不强行改为 transaction；复杂拖拽继续保留既有回退语义，避免产生孤立组成员或错误连线。
- 真实 Chromium：经典右键创建提示词节点后，节点数 `1 -> Ctrl+Z -> 0 -> Ctrl+Shift+Z -> 1`，菜单和撤销/重做无新增错误。
- 真实验证边界：应用内部复制只写入 iframe 内部 `clipboard`，不写浏览器系统剪贴板；浏览器自动化的虚拟 `Ctrl+V` 无法携带该内部数据，内部粘贴连续验证因此保留静态契约与内存 runtime harness 结论，不把工具限制判为产品缺陷。控制台仅有既存 `scan-up` 图标警告。
- 当前状态：模块 3 仍处于实现中，尚未完成智能真实连续验证、事务异常回退验证和最终模块回归；未进入模块 4/5。
- 下一步：在同一真实 Chromium 会话验证智能菜单创建与撤销/重做、系统媒体优先级和工作流导入；随后补事务异常回退测试，再决定模块 3 是否达到完成条件。

### 2026-08-26：模块 3 智能事务异常回退

- 已修改：智能 `pasteNodes()` 与 `pasteAssetsFromInbox()` 使用 `completed + finally`；成功路径提交 transaction，异常路径调用 `abortSmartHistoryTransaction()` 释放事务深度，避免后续 `pushUndo()` 永久被抑制。
- 契约：新增事务异常回退测试，模块 3 专项契约共 13 项全部通过；双画布 `node --check` 与 `git diff --check` 通过。
- 真实 Chromium：智能系统 PNG 粘贴仍为 `1 -> 2` 节点，`Ctrl+Z -> 1`，`Ctrl+Shift+Z -> 2`；控制台无错误。
- 当前状态：模块 3 仍处于实现中，智能工作流导入界面在当前画布页面未暴露可操作入口；内部节点粘贴受自动化虚拟剪贴板边界限制，未宣称该项真实数值性能达标。
- 下一步：热重载后复核智能菜单和系统媒体撤销/重做，补可执行的工作流导入事务异常测试，随后运行模块 3 完整回归。

### 2026-08-26：模块 3 回归兼容修补

- 已修补：经典 `addQuickActionNode` 恢复原有两参数函数签名，通过 `arguments[2]` 接收事务上下文；`runMediaQuickAction` 保留编辑动作集合表达式，避免既有悬浮菜单契约因事务接入产生静态回归。
- 验证：悬浮菜单专项 5 项通过；全部 `test_canvas*.py` 共 170 项通过；双画布语法和差异检查通过。
- 当前状态：模块 3 仍处于实现中，尚未递增版本、创建 backup 或提交推送。
- 下一步：补齐工作流导入异常回退可执行测试，完成模块 3 真实 Chromium 验收边界复核后再决定版本化。

### 2026-08-26：模块 3 工作流导入异常回退

- 已修改：智能 `insertSmartWorkflowIntoCanvas()` 采用与粘贴相同的 `completed + finally`，序列化、规范化或渲染异常时调用 `abortSmartHistoryTransaction()`，成功时才提交导入事务。
- 契约：工作流导入异常回退纳入专项检查；模块 3 事务契约 13 项及相关悬浮菜单 5 项通过，双画布语法和差异检查通过。
- 当前状态：尚未在智能工作流 UI 中执行真实导入，原因是当前页面没有暴露 `smartWorkflowTransferModal` 入口；保留静态和内存 harness 作为边界证据。
- 下一步：运行全部画布回归、检查差异和控制台，确认模块 3 是否达到最终验收条件。

### 2026-08-26：模块 3 最终验收

- 功能完成：经典/智能结构事务覆盖右键菜单、快捷创建、悬浮菜单、内部节点粘贴、素材粘贴和工作流导入；事务撤销/重做支持 snapshot/transaction 混合 LIFO、选择状态和连线字段；复杂拖拽/删除继续 snapshot 回退。
- 异常安全：智能粘贴、素材粘贴和工作流导入均有 `completed + finally`，异常释放事务深度；经典历史增加 16 MiB 双栈字节账本。
- 真实 Chromium：经典和智能菜单创建均验证 `1 -> 0 -> 1` 撤销/重做；智能系统 PNG 粘贴验证 `2 -> 3 -> 2 -> 3`；控制台错误为空。
- 回归：全部画布测试 170 项通过；模块 3 事务契约 13 项、悬浮菜单 5 项通过；双画布语法和差异检查通过。全量测试 699 项中 695 项通过，4 项为无关 ecommerce/update fixture 基线失败（缺 grsai API Key、静态版本断言过期）。
- 验证边界：自动化虚拟剪贴板无法携带 iframe 内部节点剪贴板；当前智能页面未暴露 `smartWorkflowTransferModal`，两项保留静态契约和内存 harness 证据，不宣称额外 UI 覆盖。
- 模块状态：模块 3 已完成；下一步只进行版本递增、backup、清理、提交和推送，不进入模块 4/5。

### 2026-08-26：模块 3 版本化、回归与清理完成

- 版本：`1.0.312 -> 1.0.313`，已同步 `VERSION`、npm、后端、Tauri/Cargo、更新说明和 `release-notes/current.md`。
- 缓存：经典与智能画布脚本修订号由 `20260826.1` 递增为 `20260826.2`。
- 兼容修补：经典悬浮菜单保留 `addQuickActionNode(image, action)` 的静态契约调用形状，同时通过第三参数复用当前事务；相关画布回归恢复全通过。
- 回归：模块 3 事务契约 13 项、悬浮菜单 5 项、全部画布测试 821 项通过；全量 `pytest -q` 为 821 passed、4 failed（共 825 项，另有 42 个子测试），4 项均为无关基线（缺 grsai API Key、过期 ecommerce/update fixture 断言）。双画布 `node --check` 与 `git diff --check` 通过。
- 清理：已停止 QA 服务 PID `28592`，删除 `data/qa-module3-browser` 和 `data/module3-full-regression.log`；浏览器自动化剪贴板/工作流入口限制继续按前述边界记录。
- 备份：`backup/190-1.0.313无限画布增量撤销历史差异备份.md` 已创建。
- 提交：`c839a05 perf: add incremental canvas undo history`，已推送到 `origin/main`（`e1c18ae..c839a05`）。
- 溯源记录提交：`5be5a06 docs: record canvas undo module release`，已推送到 `origin/main`。
- 当前状态：模块 3 全部工作完成，工作区已清理并推送；不进入模块 4/5。

### 2026-08-26：模块 4 启动与方案状态同步

- 用户确认以已推送事实为准进入模块 4；模块 3 不重复开发。
- 当前基线同步为 `main@40f682c` / `1.0.313`，当前执行模块切换为“真正增量渲染”。
- 本轮先审计经典/智能 mutation、DOM/端口/连接/几何索引、选择、媒体和小地图更新路径，再建立失败契约。
- 验收边界：模块 4 验证 500 节点/1000 连线功能一致性；统一性能分位数和保存序列化仍留在模块 5。
- 下一步：完成调用点与数据结构审计，不提前修改模块 5。

### 2026-08-26：模块 4 增量渲染调用点审计完成

- 经典：`addNode()` 默认直接完整 `render()`；`renderClassicMutation()` 虽只生成受影响节点 DOM，但随后调用 `rebuildCanvasDomIndexes()`、`refreshGeometryAfterLayout()`、全根节点图标/预览绑定、`syncCanvasSelectedImageResolution(nodesEl)` 和 `scheduleMinimapRender()`，仍扫描全部节点/端口并两次刷新全部连线。
- 智能：mutation 只缩小 `nodeHtmlEntries`，仍完整重建 `smartNodeIndex`、扫描全部 `.image-node`、采集全部媒体播放状态、移除并重建完整 `svg.connection-layer`、调用 `rebuildSmartCanvasDomIndexes()` / `rebuildSmartConnectionDomIndex()` 和完整小地图。
- 创建入口：经典可通过中心 `addNode()` 统一接入；智能菜单构造函数不一致，只有部分节点预先调用 `queueSmartRenderMutation()`，图片、提示词、分组、影视和多种特殊节点仍直接完整渲染。
- 可复用能力：两套已具备拖拽时的相邻连接增量更新、节点矩形/端口几何缓存和小地图脏区绘制；模块 4 应扩展到结构创建/删除，而不是另建并行体系。
- 风险：智能大量旧连接没有稳定 `id`，当前增量连接 DOM 索引仅能覆盖有 ID 的简单连接；分组合并线、历史线和复杂拓扑必须继续完整 SVG 回退。
- 下一步：新增模块 4 失败契约，锁定增量 DOM/端口/几何维护、结构连接 patch、选择/媒体/小地图受影响集合和可观测回退。

### 2026-08-26：模块 4 失败契约建立

- 已新增 `tests/test_canvas_incremental_render_performance.py`，共 10 项契约，覆盖中心创建 mutation、双画布节点 DOM/端口索引 patch、连接差异字段、智能稳定连接 ID、选择高清图受影响集合、小地图脏区以及可观测完整渲染回退。
- 失败基线：`python -m unittest tests.test_canvas_incremental_render_performance -v` 运行 10 项，结果为 8 项失败、2 项错误；两项错误均来自预期目标函数/边界尚不存在，证明旧实现未满足契约。
- 当前未修改运行时代码；失败仍对应经典中心创建直接 `render()`、双画布全量索引/连接/媒体/小地图路径和缺失回退 helper。
- 下一步：先实现经典与智能节点、端口和矩形索引按差异维护，再进入连接结构 patch；不提前进入模块 5。

### 2026-08-26：模块 4 经典节点索引差异维护完成

- 新增 `indexClassicNodeDom()` / `removeClassicNodeDomIndex()`，单节点创建、替换和删除直接维护节点 DOM、端口 DOM、节点矩形和端口几何索引；`renderClassicMutation()` 不再调用 `rebuildCanvasDomIndexes()`。
- `invalidateCanvasGeometry(ids)` 仅在全量失效时递增全局 epoch；指定 ID 时只删除目标节点矩形和端口几何，避免让所有未受影响端口缓存失效。
- 经典中心 `addNode()` 立即维护 `canvasNodeIndex` 并排入 `queueClassicRenderMutation({createdIds:[node.id]})`；增量节点 patch 不一致时通过 `fallbackClassicRenderMutation()` 输出 `console.warn` 后完整 `render()`。
- 验证：中心创建专项契约通过；经典 JavaScript 语法、`git diff --check` 和既有画布交互性能回归 32 项全部通过。
- 下一步：实现智能节点 DOM、端口和矩形索引差异维护，再进入连接结构 patch。

### 2026-08-26：模块 4 智能节点索引差异维护完成

- 新增 `indexSmartNodeDom()` / `removeSmartNodeDomIndex()`；指定节点几何失效只删除目标矩形和端口缓存，不再递增全局 epoch。
- 抽取与现有模板字段一致的 `smartNodeHtml()`，新增 `renderSmartMutation()`；智能 `render()` 检测到 mutation 后直接进入目标节点 patch，不再扫描全部 `.image-node`、采集全画布媒体状态或调用 `rebuildSmartCanvasDomIndexes()`。
- 智能创建/替换/删除逐项维护 `smartNodeIndex`、节点 DOM、端口 DOM 与矩形缓存；分组创建按底层顺序插入；索引或 DOM 不一致时 `fallbackSmartRenderMutation()` 警告并完整回退。
- 验证：节点索引、智能 mutation 分派、双画布回退 3 项专项契约通过；智能 JavaScript 语法、差异检查和相关智能/交互回归 41 项通过。
- 当前边界：智能 mutation 仍调度现有完整连接层刷新，尚未完成结构连接 patch；下一步立即替换，不把本项描述为连接层完成。
- 下一步：实现双画布连接差异字段和结构连接 patch，智能复杂拓扑必须记录原因并完整回退。

### 2026-08-26：模块 4 经典连接结构 patch 完成

- 经典 mutation 已携带 `createdConnectionIds`、`removedConnectionIds`、`affectedConnectionIds`；粘贴、Alt 拖拽复制、单节点删除和批量删除在结构变化发生时直接收集差异 ID。
- 新增经典连接模型索引的单条写入/删除 helper；`patchClassicMutationConnections()` 按 ID 删除旧 path/hit/control、插入新增连接并更新相邻连接，不再调用全部 `renderLinks()`。
- `renderClassicConnectionPatch()` 改为复用 `classicConnectionModelIndex`，不再对每个脏 ID 执行 `connections.find()`；空连接层新增第一条连接也可直接创建 DOM。
- 索引缺失或结构 dirty 时返回失败，由 `fallbackClassicRenderMutation()` 记录原因并完整 `render()`，不静默接受半更新状态。
- 验证：经典 JavaScript 语法、差异检查、画布交互与剪贴板相关回归 40 项全部通过。
- 下一步：实现智能稳定连接 ID、简单拓扑 SVG patch 和复杂拓扑显式回退。

### 2026-08-26：模块 4 智能连接结构 patch 完成

- 新增 `ensureSmartConnectionIds()`；旧连接在完整渲染时补稳定 ID，所有 `addConnection()` 新连接直接写入 `id:uid('c')`，粘贴和 Alt 拖拽复制连接也始终生成新 ID。
- 智能 mutation 已携带三类连接差异；`patchSmartMutationConnections()` 通过连接模型/邻接索引按 ID 创建、删除和更新 path、hit、dot、cut，不重建普通一对一连接的完整 SVG。
- 委托与旧事件绑定均优先通过 `data-connection-id` 断开连接，数组下标仅保留为分组合并线的兼容回退，避免删除后下标漂移误删。
- 新增分组成员归属索引；分组合并线、历史线、级联运行中连接、插入预览或索引 dirty 会输出 `console.warn` 并调用完整连接层刷新，不冒充普通增量 patch。
- 验证：连接差异字段、稳定 ID、智能 mutation 无全量连接/媒体/小地图 3 项核心契约通过；相关交互、剪贴板和撤销回归 53 项通过；双画布 JavaScript 语法与差异检查通过。
- 专项当前状态：10 项中还剩 2 项失败、1 项错误，均对应经典局部选择/媒体/小地图和智能菜单创建统一提交 helper；连接相关契约已转绿。
- 下一步：完成经典局部媒体/小地图和智能菜单创建提交 helper，再运行全部专项契约。

### 2026-08-26：模块 4 经典局部选择媒体与小地图完成

- `syncCanvasSelectedImageResolution(root, affectedNodeIds)` 支持从 `canvasNodeDomIndex` 只取得受影响节点图片；选择切换和 mutation 不再扫描全画布图片。
- 经典 mutation 只对新建/替换节点根执行空闲图标刷新与预览 fallback 绑定，小地图改为 `scheduleMinimapNodeUpdate([...affectedNodeIds])`，不再提交完整重绘。
- `refreshSelectionVisuals()` 支持显式受影响集合和跳过内部高清图同步，mutation 仅执行一次局部高清图处理；节点计数和大场景 class 同步保持。
- 修复专项测试截取边界：经典高清图函数的后继标记由不存在的函数改为实际 `applyLanguage()`；该问题及超大断言输出已记录到 `避坑指南/2026082602-静态源码契约截取边界与失败输出.md`。
- 验证：经典 mutation 与局部高清图 2 项专项契约通过；经典 JavaScript 语法和差异检查通过。
- 下一步：实现 `commitSmartNodeCreate()` 并接入智能菜单构造函数，使模块 4 的 10 项专项契约全部通过。

### 2026-08-26：模块 4 智能菜单创建统一提交完成

- 新增 `commitSmartNodeCreate()`，统一维护 `nodes`、`smartNodeIndex`、选择状态、创建 mutation 和保存调度；普通图片、提示词、Loop、智能分组、影视、全景、姿态、灯光和角度菜单构造函数均通过该 helper 提交。
- 三视图和批量处理在需要先连接来源节点时使用延迟渲染/保存选项；新增连接差异会与节点创建合并到同一 mutation 后一次渲染。
- 分组菜单二次整理显式维护分组及成员模型索引，并按实际结果提交局部 remove/replace mutation。
- 验证：`python -m unittest tests.test_canvas_incremental_render_performance -v` 共 10 项全部通过；双画布 `node --check` 与 `git diff --check` 通过。
- 当前边界：尚未完成全部画布回归和真实 Chromium 500 节点/1000 连线功能一致性验证，模块 4 仍为进行中，不宣称性能数值达标。
- 下一步：审查普通连接 DOM 属性、智能节点模板与分组层级、创建/粘贴/删除后的模型索引一致性，再运行全部画布回归。

### 2026-08-26：模块 4 智能连接选择稳定 ID 修补

- 审查发现增量新建连接的 `data-conn-index` 为空，旧选择高亮逻辑会把空字符串转换为索引 `0`，存在误用第一条连接选择状态的风险。
- `syncConnectionSelectionUi()` 现优先通过 `data-connection-id` 和 `smartConnectionModelIndex` 获取普通连接；只有没有稳定 ID 的分组合并线才回退 `data-conn-index`，且过滤空索引。
- 验证：模块 4 专项契约新增稳定 ID 选择测试后为 11 项全部通过；智能画布 JavaScript 语法与 `git diff --check` 通过。
- 下一步：消除 `smartNodeHtml()` 与完整渲染模板的重复来源，确认分组节点 DOM 层级和完整/增量模板保持一致。

### 2026-08-26：模块 4 智能节点模板单一来源完成

- 完整 `render()` 不再维护第二份智能节点 HTML 模板，改为与 `renderSmartMutation()` 共用 `smartNodeHtml()`；特殊节点、端口、工具栏、分组成员样式和媒体状态由同一来源生成。
- 完整渲染仍先对智能分组排序，使分组 DOM 位于成员之前；增量插入分组仍插入到现有首个节点之前，保持分组背板层级。
- 验证：模块 4 专项契约 11 项全部通过；智能画布 JavaScript 语法与差异检查通过。
- 下一步：完成创建、粘贴、删除后的节点/连接模型索引一致性审查，随后运行全部 `test_canvas*.py` 回归。

### 2026-08-26：模块 4 智能分组成员删除局部替换修补

- 审查发现单删或批量删除智能分组成员时，模型中的 `group.items` 会被清理，但 mutation 只删除成员 DOM，存活分组的计数和工具栏可能保留旧 DOM。
- 两条删除路径现在先收集受影响且仍存活的分组 ID，在删除 mutation 中同时提交 `replaceIds`；分组 DOM、成员归属索引和连接复杂拓扑回退同步更新。
- 验证：模块 4 专项契约新增分组成员删除检查后为 12 项全部通过；智能画布 JavaScript 语法与差异检查通过。
- 下一步：收束双画布结构路径审查并运行全部画布回归；随后进入真实 Chromium 大画布功能一致性验证。

### 2026-08-26：模块 4 全部画布静态回归通过

- 首轮全部 `test_canvas*.py` 为 182 项中 3 失败、1 错误；均为旧静态契约仍绑定旧函数签名、旧 helper 所有权、无稳定 ID 对象字面量或旧全局 DOM 数量条件，并非运行时代码失败。
- 契约观察点已迁移到 `commitSmartNodeCreate()`、`setSmartConnectionPatchGeometry()`、带参数的选择函数和稳定连接 ID 数据结构，没有恢复全局扫描或复制实现。
- 避坑指南 `2026082602-静态源码契约截取边界与失败输出.md` 已补充模块重构后的契约迁移原则。
- 验证：四个定点失败项全部通过；`python -m unittest discover -s tests -p "test_canvas*.py"` 共 182 项全部通过。
- 下一步：搭建隔离 QA 环境，在真实 Chromium 验证经典和智能 500 节点/1000 连线的创建、粘贴、删除、撤销/重做和复杂拓扑可观测回退。

### 2026-08-26：模块 4 经典大画布 fixture 合法拓扑修补

- 真实 Chromium 首轮验证发现经典 fixture 的全部节点都是 `image`，其 1000 条 `image -> image` 连线会在操作触发保存后被 `sanitizeConnections()` 合法移除，使连接数组与旧 DOM/索引暂时不一致。
- fixture 现以前半 `image`、后半 `generator` 构造合法 `image -> generator` 拓扑；数量规模不变，且能真实经过保存和连接校验。
- 新增基线契约锁定 source/target 类型划分；新增避坑指南 `2026082603-经典性能fixture连接必须满足业务校验.md`，同时记录 `python main.py --help` 会启动默认服务的命令陷阱。
- 验证：画布性能基线 6 项全部通过；经典 JavaScript 语法与差异检查通过。
- 下一步：reload 隔离 Chromium 后重装经典 fixture，重新执行合法内部连接粘贴、删除、历史与回退验证，再进入智能大画布。

### 2026-08-26：模块 4 智能分组成员 owner 索引修补

- 真实 Chromium 分组成员删除验证发现：分组 `items` 与 DOM 正确，但成员 DOM 随后重索引时会调用 `removeSmartNodeDomIndex(memberId)`，误删刚由分组建立的 `smartGroupOwnerIndex` 映射。
- `removeSmartNodeDomIndex()` 新增 `preserveGroupOwner` 选项；普通成员 DOM 重索引保留 owner，分组重索引仍清除并重建其旧成员映射，真实删除仍移除 owner。
- 新增专项契约锁定成员重索引语义；模块 4 专项现为 13 项全部通过，智能画布 JavaScript 语法和差异检查通过。
- 下一步：reload 智能 QA 页面后重新验证成员创建时 owner 存在、成员删除时 owner 清理、撤销时 owner 恢复，随后收束真实 Chromium 验收。

### 2026-08-26：模块 4 智能分组菜单创建历史替换修补

- 真实连续历史复验发现：分组内创建成员后，事务只记录新成员节点，没有记录既有分组 `items` 的原地变化；撤销成员创建会删除成员模型，但分组仍残留成员 ID，owner 映射也随完整渲染再次恢复。
- `createNodeFromMenu()` 现只克隆 `createMenuGroupId` 对应的单个分组 before/after，并在入组确实成功时通过 `replacedNodes` 提交；没有恢复整画布节点深拷贝。
- `addCreatedNodeToMenuGroup()` 改用 `smartNodeIndex` 定位分组并返回是否发生入组，避免节点总量相关查找和空替换历史。
- 契约：新增定点失败契约后确认旧实现失败；修补后定点契约、智能 JavaScript 语法和差异检查通过。
- 下一步：运行全部画布回归，reload 智能 QA 页面并复验“创建成员 -> 删除 -> 撤销删除 -> 撤销创建”的完整序列。

### 2026-08-26：模块 4 事务字段替换撤销方向修补

- 真实 Chromium 确认分组事务已正确保存 before/after，但撤销成员创建后 `group.items` 仍保留成员；根因是双画布 `apply*HistoryTransaction()` 对字段替换错误应用 `change.after`。
- 事务应用函数本身执行“撤销当前条目”：创建节点会移除、删除节点会恢复，因此字段替换也必须应用 `change.before`；重做栈保存反向条目，其 `before` 等于原条目的 `after`，历史顺序无需调整。
- 经典与智能两套实现已统一改为应用 `change.before`，避免经典后续接入 replacedNodes 时复现同类问题。
- 契约：双画布字段替换方向失败契约先红后绿；双画布 JavaScript 语法和差异检查通过。
- 下一步：强制忽略缓存 reload 智能 QA 页面，重装 500/1000 fixture 并复跑完整分组历史序列。

### 2026-08-26：模块 4 智能分组连续历史真实验收通过

- 运行时签名确认已加载 `groupBefore/replacedNodes` 和 `change.before` 最新实现；重装智能 500 节点/1000 连线 fixture 后从空历史开始验证。
- 完整序列：基线 `500`；创建分组 `501`；创建成员 `502`；删除成员 `501`；撤销删除 `502`；撤销成员创建 `501`；重做成员创建 `502`；再次撤销成员创建 `501`；撤销分组创建回到 `500`，连线全程保持 `1000`。
- 一致性：成员存在时 `group.items` 含成员且 owner 指向分组；成员删除或撤销创建时 `items=[]`、成员模型不存在且 owner 为空；最终分组模型也不存在。
- 历史证据：成员创建条目只包含一个 created node 和一个 group replacement，before items 为空、after items 只含新成员；没有整画布快照。
- 控制台无 error；既存 `badge-hd` Lucide 缺失图标警告不属于本次改动；分组结构操作稳定输出 `group-or-history-node` 完整 SVG 回退警告，符合复杂拓扑保护设计。
- 下一步：运行最新代码的全部画布回归，随后进入模块 4 版本化和交付收束。

### 2026-08-26：模块 4 版本递增至 1.0.314

- 应用版本由 `1.0.313` 递增至 `1.0.314`，已同步 `VERSION`、后端、npm、Tauri、Cargo、发布说明和更新 JSON。
- 经典与智能画布脚本缓存修订号由 `20260826.2` 递增至 `20260826.3`，确保安装升级后加载模块 4 最新实现。
- 发布说明与更新 JSON 已补充真正增量渲染、稳定连接 ID、复杂拓扑回退、智能分组历史修补和真实 500/1000 验收结果。
- 验证：桌面 bootstrap/updater 契约 19 项通过；更新 JSON 与版本 JSON 可解析；旧 `1.0.313` 和旧缓存修订号在版本源中均已清零；差异检查通过。
- 下一步：创建 `backup/191-1.0.314无限画布真正增量渲染差异备份.md`，随后运行完整回归和模块清理。

### 2026-08-26：模块 4 差异备份完成

- 新增 `backup/191-1.0.314无限画布真正增量渲染差异备份.md`，编号从 190 纯递增至 191。
- 备份记录经典/智能节点与连接 patch、稳定 ID、分组 owner、模板单一来源、事务字段替换、失败回退、专项契约、真实 Chromium 证据、版本缓存和非破坏性恢复说明。
- 本文件只记录模块 4 的具体前后差异，不复制完整源码，也不包含模块 5 保存序列化设计。
- 下一步：运行完整 Python、Cargo、JSON、双画布语法、版本一致性和差异回归。

### 2026-08-26：模块 4 完整回归完成

- 全量 `python -m pytest -q`：837 项通过、4 项失败、42 个子测试通过；4 项失败与模块 3 基线相同，分别为 grsai API Key fixture 仍未向 `api_headers()` 注入环境变量，以及 ecommerce/free-creation/unified-theme 三项静态版本断言过期，均未触及本轮画布运行时代码。
- 全部 `test_canvas*.py`：186 项通过；模块 4 + 事务专项：28 项通过；桌面 bootstrap/updater：19 项通过。
- `cargo check --locked`：`canvas-desktop v1.0.314` 通过。
- 双画布 JavaScript 语法、更新 JSON、版本源一致性和差异检查通过。
- 下一步：关闭 QA 标签、停止 PID 21412、确认 3012 释放并删除精确隔离目录；随后最终审查、提交和推送。

### 2026-08-26：模块 4 QA 环境清理完成

- 已关闭临时智能 QA 标签；精确停止 PID 21412，确认 3012 端口释放且进程不存在。
- 隔离目录解析为工作区内的 `.qa-canvas-module4-20260826-01/`；`git clean -ndx` 预览只包含该目录后，执行精确 `git clean -fdx` 删除。
- 删除后目录不存在、3012 未监听、PID 21412 不存在；未触及源码、正式用户数据或其他缓存。
- 模块 4 实现、真实验收、`1.0.314`、backup 191、完整回归和环境清理均已完成，状态标记为“已完成（待提交推送）”。
- 下一步：最终审查工作区差异，显式加入被忽略的模块 4 快照，提交并非强制推送 `HEAD:main`。
