# 552 - 1.0.235建筑多视图发布验收

生成时间：2026-08-22 10:43:52

## 已完成内容

修复输入变化期间旧建筑任务的本地追踪阻塞；版本由1.0.234递增至1.0.235并同步 package、Tauri、Cargo、后端和更新说明；全量pytest通过；Tauri宿主、PyInstaller后端与Inno Setup安装包全部构建成功。

## 当前模块

模块6：版本递增、全量测试与安装包编译

## 当前 Git 状态

```text
M VERSION
 M main.py
 M package-lock.json
 M package.json
 M src-tauri/Cargo.lock
 M src-tauri/Cargo.toml
 M src-tauri/tauri.conf.json
 M static/js/smart-canvas.js
 M static/update-notes.json
 M tests/test_canvas_building_multi_view.py
?? "\345\275\223\345\211\215\351\241\271\347\233\256\346\200\247\350\203\275\344\270\216\351\225\277\346\234\237\347\274\223\345\255\230\345\210\206\346\236\220\346\212\245\345\221\212.md"
```

## 代码前后对比 / Diff

```diff
diff --git a/VERSION b/VERSION
index e245dd1..876574c 100644
--- a/VERSION
+++ b/VERSION
@@ -1 +1 @@
-1.0.234
+1.0.235
diff --git a/main.py b/main.py
index 074eabc..2c8f3f4 100644
--- a/main.py
+++ b/main.py
@@ -406,7 +406,7 @@ STARTUP_MAINTENANCE_STATE = {
 }
 ACTIVE_CANVAS_BY_ACCOUNT: dict[str, str] = {}
 ACTIVE_CANVAS_ID = ""
-APP_VERSION = "1.0.234"
+APP_VERSION = "1.0.235"
 GITHUB_REPO_URL = "https://github.com/luojiang419/SHIYIN-AI-source"
 GITHUB_VERSION_URL = "https://raw.githubusercontent.com/luojiang419/SHIYIN-AI-source/main/VERSION"
 GITHUB_TREE_URL = "https://api.github.com/repos/luojiang419/SHIYIN-AI-source/git/trees/main?recursive=1"
diff --git a/package-lock.json b/package-lock.json
index fa20ce6..ad9080b 100644
--- a/package-lock.json
+++ b/package-lock.json
@@ -1,12 +1,12 @@
 {
   "name": "shiyin-ai-windows-desktop",
-  "version": "1.0.234",
+  "version": "1.0.235",
   "lockfileVersion": 3,
   "requires": true,
   "packages": {
     "": {
       "name": "shiyin-ai-windows-desktop",
-      "version": "1.0.234",
+      "version": "1.0.235",
       "devDependencies": {
         "@tauri-apps/cli": "2.11.4",
         "tailwindcss": "3.4.17"
diff --git a/package.json b/package.json
index 254fd4d..d4b8743 100644
--- a/package.json
+++ b/package.json
@@ -1,7 +1,7 @@
 {
   "name": "shiyin-ai-windows-desktop",
   "private": true,
-  "version": "1.0.234",
+  "version": "1.0.235",
   "scripts": {
     "css:build": "node ./node_modules/tailwindcss/lib/cli.js -c tailwind.config.cjs -i ./static/css/tailwind-input.css -o ./static/vendor/css/tailwind.generated.css --minify",
     "desktop:dev": "npm run css:build && tauri dev",
diff --git a/src-tauri/Cargo.lock b/src-tauri/Cargo.lock
index 4734a0b..db06bf4 100644
--- a/src-tauri/Cargo.lock
+++ b/src-tauri/Cargo.lock
@@ -449,7 +449,7 @@ dependencies = [
 
 [[package]]
 name = "canvas-desktop"
-version = "1.0.234"
+version = "1.0.235"
 dependencies = [
  "arboard",
  "open",
diff --git a/src-tauri/Cargo.toml b/src-tauri/Cargo.toml
index 23c4e5b..8a8866a 100644
--- a/src-tauri/Cargo.toml
+++ b/src-tauri/Cargo.toml
@@ -1,6 +1,6 @@
 [package]
 name = "canvas-desktop"
-version = "1.0.234"
+version = "1.0.235"
 description = "SHIYIN AI Windows portable desktop host"
 authors = ["SHIYIN AI contributors"]
 edition = "2021"
diff --git a/src-tauri/tauri.conf.json b/src-tauri/tauri.conf.json
index 7d27d26..991746f 100644
--- a/src-tauri/tauri.conf.json
+++ b/src-tauri/tauri.conf.json
@@ -1,7 +1,7 @@
 {
   "$schema": "../node_modules/@tauri-apps/cli/config.schema.json",
   "productName": "SHIYIN AI",
-  "version": "1.0.234",
+  "version": "1.0.235",
   "identifier": "com.hero8152.canvas.desktop",
   "build": {
     "frontendDist": "../desktop-placeholder"
diff --git a/static/js/smart-canvas.js b/static/js/smart-canvas.js
index 782dd08..8602846 100644
--- a/static/js/smart-canvas.js
+++ b/static/js/smart-canvas.js
@@ -9480,6 +9480,8 @@ function smartBuildingCurrentInputMatches(node){
     const signature = smartBuildingInputSignature(node);
     if(!node.buildingInputSignature || signature === node.buildingInputSignature) return true;
     node.buildingPlan = null;
+    node.buildingPendingTasks = [];
+    node.buildingActiveViews = [];
     smartBuildingSetStage(node, 'idle', '建筑输入已变化，请重新开始生成');
     smartBuildingSyncOutputs(node);
     render();
@@ -9569,7 +9571,9 @@ async function resumeSmartBuildingNodePending(node){
             smartBuildingRemovePendingTask(node, task.taskId);
             return item;
         } catch(error){
-            node.buildingErrors = {...(node.buildingErrors || {}), [task.view]:error.message || `建筑${task.view}视图恢复失败`};
+            const message = error.message || `建筑${task.view}视图恢复失败`;
+            if(/输入.*变化|重新开始生成/.test(message)) smartBuildingRemovePendingTask(node, task.taskId);
+            node.buildingErrors = {...(node.buildingErrors || {}), [task.view]:message};
             throw error;
         } finally {
             node.buildingActiveViews = (node.buildingActiveViews || []).filter(view => view !== task.view);
@@ -9634,6 +9638,13 @@ async function completeSmartBuildingMissingViews(node, inputRefs, startedAt=0){
     return settled;
 }
 async function runSmartBuildingNode(node){
+    if(Array.isArray(node.buildingPendingTasks) && node.buildingPendingTasks.length){
+        await resumeSmartBuildingNodePending(node);
+        if(node.buildingPendingTasks.length){
+            toast('已有建筑任务正在恢复，请稍后再开始新的规划');
+            return;
+        }
+    }
     const inputRefs = smartBuildingInputRefs(node);
     const connectedPrompt = smartBuildingConnectedPrompt(node);
     const hasReference = SMART_BUILDING_VIEW_KEYS.some(view => smartBuildingItemUrl(inputRefs[view]));
diff --git a/static/update-notes.json b/static/update-notes.json
index 076e379..5b9107a 100644
--- a/static/update-notes.json
+++ b/static/update-notes.json
@@ -1,5 +1,5 @@
 {
-  "version": "1.0.234",
+  "version": "1.0.235",
   "updated_at": "2026-08-22T09:21:25+08:00",
   "items": [
     {
diff --git a/tests/test_canvas_building_multi_view.py b/tests/test_canvas_building_multi_view.py
index 7a8e7e4..e3a8ccb 100644
--- a/tests/test_canvas_building_multi_view.py
+++ b/tests/test_canvas_building_multi_view.py
@@ -194,3 +194,5 @@ def test_smart_building_retry_never_submits_duplicate_while_task_is_recovering()
     assert "await resumeSmartBuildingNodePending(node);" in SMART_JS
     assert "建筑任务仍在恢复中，请稍后再次点击“重试缺失视图”" in SMART_JS
     assert "stage === BUILDING_STAGES.PARTIAL || stage === BUILDING_STAGES.ERROR" in SHARED_JS
+    assert "已有建筑任务正在恢复，请稍后再开始新的规划" in SMART_JS
+    assert "node.buildingPendingTasks = [];" in SMART_JS
```

## 验证结果

pytest -q：625 passed，16 warnings，42 subtests passed；node --check三份JS通过；npm run desktop:host-build成功；tools/build-installer.ps1 -SkipDesktop -SkipRuntimeSmoke成功；安装包：dist/installer/SHIYIN-AI-Setup-1.0.235.exe；git diff --check通过。

## 待办清单

登录态浏览器视觉回归仍待提供有效登录态后执行；发布后的真实图片API额度/视觉一致性测试需在用户配置API后进行。

## 下一步

交付1.0.235安装包与代码提交，后续在有登录态时补做浏览器视觉回归。

## 避坑记录

构建过程出现Browserslist过期提示与PyInstaller缺少CUDA/TensorRT DLL警告，但CPU后端和安装包编译均成功；浏览器视觉回归因本地登录页阻塞，未代填凭据。
