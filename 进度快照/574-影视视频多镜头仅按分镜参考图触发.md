# 进度快照 574：影视视频多镜头仅按分镜参考图触发

日期：2026-08-23
工作分支：`fix/film-reference-shot-parsing`（基于含影视制作节点的 `origin/codex/three-view-output-preview`）

## 已完成内容

- 修复「影视制作 → 生成视频」节点的视觉解析判定。
- 多镜头解析现在只统计锁定到 `storyboard`（分镜图/首帧参考）输入端口的有效图片；该端口有两张及以上图片时，才让模型按“镜头 1、镜头 2……”输出。
- 演员、服装、道具端口仍会随请求发送，确保人物与素材一致；无论这些端口连接多少图片，均不会再触发多镜头拆分。
- 可灵与其他视觉解析模型均收到同一条端口语义约束，避免 system prompt 与用户消息的规则不一致。
- 补充了静态回归测试，锁定 `storyboard` 端口过滤、阈值和演员/道具排除规则。

## 当前修改模块

| 文件 | 修改说明 |
| --- | --- |
| `static/js/canvas-film-nodes.js` | 在 `parseScene` 中按 `inputRole === 'storyboard'` 判断是否解析多镜头，并改写可灵/通用模型的消息和 system prompt。 |
| `tests/test_canvas_film_nodes.py` | 新增多镜头仅由多个分镜图端口参考触发的回归契约。 |

## 关键代码前后对比

修改前，所有已经映射的图片共用多镜头指令：

```js
const mappedRefs = map.refs.filter(...);
// 无论图来自分镜图、演员、服装还是道具，均要求：
// “再按‘镜头1、镜头2……’组织连续动作”
```

修改后，先按锁定输入端口筛选真正的镜头参考：

```js
const storyboardRefs = mappedRefs.filter(item => item.inputRole === 'storyboard');
const shouldParseMultipleShots = node.type === 'film-video' && storyboardRefs.length > 1;
```

```js
shouldParseMultipleShots
  ? '仅按分镜图输入端中多张参考图的顺序解析多镜头。'
  : '仅解析一个连续镜头；演员、服装和道具仅用于资产一致性绑定，不得因其数量拆分多镜头。'
```

## 验证结果

- `node --check static/js/canvas-film-nodes.js`：通过。
- `python -m pytest -q tests/test_canvas_film_nodes.py`：27 通过。
- `python -m pytest -q tests/test_canvas_film_nodes.py tests/test_kling_canvas_video.py`：34 通过。
- `npm run css:build`：通过；仅有既有 Browserslist 数据过期提醒。
- `python -m pytest -q`：688 通过、42 个子测试通过；仅有项目既有 FastAPI / Pydantic 弃用警告。

## 待办清单

- [x] 收紧视频多镜头解析的参考图范围。
- [x] 覆盖演员/服装/道具不触发多镜头的回归规则。
- [ ] 提交并推送本分支到 GitHub。
- [ ] 清理本轮测试缓存并复核工作区状态。

## 下一步

强制添加被 Git 忽略的进度快照与避坑指南，提交源码和测试后推送 `fix/film-reference-shot-parsing`。
