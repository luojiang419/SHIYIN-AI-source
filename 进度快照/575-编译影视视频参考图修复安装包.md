# 进度快照 575：编译影视视频参考图修复安装包

日期：2026-08-23
工作分支：`fix/film-reference-shot-parsing`

## 已完成内容

- 将应用版本从 `1.0.270` 自动递增到 `1.0.271`，并同步 `VERSION`、Python 后端、npm、Tauri、Cargo 和更新说明中的版本字段。
- 完成 PyInstaller 后端打包、Tauri Windows 桌面宿主编译、安装目录组装与 Inno Setup 安装包编译。
- 完成打包运行时冒烟：后端健康检查正常、单实例校验通过、主进程退出后后端正确停止、运行内存低于 250 MB 目标。
- 完成安装包版本、大小、签名状态和 SHA-256 校验。

## 产物

| 项目 | 结果 |
| --- | --- |
| 安装包 | `dist/installer/SHIYIN-AI-Setup-1.0.271.exe` |
| 安装包版本 | `1.0.271` |
| 文件大小 | 69,729,476 字节（66.50 MB） |
| SHA-256 | `d5fb59c526ae2df0871c08032e9417146da8151776d021a03ee8af7fed965f54` |
| 校验文件 | `dist/installer/SHIYIN-AI-Setup-1.0.271.exe.sha256` |
| 签名状态 | `NotSigned`（校验脚本允许；正式外发时建议补充代码签名） |

## 关键改动前后对比

```text
VERSION: 1.0.270 → 1.0.271
package.json / package-lock.json: 1.0.270 → 1.0.271
src-tauri/Cargo.toml / Cargo.lock / tauri.conf.json: 1.0.270 → 1.0.271
main.py APP_VERSION / static/update-notes.json: 1.0.270 → 1.0.271
```

## 构建与验收

- `npm run version:patch`：通过。
- `npm run installer:build`：后端与桌面宿主编译、运行时冒烟均通过；首次在 Inno Setup 阶段因 `ISCC.exe` 不在 PATH 中止。
- 已定位用户级 Inno Setup 6.7.3，并显式加入当次构建 PATH。
- `tools/build-installer.ps1 -SkipBackend -SkipDesktop`：复用已验证的编译产物完成暂存、冒烟和安装包生成，通过。
- `tools/verify-installer-artifact.ps1 -Version 1.0.271`：通过。
- DWPose 真人输入专项冒烟因未提供真实输入样本按脚本提示跳过；桌面通用运行时冒烟已通过。

## 待办清单

- [x] 版本递增并同步各构建清单。
- [x] 生成并校验 Windows 安装包。
- [x] 生成 SHA-256 校验文件。
- [ ] 将版本同步、构建快照和环境踩坑说明提交并推送 GitHub。
- [ ] 视发布需求上传 EXE 与 `.sha256` 至 GitHub Release；本轮仅编译，未创建 Release。

## 下一步

清理本轮可安全移除的构建缓存，确认安装包不被误删后提交版本变更和文档记录，推送到当前修复分支。
