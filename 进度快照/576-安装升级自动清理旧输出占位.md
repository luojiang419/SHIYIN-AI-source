# 进度快照 576：安装升级自动清理旧输出占位

日期：2026-08-23
工作分支：`fix/film-reference-shot-parsing`

## 已完成内容

- 新增持久化画布迁移模块 `canvas_core/canvas_placeholder_migration.py`。
- 新版后端首次启动时自动扫描 SQLite 中的全部保存画布（包括回收站），清理经典输出节点 `_pending` 内没有任务身份的旧转圈/错误占位。
- 仅同时缺少 `canvasTaskId` 与 `recoverTaskId` 的占位会被删除；有本地任务 ID 或可恢复上游任务 ID 的真实进行中/可查询任务严格保留。
- 完成后写入 `canvas_orphan_output_pending_cleanup_v1` 数据库设置，记录完成时间、应用版本、清理数量和涉及画布数；后续启动不会重复改写。
- 在 `main.py` 的服务启动流程中接入迁移，确保用户安装新版本后第一次打开应用即可完成清理，无需逐个打开画布。
- 版本从 `1.0.271` 自动递增到 `1.0.272`，并已编译、验证新的 Windows 安装包。

## 当前修改模块

| 文件 | 修改说明 |
| --- | --- |
| `canvas_core/canvas_placeholder_migration.py` | 新增幂等的旧输出占位持久化迁移。 |
| `main.py` | 启动时执行迁移并仅在实际清理到数据时输出汇总日志。 |
| `tests/test_canvas_placeholder_migration.py` | 覆盖清理范围、任务身份保留、回收站处理、幂等标记与启动接线。 |
| 版本清单 | 同步为 `1.0.272`。 |

## 关键代码前后对比

修改前，仅当前被用户打开的经典画布由前端临时清理：

```js
const retainedPending = pending.filter(item => item?.canvasTaskId || item?.recoverTaskId);
```

修改后，安装新版本后的首次后端启动会对数据库中的已保存画布执行同一安全规则：

```python
retained = [item for item in pending if has_recoverable_output_task(item)]
database.save_canvas(canvas, touch=False)
```

```python
await asyncio.to_thread(migrate_orphan_output_pending_once, DATABASE, APP_VERSION)
```

## 验证结果

- 定向迁移、影视节点和视频恢复测试：34 通过。
- 全量测试：691 通过、42 个子测试通过。
- `python -m py_compile main.py canvas_core/canvas_placeholder_migration.py`：通过。
- `node --check static/js/canvas.js`、`npm run css:build`：通过。
- 完整安装包构建、打包运行时冒烟、Inno Setup 编译：通过。
- `tools/verify-installer-artifact.ps1 -Version 1.0.272`：通过。

## 安装包产物

| 项目 | 结果 |
| --- | --- |
| 安装包 | `dist/installer/SHIYIN-AI-Setup-1.0.272.exe` |
| 大小 | 69,723,246 字节（66.49 MB） |
| SHA-256 | `5f3ecf74a05031411cfc7356db637965dbb0e89eb2e19a927feb7d93e375ffbf` |
| 签名状态 | `NotSigned`（校验脚本允许；正式外发建议补充可信代码签名） |

## 待办清单

- [x] 实现安装升级后的一次性旧占位清理。
- [x] 保护可恢复生成任务不被迁移删除。
- [x] 回归验证并编译校验 `1.0.272` 安装包。
- [ ] 清理构建缓存，提交版本、源码、测试与快照并推送 GitHub。
- [ ] 如需对外更新，上传 EXE 与 SHA-256 到 GitHub Release；本轮未创建 Release。

## 下一步

保留最终 EXE 和 SHA-256，删除可再生构建目录，提交本次迁移及版本更新到当前修复分支。
