# 进度快照 577：发布 1.0.272 安装包到 GitHub

## 已完成内容

- 核对桌面端更新器的实际更新源：`src-tauri/src/updater.rs` 固定读取 `luojiang419/SHIYIN-AI` 的 `releases/latest`，不是源码仓库 `SHIYIN-AI-source`。
- 核对本地待发布安装包为 `dist/installer/SHIYIN-AI-Setup-1.0.272.exe`，大小 `69,723,246` 字节。
- 核对校验文件为 `SHIYIN-AI-Setup-1.0.272.exe.sha256`，SHA-256 为 `5f3ecf74a05031411cfc7356db637965dbb0e89eb2e19a927feb7d93e375ffbf`。
- 核对当前公开更新仓库最新版本为 `v1.0.269`，因此 `v1.0.272` 会成为可检测的新版本。
- 将发布说明更新为本版实际修复内容，避免旧版累积说明被错误附加到本次 GitHub Release。

## 当前修改模块

- `release-notes/current.md`：本次 GitHub Release 的简明发布说明。
- `tools/publish-release.ps1`：将复用既有跨仓库事务，依次创建清单、标签、草稿、上传安装包与 SHA-256、远端核验并正式发布。

## 代码前后对比

```diff
- # SHIYIN AI v1.0.244
- （包含多版本累计说明）
+ # SHIYIN AI v1.0.272
+ - 清理生成结束后的孤儿输出占位。
+ - 首次启动迁移历史画布的错误占位，保留可恢复任务。
+ - 影视视频多镜头仅按分镜图参考端口解析。
```

## 待办清单

- [ ] 提交并推送本次发布说明与快照，固定源提交 SHA。
- [ ] 向 `luojiang419/SHIYIN-AI` 创建 `v1.0.272` 草稿 Release，上传精确命名的 EXE 与 `.sha256`。
- [ ] 核验远端资产大小、资产摘要、校验文件、Release 标签及 `releases/latest`，然后公开发布。
- [ ] 清理本次发布临时文件，并记录最终 Release URL。

## 下一步

提交发布说明与快照后，运行既有 `tools/publish-release.ps1` 进行跨仓库事务发布。
