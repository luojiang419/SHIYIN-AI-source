# 进度快照 580：发布 1.0.273 分层缓存安装包

## 已完成内容

- 已关闭经用户许可、且已核对路径的正式客户端 `D:\Program Files\SHIYIN AI\SHIYIN AI.exe`（PID `35976`），释放安装态冒烟环境。
- 已完成 `1.0.273` Windows 安装包构建：`dist/installer/SHIYIN-AI-Setup-1.0.273.exe`。
- 安装态运行验证通过：后端健康检查正常，启动约 `2527 ms`，第二实例退出正确，父进程退出后后端停止，进程树私有内存约 `232.23 MB`，低于 `250 MB` 门槛。
- 安装包验证通过，文件大小 `69,737,950` 字节，SHA-256 为 `3b86c241b37115c0ad5582efbeb50e7e9b41aeda4abe1ddfd9519b88b4b8cc6f`；签名状态为 `NotSigned`。
- 已在桌面更新器实际读取的仓库 `luojiang419/SHIYIN-AI` 发布正式版 `v1.0.273`：<https://github.com/luojiang419/SHIYIN-AI/releases/tag/v1.0.273>。
- 已核验 `releases/latest` 为 `v1.0.273`、不是草稿/预发布；EXE 的 GitHub 资产摘要、发布清单与公开下载 `.sha256` 文件均和本地哈希一致。
- 发布仓库 `main` 和 `refs/tags/v1.0.273` 均指向 `2cb3210efdb9da41ffd9ae0374d7f68392fc83fa`；发布清单记录的源码提交为 `49b21e8f693646692dbb3d7890ff52ddab6ea1e3`。

## 当前修改模块

- 分层 Web 缓存：HTML 不存储、版本化静态资源长缓存、动态媒体网络优先。
- Windows 安装包：`SHIYIN-AI-Setup-1.0.273.exe` 与对应 SHA-256 文件。
- GitHub 更新仓库：`v1.0.273` Release、资产和 `releases/v1.0.273.json` 清单。

## 发布前后对比

```diff
- releases/latest -> v1.0.272
+ releases/latest -> v1.0.273
+ installer -> SHIYIN-AI-Setup-1.0.273.exe
+ sha256 -> 3b86c241b37115c0ad5582efbeb50e7e9b41aeda4abe1ddfd9519b88b4b8cc6f
```

## 待办清单

- [x] 构建并验证 1.0.273 安装包。
- [x] 发布 GitHub 正式更新并验证 latest、标签、资产及校验文件。
- [x] 清理本次构建临时目录，保留安装包与可复用构建缓存。
- [ ] 后续如需减少 Windows SmartScreen 提示，配置代码签名证书并发布签名版安装包。

## 下一步

已安装旧版的桌面客户端可在自动或手动检查更新时发现 `v1.0.273`；首次运行未签名安装包时可能仍会遇到 Windows SmartScreen 提示。
