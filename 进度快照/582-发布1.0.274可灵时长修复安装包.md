# 进度快照 582：发布 1.0.274 可灵时长修复安装包

## 已完成内容

- 已将 `release-notes/current.md` 更新为 `SHIYIN AI v1.0.274`，并提交到源码仓库；安装包构建所用源码提交为 `cb43b15891d4efb8661a19d3eb72faa43f2c4a17`。
- 发布前门禁通过：版本解析脚本自测成功，Python 全量 `696 passed`、`42 subtests passed`，Rust 更新器 `5 passed`，所有运行时/安装器版本字段均为 `1.0.274`。
- 已完成真实 Windows 安装包构建：`dist/installer/SHIYIN-AI-Setup-1.0.274.exe`。
- 安装包暂存内容已确认包含可灵 Video 3.0 时长修复：普通“视频生成”节点默认选择 3.0 Omni，旧的无条件 3–15 秒扩展代码不存在。
- 打包态桌面冒烟通过：后端健康检查正常，启动约 `1913 ms`；第二实例正确退出；主进程退出后后端停止；进程树私有内存约 `206.91 MB`，低于 `250 MB` 目标。
- 已完成隔离安装态升级验收：使用 `1.0.273` 安装包安装旧版，再由独立更新器升级到 `1.0.274`；用户数据保留、应用目录替换、pending 清除和开始菜单目标均正确。
- 安装包校验通过：大小 `69,728,638` 字节，SHA-256 为 `a121351635c3fd6b3c9522cb44cff2afb1aad62ef46e39fcb4099795f8281188`，签名状态为 `NotSigned`。
- 已在桌面更新器实际使用的仓库 `luojiang419/SHIYIN-AI` 发布正式版 `v1.0.274`：<https://github.com/luojiang419/SHIYIN-AI/releases/tag/v1.0.274>。
- 已核验 `releases/latest` 为 `v1.0.274`，且 `draft=false`、`prerelease=false`；EXE 与 `.sha256` 资产均唯一且为 `uploaded`。
- GitHub EXE 资产摘要为 `sha256:a121351635c3fd6b3c9522cb44cff2afb1aad62ef46e39fcb4099795f8281188`；公开下载的 `.sha256` 内容与本地逐字一致。
- 更新仓库 `main`、`refs/tags/v1.0.274` 与 Release target 均指向 `95b226c18e700b6dae630f44a3205bf49fa5cfc6`。
- 发布清单 `releases/v1.0.274.json` 已记录正确的版本、标签、资产名、大小、摘要、`NotSigned` 状态和源码提交 `cb43b15891d4efb8661a19d3eb72faa43f2c4a17`。
- 已清理 `.build/installer`、`.build/desktop-smoke`、`dist/installer-stage`、`.pytest_cache` 与 Python 字节码缓存；保留正式安装包和低于 20 GiB 限制的可复用 Rust/Node 构建缓存。

## 当前修改模块

- `release-notes/current.md`：1.0.274 可灵 Video 3.0 时长修复发布说明。
- `dist/installer/SHIYIN-AI-Setup-1.0.274.exe`：正式 Windows x64 安装包。
- `dist/installer/SHIYIN-AI-Setup-1.0.274.exe.sha256`：公开完整性校验文件。
- 更新仓库 `luojiang419/SHIYIN-AI`：`v1.0.274` Release、两个资产和 `releases/v1.0.274.json` 发布清单。

## 发布前后对比

发布前：

```text
releases/latest = v1.0.273
v1.0.274 Release = 不存在
releases/v1.0.274.json = 不存在
```

发布后：

```text
releases/latest = v1.0.274
installer = SHIYIN-AI-Setup-1.0.274.exe
sha256 = a121351635c3fd6b3c9522cb44cff2afb1aad62ef46e39fcb4099795f8281188
release target / main / tag = 95b226c18e700b6dae630f44a3205bf49fa5cfc6
```

## 验证结果

- `python tools/resolve-release-version.py --self-test`：通过。
- Python 全量测试：`696 passed, 42 subtests passed`；仅有既存框架弃用警告。
- Rust 更新器测试：`5 passed`。
- 完整 PyInstaller 后端冻结：通过。
- `npm run desktop:host-build` / Tauri release 宿主编译：通过。
- 打包态桌面健康、单实例、内存和父子进程退出冒烟：通过。
- Inno Setup 6.7.3 正式安装包编译：通过。
- `1.0.273 → 1.0.274` 独立更新器隔离升级：通过。
- 产物版本、大小、SHA-256、签名状态和安装内容检查：通过。
- GitHub Draft 上传后远端资产复核、转正式、Latest 复核：通过。
- 未配置公共代码签名证书，签名状态如实记录为 `NotSigned`；Windows SmartScreen 仍可能提示。
- 本次发布未重新运行 DWPose 真实人物下载冒烟；该模块未被本次可灵时长修复修改，既有打包态桌面和更新器升级门禁均已通过。

## 源码备份与缓存

- 本次是上一模块修复的正式打包发布，没有新增大模块源码，不创建新的 `backup` 源码副本；源码提交、GitHub Release、清单与本快照共同提供完整溯源。
- 已删除约 `0.71 GiB` 的一次性冻结、暂存和桌面冒烟目录。
- 保留 `src-tauri/target` 约 `5.77 GiB` 与 `node_modules` 约 `0.03 GiB` 的高频可复用构建缓存，总量低于 `20 GiB`。

## 待办清单

- [x] 更新并推送 1.0.274 发布说明。
- [x] 完成发布前全量测试与版本一致性检查。
- [x] 构建并校验 1.0.274 Windows 安装包。
- [x] 完成打包态桌面冒烟和 1.0.273 到 1.0.274 升级验收。
- [x] 发布 GitHub 正式 Release 与 SHA-256 文件。
- [x] 复核 Latest、标签、目标提交、资产摘要和公开校验文件。
- [x] 清理一次性构建缓存并保留正式产物。
- [ ] 后续如需减少 Windows SmartScreen 提示，配置公共可信代码签名证书并发布签名版本。

## 下一步

已安装旧版的桌面客户端现在可以在自动或手动检查更新时发现 `v1.0.274`，下载精确命名的安装包并按现有更新器流程升级。后续功能开发从本快照继续；不要重复发布 `v1.0.274`。
