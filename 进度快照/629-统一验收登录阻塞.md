# 进度快照 629：统一验收登录阻塞

日期：2026-08-24

## 已完成内容

- 已读取模块 F 最新快照和主开发文档，确认 A-F 主模块代码已完成，当前只剩统一浏览器验收与性能基线复测。
- 已启动本地服务并打开 `http://127.0.0.1:3000/?canvasPerf=1`，确认服务可启动、页面可访问。
- 真实浏览器当前被重定向到 `/login`，页面需要账号/密码才能进入画布；已保留登录页供后续继续。
- 未自动注册账号、未输入未知凭据、未修改画布或用户数据。

## 当前模块

统一验收与性能基线复测，尚未进入真实 fixture 操作阶段。

## 具体代码前后对比

本次没有源码改动。模块 A-F 的 fixture、观测钩子和性能优化代码保持 628 快照状态。

## 验证命令与结果

- `python -m unittest tests.test_canvas_performance_baseline tests.test_canvas_performance_observer`：已包含于此前专项回归并通过。
- 真实页面启动检查：`GET /?canvasPerf=1` 返回 303，随后 `GET /login` 返回 200。
- 浏览器 DOM 观察确认页面为登录页，含账号、密码和登录/自动注册入口。
- 服务启动日志显示 Uvicorn 在 `127.0.0.1:3000` 正常启动；验收结束后已停止临时服务。

## 未完成待办

- 登录后安装经典/智能画布 100/300/500/1000 节点 fixture。
- 对节点拖动、画布平移、滚轮缩放、端口连线、框选和小地图拖动采集 p50/p95/p99、帧间隔和 long task。
- 对比模块 A 基线并确认主文档统一验收指标。

## 下一步

请在已保留的本地登录页完成账号登录后继续。下一轮读取本快照，重启本地服务，进入带 `canvasPerf=1` 的画布页面，优先验证经典画布，再验证智能画布；不重复 A-F 代码开发。

## 已解决踩坑点

- 本地服务可启动但未登录时，根路径会被认证中间件重定向到 `/login`；不能将登录阻塞误判为画布性能失败。
- 自动注册会产生本地账号副作用，本轮未擅自执行；需要用户先完成登录或明确授权注册测试账号。
