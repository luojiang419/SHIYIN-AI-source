# 638-平移阶段成本隔离观测

## 已完成内容

- 从快照 637 继续，复测经典/智能节点拖动和平移，并对智能 1000 节点 fixture 的平移阶段做直接函数隔离测量。
- 确认智能直接视口变换写入约 0.1 ms，单次安全 LOD 更新约 39.7 ms，单次小地图全量绘制约 33 ms；这两项是下一轮平移优化的主要候选。
- 尝试“平移期间延后安全 LOD、松手补偿”试验方案，清洁样本约 203.4 ms → 199.4 ms，差异不足以确认稳定收益，已在提交前完全回退，未改变源码。

## 当前模块

统一验收阶段的平移热点隔离观测。本轮没有新增业务源码；保留 637 智能拖动目标高亮增量优化。

## 具体代码前后对比

```diff
// 本轮不提交源码改动
- 试验：平移期间跳过安全 LOD，松手后补调度
+ 结论：单次清洁样本改善不足，已回退，不进入主分支
```

## 验证命令与结果

- `python -m unittest tests.test_canvas_interaction_performance tests.test_canvas_hotpath_indexes tests.test_canvas_performance_baseline tests.test_canvas_save_media_isolation`：41 passed（承接 637）。
- `node --check static/js/canvas.js`、`node --check static/js/smart-canvas.js`：通过。
- `git diff --check`：通过；试验回退后工作树无源码差异。
- 浏览器智能 1000 节点 fixture：`applyViewport` 直接阶段约 0.1 ms；`updateSmartSafeLod` 约 39.7 ms；`renderMinimap` 约 33 ms。
- 浏览器平移试验：203.4 ms → 199.4 ms，仍出现约 97/100 ms 长任务，无法据单次样本认定达标。

## 未完成待办

- 需要在经典/智能两套画布、100/300/500/1000 档位多次采集 LOD 与小地图阶段 p50/p95/p99。
- 真实媒体节点和不同视口范围下的小地图绘制成本尚未覆盖。
- 尚未决定采用 LOD 节流、脏区扩展或空间索引；需先有稳定证据和完整回退设计。

## 下一步

读取本快照后，继续多轮清洁平移采样；若确认 LOD 或小地图阶段稳定主导，再设计带开关、全量回退和松手补偿的窄范围方案。

## 已解决踩坑点

- 平移总耗时会被 fixture 安装、清理和浏览器长任务污染，必须直接隔离 `applyViewport`、LOD 和小地图阶段。
- 单次 LOD 延后试验只有约 4 ms 差异，不能据此提交会改变平移期间视觉更新时序的方案。
- 任何平移优化都必须保持最终松手后的 LOD、视口框、小地图和选择枢纽状态完整刷新。
