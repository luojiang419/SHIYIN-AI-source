# 639-智能平移节流试验回退

## 已完成内容

- 从快照 638 继续，完成两组智能 1000 节点平移多轮清洁采样：首轮稳定样本约 155–183 ms，第二组约 171–199 ms，仍受偶发 199–266 ms 长任务影响。
- 试验 `SMART_SAFE_LOD_PAN_THROTTLE_ENABLED` 与 96 ms 节流，并在平移/小地图拖动结束时强制补一次 LOD；多轮结果未显示稳定改善，已在提交前完全回退。
- 回退后源码保持 638 基线，不改变安全 LOD、视口变换、小地图视口框或选择枢纽刷新时序。

## 当前模块

统一验收阶段的智能平移多轮观测与试验回退。本轮没有新增业务源码。

## 具体代码前后对比

```diff
// 本轮不提交源码改动
- 试验：平移期间 96 ms 安全 LOD 节流 + 松手补偿
+ 结论：两组多轮样本未形成稳定改善，已回退
```

## 验证命令与结果

- `python -m unittest tests.test_canvas_interaction_performance tests.test_canvas_hotpath_indexes tests.test_canvas_performance_baseline tests.test_canvas_save_media_isolation`：41 passed（回退后）。
- `node --check static/js/smart-canvas.js`：通过。
- `git diff --check`：通过；回退后工作树无源码差异。
- 浏览器多轮平移：基线组稳定样本约 155.5/160.5/180.5/183.6 ms；节流试验组约 162.5/166.5/171.5/183.1/199.3 ms，未见稳定收益。

## 未完成待办

- 经典/智能平移仍需在不同视口和节点档位下采集 p50/p95/p99；当前总耗时仍受长任务调度影响。
- 若继续优化，需要更细的浏览器性能剖析、空间索引或小地图绘制策略，不能重复未经证实的 LOD 节流。
- 统一验收目标仍未全部达成。

## 下一步

读取本快照后，继续以统一验收数据收敛和交互回归为主；只有出现稳定证据时再设计新的带开关/全量回退方案。

## 已解决踩坑点

- 多轮平移中首轮 fixture 负载会显著抬高样本，必须剔除首轮或单独标注。
- 平移节流即使减少 LOD 调度，也可能因松手补偿和浏览器长任务抵消收益，必须比较多轮 p50/p95 而非单次耗时。
- 试验代码在验证失败后要完全回退，避免把没有收益证据的时序变化带入主分支。
