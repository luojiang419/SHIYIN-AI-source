# 640-统一验收清洁平移边界收敛

## 已完成内容

- 从快照 639 继续，先检查 `4492` 工作树与主开发文档，确认 A-F、连接层、框选矩形缓存、经典/智能选择反馈、经典拖动索引、智能拖动高亮和两轮智能平移节流试验均已完成或回退，不重复实现。
- 尝试连接本地 `?canvasPerf=1` 页面进行下一轮真实平移采样；当前浏览器没有可复用的本地登录会话，访问稳定重定向到 `/login`。未输入仓库文档中的账号密码、未自动注册账号、未改变画布数据。
- 在无认证状态下完成统一验收回归与静态检查；源码保持快照 639 的回退基线，没有新增业务代码或未经证实的平移时序变化。

## 当前模块

统一验收阶段的多轮清洁平移边界收敛。本轮仅完成验收收敛与风险边界记录，无大模块源码改动，不创建 `backup/` 源码备份。

## 具体代码前后对比

```diff
// 本轮不提交业务源码改动
- 继续尝试平移节流/LOD 时序优化
+ 认证不可用且既有多轮样本未形成稳定收益，保持 639 回退基线
```

## 验证命令与结果

- 浏览器：打开 `http://127.0.0.1:3000/?canvasPerf=1`，页面重定向至 `/login`；没有执行登录或注册副作用。
- `python -m unittest tests.test_canvas_interaction_performance tests.test_canvas_hotpath_indexes tests.test_canvas_performance_baseline tests.test_canvas_save_media_isolation tests.test_canvas_performance_observer`：44 passed。
- `node --check static/js/canvas.js`：通过。
- `node --check static/js/smart-canvas.js`：通过。
- `node --check static/js/canvas-performance.js`：通过。
- `git diff --check`：通过。
- `git status --short`：仅本快照文件待提交；业务源码无差异。
- 清洁平移数据沿用 639 的两组多轮结果：基线稳定样本约 155.5/160.5/180.5/183.6 ms，节流试验约 162.5/166.5/171.5/183.1/199.3 ms，未形成稳定改善，不能据此设计新方案。

## 未完成待办

- 真实浏览器登录后仍需在经典/智能画布的不同视口和 100/300/500/1000 节点档位采集平移 p50/p95/p99、帧间隔与 long task。
- 统一验收目标尚未全部达成；当前不能把认证阻塞或长任务波动误判为源码回归。
- 若后续获得稳定数据，再对安全 LOD、小地图绘制或空间索引设计带开关、全量回退的低风险方案；不得重复 96 ms LOD 节流试验。

## 下一步

保持源码基线不变，等待可复用的本地认证浏览器会话后继续统一验收；从不同节点档位与视口采样收敛 p50/p95/p99。若仍无稳定主导成本证据，则结束性能优化主模块，不扩大高风险重构。

## 已解决踩坑点

- 已确认验收浏览器若无本地登录会话会被认证中间件重定向；本轮改用专项回归和静态检查完成可执行部分，并明确不自动输入凭据或注册账号。
- 已确认在没有新浏览器样本时，不能把快照 639 的单次/小样本差异升级为新优化方案；必须保留回退基线并等待跨档位、跨视口的多轮 p50/p95/p99 证据。
