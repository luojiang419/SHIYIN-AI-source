# 641-统一验收离线全量回归完成

## 已完成内容

- 从快照 640 继续，确认当前浏览器仍无本地登录会话，无法在不输入凭据或绕过认证的前提下新增真实平移样本。
- 完成统一验收阶段的离线全量回归：测试发现 628 项执行、620 项通过，1 项既有 grsai API Key 环境缺失错误，1 项既有 pose-reference 字符串契约失败；未出现与 A-F 或平移回退基线相关的新失败。
- 重新运行性能专项回归并通过，源码保持 639/640 回退基线，没有新增业务改动、开关变化或渲染时序变化。

## 当前模块

统一验收离线全量回归。该模块仅验证当前性能基线与既有功能回归，不涉及大模块源码改动，不创建 `backup/` 源码备份。

## 具体代码前后对比

```diff
// 本轮不提交业务源码改动
- 等待浏览器平移数据后再判断是否需要优化
+ 先完成 628 项离线全量回归，确认无性能模块新增回归
```

## 验证命令与结果

- `python -m unittest discover -s tests -p 'test_*.py'`：628 项执行，620 passed，2 项既有环境/契约问题，测试进程以失败码结束；失败为 `test_grsai_nano_request_uses_documented_body_and_response`（缺少 grsai API Key）与 `test_pose_replicate_connections_persist_the_selected_input_role`（既有 `pose-reference` 字符串契约不一致）。
- `python -m unittest tests.test_canvas_interaction_performance tests.test_canvas_hotpath_indexes tests.test_canvas_performance_baseline tests.test_canvas_save_media_isolation tests.test_canvas_performance_observer`：44 passed。
- `node --check static/js/canvas.js`：通过。
- `node --check static/js/smart-canvas.js`：通过。
- `node --check static/js/canvas-performance.js`：通过。
- `git diff --check`：通过。
- 浏览器会话检查：本地验收页仍无可复用认证态，未执行登录、注册或画布写操作。

## 未完成待办

- 仍需在已有本地登录会话下采集经典/智能画布 100/300/500/1000 节点、不同视口的平移 p50/p95/p99、帧间隔与 long task。
- 统一验收指标尚未全部达标；当前没有稳定证据支持新的 LOD、小地图或空间索引方案。
- 两个既有全量测试问题需在其对应环境/契约任务中单独处理，本轮不跨模块修复。

## 下一步

继续保持源码基线不变，优先等待可复用本地登录态并完成跨档位真实平移采样；若无法获得登录态，则以 641 为离线回归基线，不扩大性能重构范围。

## 已解决踩坑点

- 全量测试输出包含大量长错误堆栈，不能仅按进程失败码判断本轮性能回归；已核对最终汇总，确认只有 2 个此前已知的环境/契约问题。
- 运行性能专项后必须再次执行三份画布脚本语法检查和 `git diff --check`，以确认验收过程没有产生临时源码污染。
