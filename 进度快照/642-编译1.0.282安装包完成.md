# 642-编译 1.0.282 安装包完成

## 已完成内容

- 从快照 641 继续，按项目版本递增规则将源码版本从 `1.0.281` 统一更新到 `1.0.282`，同步 `VERSION`、npm、Cargo、Tauri、后端和更新说明字段。
- 执行 Windows 安装包构建：后端 PyInstaller 构建成功；Tauri release 构建成功；网页缓存版本戳写入成功；运行时冒烟健康检查通过；Inno Setup 编译成功。
- 首次构建因工作树缺少 `node_modules/tailwindcss` 停止，执行 `npm ci` 恢复锁定依赖后重跑成功；问题已记录到避坑指南。

## 当前模块

Windows 安装包编译与产物验收。该模块不改业务逻辑，不创建 `backup/` 源码备份。

## 具体代码前后对比

```diff
- 版本：1.0.281
+ 版本：1.0.282

- 构建：无 1.0.282 安装包
+ 产物：dist/installer/SHIYIN-AI-Setup-1.0.282.exe
```

## 验证命令与结果

- `npm run version:patch`：`1.0.281 -> 1.0.282`，版本字段同步完成。
- `npm ci`：成功安装 75 个锁定依赖包。
- `powershell -ExecutionPolicy Bypass -File tools/build-installer.ps1 -SkipBackend`：成功；Tauri release 编译耗时约 2 分 13 秒，Inno Setup 编译成功。
- 构建运行时冒烟：`health=ok`、启动约 1334 ms、二次启动退出码 0、主进程存活、后端随父进程退出；内存目标 250 MB 未达到（测得进程树约 436.39 MB），不影响脚本成功码但需保留记录。
- DWPose 真人输入冒烟：未提供 `-DwposeSmokeInput`，按脚本规则跳过。
- `powershell -ExecutionPolicy Bypass -File tools/verify-installer-artifact.ps1 -Version 1.0.282`：通过；EXE 大小 `71,409,882` bytes，SHA-256 `cb692cfccd668403bd3d34d4801f32e0298b962fc0a33bbf594f220db527c6d4`，签名状态 `NotSigned`。
- `git diff --check`：通过。

## 未完成待办

- 如需正式发布，还需使用发布脚本执行 GitHub Release 上传；本轮仅按用户要求编译并验收本地安装包。
- 安装包仍未进行 DWPose 真人输入的缓存模型/新安装下载双冒烟，原因是没有提供测试输入图片。
- 运行时内存冒烟高于 250 MB 目标，需另立性能/打包体积模块，不在本次编译任务中擅自改动。

## 下一步

提交并推送 1.0.282 版本字段、构建踩坑记录和本快照；交付安装包路径与校验值。若后续要求正式发布，再从本快照继续执行发布流程。

## 已解决踩坑点

- 构建前缺少 Node 依赖会导致 `tailwindcss/lib/cli.js` 找不到；已通过 `npm ci` 恢复依赖并成功重建，详情见 `避坑指南/2026082415-安装包构建前必须安装Node依赖.md`。
- Tauri 构建会更新工作树中的生成文件时间戳，但内容哈希未变化；本次只提交版本字段、更新说明和明确的避坑/进度文档，不把无内容变化的生成文件误列入版本提交。
