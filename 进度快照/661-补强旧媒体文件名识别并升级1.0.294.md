# 补强旧媒体文件名识别并升级 1.0.294

## 已完成

- 通过安装目录日志确认 `/api/download-output` 对视频持续返回 `206 Partial Content`，后端 Range/MIME/本地文件读取链路正常。
- 定位截图中的破图症状：前端媒体分类只检查 URL、kind/media_type；旧数据可能只在 name/original_name 中保留 `.mp4`，导致视频被渲染成 `<img>`。
- `static/js/works.js`、`static/js/asset-manager.js`：媒体类型识别新增 `name/original_name/filename` 扩展名兜底，并补齐 flv 视频扩展名。
- `main.py`：`work_media_type` 与 `canvas_asset_kind` 同步按展示文件名兜底，保证 API 与前端类型一致。
- 版本从 1.0.293 递增到 1.0.294，更新 release-notes。

## 代码前后对比

- 修改前：仅按 `item.url` 的扩展名判断视频；URL 无扩展名时即使文件名是 `.mp4` 也落入图片分支。
- 修改后：按 `url/name/original_name/filename` 逐一判断，显式 `video` 优先，后端和两个页面保持同一判定策略。

## 验证

- `tests/test_video_playback_surfaces.py`、`tests/test_works.py`、`tests/test_desktop_updater_contract.py`：52 passed。
- 安装目录历史视频请求核验：`206 Partial Content`，无视频 4xx/5xx。

## 待办

- 编译 1.0.294 安装包。
- 使用正确工作目录启动安装包，重新验证作品管理、画布资产的视频 DOM 类型、readyState/duration、播放及“打开目录”日志。
