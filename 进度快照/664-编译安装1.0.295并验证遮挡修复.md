# 编译安装 1.0.295 并验证遮挡修复

## 已完成

- 编译并生成安装包：`dist/installer/SHIYIN-AI-Setup-1.0.295.exe`。
- 静默覆盖安装到 `D:\Program Files\SHIYIN AI`，安装版本为 `1.0.295`。
- 安装包 SHA256：`522562DCE0EEAA668C7CEC9D3EE72B6404C358C6C687B3C7E513CFE8F66079AC`。
- 实际启动桌面程序和后端，确认 WebView2 加载 `works.css?v=1.0.295`。

## 回归结果

- 作品管理视频预览：隐藏图片 `hidden=true`、计算样式 `display:none`、矩形宽高均为 0。
- 视频元素：`hidden=false`、`readyState=4`、`duration=8.041667`、分辨率 `1920x1080`。
- 直接对视频按空格播放成功：`paused=false`、`currentTime=0.937258`。
- 全屏按钮点击无异常；浏览器错误/警告日志为空。
- 后端日志中的 MP4 请求均为 `206 Partial Content`，CSS 请求为 `200 OK`，未见相关 4xx/5xx。

## 当前修改模块

作品预览 CSS 的隐藏层处理已完成，源码与安装包均为 1.0.295。

## 待办

- 提交并推送本次 1.0.295 修复。
