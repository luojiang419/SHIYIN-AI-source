# 编译安装 1.0.296 并验证全屏空格

## 已完成

- 编译生成：`dist/installer/SHIYIN-AI-Setup-1.0.296.exe`。
- 静默覆盖安装到 `D:\Program Files\SHIYIN AI`，安装版本为 `1.0.296`。
- 安装包 SHA256：`740e907e46c52ff4e8d5e924b6f99e9335612c43e196140e969c209f48d79826`。
- 安装文件确认包含 `handlePreviewFullscreenKeydown`，位置为 `app/web/js/works.js` 第 345 行附近。

## 验证结果

- 作品视频仍可加载：`readyState=4`、时长 `8.041667` 秒、后端请求 `206 Partial Content`。
- 真实桌面页面已加载 `works.js?v=1.0.296`，浏览器无错误日志。
- 全屏按钮调用无 JavaScript 异常；自动化浏览器因安全策略不允许真正系统级全屏，因此无法读取真实 fullscreen 状态。
- 全屏键盘逻辑已通过 52 项作品/视频播放/更新器契约测试，处理范围仅限全屏作品视频，空格会阻止默认滚动并切换 `play()`/`pause()`。

## 当前修改模块

作品管理视频预览的全屏键盘控制已完成，源码、安装包和安装目录均为 1.0.296。

## 待办

- 提交并推送本次 1.0.296 修复。
