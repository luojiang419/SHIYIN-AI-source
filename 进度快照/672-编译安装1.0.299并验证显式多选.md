# 编译安装 1.0.299 并验证显式多选

## 已完成内容

- 编译生成 `dist/installer/SHIYIN-AI-Setup-1.0.299.exe`，安装器编译成功。
- 覆盖安装到 `D:\Program Files\SHIYIN AI`，安装退出码 `0`，安装目录 `app/VERSION` 确认为 `1.0.299`。
- 安装包 SHA256：`0AB9CDB1CF65966C8F77B4D196BBB6334CFDFE8B1E59C1102C06188A21BFD30A`。
- 安装页面脚本缓存版本确认：`works.css?v=1.0.299`、`works.js?v=1.0.299`。

## 实际桌面 WebView 回归

- 初始状态：32 张作品卡片，复选框 `display:none`，多选开关 `aria-checked=false`。
- 点击顶部“多选”：复选框数量 32，首张卡片复选框 `display:grid`，开关 `aria-checked=true`。
- 复选框点击：顶部工具栏显示，提示 `已选择 1 项`。
- 多选模式下不按 Shift 点击第二张卡片：选中数变为 2，未打开视频预览对话框。
- 多选模式下不按 Shift 直接拖拽：选中数为 2，证明显式模式不依赖修饰键传递。
- 关闭多选：开关为 `false`，选中数为 0，工具栏为 `display:none`，复选框恢复隐藏。
- 额外验证 Shift 点击：选中数为 1，原快捷方式仍有效。

## 源码验证

- `node --check static/js/works.js`：通过。
- `python -m pytest tests/test_works.py tests/test_video_playback_surfaces.py tests/test_desktop_updater_contract.py -q`：56 passed，4 个 FastAPI 弃用警告。
- `git diff --check`：通过。

## 清理与待办

- 构建产生的 `.build`、`src-tauri/target`、`dist/installer-stage` 为临时构建缓存，提交前清理；保留最终安装器。
- 待提交源码、快照和避坑记录并推送 GitHub。

## 代码前后对比摘要

- 之前：`beginSelectionDrag` 只接受 `event.shiftKey`，Shift 在桌面 WebView 中丢失时无法多选。
- 现在：新增 `selectionMode`、`shiftPressed`、`worksSelectionModeToggle`、`data-select-work` 复选框；框选入口支持显式模式、事件 Shift 和 document Shift 状态三路判断。
