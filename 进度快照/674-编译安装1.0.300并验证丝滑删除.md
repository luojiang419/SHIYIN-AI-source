# 编译安装 1.0.300 并验证丝滑删除

## 已完成

- 编译并覆盖安装 `1.0.300` 到 `D:\Program Files\SHIYIN AI`。
- 安装器：`dist/installer/SHIYIN-AI-Setup-1.0.300.exe`。
- SHA256：`137CDE396CF402F9546460ECEFD1058EB1AFB78C1A904C3BA566E060E0C22D4C`。
- 安装目录版本：`1.0.300`；页面脚本缓存：`works.css?v=1.0.300`、`works.js?v=1.0.300`。

## 实机验证

- 作品管理加载 32 张卡片。
- 多选模式下不按 Shift 拖拽选中 2 项，选择结果稳定，页面无黑屏/卡片重建闪烁。
- 批量“移到回收站”期间页面保持卡片可见，批量按钮进入禁用忙状态；请求完成后工具栏自动清空并显示完成提示，无需手动刷新。
- 为避免破坏用户数据，未在实机点击永久删除；永久删除真实文件和记录已由临时数据库测试覆盖。
- 实机控制台 error/warn：空。
- 回归测试中的临时回收站作品已恢复原状态。

## 源码验证

- `node --check static/js/works.js`：通过。
- `python -m pytest tests/test_works.py tests/test_video_playback_surfaces.py tests/test_desktop_updater_contract.py -q`：57 passed，4 个 FastAPI 弃用警告。
- `git diff --check`：通过。

## 关键改动

- `syncSelectionVisuals()`：只更新现有卡片的选中样式、checkbox 和批量工具栏。
- `requestAnimationFrame`：合并高频拖拽事件。
- `batchBusy`：请求期间保留页面并锁定按钮。
- 批量删除成功后按选中 id 从 `state.works` 增量移除，收藏/回收站直接更新当前对象。

## 待办

- 提交并推送源码。
- 构建缓存总量仍需保持在 20GB 上限内；最终安装包保留。
