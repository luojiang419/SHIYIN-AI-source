# 完成打开目录修复验收

## 已完成内容

- 已将 Windows Explorer 的目录定位调用从带嵌套引号的参数列表改为完整命令行字符串，修复路径包含空格时静默打开“文档”目录的问题。
- 版本号已递增为 `1.0.301`，并完成安装包编译：`dist/installer/SHIYIN-AI-Setup-1.0.301.exe`。
- 安装包 SHA-256：`7797107CD99D07533474CFB7DC4A9A08FB01267E0D65F07DCCF56EF757618980`。

## 真实桌面验收

- 使用已安装的 `D:\Program Files\SHIYIN AI` 启动 1.0.301。
- 在作品管理页面实际点击首张作品的“打开目录”，页面反馈“已打开作品所在目录”。
- 通过 Windows `Shell.Application` 检查 Explorer 实际位置为：`file:///D:/Program%20Files/SHIYIN%20AI/data/media/generated`，确认没有落到“文档”目录。
- 页面浏览器错误/警告日志为空，后端返回的真实媒体文件路径与 Explorer 位置一致。

## 测试结果

- `python -m pytest tests/test_works.py tests/test_video_playback_surfaces.py tests/test_desktop_updater_contract.py -q`：`57 passed`。
- `python -m py_compile main.py`：通过。
- `git diff --check`：通过。

## 当前修改与交付状态

- 当前修改：`main.py`、`tests/test_works.py`、版本及发布说明、避坑指南、进度快照。
- 已提交并推送 GitHub：commit `7bb984e`（`main`）。
- 已安装 1.0.301，保留桌面版本供用户复测。
