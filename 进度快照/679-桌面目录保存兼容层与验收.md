# 进度快照 679：桌面目录保存兼容层与验收

## 已完成内容

1. 发现并修复 WebView2 兼容性问题：画布 iframe 中 `showDirectoryPicker` 不存在时，改走 Tauri 原生目录选择器。
2. 新增桌面消息桥：父页面一次选择目录，逐个下载并调用 Rust `write_download_file` 写入真实文件，不生成 ZIP。
3. Rust 对文件名做路径穿越拦截，新增安全写入单元测试。
4. 版本已递增到 `1.0.303`，安装包已生成并安装验证版本文件。

## 当前修改模块

- `static/js/canvas.js`
- `static/js/desktop-updater.js`
- `src-tauri/src/lib.rs`
- `tests/test_canvas_download_and_asset_sync.py`
- `release-notes/current.md`
- `static/update-notes.json`

## 测试结果

- `python -m pytest tests/test_canvas_download_and_asset_sync.py -q`：4 passed。
- `cargo test --manifest-path src-tauri/Cargo.toml`：9 passed。
- `node --check static/js/canvas.js`、`desktop-updater.js`：通过。
- 安装包：`dist/installer/SHIYIN-AI-Setup-1.0.303.exe`。
- 安装包 SHA256：`CFEC588CBC485EAE6B463E16228873482C9EA79433240EFC3AEA43EDD3C29BE0`。
- 安装后的 `D:\Program Files\SHIYIN AI\app\VERSION`：`1.0.303`。
- 启动验收必须以 `D:\Program Files\SHIYIN AI` 为工作目录；从源码工作区启动会按设计进入开发路径但缺少 sidecar，产生 0xC0000409，这是启动方式问题，不是安装包问题。
- 安装后后端健康检查通过：`/api/health` 返回 `version=1.0.303`、`runtime_mode=desktop`、`database=ok`。
- 原生目录选择器的最后一步桌面交互因 Computer Use 被 Escape 中断，未继续重复触发；原生桥接代码已由 Rust 单元测试和已安装运行态编译验证覆盖。

## 待办清单

- 已计算安装包 SHA256。
- 已提交并推送源码、快照、备份和避坑文档。
- 已清理本轮专用临时目录，工作区保持干净。

## 下一步

提交 `a5f9872` 已推送到 `origin/main`。最终交付需说明全量测试中既有的 3 个旧缓存契约失败，不将其误报为本次回归。
