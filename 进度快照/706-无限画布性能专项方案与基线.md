# 进度快照 706：无限画布性能专项方案与基线

日期：2026-08-25

版本：`1.0.310`（本模块未修改运行时代码，不递增版本）

基线：`main@6c5f15a`

## 已完成内容

- 创建本专项最高优先级事实来源：`开发文档/无限画布菜单与剪贴板性能优化方案-v2.md`。
- 固化右键菜单、悬浮功能菜单、复制、粘贴、撤销快照、增量渲染和保存序列化的根因证据。
- 明确采用 ComfyUI 的局部更新、脏区更新、按帧提交和 Overlay 菜单思路，但保留 DOM 节点、媒体、表单及复杂编辑器，不整体迁移 LiteGraph。
- 将实施拆分为 6 个后续模块，确定模块优先级、文件边界、风险、回退路径和性能预算。
- 检查现有测试缺口：现有契约仅覆盖 `structuredClone`、部分增量选择和智能菜单路径，未锁定全局图标刷新、复制查找复杂度、固定粘贴等待和撤销快照体积。

## 当前模块和涉及文件

- 已完成模块：模块 0，方案与基线。
- 当前准备模块：模块 1，菜单显示热路径。
- 新增文件：`开发文档/无限画布菜单与剪贴板性能优化方案-v2.md`。
- 新增文件：`进度快照/706-无限画布性能专项方案与基线.md`。
- 未修改运行时 JS、CSS、测试和版本文件。

## 具体修改前后对比

修改前：

```text
性能问题分散在旧文档、代码和对话诊断中；上下文压缩后没有单一恢复入口，也没有按模块约束开发范围。
```

修改后：

```text
建立 v2 专项方案作为最高优先级事实来源，包含强制恢复协议、根因、模块状态、逐模块任务、验收标准、风险和下一步。
当前状态固定为：模块 0 已完成，模块 1 准备开始。
```

## 验证命令与结果

```powershell
git status --short --branch
Get-ChildItem .\进度快照 -File | Sort-Object ...
git remote -v
```

- 当前分支为 `main`，跟踪 `origin/main`。
- 创建快照前只有专项方案文档为未跟踪文件，无其他用户改动。
- 最新历史快照为 705，本快照使用纯递增编号 706。
- 远端 `origin` 指向 `https://github.com/luojiang419/SHIYIN-AI-source.git`。
- 本模块没有运行时代码改动，因此不执行应用测试、不递增版本。

## 未完成待办

- 为菜单局部图标刷新、布局读取和 CSS 合成建立专项契约测试。
- 实施经典/智能菜单的容器级图标刷新。
- 合并或减少创建菜单的同步布局读取。
- 去除经典创建菜单、子菜单和 selection hub 热路径背景模糊。
- 完成模块 1 回归、版本递增、方案回填、快照、提交与推送。

## 下一步

第一步必须完整读取 `E:\APP\SHIYIN-AI\开发文档\无限画布菜单与剪贴板性能优化方案-v2.md`；然后仅实施模块 1，不提前修改复制粘贴、撤销历史、增量渲染或保存模块。

## 已知风险与恢复说明

- 菜单布局优化不能破坏视口边缘翻转、双列布局、子菜单方向和深浅主题。
- 静态契约测试只能锁定实现边界，不能替代真实 WebView 的 p95 性能验收。
- 恢复开发时以专项方案文档为最高优先级；若与本快照冲突，以专项方案最新内容为准。
