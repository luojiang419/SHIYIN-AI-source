# 进度快照 707：1.0.311 无限画布菜单显示热路径优化

日期：2026-08-25

版本：`1.0.311`

基线：`main@f4c02b5` + 本模块待提交改动

## 已完成内容

- 经典画布创建、连线、节点端口、图片和输出菜单只刷新各自容器内的 Lucide 图标，不再调用无作用域 `refreshIcons()` 扫描整页 DOM。
- 经典创建菜单用菜单子项结构估算单列高度，再读取一次最终菜单矩形；保留底部空间不足时的双列切换、视口夹紧和子菜单左右翻转。
- 经典创建菜单、子菜单、selection hub 和智能创建菜单移除高成本 `backdrop-filter`，改用不透明主题背景；智能节点悬浮菜单继续保持无模糊。
- 新增菜单性能专项契约，更新旧布局契约，刷新经典画布 JS/CSS 与智能画布 CSS 缓存修订号。
- 应用版本由 `1.0.310` 递增到 `1.0.311`，同步后端、npm、Tauri、Cargo、更新 JSON 和发布说明。
- 更新既有避坑指南，记录 `--data-dir` 解析晚于路径初始化以及发现测试数据污染后的精确 API 恢复流程。
- 通过真实 Chromium 验证经典空画布与智能画布菜单显示；测试画布、测试服务、端口和临时目录均已清理。

## 当前模块和涉及文件

- 已完成模块：模块 1，菜单显示热路径。
- 当前准备模块：模块 2，复制粘贴查找和连接局部处理。
- 运行时：`static/js/canvas.js`、`static/css/canvas.css`、`static/css/smart-canvas.css`。
- 缓存入口：`static/canvas.html`、`static/smart-canvas.html`。
- 测试：`tests/test_canvas_menu_performance.py`、`tests/test_canvas_create_menu_layout.py`。
- 版本与发布：`VERSION`、`main.py`、`package.json`、`package-lock.json`、`src-tauri/Cargo.toml`、`src-tauri/Cargo.lock`、`src-tauri/tauri.conf.json`、`release-notes/current.md`、`static/update-notes.json`。
- 记录：`开发文档/无限画布菜单与剪贴板性能优化方案-v2.md`、`避坑指南/2026081702-隔离数据目录必须在导入路径模块前设置.md`、本快照。

## 具体修改前后对比

菜单图标刷新：

```javascript
// 修改前：每次开菜单扫描整页图标
refreshIcons();

// 修改后：只扫描当前 Overlay
refreshIcons(createMenu);
refreshIcons(linkCreateMenu);
[nodeInputMenu, nodeOutputMenu].forEach(menu => refreshIcons(menu));
refreshIcons(imageNodeMenu);
```

创建菜单布局读取：

```javascript
// 修改前：切换双列前后连续读取两次布局
const singleColumnHeight = createMenu.getBoundingClientRect().height;
createMenu.classList.toggle('create-menu-two-column', lacksBottomSpace);
const menuRect = createMenu.getBoundingClientRect();

// 修改后：无布局读取地估算单列高度，只读取最终矩形一次
const singleColumnHeight = estimateClassicCreateMenuSingleColumnHeight();
createMenu.classList.toggle('create-menu-two-column', lacksBottomSpace);
const menuRect = createMenu.getBoundingClientRect();
```

菜单合成样式：

```css
/* 修改前 */
background:rgba(...,.96); backdrop-filter:blur(16px);

/* 修改后 */
background:var(--card-solid); /* 经典画布 */
background:var(--card);       /* 智能画布 */
```

## 验证命令与结果

```powershell
python -m unittest tests.test_canvas_menu_performance tests.test_canvas_create_menu_layout tests.test_canvas_interaction_performance tests.test_performance_contracts -v
node --check static/js/canvas.js
node --check static/js/smart-canvas.js
python -m json.tool static/update-notes.json
cargo check --manifest-path src-tauri/Cargo.toml
git diff --check
```

- 51 项 Python 相关回归全部通过。
- 双画布 JavaScript 语法检查通过。
- 更新 JSON 解析、7 个版本源一致性检查和 Git 差异检查通过。
- Tauri `cargo check` 通过，桌面宿主版本为 `1.0.311`。
- 真实 Chromium：经典菜单矩形完全位于视口内，双列正常，计算背景为不透明 `rgb(30,30,30)`，`backdrop-filter:none`；智能菜单实际打开和布局正常。
- 控制台没有本次变更新增错误；仅有既存 Lucide `scan-up`、`badge-hd` 图标缺失警告。
- 未采集 500 节点 p50/p95/p99，不宣称数值性能预算已经达标；统一统计采样保留到模块 5。

## 环境恢复与清理

- 发现 `--data-dir` 不能隔离启动早期存储后，按标题和精确 ID 双重确认本轮两个 QA 画布。
- 两个 QA 画布均经正式 API 软删除并 purge；活动列表和回收站目标剩余数均为 0。
- `127.0.0.1:3011` 已停止监听，浏览器临时标签已关闭，专用系统临时目录已删除。
- 本模块属于小范围菜单热路径修复，不生成 `backup/` 源码差异备份。

## 未完成待办

- 两套复制路径改用节点索引，消除 `selectedIds.map(id => nodes.find(...))`。
- 建立或复用节点到连接的邻接索引，只复制选中集合内部连接。
- 经典粘贴只校验新增连接，只同步受影响生成器。
- 移除经典内部节点粘贴固定 90 ms 等待，同时保持系统媒体粘贴优先。
- 为单节点、多选、全选、组节点和系统媒体粘贴补契约与行为回归。

## 下一步

第一步必须完整读取 `E:\APP\SHIYIN-AI\开发文档\无限画布菜单与剪贴板性能优化方案-v2.md`；然后仅实施模块 2，不提前修改模块 3 的撤销历史、模块 4 的增量渲染或模块 5 的保存协议。

## 已知风险与恢复说明

- `estimateClassicCreateMenuSingleColumnHeight()` 的 38px 菜单行、13px 分隔线、6px 间距和 16px 内边距与当前 CSS 规格绑定；后续修改菜单尺寸时必须同步契约。
- 经典菜单图标局部刷新已覆盖五个打开入口；其他非菜单的全局 `refreshIcons()` 不属于本模块，不能据此批量替换。
- 恢复开发时以专项方案文档为最高优先级；若与本快照冲突，以专项方案最新内容为准。
