# 进度快照 708：模块 2 复制粘贴失败契约建立

## 已完成内容

- 模块 1 已以 `5ec721b` 提交并推送到 `origin/main`，提交后本地与远端一致。
- 新增 `tests/test_canvas_clipboard_performance.py`，建立 7 项模块 2 失败契约。
- 契约覆盖节点索引复制、连接邻接索引、组节点成员重映射、经典粘贴新增连接局部校验、目标生成器同步、0 ms 粘贴回退和系统媒体优先级。

## 当前模块与文件

- 当前模块：模块 2，复制粘贴查找和连接局部处理。
- 已修改：`tests/test_canvas_clipboard_performance.py`。
- 已修改：`开发文档/无限画布菜单与剪贴板性能优化方案-v2.md`。
- 尚未修改本模块运行时代码，应用版本保持 `1.0.311`。

## 代码前后对比

修改前：现有测试只检查复制路径存在 `structuredClone`，不能识别 `selectedIds.map(...nodes.find...)`、全连接扫描、全局连接校验或固定 90 ms 等待。

修改后：新增专项测试直接检查两套复制路径使用节点/连接索引，并约束经典粘贴局部处理、组成员 ID 重映射和粘贴事件优先级。

## 验证结果

执行：

```powershell
python -m unittest tests.test_canvas_clipboard_performance -v
```

结果：7 项中 6 项失败、1 项错误。错误来自预期的新粘贴处理函数尚不存在；其余失败均对应当前旧实现，说明失败契约有效。

## 待办清单

- 实现经典/智能节点索引复制和组成员递归收集。
- 建立或维护两套模型级连接邻接索引。
- 经典粘贴只校验新增连接并只同步受影响生成器。
- 删除固定 90 ms 等待，保持系统媒体粘贴优先。
- 完成真实浏览器验证、版本递增、发布记录、模块快照、提交和推送。

## 下一步

先实现两套复制路径的节点索引、组成员收集和连接邻接索引，使前三类契约转绿；完成后立即回填方案并新增递增快照。

## 已知风险与恢复说明

- 组节点复制必须递归包含成员且去重，粘贴时只能保留本次剪贴板中能映射的新成员 ID，不能指回原画布节点。
- 邻接索引失效时允许单次全量重建，但稳定复制热路径不得扫描整张连接表。
- 恢复开发时第一步必须完整读取 `E:\APP\SHIYIN-AI\开发文档\无限画布菜单与剪贴板性能优化方案-v2.md`。
