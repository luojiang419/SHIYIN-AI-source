# 进度快照 711：模块 2 移除固定粘贴等待

## 已完成内容

- 经典与智能 `canvas.paste` 移除固定 `setTimeout(..., 90)`。
- 增加 0 ms 节点粘贴回退，真实 `paste` 事件处理成功时立即取消。
- 系统图片、视频、音频优先于内部节点；智能素材收件箱也优先处理。
- 命名粘贴处理函数同时绑定到画布窗口和同源宿主窗口。
- 通过时间戳标记和定时器清理避免真实事件与回退重复粘贴。

## 当前模块与文件

- 当前模块：模块 2，复制粘贴查找和连接局部处理。
- 修改：`static/js/canvas.js`。
- 修改：`static/js/smart-canvas.js`。
- 修改：`tests/test_canvas_clipboard_performance.py`。
- 修改：`开发文档/无限画布菜单与剪贴板性能优化方案-v2.md`。
- 已新增快照 708、709、710；本文件为 711。

## 代码前后对比

修改前：

```javascript
setTimeout(() => {
    if(lastImagePasteAt >= requestedAt) return;
    pasteNodes();
}, 90);
```

修改后：

```javascript
scheduleClassicNodePasteFallback();
// handleClassicClipboardPaste() 先处理媒体或内部节点，并取消回退。
```

智能画布采用同一事件优先、0 ms 回退结构。

## 验证结果

- `python -m unittest tests.test_canvas_clipboard_performance -v`：7 项通过。
- `node --check static/js/canvas.js` 通过。
- `node --check static/js/smart-canvas.js` 通过。
- `git diff --check` 通过，仅有 Git 的 LF/CRLF 提示。

## 待办清单

- 审计其他复制、粘贴、工作流导入和索引刷新调用点。
- 补足必要的索引失效与粘贴优先级契约。
- 运行相关画布回归。
- 真实 Chromium 验证单节点、多选、组节点和系统媒体粘贴。
- 递增版本并更新缓存修订号、版本源、发布说明和更新 JSON。
- 完成模块快照、提交并推送。

## 下一步

执行全局调用点审计，重点检查连接数组原地修改是否可能让邻接索引计数不变却内容失效，以及宿主窗口粘贴监听的生命周期是否完整。

## 已知风险与恢复说明

- 0 ms 回退依赖浏览器在快捷键默认动作期间同步派发 `paste` 事件；真实 Chromium 验证必须覆盖这一顺序。
- 任何系统媒体分支都必须在内部节点分支之前，并清除待执行回退。
- 恢复开发时第一步必须完整读取 `E:\APP\SHIYIN-AI\开发文档\无限画布菜单与剪贴板性能优化方案-v2.md`。
