# 进度快照 712：模块 2 邻接索引与组引用审计

## 已完成内容

- 审计经典与智能画布的连接数组替换、追加、过滤和索引重建调用点。
- 两套剪贴板邻接索引增加连接数组来源引用校验，覆盖“连接数量不变、数组整体替换”的失效场景。
- 经典组粘贴只保留本次复制中成功重映射的成员 ID，避免异常数据引用旧画布节点。
- 新增相同长度连接数组替换契约，并强化经典组成员重映射契约。

## 当前模块与文件

- 当前模块：模块 2，复制粘贴查找和连接局部处理。
- 修改：`static/js/canvas.js`。
- 修改：`static/js/smart-canvas.js`。
- 修改：`tests/test_canvas_clipboard_performance.py`。
- 修改：`开发文档/无限画布菜单与剪贴板性能优化方案-v2.md`。
- 本文件为纯递增快照 712。

## 代码前后对比

修改前：

```javascript
if(classicConnectionModelIndex.size !== connections.length){
    rebuildClassicConnectionModelIndexes();
}
c.items = c.items.map(id => idMap.get(id) || id);
```

修改后：

```javascript
if(classicClipboardConnectionSource !== connections
    || classicClipboardConnectionCount !== connections.length){
    rebuildClassicConnectionModelIndexes();
}
c.items = c.items.map(id => idMap.get(id)).filter(Boolean);
```

智能画布同样记录 `smartClipboardConnectionSource` 和连接数量。

## 验证结果

- `python -m unittest tests.test_canvas_clipboard_performance -v`：8 项通过。
- `node --check static/js/canvas.js` 通过。
- `node --check static/js/smart-canvas.js` 通过。
- `git diff --check` 通过，仅有 Git 的 LF/CRLF 提示。

## 待办清单

- 真实 Chromium 验证经典多选与组节点复制粘贴。
- 创建智能 QA 画布并验证单节点、多选、组节点和系统媒体粘贴。
- 检查控制台无新增错误。
- 递增版本并更新缓存修订号、版本源、发布说明和更新 JSON。
- 创建模块 2 差异备份，运行完整回归，清理、提交并推送。

## 下一步

复用端口 3012 的隔离服务，继续经典画布多选和组节点验证，然后完成智能画布同类验证。

## 已知风险与恢复说明

- 数组来源与数量双重判断覆盖当前全部替换和追加路径；若未来新增原数组内的等量位置替换，必须显式重建索引或增加 revision 标记。
- 恢复开发时第一步必须完整读取 `E:\APP\SHIYIN-AI\开发文档\无限画布菜单与剪贴板性能优化方案-v2.md`。
