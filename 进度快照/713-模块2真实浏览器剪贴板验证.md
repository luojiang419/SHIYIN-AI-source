# 进度快照 713：模块 2 真实浏览器剪贴板验证

## 已完成内容

- 在端口 3012 的隔离服务中完成经典与智能画布真实 Chromium 功能验证。
- 经典全选 7 节点粘贴一次新增 7；复制 7 成员组一次新增 8，150 ms 后均无重复。
- 智能单节点、多选 3 节点、3 成员组分别一次新增 1、3、4，150 ms 后均无重复。
- 智能内部剪贴板含 4 节点时粘贴系统 PNG，仅新增 1 个媒体节点，证明媒体优先。
- 数据库确认新旧组成员集合不相交且成员 ID 全部存在；智能控制台无错误。

## 当前模块与文件

- 当前模块：模块 2，复制粘贴查找和连接局部处理。
- 运行时代码：`static/js/canvas.js`、`static/js/smart-canvas.js`。
- 测试：`tests/test_canvas_clipboard_performance.py`。
- 主记录：`开发文档/无限画布菜单与剪贴板性能优化方案-v2.md`。
- 隔离 QA 数据：`.qa-canvas-module2-20260825-01/`，完成提交前必须精确清理。

## 行为前后对比

修改前：

```text
快捷键粘贴固定等待 90 ms；复制扫描全节点/全连接；组节点复制不保证递归成员完整映射。
```

修改后：

```text
真实 paste 事件优先、0 ms 回退；复制只访问节点索引和相邻连接；组与成员一起复制并全部重映射。
```

## 验证结果

- 经典全选：`7 -> 14 -> 14 (150 ms)`。
- 经典分组：`15 -> 23 -> 23 (150 ms)`，增量为组 1 + 成员 7。
- 智能多选：`3 -> 6 -> 6 (150 ms)`。
- 智能分组：`7 -> 11 -> 11 (150 ms)`，增量为组 1 + 成员 3。
- 智能单节点：`15 -> 16 -> 16 (150 ms)`。
- 智能系统 PNG：内部剪贴板含 4 节点时 `11 -> 12`，只新增媒体节点。
- 智能控制台错误：0。

## 待办清单

- 递增版本至 `1.0.312`，更新缓存修订号与全部版本源。
- 更新发布说明和 `static/update-notes.json`。
- 创建模块 2 差异备份文档。
- 运行画布相关回归、版本一致性、JSON、JS、Cargo 和差异检查。
- 用 shutdown token 精确停止 PID 47076，关闭 QA 标签并清理精确隔离目录。
- 提交并推送 `origin/main`。

## 下一步

执行版本递增与模块差异备份，再运行完整回归。

## 已知风险与恢复说明

- 浏览器控制层会接管高层 `Ctrl+C/V`，节点快捷键验证使用隔离配置的 `F8/F9`；系统 PNG 通过浏览器剪贴板接口和真实 `Ctrl+V` 验证。
- 数值性能预算尚未在 500 节点 fixture 上采样，不得宣称 p95/p99 已达标；该项属于模块 5。
- 恢复开发时第一步必须完整读取 `E:\APP\SHIYIN-AI\开发文档\无限画布菜单与剪贴板性能优化方案-v2.md`。
