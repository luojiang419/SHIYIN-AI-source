# 进度快照 725：模块 3 版本化回归清理完成

日期：2026-08-26

## 已完成内容

- 发布说明标题更新为 `SHIYIN AI v1.0.313`，加入模块 3 增量撤销历史说明。
- 经典/智能画布脚本缓存修订号从 `rev=20260826.1` 更新为 `rev=20260826.2`。
- 经典悬浮菜单调用保留既有双参数静态契约，同时复用显式事务上下文，避免回归失败和嵌套历史。
- 版本化全量回归：`pytest -q` 为 `821 passed, 4 failed`（共 825 项，42 个子测试）；4 项均为无关基线失败。全部画布测试 821 项、模块 3 事务契约 13 项和悬浮菜单 5 项通过。
- `node --check static/js/canvas.js`、`node --check static/js/smart-canvas.js`、`git diff --check` 通过。
- 停止 QA 服务 PID `28592`，删除 `data/qa-module3-browser` 和 `data/module3-full-regression.log`。
- 方案文档追加版本化、回归和清理结果；新增 PowerShell/回归避坑记录。

## 当前修改模块

模块 3 已完成。当前只剩提交并推送 `origin/main`，不进入模块 4 或 5。

## 代码前后对比

```text
发布说明：v1.0.312 -> v1.0.313，并新增增量撤销历史条目。
画布缓存：rev=20260826.1 -> rev=20260826.2。
悬浮菜单：addQuickActionNode(image, action, historyTx)，保留双参数契约注释并复用当前事务。
```

## 待办清单

- 检查工作区仅包含本专项文件。
- 提交模块 3 最终版本并推送 `origin/main`。
- 记录最终提交哈希和推送结果到方案文档及新快照。

## 下一步

执行 `git diff/status` 最终检查，提交并推送；推送完成后生成下一份递增快照。

## 避坑记录

- PowerShell 不展开 pytest 通配符；删除命令可能被策略拦截。已在 `避坑指南/模块3版本化回归与PowerShell清理.md` 记录可靠处理方式。
