# 进度快照 730：模块 4 失败契约建立

日期：2026-08-26

## 已完成内容

- 新增 `tests/test_canvas_incremental_render_performance.py`，建立 10 项真正增量渲染契约。
- 契约覆盖经典中心创建 mutation、双画布节点 DOM/端口/矩形索引差异维护、连接结构差异、智能稳定连接 ID、局部选择高清图、小地图脏区和可观测完整回退。
- 已运行失败基线，确认旧实现仍执行全量 DOM、连接、媒体或小地图工作。

## 当前修改模块

模块 4 失败契约完成，尚未修改运行时代码。涉及文件：专项测试、两份最高优先级方案和本快照。

## 具体代码前后对比

```text
改动前：没有职责单一的契约阻止 mutation 调用全量 DOM/连接/媒体/小地图路径。
改动后：10 项契约明确要求差异索引 helper、结构连接字段、局部选择/小地图和显式完整渲染回退。

当前旧实现：经典 addNode -> render；经典 mutation -> rebuildCanvasDomIndexes；智能 mutation -> 完整 SVG/索引/媒体扫描。
目标实现：addNode/智能创建 -> mutation；mutation -> 目标节点 DOM/端口/矩形索引 patch；失败 -> console.warn + 完整 render。
```

## 验证命令与结果

- `python -m unittest tests.test_canvas_incremental_render_performance -v`
- 结果：运行 10 项，8 项失败、2 项错误。
- 两项错误来自目标函数/分段边界尚不存在；其余失败分别对应尚未实现的增量能力，基线有效。

## 待办清单

- 实现经典与智能节点 DOM、端口和矩形索引按差异维护。
- 实现连接结构 patch 和复杂智能拓扑完整回退。
- 将选择高清图、媒体状态和小地图限制到受影响集合。
- 完成 500 节点/1000 连线功能一致性验证和模块回归。

## 下一步

先实现节点、端口和矩形索引子模块，使两套 mutation 不再调用全量 DOM 索引 rebuild；完成后立即更新方案并新增下一号快照。

## 避坑记录

- 静态失败契约的断言消息可能包含整份超大 JavaScript 源码；判断基线应以测试名称和最终失败/错误计数为准，后续可将断言改为更紧凑的信息以避免日志膨胀。
