# 进度快照 732：智能节点索引差异维护完成

日期：2026-08-26

## 已完成内容

- 新增智能单节点 DOM/端口/矩形索引写入与删除 helper。
- 将智能 mutation 从完整 `render()` 中拆分，创建、替换和删除只操作目标节点。
- 抽取智能节点模板 helper，保持原节点类型、文案、工具栏、端口和布局字段。
- 指定节点几何失效不再使全局端口缓存过期；异常可观测并完整回退。

## 当前修改模块

模块 4 的双画布节点、端口和矩形索引子模块完成。修改 `static/js/smart-canvas.js`、两份方案和本快照。

## 具体代码前后对比

```text
改动前：智能 mutation 仍进入完整 render 主体，扫描全部旧节点、采集全部媒体并 rebuildSmartCanvasDomIndexes。
改动后：render 检测 mutation -> renderSmartMutation -> 只创建/替换/删除目标 DOM -> indexSmartNodeDom。

改动前：invalidateSmartGeometry([id]) 递增全局 epoch，未受影响端口缓存也过期。
改动后：指定 ID 只删除目标矩形/端口几何；全量失效才递增 epoch。

异常前：mutation 依赖完整重建掩盖索引不一致。
异常后：模型或 DOM 缺失会 console.warn 并完整 render 回退。
```

## 验证命令与结果

- `node --check static/js/smart-canvas.js`：通过。
- 节点索引、智能 mutation 分派、回退专项契约：3 项通过。
- 智能画布与交互相关回归：41 项通过。
- `git diff --check`：通过，仅有工作区行尾提示。

## 待办清单

- 给双画布 mutation 增加创建/删除/受影响连接 ID 集合。
- 实现经典连接结构 patch。
- 为智能连接分配稳定 ID，实现简单拓扑 SVG patch；复杂拓扑警告并完整回退。
- 收口选择高清图、媒体状态和小地图，并完成 500/1000 验证。

## 下一步

先扩展 mutation 连接差异结构和调用点，再实现 `patchClassicMutationConnections()` / `patchSmartMutationConnections()`；不得用隐藏的完整连接层刷新冒充增量 patch。

## 避坑记录

- 单节点模板抽取必须保持特殊节点端口、分组层级和媒体工具栏完全一致；仅创建一个简化 DOM 会导致事件绑定与连接端口语义回归。
