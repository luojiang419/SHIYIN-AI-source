# 进度快照 735：经典局部选择媒体与小地图完成

日期：2026-08-26

## 已完成内容

- 经典选择高清图同步支持受影响节点集合。
- mutation 只刷新目标节点图标与预览 fallback。
- mutation 小地图只提交节点脏区。
- 修复专项测试的错误源码截取终点。

## 当前修改模块

模块 4 的经典选择、媒体与小地图热路径完成。修改 `static/js/canvas.js`、专项测试、两份方案、避坑指南和本快照。

## 具体代码前后对比

```text
改动前：syncCanvasSelectedImageResolution -> root.querySelectorAll(全画布图片)。
改动后：affectedNodeIds -> canvasNodeDomIndex.get(id) -> 目标节点图片。

改动前：mutation -> scheduleClassicIdleIconRefresh(nodesEl) + bindFallbacks(nodesEl) + scheduleMinimapRender。
改动后：hydrateClassicMutationNodeRoots(patched roots) + scheduleMinimapNodeUpdate(affected ids)。

测试前：body() 以不存在的函数为终点，直接 ValueError。
测试后：使用实际后继 applyLanguage，契约判断真实实现。
```

## 验证命令与结果

- 经典 mutation 无全量路径、局部高清图：2 项专项契约通过。
- `node --check static/js/canvas.js`：通过。
- `git diff --check`：通过，仅有工作区行尾提示。

## 待办清单

- 实现并接入 `commitSmartNodeCreate()`。
- 运行模块 4 的 10 项完整专项契约。
- 执行 500 节点/1000 连线功能一致性与真实 Chromium 验证。
- 完成模块版本化、backup、完整回归、清理、提交和推送。

## 下一步

统一智能菜单节点构造函数：创建模型后通过 commit helper 更新 `smartNodeIndex`、选择、mutation 和保存，不再直接完整 render。

## 避坑记录

- 静态源码契约的起止标记必须先用 `rg` 校验真实存在；否则 `ValueError` 会掩盖实现状态。
