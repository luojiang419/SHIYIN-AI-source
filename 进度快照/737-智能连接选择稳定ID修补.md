# 进度快照 737：智能连接选择稳定 ID 修补

日期：2026-08-26

## 已完成内容

- 修复增量新建普通连接在选择切换时可能误读连接索引 `0` 的问题。
- 普通连接选择反馈优先按稳定 `data-connection-id` 查询模型。
- 无稳定 ID 的分组合并线继续按 `data-conn-index` 兼容，并过滤空索引。
- 新增专项契约，模块 4 契约共 11 项全部通过。

## 当前修改模块

模块 4 的智能普通连接选择反馈语义已修补。修改 `static/js/smart-canvas.js`、`tests/test_canvas_incremental_render_performance.py` 和两份主方案。

## 具体代码前后对比

```text
改动前：String(hit.dataset.connIndex || '').split(',') -> Number('') -> 0，增量连接可能误读第一条连接。
改动后：data-connection-id -> smartConnectionModelIndex.get(id)；仅合并线回退非空 connIndex。
```

## 验证命令与结果

- `python -m unittest tests.test_canvas_incremental_render_performance -v`：11 项通过。
- `node --check static/js/smart-canvas.js`：通过。
- `git diff --check`：通过，仅有工作区行尾提示。

## 待办清单

- 统一 `smartNodeHtml()` 与完整渲染模板来源。
- 继续审查分组 DOM 层级和创建、粘贴、删除后的模型索引一致性。
- 运行全部画布回归和真实 Chromium 500 节点/1000 连线验证。
- 模块验收后完成版本化、backup、完整回归、清理、提交和推送。

## 下一步

让智能完整渲染和增量渲染复用同一节点模板 helper，避免两份模板后续漂移；随后验证分组插入顺序。

## 已知边界与恢复说明

- 分组合并线没有单一稳定连接 ID，仍按连接索引集合处理并在复杂拓扑变化时完整 SVG 回退。
