# 进度快照 738：智能节点模板单一来源完成

日期：2026-08-26

## 已完成内容

- 完整渲染和增量渲染统一复用 `smartNodeHtml()`。
- 删除完整 `render()` 内重复的智能节点模板实现。
- 保留完整渲染的分组优先排序和增量分组底层插入顺序。
- 新增模板单一来源契约，模块 4 专项 11 项全部通过。

## 当前修改模块

模块 4 的智能节点模板一致性已完成。修改 `static/js/smart-canvas.js`、专项测试和两份主方案。

## 具体代码前后对比

```text
改动前：renderSmartMutation -> smartNodeHtml；完整 render -> 独立近百行同构模板。
改动后：两条渲染路径均使用 smartNodeHtml(node)，分组排序仅由调用层负责。
```

## 验证命令与结果

- `python -m unittest tests.test_canvas_incremental_render_performance -v`：11 项通过。
- `node --check static/js/smart-canvas.js`：通过。
- `git diff --check`：通过，仅有工作区行尾提示。

## 待办清单

- 完成双画布创建、粘贴、删除后的节点/连接索引一致性审查。
- 运行全部画布回归。
- 完成真实 Chromium 500 节点/1000 连线功能一致性验证。
- 模块验收后版本化、backup、完整回归、清理、提交并推送。

## 下一步

审查结构变化路径是否在 mutation 执行前后正确维护模型索引和邻接索引，补必要契约后运行全部画布测试。

## 已知边界与恢复说明

- 智能复杂连接拓扑继续完整 SVG 回退；模板统一不改变该边界。
