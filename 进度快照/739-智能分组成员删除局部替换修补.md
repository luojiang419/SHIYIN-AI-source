# 进度快照 739：智能分组成员删除局部替换修补

日期：2026-08-26

## 已完成内容

- 修复删除智能分组成员后分组 DOM 可能保留旧计数和工具栏状态的问题。
- 单节点删除和批量删除都会收集受影响且仍存活的分组。
- 删除 mutation 同时提交成员 `removeIds` 与分组 `replaceIds`。
- 新增专项契约，模块 4 契约共 12 项全部通过。

## 当前修改模块

模块 4 的智能删除与分组局部替换一致性已完成。修改 `static/js/smart-canvas.js`、专项测试和两份主方案。

## 具体代码前后对比

```text
改动前：删除成员 -> 清理 group.items -> mutation(removeIds)，分组 DOM 不替换。
改动后：删除前收集 affectedGroupIds -> mutation(removeIds + replaceIds)，分组 DOM 与模型同步。
```

## 验证命令与结果

- `python -m unittest tests.test_canvas_incremental_render_performance -v`：12 项通过。
- `node --check static/js/smart-canvas.js`：通过。
- `git diff --check`：通过，仅有工作区行尾提示。

## 待办清单

- 收束双画布创建、粘贴、删除索引一致性审查。
- 运行全部 `test_canvas*.py` 回归。
- 完成真实 Chromium 500 节点/1000 连线功能一致性验证。
- 模块验收后版本化、backup、完整回归、清理、提交并推送。

## 下一步

运行全部画布回归；如无结构回归，搭建隔离 QA 环境执行经典和智能大画布验证。

## 已知边界与恢复说明

- 受影响分组会触发智能复杂连接完整 SVG 回退，这是保证分组合并线正确性的既定边界。
