# 进度快照 740：模块 4 全部画布静态回归通过

日期：2026-08-26

## 已完成内容

- 定位全部画布回归的 3 失败、1 错误，确认均为旧静态契约观察点失效。
- 将契约迁移到新的 helper、函数签名和稳定连接 ID 数据结构。
- 更新静态契约避坑指南。
- 全部 `test_canvas*.py` 共 182 项通过。

## 当前修改模块

模块 4 的静态回归兼容已完成。修改三份既有画布测试、避坑指南和两份主方案；没有新增运行时代码回退或全局扫描。

## 具体代码前后对比

```text
旧契约：在 renderSmartConnectionIncremental 内找全部几何细节。
新契约：外层检查脏 ID 分派，setSmartConnectionPatchGeometry 检查几何缓存。

旧契约：写死 refreshSelectionVisuals()、createNode 内 defer 条件、无 ID 连接字面量。
新契约：接受可选参数签名，检查 commitSmartNodeCreate，要求稳定 id:uid('c')。
```

## 验证命令与结果

- 四个定点失败测试：4 项通过。
- `python -m unittest discover -s tests -p "test_canvas*.py"`：182 项通过。
- 双画布 `node --check`：通过。
- `git diff --check`：通过，仅有工作区行尾提示。

## 待办清单

- 搭建隔离 QA 环境。
- 在真实 Chromium 验证经典和智能 500 节点/1000 连线。
- 检查创建、粘贴、删除、撤销/重做、普通连接端点及复杂拓扑回退日志。
- 模块验收后版本化、backup、完整回归、清理、提交并推送。

## 下一步

使用现有 CanvasPerformance fixture 在真实 Chromium 创建大画布场景，记录功能一致性和控制台结果，不采集模块 5 的统一性能分位数。

## 已知边界与恢复说明

- FastAPI `on_event` DeprecationWarning 为既存警告，与模块 4 无关。
- 静态测试迁移不能替代真实 Chromium 验证。
