# 进度快照 743：智能分组菜单创建历史替换修补

日期：2026-08-26

## 已完成内容

- 修复分组内创建成员时，历史事务遗漏既有分组 `items` 字段变化的问题。
- 只克隆目标分组的 before/after，通过单个 `replacedNodes` 记录变更，没有深拷贝整张画布。
- 分组定位改用 `smartNodeIndex`，入组辅助函数返回真实变更状态，避免空替换历史。

## 当前修改模块

模块 4 的智能分组连续历史一致性修补。修改 `static/js/smart-canvas.js`、事务契约测试和两份主方案。

## 具体代码前后对比

```text
改动前：事务只从 beforeNodes 引用集合识别新建/删除节点；分组原地写入 items 后无法被识别。
改动后：创建前克隆单个目标分组，入组成功后克隆同一分组，提交 {id, before, after} 替换记录。
```

## 验证命令与结果

- 新增定点契约先运行失败，证明旧实现未记录分组替换。
- `python -m unittest tests.test_canvas_undo_transaction_performance.CanvasUndoTransactionContractTests.test_smart_group_menu_creation_records_only_the_changed_group -v`：通过。
- `node --check static/js/smart-canvas.js`：通过。
- `git diff --check`：通过，仅有工作区行尾提示。

## 待办清单

- 运行模块 4 专项、事务专项和全部 `test_canvas*.py` 回归。
- reload 智能 QA 页面并重装 500/1000 fixture。
- 复验创建成员、删除、撤销删除、撤销创建后的 `group.items` 与 owner 映射。
- 模块验收后版本化、backup、完整回归、清理、提交并推送。

## 下一步

先运行全部画布回归，再复跑真实分组连续历史序列；不得进入模块 5。

## 已知边界与恢复说明

- 分组及成员结构变化继续显式触发完整 SVG 回退，这是复杂拓扑保护，不是增量失败。
- 当前修补只复制单个目标分组；不得改回 `cloneSmartHistoryValue(nodes)` 或完整 `snapshotForUndo()`。
