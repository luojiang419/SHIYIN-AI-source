# 进度快照 744：事务字段替换撤销方向修补

日期：2026-08-26

## 已完成内容

- 修复经典和智能结构事务在撤销字段替换时错误应用 `change.after` 的问题。
- 保持既有历史顺序：撤销应用条目 `before`，重做应用反向条目的 `before`（即原 `after`）。
- 新增双画布契约锁定字段替换方向。

## 当前修改模块

模块 4 的连续历史一致性修补。修改 `static/js/canvas.js`、`static/js/smart-canvas.js`、事务契约测试和两份主方案。

## 具体代码前后对比

```text
改动前：apply*HistoryTransaction -> Object.assign(node, change.after)，撤销仍保留操作后的字段。
改动后：apply*HistoryTransaction -> Object.assign(node, change.before)，重做继续通过反向条目恢复 after。
```

## 验证命令与结果

- 新增双画布定点契约先运行失败，证明旧实现方向错误。
- `python -m unittest tests.test_canvas_undo_transaction_performance.CanvasUndoTransactionContractTests.test_transaction_replacements_apply_before_state_for_undo -v`：通过。
- `node --check static/js/canvas.js`、`node --check static/js/smart-canvas.js`：通过。
- `git diff --check`：通过，仅有工作区行尾提示。

## 待办清单

- 强制忽略缓存 reload 智能 QA 页面并重装 500/1000 fixture。
- 复跑创建分组、创建成员、删除、撤销删除、撤销创建、撤销分组创建。
- 运行全部画布回归并完成模块 4 版本化、backup、完整回归、清理、提交推送。

## 下一步

先完成真实 Chromium 连续历史复验；不得进入模块 5。

## 已知边界与恢复说明

- `apply*HistoryTransaction()` 的动作语义是撤销条目，不是重放正向条目；重做依赖 `invert*HistoryTransaction()`。
