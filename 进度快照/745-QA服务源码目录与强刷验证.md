# 进度快照 745：QA 服务源码目录与强刷验证

日期：2026-08-26

## 已完成内容

- 识别并修复隔离 QA 服务从非目标工作目录启动、浏览器继续加载旧脚本的问题。
- 以当前 worktree 为显式 `WorkingDirectory` 重启 3012 服务，端口 owner 由 PID 36020 切换为 PID 21412。
- 使用 `Page.reload({ignoreCache:true})` 强制刷新，并以 `createNodeFromMenu.toString()` 新签名确认运行时代码已更新。
- 更新 `避坑指南/2026082404-浏览器验收缓存与小地图观测.md`。

## 当前修改模块

模块 4 真实 Chromium 验收环境恢复；不涉及产品功能逻辑。

## 具体代码前后对比

```text
问题状态：普通 reload 后运行时函数仍无 groupBefore/replacedNodes，实际测试的是旧代码。
恢复状态：显式工作目录重启服务 + ignoreCache 强刷后，两项运行时签名均存在。
```

## 验证命令与结果

- 3012 监听 owner：PID 21412。
- 运行时 `createNodeFromMenu`：`hasGroupBefore=true`、`hasReplacedNodes=true`。
- 重新安装 fixture：500 节点、1000 连线、0 分组、0 owner、0 undo。

## 待办清单

- 强刷字段方向修补后的脚本。
- 复跑完整智能分组历史序列。
- 完成模块 4 回归、版本化、backup、清理、提交推送。

## 下一步

重新加载最新字段方向修补，安装干净 fixture 后继续真实 Chromium 连续历史验证。

## 已知边界与恢复说明

- 真实验收必须核对运行时函数签名；只看磁盘 diff 或页面标题不能证明待测代码已加载。
