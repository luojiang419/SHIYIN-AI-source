# 进度快照 746：智能分组连续历史真实验收通过

日期：2026-08-26

## 已完成内容

- 在真实 Chromium、智能 500 节点/1000 连线 fixture 下完成分组连续历史复验。
- 验证创建、删除、撤销、重做期间成员模型、`group.items` 与 owner 索引始终一致。
- 验证成员创建历史只记录一个新节点和一个目标分组替换，没有整画布快照。

## 当前修改模块

模块 4 真实大画布功能一致性收束；产品代码涉及双画布字段替换方向和智能分组菜单创建历史。

## 具体代码前后对比

```text
修补前：撤销成员创建后 nodes=501，但 group.items 仍含已删除成员，owner 也被完整渲染恢复。
修补后：撤销成员创建后 nodes=501、group.items=[]、成员不存在、owner=null；重做可完整恢复。
```

## 验证命令与结果

- 节点序列：`500 -> 501 -> 502 -> 501 -> 502 -> 501 -> 502 -> 501 -> 500`。
- 连线：全程 `1000`。
- 成员创建条目：`createdNodes=[member]`，`replacedNodes=[group beforeItems=[] afterItems=[member]]`。
- 控制台：0 error；存在既有 `badge-hd` Lucide 警告和预期 `group-or-history-node` 完整 SVG 回退警告。

## 待办清单

- 运行最新代码的模块 4、事务专项和全部画布回归。
- 递增版本至 `1.0.314`，同步版本源和双画布缓存修订号。
- 新增模块 4 backup，运行完整回归，清理 QA 环境，提交并推送。

## 下一步

运行最新代码的全部画布回归；通过后进入模块 4 版本化收束，不进入模块 5。

## 已知边界与恢复说明

- 分组操作触发完整 SVG 回退是预期复杂拓扑保护。
- 统一 p50/p95/p99 和保存序列化验收仍属于模块 5。
