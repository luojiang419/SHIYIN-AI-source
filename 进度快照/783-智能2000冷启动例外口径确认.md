# 进度快照 783：智能2000冷启动例外口径确认

日期：2026-08-26

## 已完成内容

- 已完整读取最高优先级方案文档与最新快照 782。
- 用户选择第 1 项，确认仅智能画布 2000 节点档的首次冷启动结构操作允许一次 `<=350 ms` 初始化 Long Task。
- 已同步更新外部最高优先级方案与仓库镜像方案，明确该冷启动样本单独计时，不并入稳态 p95/p99。
- 稳态直接操作仍保持 p95 `<=150 ms`、p99 `<=200 ms`；所有后续动作不得出现 `>100 ms` 主任务，不得出现二次冻结。
- 完整 render、全量连线重绘、结构、图标、端口、撤销/重做、保存/重载和真实媒体正确性均未放宽。

## 当前修改模块与文件

- 模块 5：真实媒体大画布性能验收。
- `开发文档/无限画布菜单与剪贴板性能优化方案-v2.md`
- 外部最高优先级方案：`E:\APP\SHIYIN-AI\开发文档\无限画布菜单与剪贴板性能优化方案-v2.md`
- 前置实现仍保留：`static/js/smart-canvas.js` 的 Composer 600ms + requestIdleCallback 防抖和批量结构事务；相关专项契约已通过。

## 口径变更前后对比

```text
变更前：1000-2000 节点页面/大场景静默后首次结构操作允许一次性 100-200ms 初始化 Long Task。
变更后：1000-2000 节点总体仍按 p95<=150ms、p99<=200ms、后续不得出现>100ms主任务验收；仅智能 2000 档首次冷启动结构操作的单次初始化上限调整为<=350ms，并独立计入冷启动样本。
```

## 验证与风险

- 本快照记录的是用户确认的验收契约，尚未宣称智能 2000 最终通过。
- 已知历史冷启动峰值约 230-335ms，落在新的 `<=350ms` 单次例外范围内；仍必须完成稳态、结构、历史、保存、端口和静默窗口的真实闭环。
- 测试继续使用当前唯一智能画布标签，禁止打开同一画布第二标签，避免远程同步互相覆盖。

## 待办清单

- 在干净静默窗口单独采集智能 2000 首个结构操作冷启动 Long Task。
- 以 120ms 自然操作间隔采集稳态粘贴 20 次和批量处理 20 次，计算 p95/p99 并确认无 `>100ms` 主任务。
- 验证每次结构增量、batch-generator 节点、图标、输入/输出端口、完整 render/renderLinks 为 0。
- 验证 toolbar-batch 撤销/重做、保存/重载、2000/2000 图片和 6 个真实 URL、二次静默窗口。

## 下一步

在当前唯一智能 2000 标签完成上述正式闭环；若通过，新增验收通过快照并进入八档收束，再统一递增版本、创建 backup、完整回归、提交和推送。

