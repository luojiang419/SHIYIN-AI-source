# 794 - 无限画布线段断开卡顿修复

生成时间：2026-08-26 19:11:54

## 已完成内容

经典与智能画布断线改用轻量结构撤销事务；经典普通断线按 connection id 增量移除 DOM，智能普通断线按稳定 id 增量移除，合并分组线多连线与历史线按需完整回退；版本递增至 1.0.319并补充回归契约。

## 当前模块

无限画布连线断开与增量渲染

## 当前 Git 状态

```text
M VERSION
 M main.py
 M package-lock.json
 M package.json
 M release-notes/current.md
 M src-tauri/Cargo.lock
 M src-tauri/Cargo.toml
 M src-tauri/tauri.conf.json
 M static/js/canvas.js
 M static/js/smart-canvas.js
 M static/update-notes.json
 M tests/test_canvas_incremental_connections.py
```

## 代码前后对比 / Diff

```diff
diff --git a/VERSION b/VERSION
index 42ac116..968bd98 100644
--- a/VERSION
+++ b/VERSION
@@ -1 +1 @@
-1.0.318
+1.0.319
diff --git a/main.py b/main.py
index c0be9a7..b9c4c3b 100644
--- a/main.py
+++ b/main.py
@@ -408,7 +408,7 @@ STARTUP_MAINTENANCE_STATE = {
 }
 ACTIVE_CANVAS_BY_ACCOUNT: dict[str, str] = {}
 ACTIVE_CANVAS_ID = ""
-APP_VERSION = "1.0.318"
+APP_VERSION = "1.0.319"
 GITHUB_REPO_URL = "https://github.com/luojiang419/SHIYIN-AI-source"
 GITHUB_VERSION_URL = "https://raw.githubusercontent.com/luojiang419/SHIYIN-AI-source/main/VERSION"
 GITHUB_TREE_URL = "https://api.github.com/repos/luojiang419/SHIYIN-AI-source/git/trees/main?recursive=1"
diff --git a/package-lock.json b/package-lock.json
index cd6eb16..b3e7971 100644
--- a/package-lock.json
+++ b/package-lock.json
@@ -1,12 +1,12 @@
 {
   "name": "shiyin-ai-windows-desktop",
-  "version": "1.0.318",
+  "version": "1.0.319",
   "lockfileVersion": 3,
   "requires": true,
   "packages": {
     "": {
       "name": "shiyin-ai-windows-desktop",
-      "version": "1.0.318",
+      "version": "1.0.319",
       "devDependencies": {
         "@tauri-apps/cli": "2.11.4",
         "tailwindcss": "3.4.17"
diff --git a/package.json b/package.json
index 10f79d5..5040252 100644
--- a/package.json
+++ b/package.json
@@ -1,7 +1,7 @@
 {
   "name": "shiyin-ai-windows-desktop",
   "private": true,
-  "version": "1.0.318",
+  "version": "1.0.319",
   "scripts": {
     "css:build": "node ./node_modules/tailwindcss/lib/cli.js -c tailwind.config.cjs -i ./static/css/tailwind-input.css -o ./static/vendor/css/tailwind.generated.css --minify",
     "desktop:dev": "npm run css:build && tauri dev",
diff --git a/release-notes/current.md b/release-notes/current.md
index 7d4dcd2..9993db0 100644
--- a/release-notes/current.md
+++ b/release-notes/current.md
@@ -1,4 +1,5 @@
-# SHIYIN AI v1.0.318
+# SHIYIN AI v1.0.319
+- 修复无限画布点击线段断开按钮后卡顿数秒：经典与智能画布的普通断线改用轻量结构撤销事务和增量连接 DOM 更新，避免深拷贝整张画布；分组折叠线、历史线仍按需安全回退。
 - 修复无限画布框选多个节点后，按住 Alt 拖拽只复制当前节点的问题；经典与智能画布现在都会复制并整体拖动全部选中节点，同时保留副本之间的内部连线与多选状态。
 - 模块 6 Windows 安装包已通过 Inno Setup 和安装器产物校验；安装包 `SHIYIN-AI-Setup-1.0.317.exe`，SHA-256：`ee6e59a251648e489767f92a0c42c742a5bc05002a286106cc3b46ad7f1ff897`。桌面运行时冒烟因已有 SHIYIN AI 实例占用而跳过，DWPose 真实人物下载冒烟因未提供输入素材而跳过。
 - 模块 5 完成经典与智能无限画布 500/1000/1500/2000 八档真实媒体生产环境性能验收；1000 节点以上按连续操作与主观丝滑的 A 档执行，直接操作 p95 `<=150 ms`、p99 `<=200 ms`，不得出现 `>100 ms` 主任务或二次冻结。
diff --git a/src-tauri/Cargo.lock b/src-tauri/Cargo.lock
index 22de9e5..ba32114 100644
--- a/src-tauri/Cargo.lock
+++ b/src-tauri/Cargo.lock
@@ -449,7 +449,7 @@ dependencies = [
 
 [[package]]
 name = "canvas-desktop"
-version = "1.0.318"
+version = "1.0.319"
 dependencies = [
  "arboard",
  "open",
diff --git a/src-tauri/Cargo.toml b/src-tauri/Cargo.toml
index 69e9d3e..0dceab0 100644
--- a/src-tauri/Cargo.toml
+++ b/src-tauri/Cargo.toml
@@ -1,6 +1,6 @@
 [package]
 name = "canvas-desktop"
-version = "1.0.318"
+version = "1.0.319"
 description = "SHIYIN AI Windows portable desktop host"
 authors = ["SHIYIN AI contributors"]
 edition = "2021"
diff --git a/src-tauri/tauri.conf.json b/src-tauri/tauri.conf.json
index c7875bc..eac2f1d 100644
--- a/src-tauri/tauri.conf.json
+++ b/src-tauri/tauri.conf.json
@@ -1,7 +1,7 @@
 {
   "$schema": "../node_modules/@tauri-apps/cli/config.schema.json",
   "productName": "SHIYIN AI",
-  "version": "1.0.318",
+  "version": "1.0.319",
   "identifier": "com.hero8152.canvas.desktop",
   "build": {
     "frontendDist": "../desktop-placeholder"
diff --git a/static/js/canvas.js b/static/js/canvas.js
index 67e4548..6c5ed9a 100644
--- a/static/js/canvas.js
+++ b/static/js/canvas.js
@@ -15847,11 +15847,15 @@ function deleteNodeFromButton(id, event){
 function deleteConnection(id, event){
     event?.preventDefault();
     event?.stopPropagation();
-    pushUndo();
+    const removed = connections.find(connection => connection.id === id);
+    if(!removed) return;
+    // 断线只影响一条连接，使用结构事务记录撤销信息，避免 pushUndo 深拷贝整张画布及媒体数据。
+    const historyTx = beginClassicHistoryTransaction('disconnect');
     connections = connections.filter(c => c.id !== id);
-    markClassicConnectionStructureDirty();
+    commitClassicHistoryTransaction(historyTx);
     if(hoveredConnectionId === id) hoveredConnectionId = '';
-    syncGeneratorInputs();
+    syncGeneratorInputs(new Set([removed.to]));
+    queueClassicRenderMutation({removedConnectionIds:[id]});
     render();
     scheduleSave();
 }
diff --git a/static/js/smart-canvas.js b/static/js/smart-canvas.js
index 45fe84b..720917a 100644
--- a/static/js/smart-canvas.js
+++ b/static/js/smart-canvas.js
@@ -9814,7 +9814,8 @@ function queueSmartRenderMutation(mutation={}){
         replaceIds:merge('replaceIds'),
         createdConnectionIds:merge('createdConnectionIds'),
         removedConnectionIds:merge('removedConnectionIds'),
-        affectedConnectionIds:merge('affectedConnectionIds')
+        affectedConnectionIds:merge('affectedConnectionIds'),
+        requiresFullConnectionRender:Boolean(smartRenderMutation?.requiresFullConnectionRender || mutation.requiresFullConnectionRender)
     };
     clearPendingSmartConnectionMutation();
 }
@@ -9829,10 +9830,10 @@ function renderSmartMutation(mutation){
     const removeIds = mutation?.removeIds || new Set();
     const replaceIds = mutation?.replaceIds || new Set();
     const affectedNodeIds = new Set([...createdIds, ...removeIds, ...replaceIds]);
-    mutation.requiresFullConnectionRender = [...affectedNodeIds].some(id => {
+    mutation.requiresFullConnectionRender = Boolean(mutation.requiresFullConnectionRender || [...affectedNodeIds].some(id => {
         const node = smartNodeIndex.get(id);
         return isSmartGroupNode(node) || isHistoryGroupNode(node) || smartGroupOwnerIndex.has(id);
-    });
+    }));
     const missingModels = new Set([...createdIds, ...replaceIds].filter(id => !smartNodeIndex.has(id)));
     for(let index = nodes.length - 1; index >= 0 && missingModels.size; index--){
         const node = nodes[index];
@@ -12322,7 +12323,11 @@ function disconnectConnections(spec){
     const set = new Set(indices);
     const removed = canvas.connections.filter((_, i) => set.has(i));
     if(!removed.length) return;
-    pushUndo();
+    // 普通断线使用轻量结构事务，避免 pushUndo 深拷贝整张画布及媒体数据。
+    // 历史线断开会降级历史分组节点，仍使用快照确保该模型变更可完整撤销。
+    const needsSnapshotUndo = removed.some(conn => (conn.kind || 'flow') === 'history');
+    const historyTx = needsSnapshotUndo ? null : beginSmartHistoryTransaction('disconnect');
+    if(needsSnapshotUndo) pushUndo();
     canvas.connections = canvas.connections.filter((_, i) => !set.has(i));
     removed.forEach(conn => {
         const toNode = nodes.find(n => n.id === conn.to);
@@ -12335,6 +12340,12 @@ function disconnectConnections(spec){
             demoteHistoryGroupNode(group);
         }
     });
+    if(historyTx) commitSmartHistoryTransaction(historyTx);
+    queueSmartRenderMutation({
+        removedConnectionIds:removed.map(connection => connection.id),
+        // 合并到分组的可视线没有单一 connection id，必须重建连接 SVG 才能移除整条合并线。
+        requiresFullConnectionRender:removed.length > 1
+    });
     render();
     scheduleSave();
 }
diff --git a/static/update-notes.json b/static/update-notes.json
index af94aec..75bc04a 100644
--- a/static/update-notes.json
+++ b/static/update-notes.json
@@ -1,7 +1,11 @@
 {
-  "version": "1.0.318",
+  "version": "1.0.319",
   "updated_at": "2026-08-26T00:00:00+08:00",
   "items": [
+    {
+      "type": "fix",
+      "text": "修复无限画布点击线段断开按钮后卡顿数秒；普通断线改用轻量结构撤销事务和增量连接 DOM 更新，分组折叠线与历史线按需安全回退"
+    },
     {
       "type": "fix",
       "text": "修复无限画布多选节点后按住 Alt 拖拽只复制单个节点；经典与智能画布现在会复制并整体拖动全部选中节点，同时保留内部连线和多选状态"
diff --git a/tests/test_canvas_incremental_connections.py b/tests/test_canvas_incremental_connections.py
index 45c8eb7..b402e46 100644
--- a/tests/test_canvas_incremental_connections.py
+++ b/tests/test_canvas_incremental_connections.py
@@ -55,6 +55,19 @@ class IncrementalConnectionTests(unittest.TestCase):
         self.assertIn("svg.appendChild(path)", temp)
         self.assertIn("clearPortDragVisual", SMART)
 
+    def test_disconnect_uses_lightweight_history_and_incremental_render(self):
+        classic = body(CLASSIC, "function deleteConnection", "function outputDownloadName")
+        self.assertIn("beginClassicHistoryTransaction('disconnect')", classic)
+        self.assertIn("commitClassicHistoryTransaction(historyTx)", classic)
+        self.assertIn("queueClassicRenderMutation({removedConnectionIds:[id]})", classic)
+        self.assertNotIn("pushUndo();", classic)
+
+        smart = body(SMART, "function disconnectConnections(spec)", "function connectionMidpoint")
+        self.assertIn("beginSmartHistoryTransaction('disconnect')", smart)
+        self.assertIn("commitSmartHistoryTransaction(historyTx)", smart)
+        self.assertIn("removedConnectionIds:removed.map(connection => connection.id)", smart)
+        self.assertIn("requiresFullConnectionRender:removed.length > 1", smart)
+
 
 if __name__ == "__main__":
     unittest.main()
```

## 验证结果

- `python -m pytest tests/test_canvas_incremental_connections.py tests/test_canvas_incremental_render_performance.py tests/test_canvas_undo_transaction_performance.py -q`：36 passed。
- `python -m pytest tests/test_canvas_*.py -q`（PowerShell 先展开文件列表）：310 passed。
- `node --check static/js/canvas.js`、`node --check static/js/smart-canvas.js`：通过。
- `powershell -ExecutionPolicy Bypass -File tools/assert-version-sync.ps1`：通过，9 个版本源均为 1.0.319。
- 全量 `python -m pytest -q`：860 passed，3 failed；失败为既有电商 free-creation iframe 入口契约和统一主题页面缓存版本契约，与本次改动无关。

## 待办清单

检查工作区最终 diff，提交并推送 GitHub；如需发布安装包，再执行安装器构建。

## 下一步

读取本快照后完成 git diff 检查、版本同步检查、提交并推送。

## 避坑记录

PowerShell 不会按 Unix 方式展开 `tests/test_canvas_*.py`，需先用 `Get-ChildItem` 生成文件列表。全量回归中的 3 个失败属于仓库既有入口/缓存契约漂移，已记录在 `避坑指南/20260826-断线卡顿回归说明.md`，未修改无关模块。
