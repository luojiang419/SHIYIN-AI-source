# 796 - 编译 1.0.319 安装包

生成时间：2026-08-26 19:31:00

## 已完成内容

基于当前 `1.0.319` 无限画布断线卡顿修复版本完成 Windows 安装包编译。后端 PyInstaller sidecar 与 Tauri release 桌面宿主均成功编译，随后使用 Inno Setup 6.7.1 生成安装包。

## 当前模块

Windows 安装包构建与产物校验

## 构建产物

- 安装包：`dist/installer/SHIYIN-AI-Setup-1.0.319.exe`
- 大小：71,647,697 bytes（68.33 MiB）
- SHA-256：`6a5a86100b312e6a8fbbf4d5fb2f34f304ed6e39ad9efd2a119781baf62ff7ed`
- `verify-installer-artifact.ps1`：通过，并生成同名 `.sha256` 校验文件。
- `assert-version-sync.ps1`：通过，9 个版本源均为 `1.0.319`。

## 构建过程说明

- 首次完整构建的后端与 Tauri 编译均成功。
- 首次运行时冒烟因当前已有 `SHIYIN-AI.exe` 实例（PID 45140）被脚本安全阻止；未终止用户进程，复用已生成产物并跳过运行时冒烟后完成安装器编译。
- 无限画布经典与智能画布真实页面断线测试已在前一快照完成：两种画布各连续断开 20 条临时连线，均最终剩余 0 条，未出现秒级冻结。

## 代码前后对比 / Diff

```diff
(本次仅生成构建产物与构建记录，无源代码变更)
```

## 验证结果

- Inno Setup 编译：通过（38.688 秒）。
- 安装包产物校验：通过。
- 版本同步校验：通过。
- 本次运行时冒烟：因已有应用实例占用而跳过；不影响编译产物生成。

## 待办清单

- 用户可关闭所有 SHIYIN AI 实例后重新执行 `tools/smoke-desktop.ps1` 做安装包运行时冒烟；如无 DWPose 输入素材，DWPose 真实人物下载测试仍保持跳过。

## 下一步

向用户交付安装包路径、大小和 SHA-256，并保留当前工作区为干净状态。

## 避坑记录

已有桌面实例会被 `smoke-desktop.ps1` 主动拒绝，避免与正在运行的应用抢占端口或文件。构建失败时优先保留实例不动，使用已成功生成的后端/桌面产物执行 `-SkipBackend -SkipDesktop -SkipRuntimeSmoke` 完成安装器封装。
