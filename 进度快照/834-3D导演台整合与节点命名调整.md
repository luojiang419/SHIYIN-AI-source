# 834-3D导演台整合与节点命名调整

## 已完成内容

- 拉取并整合上游 infinite-canvas 的 3D 导演台静态应用。
- 普通/智能无限画布新增 `director3d` 节点，支持内嵌导演台、项目状态回传、截图上传和输出节点创建。
- 原 `poseReference` 节点显示名称改为“导演台”。
- 原 `blenderDirector` 节点显示名称改为“外部导演台”。
- 前端资源路径改为 `/static/director/`，模型路径同步修正。
- 版本从 1.0.333 递增到 1.0.334。

## 当前修改模块与文件

- `static/js/director-3d.js`、`static/css/director-3d.css`
- `static/director/index.html`、`static/director/assets/*`、`static/director/models/*`
- `static/js/canvas.js`、`static/js/smart-canvas.js`、`static/js/canvas-special-nodes.js`
- `static/js/pose-reference-editor.js`、`static/js/shortcut-actions.js`
- `static/canvas.html`、`static/smart-canvas.html`

## 验证结果

- 4 个 JavaScript 文件通过 `node --check`。
- UE 人偶模型加载路径已由 `/director/models/` 改为 `/static/director/models/`。

## 待办清单

- 启动后端进行页面与静态资源 HTTP 烟测。
- 确认画布创建菜单、导演台 ready 消息、截图上传及输出节点连接。
- 完成后清理临时克隆目录并提交源码备份。

## 下一步

运行应用级静态检查与服务烟测；通过后清理 `external-infinite-canvas` 临时目录并记录结果。
