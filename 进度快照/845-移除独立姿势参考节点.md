# 进度快照 845：移除独立姿势参考节点

## 已完成内容

- 从普通无限画布和智能无限画布移除独立的“姿势参考/导演台”节点创建入口。
- 移除独立姿势编辑器脚本 `static/js/pose-reference-editor.js` 及样式 `static/css/pose-reference-editor.css`。
- 从两个画布页面移除编辑器资源、工具栏入口、创建菜单入口和快捷动作注册。
- 从 `canvas-special-nodes.js` 移除独立节点的标题、节点主体和绑定逻辑。
- 从普通/智能画布的节点创建、渲染、输出和连线类型列表移除独立节点类型。
- 保留“一键复刻”节点使用的 `pose-reference` 输入角色、动作参考图和骨架输出逻辑；保留电商工作台中的“动作参考”语义。
- 将原有姿势节点契约测试改为“节点与资源已移除、动作参考输入仍保留”的回归测试。

## 当前修改模块

独立姿势参考节点清理模块已完成，下一步处理工程页新建画布弹窗和整理画布按钮。

## 主要代码前后对比

### 页面入口

- 修改前：`canvas.html`、`smart-canvas.html` 加载姿势编辑器资源，并展示“导演台”创建按钮。
- 修改后：两个页面不再加载姿势编辑器资源，也不再展示独立“导演台”创建入口；“3D导演台”保持不变。

### 节点适配器

- 修改前：`CanvasSpecialNodes` 暴露 `poseReferenceBodyHtml` 与 `bindPoseReference`。
- 修改后：仅保留全景、动作提取、3D导演台、一键复刻、灯光重塑和角度调整等节点适配器。

### 连线与创建

- 修改前：普通画布和智能画布的菜单、快捷动作、节点类型数组包含 `poseReference` / `pose-reference`。
- 修改后：移除独立节点类型；一键复刻的 `pose-reference` 输入端口仍在，确保动作参考功能不受影响。

## 待办清单

- 改造新建画布弹窗：背景模糊变暗、尺寸放大、回车后直接进入画布。
- 工程页增加“整理画布”按钮并持久化网格布局。
- 修复批量导入图片/视频后的自动编组与网格排版，确保节点不重叠。
- 全量回归、版本递增、备份、缓存清理和 GitHub 推送。

## 验证结果

- 三个相关 JavaScript 文件语法检查通过。
- 姿势节点、特殊节点、快捷动作和增量渲染相关测试共 62 项通过。
