# 856 - 影视制作二级菜单悬停关闭修复

生成时间：2026-08-28 23:11:23

## 已完成内容

修复 document pointerover 捕获监听在影视菜单区域错误关闭子菜单的问题；影视主菜单与移到 body 的固定定位二级菜单之间移动时保持展开；新增回归断言；版本递增至 1.0.344，并同步发布说明。

## 当前模块

经典画布影视制作二级菜单交互

## 当前 Git 状态

```text
M VERSION
 M main.py
 M package-lock.json
 M package.json
 M release-notes/current.md
 M src-tauri/Cargo.lock
 M src-tauri/Cargo.toml
 M src-tauri/tauri.conf.json
 M static/js/canvas.js
 M static/update-notes.json
 M tests/test_canvas_menu_performance.py
```

## 代码前后对比 / Diff

```diff
diff --git a/VERSION b/VERSION
index 2450a4b..2ed9a2a 100644
--- a/VERSION
+++ b/VERSION
@@ -1 +1 @@
-1.0.343
+1.0.344
diff --git a/main.py b/main.py
index 2b774f3..24d83e8 100644
--- a/main.py
+++ b/main.py
@@ -416,7 +416,7 @@ STARTUP_MAINTENANCE_STATE = {
 }
 ACTIVE_CANVAS_BY_ACCOUNT: dict[str, str] = {}
 ACTIVE_CANVAS_ID = ""
-APP_VERSION = "1.0.343"
+APP_VERSION = "1.0.344"
 GITHUB_REPO_URL = "https://github.com/luojiang419/SHIYIN-AI-source"
 GITHUB_VERSION_URL = "https://raw.githubusercontent.com/luojiang419/SHIYIN-AI-source/main/VERSION"
 GITHUB_TREE_URL = "https://api.github.com/repos/luojiang419/SHIYIN-AI-source/git/trees/main?recursive=1"
diff --git a/package-lock.json b/package-lock.json
index f3711b0..5ffe665 100644
--- a/package-lock.json
+++ b/package-lock.json
@@ -1,12 +1,12 @@
 {
   "name": "shiyin-ai-windows-desktop",
-  "version": "1.0.343",
+  "version": "1.0.344",
   "lockfileVersion": 3,
   "requires": true,
   "packages": {
     "": {
       "name": "shiyin-ai-windows-desktop",
-      "version": "1.0.343",
+      "version": "1.0.344",
       "devDependencies": {
         "@tauri-apps/cli": "2.11.4",
         "tailwindcss": "3.4.17"
diff --git a/package.json b/package.json
index 9cf0be4..c405662 100644
--- a/package.json
+++ b/package.json
@@ -1,7 +1,7 @@
 {
   "name": "shiyin-ai-windows-desktop",
   "private": true,
-  "version": "1.0.343",
+  "version": "1.0.344",
   "scripts": {
     "css:build": "node ./node_modules/tailwindcss/lib/cli.js -c tailwind.config.cjs -i ./static/css/tailwind-input.css -o ./static/vendor/css/tailwind.generated.css --minify",
     "desktop:dev": "npm run css:build && tauri dev",
diff --git a/release-notes/current.md b/release-notes/current.md
index 55c8544..6e53de0 100644
--- a/release-notes/current.md
+++ b/release-notes/current.md
@@ -1,4 +1,5 @@
-# SHIYIN AI v1.0.343
+# SHIYIN AI v1.0.344
+- 修复无限画布影视制作二级菜单悬停交互：指针从主菜单移动到二级菜单时保持展开，不再被捕获的 `pointerover` 事件立即关闭。
 - 修正无限画布底部 Dock 布局：选择/移动按钮、常用功能节点和设置按钮统一为单行，Dock 按内容收缩并取消左右空白；空间不足时横向滚动，不再拆分成两行或上浮显示。
 - 清理测试与运行时弃用告警：FastAPI 生命周期迁移到 lifespan，Pydantic 模型序列化迁移到 `model_dump()`，补齐 `httpx2` 测试依赖并修复 Blender 测试 socket 资源释放；全量测试 886 项通过且无告警。
 - 自由创作页面彻底移除：主壳不再提供自由创作入口，旧 `workspace=free-creation` 地址会自动规范到电商工作区；同时补齐缓存版本标记，避免旧页面继续进入已废弃模式。
diff --git a/src-tauri/Cargo.lock b/src-tauri/Cargo.lock
index ab962e1..f4095f7 100644
--- a/src-tauri/Cargo.lock
+++ b/src-tauri/Cargo.lock
@@ -449,7 +449,7 @@ dependencies = [
 
 [[package]]
 name = "canvas-desktop"
-version = "1.0.343"
+version = "1.0.344"
 dependencies = [
  "arboard",
  "open",
diff --git a/src-tauri/Cargo.toml b/src-tauri/Cargo.toml
index 53844e6..6866e6a 100644
--- a/src-tauri/Cargo.toml
+++ b/src-tauri/Cargo.toml
@@ -1,6 +1,6 @@
 [package]
 name = "canvas-desktop"
-version = "1.0.343"
+version = "1.0.344"
 description = "SHIYIN AI Windows portable desktop host"
 authors = ["SHIYIN AI contributors"]
 edition = "2021"
diff --git a/src-tauri/tauri.conf.json b/src-tauri/tauri.conf.json
index 71d4511..811166a 100644
--- a/src-tauri/tauri.conf.json
+++ b/src-tauri/tauri.conf.json
@@ -1,7 +1,7 @@
 {
   "$schema": "../node_modules/@tauri-apps/cli/config.schema.json",
   "productName": "SHIYIN AI",
-  "version": "1.0.343",
+  "version": "1.0.344",
   "identifier": "com.hero8152.canvas.desktop",
   "build": {
     "frontendDist": "../desktop-placeholder"
diff --git a/static/js/canvas.js b/static/js/canvas.js
index bcd195b..258af9c 100644
--- a/static/js/canvas.js
+++ b/static/js/canvas.js
@@ -5487,7 +5487,6 @@ function closeInactiveCanvasSubmenus(event){
         clearTimeout(canvasSubmenuCloseTimer);
         canvasSubmenuCloseTimer = 0;
     }
-    if(inFilm) closeCanvasSubmenu(filmMenuHost,filmSubmenu,filmMenuTrigger);
 }
 document.addEventListener('pointerover', closeInactiveCanvasSubmenus, true);
 if(filmMenuTrigger){
diff --git a/static/update-notes.json b/static/update-notes.json
index b577e6f..603dc67 100644
--- a/static/update-notes.json
+++ b/static/update-notes.json
@@ -1,5 +1,5 @@
 {
-  "version": "1.0.343",
+  "version": "1.0.344",
   "updated_at": "2026-08-28T00:00:00+08:00",
   "items": [
     {
diff --git a/tests/test_canvas_menu_performance.py b/tests/test_canvas_menu_performance.py
index eb8d360..869b0c2 100644
--- a/tests/test_canvas_menu_performance.py
+++ b/tests/test_canvas_menu_performance.py
@@ -45,6 +45,8 @@ class CanvasMenuPerformanceTests(unittest.TestCase):
 
         self.assertIn("if(!inFilm)", routing)
         self.assertIn("scheduleCanvasSubmenuClose();", routing)
+        self.assertIn("if(canvasSubmenuCloseTimer)", routing)
+        self.assertNotIn("closeCanvasSubmenu(filmMenuHost,filmSubmenu,filmMenuTrigger)", routing)
         self.assertIn("setTimeout(() =>", closing)
         self.assertIn("filmMenuHost?.classList.remove('submenu-open','submenu-flip')", closing)
         self.assertNotIn("ecommerceMenuHost", closing)
```

## 验证结果

python -m pytest -q：887 passed；tools/assert-version-sync.ps1：Version sources synchronized: 1.0.344 (9 sources)。

## 待办清单

无

## 下一步

提交并推送源码；如需发布桌面安装包，可使用当前版本执行 installer:build。

## 避坑记录

版本递增后同步校验发现 release-notes/current.md 仍为 1.0.343，已同步为 1.0.344，并记录于 避坑指南/影视制作二级菜单pointerover捕获关闭.md。
