# 860 - 图片节点提示词面板下移

生成时间：2026-08-28 23:33:31

## 已完成内容

将图片节点提示词、模型和摄影机输入面板改为锚定节点下方；保留媒体快捷菜单在节点上方，并加入画布底部边界限制；同步更新回归测试、版本至1.0.346和发布说明。

## 当前模块

经典画布图片节点悬浮布局

## 当前 Git 状态

```text
M VERSION
 M main.py
 M package-lock.json
 M package.json
 M release-notes/current.md
 M src-tauri/Cargo.lock
 M src-tauri/Cargo.toml
 M src-tauri/tauri.conf.json
 M static/js/canvas.js
 M static/update-notes.json
 M tests/test_canvas_menu_preferences.py
```

## 代码前后对比 / Diff

```diff
diff --git a/VERSION b/VERSION
index 2587cf1..819d259 100644
--- a/VERSION
+++ b/VERSION
@@ -1 +1 @@
-1.0.345
+1.0.346
diff --git a/main.py b/main.py
index 18ab5a9..e1104bc 100644
--- a/main.py
+++ b/main.py
@@ -416,7 +416,7 @@ STARTUP_MAINTENANCE_STATE = {
 }
 ACTIVE_CANVAS_BY_ACCOUNT: dict[str, str] = {}
 ACTIVE_CANVAS_ID = ""
-APP_VERSION = "1.0.345"
+APP_VERSION = "1.0.346"
 GITHUB_REPO_URL = "https://github.com/luojiang419/SHIYIN-AI-source"
 GITHUB_VERSION_URL = "https://raw.githubusercontent.com/luojiang419/SHIYIN-AI-source/main/VERSION"
 GITHUB_TREE_URL = "https://api.github.com/repos/luojiang419/SHIYIN-AI-source/git/trees/main?recursive=1"
diff --git a/package-lock.json b/package-lock.json
index ed6e0a3..eb74d4a 100644
--- a/package-lock.json
+++ b/package-lock.json
@@ -1,12 +1,12 @@
 {
   "name": "shiyin-ai-windows-desktop",
-  "version": "1.0.345",
+  "version": "1.0.346",
   "lockfileVersion": 3,
   "requires": true,
   "packages": {
     "": {
       "name": "shiyin-ai-windows-desktop",
-      "version": "1.0.345",
+      "version": "1.0.346",
       "devDependencies": {
         "@tauri-apps/cli": "2.11.4",
         "tailwindcss": "3.4.17"
diff --git a/package.json b/package.json
index e4bdf25..6e16e92 100644
--- a/package.json
+++ b/package.json
@@ -1,7 +1,7 @@
 {
   "name": "shiyin-ai-windows-desktop",
   "private": true,
-  "version": "1.0.345",
+  "version": "1.0.346",
   "scripts": {
     "css:build": "node ./node_modules/tailwindcss/lib/cli.js -c tailwind.config.cjs -i ./static/css/tailwind-input.css -o ./static/vendor/css/tailwind.generated.css --minify",
     "desktop:dev": "npm run css:build && tauri dev",
diff --git a/release-notes/current.md b/release-notes/current.md
index 6da1089..9a33530 100644
--- a/release-notes/current.md
+++ b/release-notes/current.md
@@ -1,4 +1,5 @@
-# SHIYIN AI v1.0.345
+# SHIYIN AI v1.0.346
+- 修正图片节点悬浮层布局：图片节点顶部仅显示媒体快捷菜单，提示词、模型和摄影机输入面板移动到节点下方，并保持画布边界内可见。
 - 图片节点顶部悬浮菜单改为单行最多 4 个（3 个可配置动作 +“更多”），预览、宫格切分等功能不再与提示词输入面板混排；设置中可选择顶部显示的图片功能。
 - 修复无限画布影视制作二级菜单悬停交互：指针从主菜单移动到二级菜单时保持展开，不再被捕获的 `pointerover` 事件立即关闭。
 - 修正无限画布底部 Dock 布局：选择/移动按钮、常用功能节点和设置按钮统一为单行，Dock 按内容收缩并取消左右空白；空间不足时横向滚动，不再拆分成两行或上浮显示。
diff --git a/src-tauri/Cargo.lock b/src-tauri/Cargo.lock
index 2a425e2..ee47a3a 100644
--- a/src-tauri/Cargo.lock
+++ b/src-tauri/Cargo.lock
@@ -449,7 +449,7 @@ dependencies = [
 
 [[package]]
 name = "canvas-desktop"
-version = "1.0.345"
+version = "1.0.346"
 dependencies = [
  "arboard",
  "open",
diff --git a/src-tauri/Cargo.toml b/src-tauri/Cargo.toml
index 52bd80c..78ba9b7 100644
--- a/src-tauri/Cargo.toml
+++ b/src-tauri/Cargo.toml
@@ -1,6 +1,6 @@
 [package]
 name = "canvas-desktop"
-version = "1.0.345"
+version = "1.0.346"
 description = "SHIYIN AI Windows portable desktop host"
 authors = ["SHIYIN AI contributors"]
 edition = "2021"
diff --git a/src-tauri/tauri.conf.json b/src-tauri/tauri.conf.json
index bb4f848..1aa0ee4 100644
--- a/src-tauri/tauri.conf.json
+++ b/src-tauri/tauri.conf.json
@@ -1,7 +1,7 @@
 {
   "$schema": "../node_modules/@tauri-apps/cli/config.schema.json",
   "productName": "SHIYIN AI",
-  "version": "1.0.345",
+  "version": "1.0.346",
   "identifier": "com.hero8152.canvas.desktop",
   "build": {
     "frontendDist": "../desktop-placeholder"
diff --git a/static/js/canvas.js b/static/js/canvas.js
index 504ff36..e153de8 100644
--- a/static/js/canvas.js
+++ b/static/js/canvas.js
@@ -19208,13 +19208,18 @@ function positionSelectionHub(anchor){
     const maxLeft = Math.max(margin, boardRect.width - hubRect.width - margin);
     const left = Math.max(margin, Math.min(maxLeft, anchorRect.left - boardRect.left + (anchorRect.width - hubRect.width) / 2));
     const gap = 14;
-    // 保留旧定位表达式的语义，同时额外增加 4px 安全间距。
-    const mediaToolbarRect = selectionHub.classList.contains('image-prompt-hub')
-        ? anchor.querySelector?.('[data-node-media-toolbar]')?.getBoundingClientRect?.()
-        : null;
-    const mediaToolbarGap = mediaToolbarRect?.height ? mediaToolbarRect.height + 14 : 0;
-    let top = anchorRect.top - boardRect.top - hubRect.height - 10 - 4 - mediaToolbarGap;
-    if(top < margin) top = Math.min(boardRect.height - hubRect.height - margin, anchorRect.bottom - boardRect.top + gap);
+    const isImagePromptHub = selectionHub.classList.contains('image-prompt-hub');
+    // 图片节点的提示词面板与节点顶部工具条分离：面板固定锚定在节点下方，
+    // 工具条继续由节点自身的 .node-media-toolbar 定位到节点上方。
+    let top = isImagePromptHub
+        ? anchorRect.bottom - boardRect.top + gap
+        : anchorRect.top - boardRect.top - hubRect.height - 10 - 4;
+    if(isImagePromptHub){
+        const maxTop = boardRect.height - hubRect.height - margin;
+        top = Math.min(top, maxTop);
+    } else if(top < margin){
+        top = Math.min(boardRect.height - hubRect.height - margin, anchorRect.bottom - boardRect.top + gap);
+    }
     selectionHub.style.left = `${Math.round(left)}px`;
     selectionHub.style.top = `${Math.round(Math.max(margin, top))}px`;
 }
diff --git a/static/update-notes.json b/static/update-notes.json
index fae6cca..539934b 100644
--- a/static/update-notes.json
+++ b/static/update-notes.json
@@ -1,5 +1,5 @@
 {
-  "version": "1.0.345",
+  "version": "1.0.346",
   "updated_at": "2026-08-28T00:00:00+08:00",
   "items": [
     {
diff --git a/tests/test_canvas_menu_preferences.py b/tests/test_canvas_menu_preferences.py
index 5589074..588d856 100644
--- a/tests/test_canvas_menu_preferences.py
+++ b/tests/test_canvas_menu_preferences.py
@@ -46,7 +46,9 @@ def test_image_prompt_panel_is_separate_from_top_media_actions():
     assert "selectionHub.classList.remove('open','image-prompt-hub')" in render_source
     assert "const actionPanel = imageNode ? ''" in render_source
     assert "selectionHub.classList.toggle('image-prompt-hub', Boolean(imageNode))" in render_source
-    assert "const mediaToolbarGap = mediaToolbarRect?.height ? mediaToolbarRect.height + 14 : 0;" in JS
+    assert "const isImagePromptHub = selectionHub.classList.contains('image-prompt-hub');" in JS
+    assert "? anchorRect.bottom - boardRect.top + gap" in JS
+    assert "const maxTop = boardRect.height - hubRect.height - margin;" in JS
 
 
 def test_image_media_toolbar_exposes_grid_and_downstream_actions_with_two_row_cap():
```

## 验证结果

`node --check static/js/canvas.js` 通过；`python -m pytest -q tests/test_canvas_menu_preferences.py`：5 passed；全量 `python -m pytest -q`：889 passed；`tools/assert-version-sync.ps1`：Version sources synchronized: 1.0.346 (9 sources)；`git diff --check` 通过。

## 待办清单

无

## 下一步

提交并推送本次布局修复；如需安装包，关闭运行中的 canvas-backend.exe 后执行 npm run installer:build。

## 避坑记录

无
