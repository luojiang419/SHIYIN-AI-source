# 917 - AI助手基础图片操作与拖拽复用

生成时间：2026-08-31 11:18:44

## 已完成内容

static/gpt-chat.html 已增加生成图片复用/编辑/下载按钮、图片右键快捷菜单、内部图片拖拽到参考图托盘、共享预览组件接入及滚轮缩放/平移入口；保留原文件上传和 reference_images 请求协议。已通过 node 内联脚本语法检查、git diff --check、3 个相关测试文件共 19 项测试。

## 当前模块

AI助手前端基础交互

## 当前 Git 状态

```text
M static/gpt-chat.html
?? "\345\274\200\345\217\221\344\273\273\345\212\241/AI\345\212\251\346\211\213\350\277\236\347\273\255\350\247\206\350\247\211\345\257\271\350\257\235\344\270\216\345\217\202\350\200\203\350\265\204\344\272\247\345\244\215\347\224\250-v1.md"
```

## 代码前后对比 / Diff

```diff
diff --git a/static/gpt-chat.html b/static/gpt-chat.html
index 617142c..8a0b9c8 100644
--- a/static/gpt-chat.html
+++ b/static/gpt-chat.html
@@ -25,6 +25,7 @@
     <script src="/static/js/theme.js?v=2026.07.31.theme-picker.1"></script>
     <script src="/static/js/focus-guard.js?v=2026.08.02.ime-focus.1"></script>
     <script src="/static/js/media-cache-client.js?v=2026.08.30.static-assets-cache.1"></script>
+    <script src="/static/js/image-preview.js?v=2026.08.05.shared-preview.1"></script>
     <script src="/static/js/i18n.js?v=2026.06.30.1782788290"></script>
     <link rel="stylesheet" href="/static/css/theme.css?v=2026.06.30.1782781876">
     <style>
@@ -150,10 +151,21 @@
         .generated-grid.count-2,
         .generated-grid.count-3,
         .generated-grid.count-4 { grid-template-columns:repeat(2,minmax(0,1fr)); }
+        .generated-item { position:relative; min-width:0; border-radius:14px; overflow:hidden; }
         .bubble img.generated { display:block; width:auto; max-width:min(560px,100%); max-height:min(42vh,420px); height:auto; object-fit:contain; border-radius:14px; margin-top:0; border:1px solid var(--chat-line); cursor:zoom-in; }
         .generated-grid.count-2 img.generated,
         .generated-grid.count-3 img.generated,
         .generated-grid.count-4 img.generated { width:100%; max-width:280px; }
+        .generated-item-actions { position:absolute; left:8px; right:8px; bottom:8px; display:flex; align-items:center; justify-content:flex-end; gap:5px; opacity:0; transform:translateY(4px); transition:opacity .16s var(--ease), transform .16s var(--ease); pointer-events:none; }
+        .generated-item:hover .generated-item-actions,
+        .generated-item:focus-within .generated-item-actions { opacity:1; transform:translateY(0); pointer-events:auto; }
+        .generated-item-action { width:30px; height:30px; display:flex; align-items:center; justify-content:center; border-radius:9px; background:rgba(25,22,18,.76); color:#fffaf4; border:1px solid rgba(255,255,255,.28); box-shadow:0 8px 18px rgba(0,0,0,.18); backdrop-filter:blur(8px); }
+        .generated-item-action:hover { background:rgba(25,22,18,.92); transform:translateY(-1px); }
+        .generated-item-action i,.generated-item-action svg { width:14px; height:14px; }
+        .chat-context-menu { position:fixed; min-width:156px; padding:5px; display:flex; flex-direction:column; gap:2px; border:1px solid var(--chat-line); border-radius:11px; background:var(--chat-panel); color:var(--chat-text); box-shadow:0 16px 34px rgba(15,23,42,.18); z-index:12000; }
+        .chat-context-menu button { width:100%; height:30px; display:flex; align-items:center; gap:8px; padding:0 9px; border-radius:8px; color:inherit; background:transparent; text-align:left; font-size:11px; font-weight:800; }
+        .chat-context-menu button:hover { background:var(--chat-soft); }
+        .chat-context-menu i,.chat-context-menu svg { width:14px; height:14px; color:var(--chat-muted); }
         .thumbs { display:flex; flex-wrap:wrap; gap:8px; margin-top:10px; }
         .thumb { width:58px; height:58px; border-radius:12px; object-fit:cover; border:1px solid rgba(255,255,255,.28); }
         .thumb.file-thumb { width:auto; max-width:210px; padding:0 10px; display:flex; align-items:center; gap:7px; background:rgba(148,163,184,.12); color:currentColor; font-size:11px; font-weight:850; }
@@ -262,9 +274,14 @@
         .ref-chip.file i,.ref-chip.file svg { width:16px; height:16px; color:var(--chat-muted); flex:0 0 auto; }
         .ref-chip.file .ref-file-name { min-width:0; overflow:hidden; text-overflow:ellipsis; white-space:nowrap; font-size:11px; font-weight:850; }
         .ref-chip button { position:absolute; top:3px; right:3px; width:18px; height:18px; border-radius:50%; background:rgba(255,255,255,.92); color:#111827; font-size:12px; line-height:18px; }
-        .image-preview-modal { position:fixed; inset:0; display:none; align-items:center; justify-content:center; padding:24px; background:rgba(15,23,42,.62); backdrop-filter:blur(14px); z-index:10000; }
+        .image-preview-modal { position:fixed; inset:0; display:none; flex-direction:column; align-items:center; justify-content:center; gap:10px; padding:24px; background:rgba(15,23,42,.62); backdrop-filter:blur(14px); z-index:10000; }
         .image-preview-modal.open { display:flex; animation:popIn .16s var(--ease); }
-        .image-preview-modal img { max-width:min(1180px,94vw); max-height:88vh; object-fit:contain; border-radius:16px; box-shadow:0 24px 70px rgba(0,0,0,.32); background:var(--chat-panel); }
+        .chat-preview-frame { width:min(1180px,94vw); height:min(820px,78vh); max-width:100%; border-radius:16px; box-shadow:0 24px 70px rgba(0,0,0,.32); background:var(--chat-panel); }
+        .chat-preview-frame .studio-preview-img { max-width:none; max-height:none; border-radius:16px; }
+        .image-preview-toolbar { display:flex; align-items:center; gap:6px; padding:6px; border:1px solid rgba(255,255,255,.24); border-radius:12px; background:rgba(25,22,18,.72); box-shadow:0 10px 24px rgba(0,0,0,.18); backdrop-filter:blur(12px); }
+        .image-preview-tool { height:30px; padding:0 10px; display:flex; align-items:center; justify-content:center; gap:5px; border-radius:9px; color:#fffaf4; background:transparent; font-size:11px; font-weight:850; }
+        .image-preview-tool:hover { background:rgba(255,255,255,.14); }
+        .image-preview-tool i,.image-preview-tool svg { width:14px; height:14px; }
         .image-preview-close { position:fixed; top:18px; right:18px; width:36px; height:36px; border-radius:12px; display:flex; align-items:center; justify-content:center; background:rgba(248,250,252,.92); color:#111827; border:1px solid rgba(255,255,255,.38); box-shadow:0 12px 28px rgba(0,0,0,.18); }
         .image-preview-close i,.image-preview-close svg { width:18px; height:18px; }
         @keyframes popIn { from { opacity:0; transform:translateY(-6px) scale(.98); } to { opacity:1; transform:translateY(0) scale(1); } }
@@ -608,7 +625,14 @@
     </div>
     <div id="imagePreviewModal" class="image-preview-modal" onclick="closeImagePreview()">
         <button class="image-preview-close" title="关闭" aria-label="关闭" onclick="event.stopPropagation();closeImagePreview()"><i data-lucide="x"></i></button>
-        <img id="imagePreviewImg" src="" alt="preview" onclick="event.stopPropagation()">
+        <div id="imagePreviewViewport" class="studio-preview-frame chat-preview-frame" onclick="event.stopPropagation()">
+            <img id="imagePreviewImg" class="studio-preview-img" src="" alt="preview" draggable="false">
+        </div>
+        <div class="image-preview-toolbar" onclick="event.stopPropagation()">
+            <button type="button" class="image-preview-tool" title="复位" onclick="resetImagePreview()"><i data-lucide="scan"></i><span>复位</span></button>
+            <button type="button" class="image-preview-tool" title="复用为参考图" onclick="reuseImagePreview()"><i data-lucide="image-plus"></i><span>复用</span></button>
+            <button type="button" class="image-preview-tool" title="下载图片" onclick="downloadImagePreview()"><i data-lucide="download"></i><span>下载</span></button>
+        </div>
     </div>
 
     <script>
@@ -683,8 +707,13 @@
         let modelPickerHome = null;
         let resolutionPickerHome = null;
         let refs = [];
+        let imagePreviewController = null;
+        let currentPreviewRef = null;
         let config = { chat_model: 'gpt-5.5', image_model: 'gpt-image-2', ms_chat_models: [] };
 
+        const CHAT_OUTPUT_MEDIA_DRAG_TYPE = 'application/x-canvas-output-media';
+        const CHAT_OUTPUT_IMAGE_DRAG_TYPE = 'application/x-canvas-output-image';
+
         const SIZE_OPTIONS = {
             square: [['1024x1024','1k'], ['1536x1536','2k'], ['2048x2048','4k']],
             portrait: [['720x1080','1k'], ['1024x1536','2k'], ['1365x2048','4k']],
@@ -1359,12 +1388,35 @@
             if(imageUrls.length){
                 const grid = document.createElement('div');
                 grid.className = `generated-grid count-${Math.min(imageUrls.length, 4)}`;
-                imageUrls.forEach(url => {
+                imageUrls.forEach((url, index) => {
+                    const ref = generatedImageRef(url, msg, index);
+                    const item = document.createElement('div');
+                    item.className = 'generated-item';
+                    item.dataset.url = url;
+                    item.dataset.messageId = msg.id || '';
+                    item.draggable = true;
+                    item.addEventListener('dragstart', event => setChatImageDragData(event, ref));
+                    item.addEventListener('contextmenu', event => {
+                        event.preventDefault();
+                        showImageContextMenu(event.clientX, event.clientY, ref);
+                    });
                     const img = document.createElement('img');
                     img.className = 'generated';
                     img.src = url;
-                    img.onclick = () => openImagePreview(url);
-                    grid.appendChild(img);
+                    img.alt = attachmentName(ref);
+                    img.onclick = () => openImagePreview(ref);
+                    item.appendChild(img);
+                    const actions = document.createElement('div');
+                    actions.className = 'generated-item-actions';
+                    actions.innerHTML = `
+                        <button type="button" class="generated-item-action" data-chat-image-action="reuse" title="复用为参考图" aria-label="复用为参考图"><i data-lucide="image-plus"></i></button>
+                        <button type="button" class="generated-item-action" data-chat-image-action="edit" title="编辑这张图" aria-label="编辑这张图"><i data-lucide="wand-sparkles"></i></button>
+                        <button type="button" class="generated-item-action" data-chat-image-action="download" title="下载图片" aria-label="下载图片"><i data-lucide="download"></i></button>`;
+                    actions.querySelector('[data-chat-image-action="reuse"]').onclick = event => { event.stopPropagation(); addReference(ref); };
+                    actions.querySelector('[data-chat-image-action="edit"]').onclick = event => { event.stopPropagation(); addReference(ref); document.getElementById('messageInput')?.focus(); };
+                    actions.querySelector('[data-chat-image-action="download"]').onclick = event => { event.stopPropagation(); downloadChatImage(ref.url, attachmentName(ref)); };
+                    item.appendChild(actions);
+                    grid.appendChild(item);
                 });
                 bubble.appendChild(grid);
             }
@@ -1383,11 +1435,48 @@
             return {row, bubble, text};
         }
 
-        function openImagePreview(url){
+        function generatedImageRef(url, message, index=0){
+            const generatedAssets = Array.isArray(message?.generated_assets) ? message.generated_assets : [];
+            const asset = generatedAssets[index] && typeof generatedAssets[index] === 'object' ? generatedAssets[index] : {};
+            return {
+                ...asset,
+                url,
+                name: asset.name || `${message?.content || '生成图片'}-${index + 1}`,
+                kind: 'image',
+                source: 'conversation',
+                message_id: message?.id || '',
+                image_index: index,
+            };
+        }
+
+        function addReference(ref){
+            if(!ref?.url) return false;
+            const existing = refs.some(item => item?.url === ref.url);
+            if(existing){ renderRefs(); document.getElementById('messageInput')?.focus(); return false; }
+            if(refs.length >= CHAT_ATTACHMENT_MAX){ alert(`最多添加 ${CHAT_ATTACHMENT_MAX} 个参考素材`); return false; }
+            refs.push({...ref, kind: ref.kind || 'image'});
+            renderRefs();
+            document.getElementById('messageInput')?.focus();
+            return true;
+        }
+
+        function imagePreviewRef(value){
+            if(typeof value === 'string') return {url:value, name:attachmentName({url:value}), kind:'image'};
+            return value && value.url ? value : null;
+        }
+
+        function openImagePreview(value){
+            const ref = imagePreviewRef(value);
             const modal = document.getElementById('imagePreviewModal');
             const img = document.getElementById('imagePreviewImg');
-            if(!modal || !img || !url) return;
-            img.src = url;
+            const viewport = document.getElementById('imagePreviewViewport');
+            if(!modal || !img || !viewport || !ref?.url) return;
+            currentPreviewRef = ref;
+            if(!imagePreviewController && window.StudioImagePreview){
+                imagePreviewController = StudioImagePreview.attach(viewport, {minZoom:1, maxZoom:8});
+            }
+            imagePreviewController?.reset();
+            img.src = ref.url;
             modal.classList.add('open');
             lucide.createIcons();
         }
@@ -1395,10 +1484,84 @@
         function closeImagePreview(){
             const modal = document.getElementById('imagePreviewModal');
             const img = document.getElementById('imagePreviewImg');
+            imagePreviewController?.reset();
             modal?.classList.remove('open');
             if(img) img.src = '';
+            currentPreviewRef = null;
+        }
+
+        function resetImagePreview(){ imagePreviewController?.reset(); }
+
+        function reuseImagePreview(){
+            if(currentPreviewRef) addReference(currentPreviewRef);
+        }
+
+        function downloadImagePreview(){
+            if(currentPreviewRef?.url) downloadChatImage(currentPreviewRef.url, attachmentName(currentPreviewRef));
+        }
+
+        function imageFileName(url, fallback='chat-image'){
+            const raw = String(url || '').split('?')[0].split('#')[0].split('/').pop() || '';
+            let name = raw;
+            try { name = decodeURIComponent(raw); } catch(e) {}
+            if(!name || !/\.[a-z0-9]{2,5}$/i.test(name)) name = `${fallback}.png`;
+            return name.replace(/[\\/:*?"<>|]+/g, '_').slice(0, 160) || `${fallback}.png`;
+        }
+
+        function setChatImageDragData(event, ref){
+            if(!event?.dataTransfer || !ref?.url) return;
+            const payload = {url:ref.url, name:attachmentName(ref), kind:'image', source:'conversation', message_id:ref.message_id || '', image_index:ref.image_index || 0};
+            event.dataTransfer.effectAllowed = 'copy';
+            event.dataTransfer.setData(CHAT_OUTPUT_MEDIA_DRAG_TYPE, JSON.stringify(payload));
+            event.dataTransfer.setData(CHAT_OUTPUT_IMAGE_DRAG_TYPE, ref.url);
+            event.dataTransfer.setData('text/uri-list', ref.url);
+            event.dataTransfer.setData('text/plain', ref.url);
+        }
+
+        function downloadChatImage(url, name=''){
+            if(!url) return;
+            const filename = imageFileName(name || url);
+            const link = document.createElement('a');
+            link.href = `/api/download-output?url=${encodeURIComponent(url)}&name=${encodeURIComponent(filename)}`;
+            link.download = filename;
+            link.rel = 'noopener';
+            document.body.appendChild(link);
+            link.click();
+            link.remove();
+        }
+
+        function closeImageContextMenu(){ document.querySelector('.chat-context-menu')?.remove(); }
+
+        function showImageContextMenu(x, y, ref){
+            closeImageContextMenu();
+            if(!ref?.url) return;
+            const menu = document.createElement('div');
+            menu.className = 'chat-context-menu';
+            menu.innerHTML = `
+                <button type="button" data-context-action="reuse"><i data-lucide="image-plus"></i><span>复用为参考图</span></button>
+                <button type="button" data-context-action="edit"><i data-lucide="wand-sparkles"></i><span>编辑这张图</span></button>
+                <button type="button" data-context-action="download"><i data-lucide="download"></i><span>下载图片</span></button>
+                <button type="button" data-context-action="copy"><i data-lucide="copy"></i><span>复制图片地址</span></button>`;
+            document.body.appendChild(menu);
+            const left = Math.max(8, Math.min(x, window.innerWidth - menu.offsetWidth - 8));
+            const top = Math.max(8, Math.min(y, window.innerHeight - menu.offsetHeight - 8));
+            menu.style.left = `${left}px`;
+            menu.style.top = `${top}px`;
+            menu.querySelector('[data-context-action="reuse"]').onclick = () => { addReference(ref); closeImageContextMenu(); };
+            menu.querySelector('[data-context-action="edit"]').onclick = () => { addReference(ref); document.getElementById('messageInput')?.focus(); closeImageContextMenu(); };
+            menu.querySelector('[data-context-action="download"]').onclick = () => { downloadChatImage(ref.url, attachmentName(ref)); closeImageContextMenu(); };
+            menu.querySelector('[data-context-action="copy"]').onclick = async () => {
+                try { await navigator.clipboard?.writeText(ref.url); } catch(e) {}
+                closeImageContextMenu();
+            };
+            lucide.createIcons();
         }
 
+        document.addEventListener('click', event => {
+            if(!event.target.closest('.chat-context-menu')) closeImageContextMenu();
+        });
+        window.addEventListener('resize', closeImageContextMenu);
+
         function isImageRef(ref){
             const kind = String(ref?.kind || '').toLowerCase();
             const mime = String(ref?.mime || '').toLowerCase();
@@ -1464,22 +1627,44 @@
 
         const composerBody = document.getElementById('composerBody');
         composerBody.addEventListener('dragover', e => {
-            if(!hasUploadFiles(e.dataTransfer?.items)) return;
+            if(!hasChatImageDrop(e.dataTransfer)) return;
             e.preventDefault();
+            e.dataTransfer.dropEffect = 'copy';
             composerBody.classList.add('drag-over');
         });
         composerBody.addEventListener('dragleave', e => {
             if(!composerBody.contains(e.relatedTarget)) composerBody.classList.remove('drag-over');
         });
         composerBody.addEventListener('drop', e => {
-            if(!hasUploadFiles(e.dataTransfer?.items)) return;
+            if(!hasChatImageDrop(e.dataTransfer)) return;
             e.preventDefault();
             composerBody.classList.remove('drag-over');
+            const ref = chatImageDropRef(e.dataTransfer);
+            if(ref){ addReference(ref); return; }
             const files = [...(e.dataTransfer?.files || [])];
             uploadFiles(files);
         });
 
         function hasUploadFiles(items){ return [...(items || [])].some(item => item.kind === 'file'); }
+        function readTransferData(dataTransfer, type){
+            try { return dataTransfer?.getData?.(type) || ''; } catch(e) { return ''; }
+        }
+        function chatImageDropRef(dataTransfer){
+            if(!dataTransfer) return null;
+            const rawMedia = readTransferData(dataTransfer, CHAT_OUTPUT_MEDIA_DRAG_TYPE);
+            if(rawMedia){
+                try {
+                    const payload = JSON.parse(rawMedia);
+                    if(payload?.url && String(payload.kind || 'image').toLowerCase() === 'image') return {...payload, source:'conversation-drag', kind:'image'};
+                } catch(e) {}
+            }
+            const outputUrl = readTransferData(dataTransfer, CHAT_OUTPUT_IMAGE_DRAG_TYPE);
+            if(outputUrl) return {url:outputUrl.trim(), name:attachmentName({url:outputUrl}), kind:'image', source:'conversation-drag'};
+            const uri = readTransferData(dataTransfer, 'text/uri-list').split(/\r?\n/).map(item => item.trim()).find(item => item && !item.startsWith('#'));
+            if(uri && /^(https?:\/\/|\/assets\/|\/output\/)/i.test(uri)) return {url:uri, name:attachmentName({url:uri}), kind:'image', source:'conversation-drag'};
+            return null;
+        }
+        function hasChatImageDrop(dataTransfer){ return hasUploadFiles(dataTransfer?.items) || Boolean(chatImageDropRef(dataTransfer)); }
         function updateComposerOverflow(){
             const body = document.getElementById('composerBody');
             if(!body) return;
```

## 验证结果

未填写

## 待办清单

图片级删除与回收站、对话视觉资产 ID、批量输出元数据、自动语义选择历史参考图、发送前引用确认。

## 下一步

先检查当前快照和代码差异，完善图片消息元数据与后端资产记录接口，再做图片级删除；保持旧消息和旧客户端兼容。

## 避坑记录

无
