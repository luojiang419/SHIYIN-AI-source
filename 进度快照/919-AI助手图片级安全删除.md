# 919 - AI助手图片级安全删除

生成时间：2026-08-31 11:25:14

## 已完成内容

main.py 新增 delete_chat_message_asset 与 DELETE /api/conversations/{conversation_id}/messages/{message_id}；gpt-chat 右键菜单新增从对话移除，渲染过滤软删除消息；保留 deleted_assets 和物理文件。验证：py_compile、JS syntax、git diff --check、24 项相关测试通过；接口冒烟因现有账号鉴权返回 401，未作为运行通过结论。

## 当前模块

对话图片生命周期

## 当前 Git 状态

```text
(no output)
```

## 代码前后对比 / Diff

```diff
(no output)
```

## 验证结果

未填写

## 待办清单

参考图角色/锁定展示、自动引用候选与确认、语义检索、多图供应商降级、批量操作。

## 下一步

实现参考图托盘的来源/角色/锁定交互，并新增自动引用准备接口；先不触发图片生成，确认后再复用现有 chat_agent。

## 避坑记录

无
